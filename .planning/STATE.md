# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-17)

**Core value:** Conversations automatically become tracked commitments and executed deliverables -- the founder's daily operating system
**Current focus:** v22.0 Skill Platform Consolidation

## Current Position

Milestone: v22.0 Skill Platform Consolidation
Phase: 152 (Retirement) — Plan 01 of 4 COMPLETE; Plan 02 of 4 COMPLETE (2026-04-19T02:02Z, option-d deferral of external per-skill repo archival); Plan 03 of 4 COMPLETE (2026-04-18T17:59Z, commit 92e2a42). Plan 04 unblocked (SC1 tag anchor + SC3 + SC4 gates all PASS). Phase 151 CODE-COMPLETE (phase-verify human gate still pending: DOGFOOD-RUNBOOK.md execution + 151-LATENCY.md review).
Plan (latest complete): 152-02 COMPLETE (2026-04-19T02:02Z, option-d deferral). Finalized the retirement anchor plan: Task 1 (annotated git tag `legacy-skills-final` at tag object SHA 410a8fa615a3475385bbc7073ff7f69dd8fb3b4f → dereferenced commit 2c4450dcf3187eff3b27bbadc485b99050e85842 = Plan 01 retirement HEAD) was already complete pre-checkpoint with push to both origin (Sharan0516/flywheel-v2) + lumifai (Lumif-ai/flywheel-v2) remotes. User resolved Task 2 checkpoint:decision to **option-d** (defer per-skill external repo archival): `Sharan0516/claude-skill-web-scraper` is a standalone skill repo outside the v22.0 monorepo consolidation scope; archival requires GitHub UI (no first-class `gh` CLI for repo archive without admin token) and is non-blocking for Phase 152 close. Task 3 (checkpoint:human-action) skipped per plan's `<done>` clause which permits the skipped path when Task 2 resolves to option-d, provided the executor records the deferral reason in the summary (done in SUMMARY.md "Task 3 Evidence" + "Residual Task" sections). Residual task hand-off: Plan 04's retirement PR description will carry forward the single deferred action — archive `Sharan0516/claude-skill-web-scraper` on GitHub via Settings → Danger Zone → "Archive this repository" with `curl -s https://github.com/Sharan0516/claude-skill-web-scraper | grep "This repository has been archived"` verification — as a post-merge residual task owned by the user. Since Plan 01 already annotated every in-repo legacy reference with the `DEPRECATED (Phase 152)` sentinel and Plan 03 scrubbed install instructions from all READMEs (including the sole in-repo link to the external scraper), the external-repo archive is belt-and-suspenders hardening rather than a correctness prerequisite. Rule 5 Spec-gap deviation from Task 1 (pre-flight dirty-tree guard interpretation) preserved in SUMMARY.md. Plan-level commit (captures SUMMARY + STATE updates as the plan's persistent record; the tag itself was pushed during Task 1 and has no file artifact) to be created as final step of this execution. SUMMARY.md at .planning/phases/152-retirement/152-02-SUMMARY.md. Plan 04 now inherits: (a) `legacy-skills-final` tag on both GitHub remotes as immutable rollback anchor = SC1 satisfied; (b) one documented residual task for post-merge archival of standalone skill repo; (c) zero open checkpoints in Phase 152 Plans 01-03 — Plan 04 (telemetry gate + retirement PR) is now fully unblocked.

Previous plan: 152-03 COMPLETE (2026-04-18T17:59Z, commit 92e2a42). Added MCP-only Skill Delivery block to root README.md (new file; untracked at plan start; 55 insertions). Task 1 (verification-only) re-confirmed scripts/install.sh, cli/flywheel_cli/main.py, cli/flywheel_mcp/templates/CLAUDE.md are free of legacy ~/.claude/skills/ manipulation — 4 assertion greps all printed CLEAN. Task 2 inserted verbatim Skill Delivery block between intro paragraph and ## Stack (no existing Installation section; plan Rule 2 invoked). Task 3 ran full SC3 + SC4 gate greps across backend/ cli/ skills/ scripts/ README.md + cli/flywheel_mcp/templates/CLAUDE.md. SC3 PASS: 395 residual ~/.claude/skills/ hits across 85 files, all classified into allowed buckets (139 in skills/_archived/, 1 in backend/src/tests/, 239 carrying Plan 01 Phase 152 deprecation annotation, 12 in MIGRATION docs with Historical sentinel, 1 in README.md new block, 4 non-/skills/ false positives of the expanduser("~/.claude regex in scripts/check_portal_state_paths.sh policy comment + skills/gtm-shared/gtm_utils.py ~/.claude/gtm-stack paths). SC4 PASS after semantic-intent filter extension (see deviations). Two Rule 3 deviations: (a) plan's SC4 regex (git clone|git pull|unzip) flagged the word "unzipping" inside the Task 2 verbatim denial sentence, producing self-contradictory FAIL on content the plan mandates verbatim — extended SC4 pre-filter by one grep -v clause to match gate intent ("no instructions", not "no literal substring"); (b) plan's verify step 3 assumed README.md was tracked — it was untracked at session start (?? README.md in gitStatus preamble), interpreted by intent as "Plan 03 only touched README.md". SUMMARY at .planning/phases/152-retirement/152-03-SUMMARY.md.

Previous plan: 151-04 COMPLETE (latency measurement + SLO regression baseline). Created .planning/phases/151-broker-dogfood-resilience/scripts/measure_latency.py (425 LOC, stdlib-only) — argparse CLI w/ 4 flags (--n default 100, --skill default broker-parse-contract, --no-assert-slo, --out), 100 cold subprocess-per-call methodology (explicit PYTHONPATH injection, bypass_cache=True inner call, cache wipe per iteration, 120s timeout), 100 warm same-process methodology after priming call, statistics.quantiles(values, n=100) indices 49/94/98 for p50/p95/p99, immutable SLO gates (SLO_P99_COLD_MS=500.0, SLO_P99_WARM_MS=50.0), exits 1 on violation unless --no-assert-slo, programmatic bundle-size probe, pretty markdown renderer with metadata + cold/warm tables + raw sample <details> blocks + Regression Protocol section. Ran full 100+100 against live ngrok (https://methodical-jessenia-unannotated.ngrok-free.dev): bundle size confirmed 9283 bytes (17x smaller than ROADMAP SC5's stale 160KB estimate — callout in LATENCY.md), warm p99 = 0.4000 ms PASS (125x under 50 ms SLO, confirms Plan 01 content-addressed cache is effectively free), cold p99 = 3279.64 ms FAIL (6.5x over 500 ms SLO — root cause is Python interpreter startup + ngrok TLS handshake + geographic RTT, NOT backend bundle build time; breakdown: ~1.5s Python import + ~0.4s TLS + ~0.3s server + ~0.1s deserialize = ~2s baseline). Cold stats: p50=1889.82 / p95=2854.14 / p99=3279.64 / mean=2014.57 / stddev=331.11 / min=1694.90 / max=3282.74 / n=100. Warm stats: p50=0.1994 / p95=0.2883 / p99=0.4000 / mean=0.2042 / stddev=0.0421 / min=0.1588 / max=0.4002 / n=100. Deviations: (1) [Rule 3 Blocking] Bundle endpoint rate-limited at 10/min/tenant; first 100-run attempt crashed at iteration 6 with HTTP 429. Fix: added min_interval_s=6.5 pacing floor between cold subprocesses + exponential backoff (12/24/48s, max 5 retries) on 429 detection in subprocess stderr. Paced sleep is BETWEEN subprocesses, not inside timed region, so measured latencies reflect real fetch time only. Second run completed all 100 iterations with zero 429 events. (2) [Rule 2 Critical] Added bypass_cache=True to cold subprocess inner script for belt-and-suspenders cold-path correctness (disk wipe + bypass_cache eliminates any cache short-circuit race). SLO thresholds kept IMMUTABLE — phase-verify decides optimize cold path vs amend SLO via ROADMAP revision. Single atomic plan-scoped commit per commit_strategy=per-plan: be52bdf "feat(151-04): latency measurement + SLO baseline" (3 files / 742 insertions, all under .planning/, force-added via git add -f since .planning/ is gitignored — matches 151-03 precedent). SUMMARY.md at .planning/phases/151-broker-dogfood-resilience/151-04-SUMMARY.md. **Phase 151 Plan 04 receives NOTHING — this is the last plan.** Phase 151 code is complete; only manual DOGFOOD-RUNBOOK.md execution + LATENCY.md review remain as phase-verify human gates.
Status: Phase 151 Plan 01 implementation complete. Shipped settings.subsidy_allowed_skills = {"company-intel"} as single source of truth in backend/src/flywheel/config.py (+8 LOC); backend/src/flywheel/api/broker/_enforcement.py (NEW, 136 LOC) with SubsidyDecision frozen dataclass + require_subsidy_decision FastAPI dependency implementing the 4-cell truth table (allowlisted+no-key → subsidy; allowlisted+key → BYOK; non-allowlisted+no-key → 403 subsidy_not_allowed; non-allowlisted+key → BYOK). Module docstring declares BYOK wire format = body-based `api_key` field (Info-9 requirement for Plan 02/03 consumers); POC verified body-double-read works cleanly on pinned stack (fastapi=0.135.1, starlette=0.52.1) — FastAPI caches request._body so dep + Pydantic body param read identical payload; 50× stress test passed. backend/src/flywheel/services/skill_executor.py:~530 refactored from hardcoded ("company-intel", "meeting-prep") tuple to settings.subsidy_allowed_skills lookup; inline comment clarifies _execute_web_run (line ~3273) is a separate BYOK-gated path that does NOT hit this fallback — prevents future reviewer confusion about a "web-path backdoor". backend/src/tests/test_subsidy_enforcement.py (NEW, 375 LOC, 13 tests all passing in 0.48s): 4-cell truth table + missing-header × 2 + subsidy-key-unset + body-double-read × 2 + skill_executor source-of-truth × 2 + test_skill_executor_blocks_gtm_skills_without_byok (Blocker-1 closure using REAL GTM skill names gtm-pipeline + gtm-company-fit-analyzer + gtm-outbound-messenger from skills/gtm-*) + settings-default sanity. Zero regressions vs HEAD baseline confirmed by git show HEAD:... file-swap technique (identical pre-existing failure set: MockLead schema drift in test_skills_api.py + test_rate_limit.py concurrent fixtures + test_store_api_key_invalid_rejected). Audit artifact .planning/phases/150.1-cc-as-brain-enforcement/150.1-01-AUDIT.md (177 LOC) froze the 15-site subsidy-reachable leak inventory (13 direct settings.flywheel_subsidy_api_key references + 2 AsyncAnthropic() env-var fallback shapes at solicitation_drafter.py:135 and recommendation_drafter.py:153) with file:line + caller/endpoint + category for Plan 02 tick-off; 5 broker-scoped migration sites flagged with checkbox (contract_analyzer.py:796 + :1182, quote_extractor.py:339, solicitation_drafter.py:135, recommendation_drafter.py:153); 7 non-broker-non-gtm sites catalogued as OUT_OF_SCOPE_PER_CONTEXT for future phase triage. Prod read-only SELECT (2026-04-18T09:58:40Z) against Supabase meeting-prep blast radius: 12 SkillRuns in 30d / 1 in 7d / 12 all-time / 2 distinct users / 0 anonymous (auth.users.is_anonymous=false for all 12) / 1 user with BYOK key / 1 user WITHOUT BYOK (the one who will break post-deploy). Impact classification SAFE: the feared anonymous-onboarding-funnel breakage is empirically absent — Plan 03 deploy gate can proceed with a one-to-one DM to the 1 affected user, no rollback plan required. [Rule 5 spec-gap observation] Two unrelated hardcoded ("company-intel", "meeting-prep") tuples remain at api/auth.py:84 (_ONBOARDING_SKILLS for compute_lifecycle_state) and services/briefing.py:125 (_onboarding_skills for first-visit post-onboarding-run detection) — these are ONBOARDING-LIFECYCLE semantics (funnel analytics), NOT subsidy, and explicitly out of scope per CONTEXT.md. Single atomic commit per commit_strategy=per-plan: 45338a1. Evidence logs: /tmp/150.1-01-body-poc.log + /tmp/150.1-01-meeting-prep-count.log. SUMMARY.md at .planning/phases/150.1-cc-as-brain-enforcement/150.1-01-SUMMARY.md. Plan 02 receives: shared constant + shared FastAPI Depends to attach to every extract/save endpoint + 5 broker leak checklist + locked body-based BYOK wire format + evidence that meeting-prep removal is safe. flywheel_fetch_skill_assets MCP tool registered in cli/flywheel_mcp/server.py returning ToolResult(content=[File(format='zip')...], structured_content={skill, deps, rollup_sha, shas}). Tool overrides CONTEXT-locked tuple[dict, list[File]] return shape per RESEARCH Pitfall 5 (fastmcp doesn't document tuple returns — verified against installed fastmcp.tools.base.ToolResult source). BundleError + FlywheelAPIError caught at MCP boundary, mapped to ToolResult(content=[TextContent(...)]) so Claude sees clean actionable messages instead of tracebacks. FLYWHEEL_DEBUG=1 gates logger.exception stderr detail for operators. cli/tests/test_mcp_assets_tool.py (~380 LOC): TestContract x5 hermetic + TestE2EFromProd x6 live-prod opt-in via -m e2e. cli/tests/conftest.py (~40 LOC): e2e marker registration + auto-skip unless -m e2e. Full cli/tests/ 52 passed, 6 skipped — zero regressions vs Plan 02 baseline of 47 tests. Plan 03 test breakdown: 5 contract tests prove wire shape, topological order (library-first, consumer-last), SHA pass-through via structured_content, graceful BundleError handling, and static regression guard for Pitfall 4 (File.metadata kwarg absent from tool source — belt-and-suspenders via inspect.getsource). 6 E2E tests cover all 5 Phase 150 ROADMAP SCs green with evidence: SC1 broker-parse-contract fanout returns n:1 with broker SHA=217ebdc1... / 7239B byte-matching Phase 149 capture + api_client.py + field_validator.py present in zip + MCP-surface ToolResult shape sanity; SC2 ../etc/passwd malicious zip rejected before extractall; SC3 tampered-byte raises BundleIntegrityError with zipfile.ZipFile call_count=0 (verify-before-extract ordering structurally proven at MCP layer); SC4 field_validator imports from broker library bundle via sys.path[0]==tmp; SC5 snapshot-before/after cache-diff shows zero new bytes under ~/.cache/flywheel + tempdir gone + no /tmp/flywheel-skill-* siblings. Byte-identity preserved end-to-end: Phase 147 deterministic zip → Phase 148 server → Phase 149 seed capture → Plan 01 fanout → Plan 02 client → Plan 03 MCP tool, all SHA-equal at every layer. Deviations: (1) [Rule 1 pre-approved] ToolResult override of CONTEXT's tuple return — RESEARCH Pitfall 5 justified, plan critical directive #1 locked; (2) [Rule 3] Prod UAT DNS unreachable from this dev machine — used documented Plan 03 fallback via local backend (port 8000) connecting to same prod Supabase, byte-identity preserved because DB layer unchanged. Zero new runtime deps (only added imports: fastmcp.utilities.types.File + flywheel_mcp.bundle.BundleError, both already present in cli venv from Plan 02). Single atomic commit per commit_strategy=per-plan: 28271f3. Evidence artifacts: /tmp/150-03-e2e.log + /tmp/150-03-wire-hashes.log + /tmp/150-03-db-state.log. SUMMARY.md at .planning/phases/150-mcp-tool-unpack-helper/150-03-SUMMARY.md. Phase 150 is code-complete; ready for gsd-verifier invocation.
Last activity: 2026-04-19 -- 152-02 COMPLETE (option-d deferral). User resolved Task 2 checkpoint:decision to option-d: `Sharan0516/claude-skill-web-scraper` archival deferred as a documented residual task in Plan 04's retirement PR description — standalone skill repo outside the v22.0 monorepo consolidation scope; non-blocking for Phase 152 close. Task 3 skipped per plan's `<done>` clause permitting the skipped path when Task 2 = option-d. Task 1 (tag `legacy-skills-final` → 410a8fa6…/2c4450d, pushed to origin + lumifai) was already complete pre-checkpoint. SUMMARY.md finalized with Task 2/3 evidence + Residual Task section documenting post-merge archival instructions + evidence format (curl-verify + timestamp). Per-plan commit (SUMMARY.md + STATE.md only) created as the plan's persistent record — the tag itself has no file artifact since it was pushed during Task 1. Rule 5 spec-gap deviation from Task 1 preserved in SUMMARY. Plan 04 now fully unblocked: all of Plans 01-03 COMPLETE, Plan 02 COMPLETE with documented residual task hand-off.

Previous activity: 2026-04-19 -- 152-01 COMPLETE. Plan 01 of Phase 152 (Retirement) annotated the legacy skill surface for archival: inserted the Phase 152 deprecation banner on 19 legacy SKILL.md files (18 canonical broker-*/gtm-*/meeting-*/call-intelligence/outreach-drafter/gtm-company-fit-analyzer + 1 Rule 3 addition for gtm-web-scraper-extractor/SKILL.md, placed between YAML frontmatter and body); stamped the "DEPRECATED (Phase 152 — 2026-04-19)" single-line comment next to every ~/.claude/skills/ reference in shared engines (slack_bot.py:165, skill_governance.py:23 SKILLS_DIR, skill_converter.py:95, skill_promoter.py:52 DEFAULT_SKILLS_ROOT, engines/context_utils.py:573, validate_skills.py:23) plus backend/src/flywheel/context_utils.py:573 and the separate skills/_shared/context_utils.py module (Rule 3); deleted all 13 enumerated SKILL.md backup files (.bak/.bak_*/.backup/.v*.bak) outside skills/_archived/; replaced legacy git clone / unzip install instructions in skills/gtm-web-scraper-extractor/README.md and skills/gtm-company-fit-analyzer/README.md with a byte-identical MCP-delivery paragraph; rewrote the "After Phase 152 ... will be tagged ... archived read-only" sentence in skills/broker/MIGRATION-NOTES.md to past tense anchored at 2026-04-19 and added a top-of-file DEPRECATED marker; added a historical-document banner with embedded DEPRECATED marker atop skills/MIGRATION-PLAN.md; added top-of-file deprecation banners to 4 ancillary markdown/ref files surfaced by the plan-level SC3 gate (skills/_shared/recipe-protocol.md, skills/_shared/context-protocol.md, skills/outreach-drafter/references/persona-schema.md, skills/gtm-outbound-messenger/references/tracker-logging.md). Plan-level SC3 gate passes with zero unannotated hits across skills/, backend/src/flywheel/, cli/ (excluding skills/_archived/ and /tests/). All 8 touched Python files parse clean via ast.parse. 1 deviation (Rule 3 Blocking): SC3 gate surfaced 9 files outside the explicit task-list targets that required markers to pass the plan's own success criterion — annotated each with the smallest-footprint banner appropriate to file type rather than carving gate exceptions. No runtime behavior changes; archival grooming only. Single per-plan commit: 2c4450d "feat(152-01): annotate legacy skill surface with Phase 152 deprecation banners" (35 files modified, 84 insertions / 8285 deletions — the large deletion count comes from removing 13 dead backup files). SUMMARY at .planning/phases/152-retirement/152-01-SUMMARY.md. Plan 02 (tag + archive legacy repo) receives: a fully-annotated working tree where every remaining ~/.claude/skills/ reference outside _archived/ is sitting next to a Phase 152 marker, so `git tag legacy-skills-final` on HEAD captures the final annotated archival snapshot; expanded SC3 coverage is already in place so Plan 02 inherits zero grep-surface backlog; zero backup cruft remains under skills/.

Previous activity: 2026-04-19 -- 151-04 COMPLETE. Phase 151 CODE-COMPLETE. See "Plan:" section above for full 151-04 summary (measure_latency.py 425 LOC + 151-LATENCY.md 88 LOC; 100+100 run against live ngrok; cold p99 = 3279.64 ms FAIL 6.5x over 500 ms SLO, warm p99 = 0.4000 ms PASS 125x under 50 ms SLO; bundle 9283 B vs ROADMAP's stale 160KB; immutable SLO gates in script as regression guard; 2 deviations auto-fixed — Rule 3 rate-limit pacing for 10/min endpoint + Rule 2 bypass_cache=True belt-and-suspenders). Single atomic commit be52bdf "feat(151-04): latency measurement + SLO baseline" force-added via git add -f. SUMMARY at .planning/phases/151-broker-dogfood-resilience/151-04-SUMMARY.md.

Previous activity: 2026-04-18 -- 151-03 COMPLETE. Task 1 created `.planning/phases/151-broker-dogfood-resilience/scripts/dogfood_harness.py` (459 LOC, stdlib-only except httpx via FlywheelClient) — argparse CLI with 4 flags (`--project-id` required, `--skip-rename`, `--offline-sim`, `--only` optional). 5 step_* functions matching canonical broker steps exported via `STEPS: list[tuple[str, Callable]]` for Plan 04 reuse: step_parse_contract, step_parse_policies, step_gap_analysis, step_select_carriers, step_draft_emails. Pattern 3a steps (parse-contract, parse-policies, draft-emails) do 3-phase probe via private helpers: `_materialize_and_probe(skill)` using `materialize_skill_bundle(name)` context manager (from cli/flywheel_mcp/bundle.py — asserts tmp root exists + `sys.path[0] == str(tmp)` + spot-checks .py file count) + `_probe_extract(op, project_id, cid, skill)` POSTing to `/api/v1/broker/extract/<op>` with `X-Flywheel-Correlation-ID` + `X-Flywheel-Skill` headers (asserts Pattern 3a envelope {prompt, tool_schema, documents} present) + `_probe_save(op, project_id, cid, skill, canned_tool_use)` POSTing minimal-valid structural fixture to `/api/v1/broker/save/<op>` + `_verify_db_rows(project_id, {field: min_count})` via `GET /api/v1/broker/projects/{id}`. Non-Pattern-3a steps do `_materialize_and_probe` + single-endpoint call (POST /analyze-gaps / GET /carrier-matches). 3 module-level canned tool_use fixtures: CANNED_CONTRACT_TOOL_USE (extract_coverage_requirements → 1 general_liability coverage, required_limit=1000000 USD), CANNED_POLICY_TOOL_USE (extract_policy_coverage → 1 general_liability policy, current_limit=500000), CANNED_SOLICITATION_TOOL_USE (draft_solicitation_email → 1 recipient) — each matches its operation's tool_schema.input_schema for save-endpoint probing without real Claude inference. atexit+SIGINT+SIGTERM cooperative cleanup: `_rename_broker_away()` and `_restore_broker()` both idempotent (guards on `BROKER_BAK.exists() and not BROKER_DIR.exists()` — safe to re-invoke), signal handlers use 128+signo exit convention (130 SIGINT, 143 SIGTERM). Repo-root auto-detection at import time via `_REPO_ROOT = Path(__file__).resolve().parents[3]` + `sys.path.insert(0, str(_REPO_ROOT / "cli"))` — harness runnable from any cwd without PYTHONPATH ceremony. --offline-sim does 2-phase probe: warm cache via live FlywheelClient().fetch_skill_assets_bundle + flip FLYWHEEL_API_URL=http://127.0.0.1:1 (RST-ing port → httpx.ConnectError) + re-fetch (expect Plan 01's WARN stderr line + cache-served non-empty bundle tuple); try/finally restores original env var. Task 1 also created `fixtures/README.md` (72 LOC) documenting MSA source as `backend/scripts/seed_broker_pdfs.py::generate_msa_regio` (verified line 67, returns bytes of Constructora Regio MSA PDF) with runtime-generate snippet + seed_broker_demo.py fallback + rationale for no-binary-commit (binary churn + PII ambiguity + byte-determinism invariant). Task 2 created `DOGFOOD-RUNBOOK.md` (222 LOC) with 5 numbered user-executable steps covering SC1 + SC2 manual evidence capture: Step 1 belt-and-suspenders rename (`if [ -d ... ] && [ ! -d .bak ]`), Step 2 fresh Claude Code session + PDF upload + `/broker:parse-contract <id>` + watch for flywheel_fetch_skill_assets tool invocation + summary, Step 3 DB verification via `curl ... | jq '{coverages: .coverages | length}'` expecting >=1, Step 4 offline simulation via harness `--offline-sim --skip-rename` expecting stderr "WARN: Backend unreachable. Using cached broker-parse-contract bundle" + exit 0, Step 5 unconditional cleanup insurance `mv .broker.bak → broker/` if still present. Evidence paste-back template with 3 fenced code blocks (CC transcript, DB JSON, offline stderr+exit code). 9-row troubleshooting table mapping symptoms (stale login/missing skill/backend down/tampered cache/expired cache/missing WARN/hang/atexit didn't fire/ModuleNotFoundError) to one-line fixes. Verification passes: `python -c "import ast; ast.parse(...)"` OK, `python dogfood_harness.py --help` shows all 4 flags, shebang `#!/usr/bin/env python3`, 0755 executable bit set, 459 LOC (well above 250 min), 222 LOC runbook (well above 80 min), 72 LOC fixtures README (well above 30 min), `grep -c "^## Step [1-5]" DOGFOOD-RUNBOOK.md` returns 5, `FLYWHEEL_API_URL=http://127.0.0.1:1` appears 2x, `broker.bak` 7x, `WARN: Backend unreachable` 4x, `generate_msa_regio` 6x in fixtures/README.md. Import smoke via `sys.path.insert + import dogfood_harness`: hasattr(main)=True, hasattr(STEPS)=True, len(STEPS)=5, STEPS names == [broker-parse-contract, broker-parse-policies, broker-gap-analysis, broker-select-carriers, broker-draft-emails] exactly. Deviations: (1) [Rule 1 — Bug] Plan's harness stub code called `materialize_skill_bundle(metadata, bundles)` (2-arg) but actual Phase 150 signature at cli/flywheel_mcp/bundle.py:187 is `materialize_skill_bundle(name: str)` (1-arg, context manager internally constructs FlywheelClient + calls fetch_skill_assets_bundle). 2-arg call would raise TypeError blocking first probe. Fix: `_materialize_and_probe(skill_name)` calls `materialize_skill_bundle(skill_name)` single-arg form; replaced hard-coded api_client.py + field_validator.py asserts with generic `py_files = list(tmp.rglob("*.py"))` count + conditional print to avoid brittle file-set coupling. (2) [Rule 2 — Critical functionality] Plan didn't spec repo-root auto-detection but harness invoked from `.planning/phases/.../scripts/` can't resolve `from flywheel_mcp.api_client import FlywheelClient` without PYTHONPATH=cli or cd cli/ — hostile UX. Fix: added `_REPO_ROOT = Path(__file__).resolve().parents[3]` + `sys.path.insert(0, str(_REPO_ROOT / "cli"))` at module top after constants. Troubleshooting table row 9 documents ModuleNotFoundError → cd repo root fallback. (3) [Rule 3 — Blocking] `.planning/` gitignored by default (.gitignore:39) — naive `git add .planning/phases/...` silently no-ops producing empty changeset. Prior 151-01/151-02 commits had cli+backend code to commit so didn't hit this; Plan 03 is doc-only with zero production code. Fix: used `git add -f` to force-add the 3 Plan 03 deliverables; commit message documents the force-add + reason for future readers. Single atomic commit per commit_strategy=per-plan: 272abca "feat(151-03): dogfood harness + user-executable runbook" (3 files / 753 insertions, all under .planning/). SUMMARY.md at `.planning/phases/151-broker-dogfood-resilience/151-03-SUMMARY.md`. **Plan 04 receives:** dogfood_harness.STEPS canonical 5-step enumeration importable as `from dogfood_harness import STEPS` — Plan 04's measure_latency.py should iterate over this, NOT re-declare, to prevent drift; canned tool_use fixtures reusable for save-path latency timing; fixtures/README.md generator command gives latency script byte-deterministic MSA without its own fixture decision; locked `WARN: Backend unreachable.` assertion pattern reusable if latency script ships offline-mode measurement.

Previous activity: 2026-04-18 -- 151-02 COMPLETE. Task 1 created `cli/flywheel_mcp/errors.py` (81 LOC) with 6 locked user-facing error-message constants (7 counting 503 retry + terminal): `ERR_401 = "Session expired. Run \`flywheel login\` and retry."`, `ERR_403 = "Skill not licensed for your tenant. Contact your admin."`, `ERR_404_TEMPLATE = "Skill not found: {name}. Check spelling or run \`flywheel_refresh_skills\`."`, `ERR_503_RETRY_TEMPLATE = "Flywheel backend unreachable. Retrying in {delay}s..."`, `ERR_503_TERMINAL = "Flywheel backend unreachable after 3 attempts. Retry in a moment or run \`flywheel_refresh_skills\` when online."`, `ERR_CHECKSUM_TEMPLATE = "Bundle integrity check failed for {skill}. Run \`flywheel refresh-skills\` to re-fetch."`, `ERR_OFFLINE_EXPIRED = "Cached bundle expired (>24h) and backend unreachable. Connect to network and retry, or run \`flywheel refresh-skills\` when online."`. All match CONTEXT §Error taxonomy byte-for-byte; CONTEXT's dual-form invocation copy preserved exactly: UNDERSCORED `flywheel_refresh_skills` (MCP-tool form) in 404+503-terminal, HYPHENATED `flywheel refresh-skills` (CLI-subcommand form) in checksum+offline-expired. `ALL_ERROR_MESSAGES` convenience list exports 7 pre-interpolated (id, msg) tuples for parametrized regression tests. Wired into api_client.py: 401 terminal uses ERR_401, 403 uses ERR_403, 404 uses ERR_404_TEMPLATE.format(name=name), 503 retry stderr line emitted BEFORE each pre-retry sleep via `sys.stderr.write(ERR_503_RETRY_TEMPLATE.format(delay=delay) + "\n")` (user sees backoff progress in real time), 503 terminal raises `BundleFetchError(name, None, ERR_503_TERMINAL) from last_exc`, offline+expired cache raises `BundleCacheError(skill_name=name, reason=ERR_OFFLINE_EXPIRED)` replacing Plan 01's close-enough placeholder. Extended BundleIntegrityError with keyword-only `reason: str | None = None` kwarg (preserves Phase 150 default shape with diagnostic SHA tail when None; allows cache._load_bundle_with_validation to plug in `reason=ERR_CHECKSUM_TEMPLATE.format(skill=skill_name_hint)` verbatim). Added `test_error_messages.py` (382 LOC, 24 tests): 7 byte-exact constant tests with HARD-CODED literal RHS per plan's critical rule (hard-codes the expected string on the RHS of `==`, NOT the constant itself — catches drift where a tautology would false-green), 2 form-placement exclusivity tests (underscored-in-404+503-terminal AND absent-from-checksum+offline-expired; vice versa), 7 parametrized substring sanity checks, 6 integration raise-site tests mocking httpx.Response at 401/403/404/503 and tampered-cache, 2 ALL_ERROR_MESSAGES coherence tests. Task 2 registered `flywheel_refresh_skills(name: str | None = None) -> ToolResult` MCP tool in `cli/flywheel_mcp/server.py` (+85 LOC) immediately after `flywheel_fetch_skill_assets`. Return shape per plan: `ToolResult.structured_content = {evicted: int, refetched: int, tampered: int, per_skill: list[dict]}` with each per_skill entry `{name, old_sha, new_sha, status}` where status ∈ {refreshed, unchanged, new, tampered-healed, tampered, "error: ..."}. Stderr summary emits `OK: Refreshed N skills (M evicted, K tampered-auto-healed).` for no-arg and `OK: Refreshed <name>.` for per-skill. Delegates to `BundleCache.refresh(name, fetcher=client.fetch_skill_assets_bundle-bound-with-bypass_cache=True)` per Plan 01's caller-injected-fetcher pattern. Enhanced `cache.refresh()` with pre-refresh tamper detection pass (calls `_load_entry_from_index(skill_name)` BEFORE fetcher; on BundleIntegrityError the tampered `<sha>/` dir is already auto-deleted + index entry dropped; refresh emits `cache_entry_tampered: skill=... old_sha=... authoritative_sha=... correlation_id=...` stderr line, increments `tampered_count`, THEN calls fetcher to repopulate with authoritative bytes — per_skill status becomes `tampered-healed`). Preserved `except Exception as exc:` fallback inside per-skill loop so one offline skill failure doesn't crash the whole refresh walk (user sees error status on that skill but healed status on others). Added `test_refresh_skills.py` (594 LOC, 10 tests): no-arg walks all cached skills, with-name targets single skill, empty cache noop, tampered entry auto-heal with regex-match on stderr line shape, per_skill dict shape verification, stderr summary format (full + per-skill), backend-down error propagation, offline sim with fresh cache serves + WARN (via monkeypatch.setenv FLYWHEEL_API_URL=http://127.0.0.1:1 + httpx.ConnectError mock), offline sim with expired cache raises BundleCacheError(ERR_OFFLINE_EXPIRED). Full cli/tests/ suite: 104 passed / 6 skipped in 1.52s — zero regressions on Plan 01 cache tests (18/18 green) or Phase 150 tests (15 bundle + 5 MCP contract). MCP tool registration verified: `asyncio.run(server.mcp.list_tools())` returns 31 tools including both `flywheel_refresh_skills` + `flywheel_fetch_skill_assets`; `inspect.signature(server.flywheel_refresh_skills)` == `(name: 'str | None' = None) -> 'ToolResult'`. grep count: 14 `ERR_` references in api_client.py (well above 6-site minimum). Deviations: (1) [Rule 3 — Blocking] cache.refresh() needed pre-refresh tamper-detection pass to satisfy SC4 — original Plan 01 implementation only called fetcher and never SHA-validated cached bytes, so a tampered byte flipped in bundle.zip would go undetected during refresh (fetcher would overwrite with authoritative bytes, per_skill status would be 'new' not 'tampered-healed', no cache_entry_tampered line would fire). Fix: added `self._load_entry_from_index(skill_name)` call BEFORE fetcher; on BundleIntegrityError the loader auto-deletes the tampered dir + drops index + raises; refresh catches + logs + increments tampered_count + THEN calls fetcher. (2) [Rule 3 — Blocking] Restored `except Exception as exc:` fallback inside cache.refresh() loop after rewrite — without it a single network-down skill propagates BundleFetchError up through cache.refresh → server's except BundleError catch → returns ToolResult with only `content` not `structured_content`, breaking tests that expect per_skill error entries. (3) [Rule 1 — Bug] Changed `raise last_exc` to `raise BundleFetchError(name, None, ERR_503_TERMINAL) from last_exc` in _do_get retry-exhaustion path — preserves cause chain for operators while surfacing locked user-facing copy at the top. Single atomic commit per commit_strategy=per-plan: PENDING. SUMMARY.md at `.planning/phases/151-broker-dogfood-resilience/151-02-SUMMARY.md`. **Plan 03 receives:** byte-exact locked error copy for stderr regression assertions in dogfood smoke tests; `flywheel_refresh_skills(name?)` MCP tool available for cache-priming before 5-step broker dogfood; `cache_entry_tampered: skill=... old_sha=... authoritative_sha=... correlation_id=...` stderr forensic line live for surfacing any prod/cache integrity drift; pre-refresh tamper-detection pass proves belt-and-suspenders contract (cache SHA-validate on load + materializer SHA-validate before extract).

Previous activity: 2026-04-18 -- 151-01 COMPLETE. Task 1 (backend) added `?shas_only=true` query param on `GET /api/v1/skills/{name}/assets/bundle` as an early return AFTER the fanout walk + protected check (so 403 for protected deps still fires) but BEFORE the base64 encode — returns full response envelope with EMPTY `bundle_b64` per entry (signals shas-only mode to client with zero schema change; `bundle_b64: str` already unconstrained). Same endpoint now reads `X-Flywheel-Correlation-ID` request header and passes it as `extra={"correlation_id": cid}` to every `logger.info` emission in `get_skill_assets_bundle` (both the shas_only branch and the full-fetch branch emit structured field, falls back to `"-"` when absent). +56 LOC in `backend/src/flywheel/api/skills.py`. 4 new tests in `TestAssetBundleEndpoint` (shas_only empty bundle_b64 + shas_only SHA parity with full fetch + shas_only still 403 on protected + correlation-id `X-Flywheel-Correlation-ID=deadbeef` lands on LogRecord.correlation_id structured attribute). Task 2 created `cli/flywheel_mcp/cache.py` (644 LOC, stdlib-only): `BundleCache` class with content-addressed `~/.cache/flywheel/skills/<sha256>/` layout (bundle.zip + metadata.json + top-level index.json), 24h TTL, SHA-256 validate on every load (tamper → auto-delete `<sha>/` dir + drop index entry + raise `BundleIntegrityError`), 100MB hard cap LRU eviction with 80MB soft-cap stderr WARN, atomic writes via `_atomic_write` helper using `tempfile + os.replace`, `CacheEntry` + `RefreshResult` dataclasses, shared-SHA dedup (two skill names with identical bundle bytes share one `<sha>/` dir; `remove()` only deletes dir when no other skill references it), `extend_ttl_if_sha_match(skill_name, server_shas)` bumps cached_at on match without re-downloading, `refresh(name?, *, fetcher)` accepts caller-injected fetcher callable to sidestep `FlywheelClient` circular import. Cache dir perms 0o755 (default umask, NOT 0o700 per CONTEXT Claude's Discretion — bundle bytes already over HTTPS). 11 hermetic unit tests in `cli/tests/test_cache.py` (721 LOC) covering put/get-fresh-TTL/SHA-validate-on-load-+-auto-delete/atomic-put-survives-interrupt/LRU-eviction-under-hard-cap/soft-cap-WARN/extend-TTL-match-and-mismatch/shared-SHA-dedup/cache-dir-perms/metadata-schema. Task 3 rewired `FlywheelClient.fetch_skill_assets_bundle` signature to `(name, *, bypass_cache: bool = False, correlation_id: str | None = None)`. Added `_fetch_shas_only` private helper (best-effort, no retry) and `_has_any_cache_trace` staticmethod. Correlation ID auto-generated via `secrets.token_hex(4)` at entry if None; threaded through EVERY retry attempt with SAME value as `X-Flywheel-Correlation-ID` header (so backend logs tie 3 retries to single user-visible fetch). Cache flow: fresh hit → zero-network return; stale hit → shas_only pre-check + `extend_ttl_if_sha_match` on match → zero-bundle-bytes return; SHA mismatch → full refetch + cache repopulate. Offline fallback: ConnectError + cached (fresh OR stale) → serve bytes + stderr WARN (`WARN: Backend unreachable. Using cached {name} bundle (cached {age_human} ago, {ttl_remaining_human}).`); ConnectError + cache trace exists but unusable → `BundleCacheError` with close-enough-to-locked message (Plan 02 will assert exact copy in test_error_messages.py); ConnectError + no cache trace → raw `BundleFetchError` preserved. Cache write post-fetch wrapped in try/except — disk full/perm error logs warning but does NOT fail user fetch (best-effort semantic). +200 LOC in `api_client.py`. 7 integration tests added to `test_cache.py` covering all flow paths (fresh hit zero-network, stale+sha-match no-download, stale+sha-mismatch full-refetch, bypass_cache skips cache, correlation-id threaded across 3 retry attempts with `httpx.ConnectError` twice then success, offline+fresh cache + offline+stale-cache both serve with stderr WARN, offline+unusable-cache raises BundleCacheError). Deviations: (1) [Rule 3 - Blocking] Added `_has_any_cache_trace` staticmethod to classify offline errors correctly — plan spec said offline+expired → `BundleCacheError` but original `has_stale()` requires bundle.zip on disk; when test fixture deletes bundle bytes to simulate "stale beyond serving", has_stale returns False and wrong error class fires. Fix: peek at `index.json` entry regardless of bundle-bytes loadability so "had a cache, now unusable" correctly surfaces as `BundleCacheError`. Backend tests: 32 passed / 6 pre-existing failures unchanged (MockLead schema drift + SSE stream auth fixtures, baseline from Phase 150.1). CLI tests: 70 passed / 6 e2e skipped (opt-in); zero Phase 150 regressions. Signature verification: `inspect.signature(FlywheelClient.fetch_skill_assets_bundle)` shows `(self, name, *, bypass_cache=False, correlation_id=None)`. Cache dir perm verification: `oct(stat.S_IMODE(BundleCache()._dir.stat().st_mode))` == `0o755`. Single atomic per-plan commit: 55e1800 "feat(151-01): backend shas_only + cache module + FlywheelClient integration". SUMMARY at `.planning/phases/151-broker-dogfood-resilience/151-01-SUMMARY.md`. **Plan 02 receives:** `BundleCache.refresh(name?, *, fetcher)` method ready to wire to `flywheel_refresh_skills` MCP tool with caller injecting `FlywheelClient.fetch_skill_assets_bundle` with `bypass_cache=True`; `BundleCacheError` now ACTUALLY raised (was placeholder from Phase 150); correlation-ID plumbing end-to-end stable for any new error-class message body; backend `?shas_only=true` tested and ready for Plan 03 dogfood latency p99 < 50ms SLO measurement.

Previous activity: 2026-04-18 -- 150.1-04 COMPLETE. **Phase 150.1 is fully complete.** Info-10 pre-flight green: `grep -c "CAPTURED AFTER RE-SEED" skills/broker/MIGRATION-NOTES.md` returns 0 (Plan 03 re-seed captured real SHA 60cb34a0... at line 157). Task 1 flipped all 4 legacy broker endpoints to HTTP 410 Gone via a shared helper `raise_endpoint_deprecated(operation=...)` added to backend/src/flywheel/api/broker/_enforcement.py (+47 LOC): POST /projects/{id}/analyze → operation=contract-analysis, POST /quotes/{id}/extract → quote-extraction, POST /projects/{id}/draft-solicitations → solicitation-draft, POST /projects/{id}/draft-recommendation → recommendation-draft. All 4 handlers reduced to single `raise_endpoint_deprecated(...)` call bodies; kept `@router.post` decorators so URL resolution still works + FastAPI UUID path validation still fires (so garbage paths get 422 before 410, both client errors, acceptable). Deleted `_run_analysis` background-task wrapper in projects.py alongside the flip — it was the sole in-backend caller of the legacy `analyze_contract`. Kept `_get_project_pdfs` helper (line ~1300) because the NEW /extract/contract-analysis endpoint (from Plan 02) ALSO calls it. Task 2 DELETED 5 legacy engine functions: contract_analyzer.py lost `analyze_contract` (~570 LOC) + `extract_current_policies` (~290 LOC) for −864 LOC total (2064→1200); quote_extractor.py lost `extract_quote` (−327 LOC, 807→480); solicitation_drafter.py lost `draft_solicitation_email` (−59 LOC, 303→244); recommendation_drafter.py lost `draft_recommendation_email` (−62 LOC, 327→265). Total engine deletion: −1312 LOC. Import cleanup fell out: removed `import anthropic` from contract_analyzer + quote_extractor, removed `from anthropic import AsyncAnthropic` from solicitation_drafter + recommendation_drafter, removed newly-unused `base64`/`settings`/`get_engine_model`/`BackgroundTasks`/`asyncio` imports. Drive-by: DELETED `backend/src/flywheel/api/broker_legacy.py` (2911 LOC dead monolith — `grep -rn broker_legacy` confirmed zero downstream imports in backend/src + scripts/ + cli/). **Total source LOC removed: −4223 across 5 files (1312 engine + 2911 broker_legacy).** Warning-8 Truth-1 verified empty: `grep -rn "extract-current-policies\|current-policies" backend/src/flywheel/api/broker/` returns empty — no standalone HTTP endpoint ever existed; policy-extraction is a Pattern 3a operation via /extract/policy-extraction (Plan 02). Warning-8 Truth-2 verified empty: `grep -rn "def extract_current_policies" backend/src/flywheel/engines/contract_analyzer.py` returns empty — function deleted. Zero-AsyncAnthropic final invariant: `grep -rn "AsyncAnthropic\|Anthropic(api" backend/src/flywheel/engines/{contract_analyzer,quote_extractor,solicitation_drafter,recommendation_drafter}.py` returns empty across all 4 files. Zero `from anthropic import ... AsyncAnthropic` anywhere in engines/. New test file backend/src/tests/test_broker_410_cutover.py (111 LOC, 6 tests): 4 parametrized `test_legacy_endpoint_returns_410` cases asserting 410 + body markers (endpoint_deprecated / operation label / migrated_in=150.1 / replacement contains /extract/{op} and /save/{op}) against both raw FastAPI detail shape and Flywheel's global error envelope `message` shape; sanity tests asserting the 10 Pattern 3a endpoints still registered AND the 4 legacy paths still registered (but return 410). Extended test_broker_zero_anthropic.py with 2 new grep-guard classes: TestBrokerEnginesHaveNoAnthropicImport (3 tests: `AsyncAnthropic(` construction regex, `^from anthropic import ... AsyncAnthropic` multiline regex, `anthropic.AsyncAnthropic(` attribute-access regex — all against 4 engine files); TestFrontendDoesNotCallDeprecatedEndpoints (3 tests: 4 deprecated-path patterns w/ `/analyze(?![\-a-zA-Z])` negative lookahead to exclude still-live /analyze-gaps, X-Flywheel-Skill header count ≥ 4 Warning-5 minimum (found 6), `/extract/` path count ≥ 4 (found 8)). Plan 02's zero_anthropic_guards fixture hardened with hasattr-gated monkeypatch: patches activate only if the attribute still exists (today: no-op after deletions; tomorrow: re-fires if regression re-introduces the shape) — double defense with grep-guards (either source-level OR runtime sentinel catches a regression). All 17 tests in test_broker_zero_anthropic.py pass (11 runtime + 6 grep-guards). Combined Phase 150.1 regression: 63 tests passing in 1.35s across test_broker_zero_anthropic.py + test_broker_410_cutover.py + test_broker_extract_save_endpoints.py + test_subsidy_enforcement.py. Full suite: 340 passed / 46 failed / 16 errors vs Plan 03 baseline 328/46/16 → net +12 passing (= 6 × 410-cutover + 6 × grep-guards, matches Plan 04 additions exactly), zero new failures, zero new errors. Deviations: (1) [Rule 3] Regex /analyze\b false-positive on /analyze-gaps — fixed with negative lookahead; (2) [Rule 3] pytest.monkeypatch.setattr requires target attribute to exist — fixed with hasattr-gated patch wrapping preserving Plan 02 regression-guard semantics; (3) [Rule 2] Engine docstrings contained literal `AsyncAnthropic(` / `from anthropic import` substrings that tripped the new grep-guards — rewrote to "async Anthropic client" / "module-local import binding" phrasing; (4) [Rule 1] Removed 5 now-unused imports (BackgroundTasks, asyncio, base64, settings, get_engine_model). Warning-7 CI-invocation check — NO CI wiring found (no .github/, no Makefile, no scripts/ci*); documented in SUMMARY.md as deferred follow-up for Phase 151 or operator (grep-guards still run on every local `pytest src/tests/` invocation). Single atomic commit per commit_strategy=per-plan: (pending). SUMMARY.md at .planning/phases/150.1-cc-as-brain-enforcement/150.1-04-SUMMARY.md. **Phase 150.1 complete; handoff to Phase 151 Broker Dogfood.**

Previous activity: 2026-04-18 -- 150.1-03 COMPLETE. Task 1a rewrote skills/broker/api_client.py v1.0→v1.2 adding 10 async Pattern 3a methods (5 extract_* + 5 save_* pairs for contract-analysis / policy-extraction / quote-extraction / solicitation-draft / recommendation-draft) — every call emits X-Flywheel-Skill: <skill-name> header (13 emissions counted) so backend require_subsidy_decision sees a skill name on every request; _post/_get/_patch helpers extended with extra_headers kwarg (single source of truth for HTTP calls preserved, no fetch-logic duplication). All 10 broker-* consumer SKILL.md files version-bumped 1.1 → 1.2 (post-seed SELECT: zero v1.1 remain). Task 1b applied 6 Step N Pattern 3a rewrites (broker-parse-contract Step 4, broker-parse-policies Steps 4+5, broker-extract-quote Step 6, broker-draft-emails Step 3 per-carrier, broker-compare-quotes Pattern 3a note, broker-draft-recommendation Step 5) — each rewrite preserves numbered headers + surrounding prose and adds "Why this is different from v1.1" epilogue explaining the compute-boundary shift (backend-pays LLM → Claude-in-conversation inline tool-use). broker-fill-portal/gap-analysis/process-project/select-carriers got version-only bumps (no deprecated-endpoint references in their steps). Task 1c Blocker-3 pre-flight: `POST /skills/{name}/runs` EXISTS at backend/src/flywheel/api/skills.py:741 BUT start_run rejects web_tier=3 skills with HTTP 422 ("requires the local Claude Code agent"); all broker-* skills are web_tier=3, so a warm+enqueue flow would silently degrade at the enqueue step — explicitly forbidden by Blocker-3 invariant. Branch P3 (warm + Claude Code handoff) chosen: frontend/src/features/broker/api.ts migrated 4 call sites to /broker/extract/{contract-analysis,quote-extraction,solicitation-draft,recommendation-draft} with X-Flywheel-Skill header on every call (6 emissions total, exceeds Warning-5 minimum of 4); zero deprecated-path references remain (non-comment). Post-checkpoint UX extension: new ClaudeCommandModal.tsx (143 LOC) reusable component with pre-filled /broker:* slash command + copy-to-clipboard (execCommand fallback for http contexts) + Lumif.ai brand tokens (#E94D35 accent, rounded-xl, Inter) — wired via onHandoff opt-in on 4 hooks (useAnalyzeProject, useBrokerQuotes, useSolicitations, useDelivery) + CarrierSelection + DeliveryPanel, converting Branch P3's "silent extract-only" into a proper handoff (user sees pre-filled command + one-click copy instead of dead button). TODO(150.2) comments at all 4 call sites documenting follow-up work (add web_tier-safe broker enqueue path, swap to warm+enqueue). apiPost extended with optional extraHeaders arg in frontend/src/lib/api.ts. Task 2 destructive-prod approval gate: dry-run via `./backend/.venv/bin/python scripts/seed_skills.py --verbose --skills-dir skills --dry-run` computed expected SHA 60cb34a0e32ef96b3d1aae8f9ac94c1ab5eda9ece33a8b37fe5fb655ee78ce46 / 9283 bytes (delta +2044 vs Phase 149-02 SHA 217ebdc1/7239B) + row-count delta Updated: 11 Added: 0 Errors: 0; 3-option prompt (GO/HALT/GO-STAGED) presented with template reference to .planning/phases/149-broker-scripts-migration/149-02-SUMMARY.md (Warning-4 pre-flight: confirmed on disk); operator approved option-a GO. Task 3 real prod re-seed executed: `./backend/.venv/bin/python scripts/seed_skills.py --verbose --skills-dir skills` exit 0 with Summary: Added 0 / Updated 11 / Unchanged 12 / Orphaned 36 (pre-existing, informational) / Errors 0; single atomic COMMIT at end of skill processing (Phase 147 all-or-nothing invariant held). Post-seed verification SQL (via ./backend/.venv/bin/python + sys.path+.env shim replicating seed_skills.py bootstrap): broker,1.2,9283,60cb34a0e32ef96b3d1aae8f9ac94c1ab5eda9ece33a8b37fe5fb655ee78ce46 — SHA byte-matches dry-run capture, Phase 147 ZipInfo invariant (epoch 1980-01-01, sorted alphabetical walk, external_attr=0o644<<16) empirically preserved through v1.0 → v1.2 bundle upgrade; 10 broker-* consumer rows on version=1.2 with bundle_size_bytes NULL / bundle_sha256 NULL (correct per Phase 147 assets:[] contract — consumers depend on broker library via single-level fanout). MIGRATION-NOTES.md <CAPTURED AFTER RE-SEED> placeholder replaced with actual SHA 60cb34a0... in same atomic commit. Deviations: (1) [Post-checkpoint UX upgrade] Branch P3 silent {status:'extract-only'} replaced with ClaudeCommandModal — operator evaluated dead-button UX and approved superior handoff; zero typecheck errors in 7 touched files (baseline 18 pre-existing unrelated errors in ComparisonGrid/ComparisonTabs/etc. unchanged); (2) [Rule 3 — Blocking] Library version jumped 1.0 → 1.2 (skipped 1.1) — plan assumed broker library was on v1.1 matching consumers but on disk was v1.0 (Phase 149 did not separately version the library); bumped directly to v1.2 to align library SHA bump with consumer v1.1→v1.2 step. Single atomic commit per commit_strategy=per-plan: 93dd109. Evidence logs: /tmp/150.1-03-seed.log + /tmp/150.1-03-verify-sql.txt + /tmp/150.1-03-dryrun-summary.txt + /tmp/150.1-approval-summary.txt. SUMMARY.md at .planning/phases/150.1-cc-as-brain-enforcement/150.1-03-SUMMARY.md. Plan 04 receives: legacy /analyze + /quotes/{id}/extract + /draft-solicitations + /draft-recommendation still live on backend (additive invariant preserved); frontend is fully off deprecated paths; library v1.2 live on prod with new SHA; 5 legacy AsyncAnthropic call sites (contract_analyzer.py:796+1182, quote_extractor.py:339, solicitation_drafter.py:135, recommendation_drafter.py:153) ready for removal in Plan 04's coordinated 410-flip + delete + final regression pass; MIGRATION-NOTES.md SHA placeholder already filled (Plan 04 pre-flight confirms); one-to-one DM needed for 1 meeting-prep user w/o BYOK before Plan 04 deploy (blast radius from Plan 01).

Previous activity: 2026-04-18 -- 150.1-02 COMPLETE. Task 1 refactored 4 broker engine modules (contract_analyzer, quote_extractor, solicitation_drafter, recommendation_drafter) to expose Pattern 3a public helpers (build_*_prompt, *_TOOL tool schema dicts, persist_*) alongside legacy async functions that remain UNCHANGED for now (Plan 04 renames to _legacy and removes the 5 AsyncAnthropic call sites). Task 2 shipped 10 new endpoints: POST /api/v1/broker/extract/{contract-analysis,policy-extraction,quote-extraction,solicitation-draft,recommendation-draft} + matching /save/{op} — every handler's signature includes Depends(require_module('broker')) + Depends(get_tenant_db) + Depends(require_subsidy_decision) (Blocker-2 invariant verified by introspection script: 10/10 handlers carry the subsidy dep). Save endpoints are Pydantic-validated against tool_schema.input_schema, reject tool_schema_version mismatch with 400. Task 3 shipped 38 new tests across 2 files: test_broker_zero_anthropic.py (556 LOC, 11 tests) is the centerpiece regression harness — patches AsyncAnthropic at FIVE module-local paths (contract_analyzer.anthropic.AsyncAnthropic + quote_extractor.anthropic.AsyncAnthropic for package-level calls, solicitation_drafter.AsyncAnthropic + recommendation_drafter.AsyncAnthropic for from-import shapes which ordinary patches miss, plus belt-and-suspenders package-level anthropic.AsyncAnthropic) with sentinel MagicMock whose instantiation raises AssertionError; asserts call_count==0 across all 10 endpoint flows + explicit env-var-leak regression test that sets ANTHROPIC_API_KEY=sk-ant-FAKE and hits solicitation + recommendation extract paths. test_broker_extract_save_endpoints.py (669 LOC, 27 tests) covers shape contract (prompt + tool_schema + documents + metadata), tool_schema_version rejection, enforcement on BOTH extract AND save (Blocker-2 functional — 5 test_save_enforcement_blocker2_* tests proving each save endpoint returns 403 without BYOK or allowlisted skill), and OpenAPI schema guards. All 38 new tests pass; combined Phase 150.1 suite (Plan 01's 13 + Plan 02's 38) = 51 passing in 1.51s. Zero regressions vs baseline: 46 failed / 16 errors unchanged on main vs branch (identical pre-existing failure set: MockLead schema drift, MockInvite schema drift, DB-required seed/asset tests, rate-limit concurrent fixtures); net +38 passing. All 10 legacy AsyncAnthropic call sites remain present in engines (2+1+1+1=5 matches AUDIT §1c exactly — Plan 04 scope). Zero new runtime dependencies. Zero AsyncAnthropic imports/constructs in api/broker/*.py files (verified by test_no_asyncanthropic_in_broker_api_files + grep). Extract response shape locked as {prompt: str, tool_schema: dict (full Anthropic tool-use schema), documents: list[{file_id, filename, pdf_base64, document_type}], metadata: dict}; tool_schema_version=1.0 is the coupling point between extract and save. BYOK wire format body-based api_key per Plan 01; every save Pydantic model has api_key: str | None = None. Deviations: (1) [Rule 5 spec-observation] Kept legacy engine function names UNCHANGED (plan suggested _legacy suffix but plan also said "Plan 02 doesn't touch them [legacy endpoints]" — renaming would force touching the legacy endpoint call site at projects.py:1376, breaking the additive invariant; Plan 04 does the coordinated rename+delete). (2) [Rule 3] Added __iter__ to test _Result mock (solicitations._load_carrier_contacts iterates result directly not via .all()). (3) [Rule 3] Tests assert error envelope shape {error, message, code} not raw FastAPI {detail} — flywheel.api.errors.py wraps all HTTPException responses. Handoff to Plan 03: new endpoint paths under /api/v1/broker/extract/{op} + /save/{op}; new public helpers to import from engines (EXTRACTION_TOOL, POLICY_EXTRACTION_TOOL, QUOTE_EXTRACTION_TOOL, SOLICITATION_TOOL, RECOMMENDATION_TOOL + matching build_*_prompt); legacy /analyze, /quotes/{id}/extract, /draft-solicitations, /draft-recommendation still live until Plan 04 flips them to 410; meeting-prep blast radius still 1 user from Plan 01 audit. Single atomic commit per commit_strategy=per-plan. SUMMARY at .planning/phases/150.1-cc-as-brain-enforcement/150.1-02-SUMMARY.md.

Previous activity: 2026-04-18 -- 150.1-01 COMPLETE. Task 1 froze 15-site subsidy leak inventory + ran prod read-only meeting-prep SELECT (12 runs/30d, 0 anon, 1 user-without-BYOK affected) + POC-verified body-double-read works cleanly on pinned stack (fastapi=0.135.1, starlette=0.52.1) → recommended body-based `api_key` BYOK wire format. Task 2 shipped config.py +8 LOC (subsidy_allowed_skills: set[str] = {"company-intel"}), api/broker/_enforcement.py NEW 136 LOC (SubsidyDecision + require_subsidy_decision with BYOK-mechanism docstring), skill_executor.py ~line 530 refactor (hardcoded tuple → settings lookup + inline comment about _execute_web_run), test_subsidy_enforcement.py NEW 375 LOC / 13 tests all passing including test_skill_executor_blocks_gtm_skills_without_byok (Blocker-1 closure: gtm-pipeline + gtm-company-fit-analyzer + gtm-outbound-messenger real names). Zero regressions vs HEAD baseline. [Rule 5] Kept 2 unrelated tuples at api/auth.py:84 + briefing.py:125 — onboarding-lifecycle semantics, not subsidy. Single commit: 45338a1. Audit+SUMMARY at .planning/phases/150.1-cc-as-brain-enforcement/150.1-01-{AUDIT,SUMMARY}.md.

Previous activity: 2026-04-18 -- 150-03 COMPLETE. Task 1 registered flywheel_fetch_skill_assets MCP tool with ToolResult return (CONTEXT override per RESEARCH Pitfall 5). Task 2 shipped 11 tests: 5 contract (hermetic, 0.57s) + 6 E2E (live prod, 10.08s, opt-in via -m e2e). All 5 Phase 150 ROADMAP SCs green with evidence. Byte-match verified: wire broker SHA == Phase 149 capture (217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f / 7239B). Zero regressions (52 passed / 6 skipped across full cli/tests/). Zero new runtime deps. cli/tests/conftest.py added for e2e marker registration with auto-skip pytest_collection_modifyitems pattern. Prod UAT backend DNS unreachable from dev machine — used documented local-backend fallback (same prod Supabase) per plan critical directive #6. Single commit per commit_strategy=per-plan: 28271f3.

Previous activity: 2026-04-18 -- 150-02 COMPLETE. Task 1 shipped 4 exception classes + FlywheelClient.fetch_skill_assets_bundle method. Task 2 shipped materialize_skill_bundle context manager + _safe_extract helper + 15 unit tests. Verify-before-extract ordering asserted structurally via monkeypatched zipfile.ZipFile with call_count==0 on SHA mismatch. 4 zip-slip attack variants all rejected. sys.path lifecycle uses .remove() by value in finally block. TemporaryDirectory without ignore_cleanup_errors (SC5 zero-byte invariant). 401 handling exactly one-shot per Pitfall 9. Zero regressions (47/47 cli/tests/). Single commit: 81a1110.

Previous activity: 2026-04-18 -- 150-01 COMPLETE. Operator approved GO at destructive-prod gate after reviewing proposed ALTER TABLE + re-seed plan. ALTER TABLE skill_definitions ADD COLUMN IF NOT EXISTS depends_on TEXT[] NOT NULL DEFAULT '{}'::text[] applied to prod Supabase via per-statement-commit script, then stamped alembic_version='065_depends_on_column'. Re-seed: exit 0 with depends_on written unconditionally via ON CONFLICT DO UPDATE. backend/src/flywheel/api/skills.py +230 LOC (fanout endpoint w/ graphlib.TopologicalSorter + server-side SHA re-hash + rollup_sha). backend/src/tests/test_skills_api.py +430 LOC (14-test TestAssetBundleEndpoint). Live smoke against prod confirmed byte-identity with Phase 149 capture. Commit 3fe18bb.

Progress: [██████████] 95% (6/7 phases complete + Phase 151 CODE-COMPLETE (all 4 plans shipped) + Phase 152 Plans 01/02/03 of 4 complete (legacy skill surface annotated + dead backups deleted + install READMEs scrubbed in Plan 01; legacy-skills-final retirement-anchor tag pushed to origin + lumifai in Plan 02 with option-d deferral of external per-skill repo archival as residual task in Plan 04 PR; MCP-only Skill Delivery block added to root README in Plan 03); Phase 150.1 CC-as-Brain Enforcement COMPLETE + Phase 151 cache foundation + correlation-id plumbing + error-copy lock + flywheel_refresh_skills MCP tool + dogfood harness + user-executable runbook + latency regression baseline shipped; phase-verify human gate pending: DOGFOOD-RUNBOOK.md execution + 151-LATENCY.md review; residual task (Sharan0516/claude-skill-web-scraper archival) to be carried in Plan 04 retirement PR description; Phase 152 Plan 04 (telemetry gate + retirement PR) remaining)

### In-Flight from v21.0

v21.0 Document Viewer has one open loop: Phase 145 Plan 04 Task 4 human-verify checkpoint (scenarios A/B/C/D). Resume at .planning/phases/145-stage-1-intake-correctness-typed-upload-and-gap-completion/145-04-PLAN.md. Not blocking v22.0 — no code dependency.

## Performance Metrics

**Previous milestones:**
- v15.0 Broker Module MVP: 8 phases, 25 plans
- v16.0 Briefing Intelligence Surface: 2 phases, 4 plans
- v17.0 Broker Frontend: 7 phases, 16 plans
- v18.0 Broker Data Model v2: 4 phases, 11 plans
- v19.0 Broker Redesign: 7 phases, 16 plans
- v20.0 Coverage Taxonomy: 1 phase, 4 plans

**v22.0 plan-level execution metrics:**
| Phase | Plan | Duration | Tasks | Files | Commit |
| --- | --- | --- | --- | --- | --- |
| 152-retirement | 01 | 6m | 3 | 35 | 2c4450d |
| 152-retirement | 02 | 15m | 3 | 2 | (pending — per-plan SUMMARY+STATE commit below) |
| 152-retirement | 03 | 12m | 3 | 1 | 92e2a42 |

## Accumulated Context

### Decisions

All v1.0-v20.0 decisions archived in PROJECT.md Key Decisions table.

- v22.0 152-02: Task 2 checkpoint:decision resolved to **option-d** — defer per-skill external repo archival. `Sharan0516/claude-skill-web-scraper` is a standalone skill repo outside the v22.0 monorepo consolidation scope; archival requires GitHub UI (no first-class `gh` CLI for repo archive without admin token) and is non-blocking for Phase 152 close. Documented as a residual task in Plan 04's retirement PR description for post-merge user action with exact instructions (Settings → Danger Zone → "Archive this repository") + evidence format (archive timestamp + `curl -s https://github.com/Sharan0516/claude-skill-web-scraper | grep "This repository has been archived"` verification). SC1 is satisfied by the `legacy-skills-final` git tag (immutable rollback anchor) on both GitHub remotes; the external-repo archive step is belt-and-suspenders hardening beyond SC1's tag requirement, not a correctness prerequisite.
- v22.0 152-02: Per-plan commit convention for plans whose primary artifact is a git ref (not file changes): the commit captures SUMMARY.md + STATE.md updates only (the tag push IS the primary plan artifact and has no file footprint in the working tree). Stage ONLY the SUMMARY + STATE files; never use `git add .` or `git add -A`. `.planning/` is gitignored, so both files require `git add -f` (matches 151-03/151-04 precedent).
- v22.0 152-03: Root README.md was untracked at plan start; Skill Delivery block inserted after intro paragraph and before ## Stack (no pre-existing Installation heading). Decision rationale per plan Task 2 rule 2 — and creates the repo's first tracked README.md pointing onboarding readers at MCP-only delivery.
- v22.0 152-03: SC4 gate extended by one `grep -v` clause to filter the Task 2 verbatim denial sentence ("No per-skill cloning, unzipping, or path management is required"). The gate's stated criterion is "no instructions to git clone / git pull / unzip"; the denial sentence contains `unzipping` as a negation, not an instruction. Extended filter honors stated intent over literal substring match.
- v22.0 152-03: No code change to install flow or CLAUDE.md template needed — research §2 assertion re-verified via 4 assertion greps (all printed CLEAN). `scripts/install.sh`, `flywheel setup-claude-code`, and `cli/flywheel_mcp/templates/CLAUDE.md` are already MCP-only by construction.
- v22.0 152-03: SC3 classification buckets identified as a reusable pattern for future retirement/cleanup phases: (1) _archived/, (2) tests/, (3) annotated-with-sentinel-phrase, (4) non-skills false positives of broad regex patterns.
- v21.0 141-01: Named API function getDocumentRendition (not getDocumentDownload) for seamless future migration to /rendition endpoint
- v21.0 141-01: 45min staleTime for signed URL cache (Supabase URLs expire ~60min)
- v21.0 141-02: Single-page rendering for Phase 1a; zoom uses button group; PDF detection via metadata.documents mimetype
- v21.0 143-01: source_excerpt required in EXTRACTION_TOOL schema but source_page NOT required -- preserves scanned-PDF fallback path for NAV-04 all-pages search
- v21.0 143-01: pdf_by_name map computed once unconditionally and shared between primary-contract backfill and per-coverage source_document_id resolution -- single source of truth per analysis run
- v21.0 143-01: case-insensitive filename fallback added to _process_extracted_coverage -- defensive against Claude's occasional case normalization drift; warn-log on total miss for Plan 02/03 monitoring
- v21.0 143-01: TS field type is string | null (not string | undefined) on BrokerProject.source_document_id and ProjectCoverage.source_document_id -- matches backend JSON wire format (None serializes as JSON null)
- v21.0 143-02: FullDocumentViewer now controlled-component; activeFileId + currentPage + highlight lifted to AnalysisTab. onFileChange/onPageChange/onHighlightClear callbacks are the control surface. Shape is frozen for Plan 03.
- v21.0 143-02: cross-doc page-preservation fix -- the [activeFileId] reset effect no longer calls onPageChange(1); reset happens in onDocumentLoadSuccess guarded on out-of-range currentPage. Required so handleClauseClick's synchronous setActiveFileId + setCurrentPage batch survives React's re-render without clobbering the target page.
- v21.0 143-02: onClauseClick signature migrated from (clause: string) to (coverage: ProjectCoverage) -- handler needs source_document_id + source_page + source_excerpt + id; grep-verified no other callers.
- v21.0 143-02: active-card persistence policy -- doc switch clears both highlight AND activeCoverageId; Plan 03's 5s decay timer and scroll-away IntersectionObserver will clear ONLY highlight. Rationale: yellow highlight = short-lived nav aid; coral border = persistent selection marker.
- v21.0 143-02: highlight intent key = coverage.id + ':' + Date.now() -- repeat clicks on the same card produce a new key so Plan 03 effects re-fire (card click is NOT idempotent).
- v21.0 143-03: customTextRenderer chosen over SPEC §4.7 DOM-post-render -- memoized on [highlight?.excerpt] only; auto-reruns on page/excerpt change; documented override in FullDocumentViewer JSDoc.
- v21.0 143-03: all decay machinery (5s timer, scroll-away IntersectionObserver, NAV-04 search, scrollIntoView) guarded on highlight?.excerpt (not highlight) -- page-only jumps (scanned-PDF, excerpt=null) skip all decay/search so no <mark> churn on a non-visual jump.
- v21.0 143-03: 500ms setTimeout before IntersectionObserver.observe() -- matches smooth-scrollIntoView duration; avoids Pitfall 7 mount-fire where observer would immediately report !isIntersecting before scroll completes.
- v21.0 143-03: PDFDocumentProxy imported from top-level 'pdfjs-dist' (not deep path types/src/display/api) -- package.json re-exports it; typed state is PDFDocumentProxy | null, never any.
- v21.0 143-03: textLayerTick counter bumped in onRenderTextLayerSuccess -- re-keys the scroll-away observer effect on every fresh render without tying it to opaque react-pdf internals.
- v21.0 143-03: Bidirectional substring match (item in excerpt OR excerpt in item) in shouldHighlightItem -- ~90% visual coverage of GH wojtekmaj/react-pdf#306 phrase-span limitation; sub-item excerpt splitting explicitly out of Phase 143 scope.
- v21.0 145-01: document_type is a single scalar per POST, not per-file -- frontend sends one request per zone (research Pitfall 4); simpler than parallel Form arrays and maps cleanly to FastAPI Annotated[str, Form()] = "requirements".
- v21.0 145-01: Tenant-scoping retrofit inline on upload endpoint (not deferred) -- pre-Phase-145 latent cross-tenant read; Task 1's own plan step 4 already edited the same where-clause, and the new PATCH reference implementation required the filter for parity. Zero split-brain surface area.
- v21.0 145-01: PATCH body uses Pydantic Literal["requirements","coverage"] (not string + manual whitelist) -- framework returns structured 422; upload Form field uses manual check + 400 because Form scalars with defaults don't plug as cleanly into Literal without extra Annotated trickery.
- v21.0 145-01: PATCH clears `misrouted` flag on successful document_type change -- user asserting the correct zone IS the resolution of the soft warning; re-analysis can re-flag if genuinely wrong.
- v21.0 145-01: PATCH does NOT auto-trigger re-analysis -- `/analyze` is a 60s Claude call; auto-trigger would be expensive and surprising (Open Q 2 resolution from RESEARCH).
- v21.0 145-01: Spread-and-reassign `project.metadata_ = {**existing_meta}` matches key_links.pattern in plan frontmatter and the existing idiom at projects.py:1064; defensive vs. any future in-place mutation upstream even though existing_meta is already a fresh dict.
- v21.0 145-02: delete_stmt hoisted out of `if requirements_pdfs:` guard to run unconditionally -- coverage-only re-runs (broker removes MSA, keeps COI) would otherwise leave stale ai_extraction ProjectCoverage rows forever; `is_manual_override=False` filter preserved on the delete so broker edits survive.
- v21.0 145-02: `_format_taxonomy` factored out of `build_extraction_prompt`'s inline comprehension so `build_policy_extraction_prompt` can reuse identical taxonomy rendering -- prevents drift between the two passes' taxonomy views.
- v21.0 145-02: `_normalize_coverage_key` is strip+lower only, explicitly distinct from quote_extractor's `_normalize_coverage_type` which replaces underscores with spaces (display-name fuzzy matcher). Post-Phase-140 keys are canonical snake_case; exact match on the normalized value is the right shape.
- v21.0 145-02: `Decimal(str(limit_amount))` for ProjectCoverage.current_limit writes -- str() intermediate avoids float-binary drift (e.g., Decimal(0.1) vs Decimal('0.1')); wrapped in InvalidOperation/TypeError/ValueError catch.
- v21.0 145-02: Misrouted detected_type enums are mirror-image symmetric across passes: EXTRACTION_TOOL (requirements pass) emits ['coverage','quote','unknown'], POLICY_EXTRACTION_TOOL emits ['requirements','quote','unknown']. Each pass names what the OTHER kind of doc looks like, so frontend can render "This COI was uploaded to the Requirements zone; detected as coverage."
- v21.0 145-02: Policy-pass misrouted wins on merge collisions (`merged_misrouted.update(req_misrouted)` then `.update(policy_result.misrouted)`). File_id is PK-unique and partition makes collisions impossible; ordering is documented defensively.
- v21.0 145-02: Policy-pass failure is non-fatal for analyze_contract -- if extract_current_policies returns status=failed, log and continue so successful requirements extraction isn't lost to a coverage-pass API hiccup; orphan_policies + misrouted setdefault to empty so downstream reads are safe.
- v21.0 145-02: Initialize coverages_created/contract_language/contract_summary/req_misrouted BEFORE the `if requirements_pdfs:` guard so always-run metadata writes + return dict have well-defined fallback values; contract_language defaults to project.language or 'es', contract_summary to ''.
- v21.0 145-02: 185-191 guard text preserved byte-for-byte per critical-notes; new misrouted_documents instruction paragraph APPENDED after the guard, not interleaved -- keeps the semantic safety net intact (Claude hitting "DO NOT extract" from top-down prompt read) even if the structured field is ignored.
- v21.0 145-03: Semantic anchor re-location over trusting plan line numbers -- Plan 03 referenced lines 766/769/788 from pre-Plan-02 snapshot; Plan 02's +893/-142 shift moved them; used `grep -n` on anchor tokens to find actual positions (1441 flush, 1490 activity, 1421 project_meta rebuild, 1384-1386 merged_misrouted, 1141 delete_stmt flush).
- v21.0 145-03: is_manual_override preservation in inline detect_gaps requires `if 'gap_status' in updated` guard on ORM assignment -- detect_gaps passes manual-override rows through as shallow copies WITHOUT setting gap_status/gap_amount keys; naive `.get(None)` would clobber broker manual values. Essential correctness fix vs. plan's sample code.
- v21.0 145-03: Task 2a's pre-reset is UNCONDITIONAL; Task 2b's misrouted-persist is CONDITIONAL on `if merged_misrouted` -- clean analyses skip pointless second JSONB rebuild (one reset flush only); misroute analyses do two writes (reset + persist). Step 11's always-run project_meta reassignment handles metadata writes independently.
- v21.0 145-03: Inline coverage serialization (4-field dict at call site) instead of importing projects._coverage_to_dict -- contract_analyzer in engines/ must not depend on api/broker/ (reverse layering); inlined shape is minimal (only fields detect_gaps reads).
- v21.0 145-03: Re-query ProjectCoverage inline before detect_gaps (not reuse coverages_created list) -- coverages_created is list-of-dicts from extraction, not ORM objects. gap_status/gap_amount writes need ORM handles; single order_by(created_at) reload captures post-policy-pass state (inserts from req pass + current_* updates from policy pass).
- v21.0 145-03: gap_summary "unknown" count can be non-zero and is CORRECT -- rows with required_limit=None get gap_status="unknown" (string) per gap_detector.py:49-51, which satisfies "no NULL gap_status" invariant (NULL column ≠ string "unknown"); frontend renders as "data missing" copy.
- v21.0 145-03: Ordering invariant between Task 2b and Step 11 is load-bearing -- Task 2b's misrouted-flag write MUST happen BEFORE Step 11's `project_meta = dict(project.metadata_)` shallow copy, or the rebuild silently drops the flags. Documented explicitly in the Task 2b block's inline comment for future editors.
- v21.0 145-04: DocumentEntry centralization to types/broker.ts over per-consumer definition -- AnalysisTab previously had a narrower 3-field subset (file_id/name/mimetype) vs OverviewTab/DocumentUploadZone's 6-field versions; centralized version is a strict superset PLUS the Phase 145 fields (document_type, misrouted, storage_path). Reduces drift risk AND fixed 3 pre-existing narrow-type TS errors as a side effect (baseline: 21 errors, post: 18).
- v21.0 145-04: DocumentZoneKind literal union exported from types/broker.ts (not api.ts) -- zone is a domain concept the UI reasons about, not just a wire-format detail; all three consumers (api.ts + both hooks) import from types.
- v21.0 145-04: useDocumentUpload default kind='requirements' matches backend POST default from Plan 01 -- older/forgotten callers silently send requirements (safest default: backend extraction handles it AND user can move via chip; silent miscategorization of coverage would be worse).
- v21.0 145-04: Misrouted chip palette is amber (border-amber-300 / bg-amber-50 / text-amber-800), NOT red -- per user's global design-guidelines Lumif.ai warn aesthetic; misroute is recoverable context not blocking error; harmonizes with #E94D35 primary.
- v21.0 145-04: otherKind computed once at component top (`kind === 'requirements' ? 'coverage' : 'requirements'`) -- single line to extend for any future third zone; ternary is explicit about current two-zone scope.
- v21.0 145-04: stopPropagation on chip click essential -- file card itself has onClick that opens signed-URL download in new tab; without stopPropagation, clicking Move would simultaneously fire download handler (visual bug).
- v21.0 145-04: File-card React key upgraded from `doc.name ?? idx` to `doc.file_id ?? doc.name ?? idx` -- after useDocumentMove query invalidation same file appears in different zone's list; file_id is backend PK and stable across zone moves (PATCH rewrites document_type, not file_id).
- v21.0 145-04: Skill docs are DOCUMENTATION-ONLY additive (markdown blockquote callouts near headings) -- matches research Finding 1/7 locking in "skills are clipboard-paste handoffs"; Phase 145 flow routes through frontend+backend not skill commands; callouts searchable by "Phase 145" tag for future removal.
- v21.0 145-04: npm run build INTENTIONALLY skipped as verification -- baseline on main has 21 pre-existing TS errors across 5 unrelated files (GapCoverageGrid/ComparisonGrid/etc); `tsc -b --noEmit` filtered for touched files is the correct verification shape for a dirty baseline. My changes reduced total to 18 (centralization fixed pre-existing inline narrow-type drift).
- v22.0 146-01: skill_assets.updated_at has NO `onupdate` and NO trigger -- matches parent SkillDefinition convention (zero onupdate uses in backend/src/flywheel/db/). Phase 147 seed code must set `updated_at = now()` explicitly on upsert. Conscious deviation from CONTEXT.md's `onupdate=func.now()` prescription; deviating from zero-precedent would introduce new SQLAlchemy import style for no reliability gain given centralized writer.
- v22.0 146-01: Defensive `CREATE EXTENSION IF NOT EXISTS pgcrypto` as first STATEMENT in apply_064 script -- Supabase has it pre-enabled (41 gen_random_uuid() uses, 0 CREATE EXTENSION statements) but local Docker Postgres may not; IF NOT EXISTS = no-op on Supabase. Resolves RESEARCH Open Q2.
- v22.0 146-01: Index naming uq_skill_assets_skill_id / idx_skill_assets_bundle_sha256 -- mirrors nearest-neighbor skill_definitions (uq_skill_defs_name / idx_skill_defs_*), NOT newer ix_broker_* style from 058. Skill-family tables share a convention.
- v22.0 146-01: CHECK (bundle_size_bytes >= 0) stays at DDL layer only, NOT mirrored in ORM __table_args__ -- matches broker-tables convention. ORM path for defensive validation lives at service/API layer.
- v22.0 146-01: Apply script stamp target = '064_skill_assets_table' EXACTLY, no _false/_migration suffix -- fixes the 062-template bug (Pitfall 3) where stamp drifted from migration revision. Plan 02 will UPDATE alembic_version via raw SQL.
- v22.0 146-01: Pre-existing alembic chain gap at 051_create_waitlist_table (referenced by 052 but file missing) -- blocks local alembic history/heads/current walk with KeyError. Second known gap at 062_broker_schema_mods_03 (referenced by 063). Neither fixed in Phase 146 scope; Plan 02 apply script bypasses alembic for the stamp step so the gap doesn't block prod. Separate chore-PR recommended before any future `alembic upgrade head` attempt.
- v22.0 146-01: SkillAsset first use of uselist=False (1:1 scalar) in models.py -- DB-level 1:1 enforced by unique index uq_skill_assets_skill_id on child's skill_id column (SQLAlchemy uselist=False is Python-side hint only, not a constraint). Cascade = 'all, delete-orphan' on parent side + ondelete='CASCADE' on FK child side = belt-and-suspenders delete propagation.
- v22.0 146-02: Preflight alembic_version check BEFORE apply is load-bearing -- 146-01 SUMMARY handoff flagged operator-pre-check-needed; verified prod was exactly '063_skill_protected_default' before running apply_064. If it had drifted (e.g., to a stale '062_*_false' name), halt + surface would have prevented stamping over a malformed chain position.
- v22.0 146-02: SC5 evidence via direct `SELECT version_num FROM alembic_version` on prod -- NOT `alembic current` which fails locally with KeyError('051_create_waitlist_table') per pre-existing chain gap. Direct SELECT is strictly stronger proof of the semantic (head revision on prod) and doesn't require a walkable local chain.
- v22.0 146-02: Idempotency proven by re-running apply_064_skill_assets_table.py, NOT `alembic upgrade head` -- both blocked by same chain gap. Re-run exercises every DDL statement (pgcrypto, CREATE TABLE, UNIQUE INDEX, INDEX, UPDATE alembic_version) as no-op via IF NOT EXISTS -- stronger than upgrade-head's passive chain traversal.
- v22.0 146-02: 3 regression tests (not 2) added to lock SC2/SC3/SC4 permanently -- test_bytea_round_trip + test_cascade_delete_removes_asset + test_unique_skill_id_rejects_duplicate. Third test cheap to add because admin_session + raw SQL makes one more assertion nearly free, and SC4 (unique index) is a ROADMAP criterion that deserves a pytest gate alongside SC2/SC3.
- v22.0 146-02: pytest.mark.postgres is DECLARATIVE for `-m "not postgres"` filtering, NOT auto-skip -- codebase convention per test_storage.py (34 errors on no-Docker). Tests structure + collection verified correct (3 tests + filter deselects 3); they'd pass on Docker Postgres with full test schema.
- v22.0 146-02: Upstream alembic chain gaps (051_create_waitlist_table, 062_broker_schema_mods_03) remain unfixed -- out of scope for Phase 146 + documented as chore-PR recommendation. Zero impact on prod (apply_064 bypasses alembic for stamp via raw SQL); only affects local `alembic history/current/upgrade head` commands.
- v22.0 147-01: on_conflict_do_update on skill_assets MUST use index_elements=['skill_id'], NOT constraint='uq_skill_assets_skill_id' -- Phase 146 created the uniqueness via CREATE UNIQUE INDEX (not ALTER TABLE ADD CONSTRAINT UNIQUE). Postgres's ON CONFLICT ON CONSTRAINT clause only resolves NAMED constraints; column-set inference via index_elements works for both shapes. Verified via SELECT conname FROM pg_constraint WHERE conrelid='skill_assets'::regclass — only pkey/fkey/check exist, no uq_skill_assets_skill_id. Future Phase 148+ ORM writers must use the same pattern or accept UndefinedObjectError.
- v22.0 147-01: Deterministic DEFLATE zip via stdlib — ZipInfo(filename, date_time=(1980,1,1,0,0,0)) + external_attr=0o644<<16 + sorted entries + writestr(zinfo,bytes). Produces byte-stable SHA-256 across re-seeds without any external dep (no repro-zipfile). Constant is zipfile.ZIP_DEFLATED (past tense; NOT ZIP_DEFLATE — plan template had the typo, fixed inline).
- v22.0 147-01: Three-step per-skill write sequence honors PUBLISH-06 atomicity — (1) ensure skill_definitions row via ON CONFLICT DO NOTHING name-only insert; (2) UPSERT skill_assets (index_elements=['skill_id']) BEFORE prompt update; (3) full skill_definitions UPSERT with system_prompt + all fields. Single session.commit() at end means UnknownDependencyError raised in pass 2 (pre-commit) rolls back everything — zero partial writes guaranteed.
- v22.0 147-01: Two-pass depends_on validation: pass 1 builds discovered={s.name for s in parsed_skills}, pass 2 raises UnknownDependencyError(skill, missing) BEFORE any DB write. Library skills (_shared, gtm-shared) are BLOCKED from declaring depends_on (single-level fanout enforced in scan_skills). Consumers reference by raw name (e.g. 'depends_on: ["_shared"]', not 'skills/_shared').
- v22.0 147-01: Library-skill injection fires on DIR NAME (entry ∈ {'_shared','gtm-shared'}), not frontmatter name. Forces enabled=False, adds 'library' to tags via fresh list [*tags, 'library'] (NEVER .append on JSONB-bound list), provides "Library module, not directly invokable" default prompt if SKILL.md body empty.
- v22.0 147-01: Task 8 smoke against prod via isolated tmp-dir + prefix cleanup is the safer pattern when Docker Postgres local is unavailable — creates throw-away skills with 'phase-147-smoke-*' names, runs REAL seed pipeline against prod (exercises same connection pool + schema as production ops), captures log+SQL evidence for all 5 SCs, DELETEs all prefix-scoped rows in try/finally. Zero row leakage verified via post-cleanup SELECT. Template for any future phase needing write-path verification without local infra.
- v22.0 147-01: skill_assets.updated_at has NO onupdate and NO trigger (Phase 146 convention) — seed code explicitly sets "updated_at": func.now() on UPSERT. Phase 148+ writers MUST do the same or updated_at won't advance.
- v22.0 147-01: --skip-assets CLI flag DEFERRED (Claude's Discretion per CONTEXT.md) — plan marked it optional nice-to-have; not load-bearing. scripts/seed_skills.py signature preserved exactly. Future chore-PR can land it if developers need prompt-only iteration without bundle rebuilds.
- v22.0 147-01: parse_frontmatter docstring fixed as drive-by — was 'Returns: Dict of parsed frontmatter fields.' but actual return is (dict, str). Now 'Returns: Tuple of (parsed dict, full file content).' Zero behavior change; prevents future test authors from mis-binding the return.
- v22.0 148-01: Added optional version + updated_at fields to SkillAssetsResponse (Claude's discretion per CONTEXT) — ~60 bytes/response cost, future-proofs Phase 151 caching before Phase 150 clients start caching against the wire format; version sourced from SkillDefinition.version, updated_at from SkillAsset.updated_at. Impossible to add later without wire-format break.
- v22.0 148-01: Preserved 404 (NOT 403) for tenant-override denial to match get_skill_prompt parity — research-corrected from CONTEXT's original "403 if module missing" wording. get_skill_prompt uses implicit tenant_skills override filtering (empty result → 404), not explicit tenant.settings.modules 403 check. Introducing 403 here would create two inconsistent error shapes for "tenant can't access skill" across the endpoint family; if product intent ever requires observable 403 for module-denial, it becomes a separate cross-cutting phase touching both endpoints.
- v22.0 148-01: TWO separate db.execute() calls with 'if skill.protected' gate BETWEEN them — explicitly rejected selectinload(SkillDefinition.asset) optimization because eager-loading would read bundle bytes for protected skills before the 403 check, defeating DELIVER-03. Inline code comment documents this as intentional non-optimization so future editors don't "fix" it. Protected-skill test asserts mock_db.execute.await_count==2 as regression guard.
- v22.0 148-01: bytes(asset.bundle) wrapper before base64.b64encode — asyncpg/psycopg drivers may return memoryview for LargeBinary; base64.b64encode(memoryview) is undefined across CPython implementations. Matches pattern at test_skill_assets_model.py:54 ('assert bytes(row.bundle) == payload'). Cheap defense; no known production hit but catches CI-only driver drift.
- v22.0 148-01: limiter.reset() added to EVERY test method in TestAssetEndpoint (not just the plan-specified rate_limit + requires_auth tests) — Rule 3 deviation. slowapi keys rate limits on user.sub; all 8 tests share TEST_USER_ID via _make_user(), so pytest reordering rate_limit BEFORE success would inherit an exhausted bucket and fail success with spurious 429. Class-scoped fixture hygiene pattern for any future TestClassFoo testing rate-limited endpoints.
- v22.0 148-01: TestClient + AsyncMock-based _mock_db pattern (NOT @pytest.mark.postgres admin_session) — consistent with existing 5 test classes in test_skills_api.py, runs in <1s, no Docker dependency. Postgres-backed belt-and-suspenders test deferred; Phase 146/147 tests already exercise the DB round-trip for bytea/cascade/uniqueness so API-layer tests don't need to re-prove DB behavior.
- v22.0 148-01: 404 messages must be distinguishable between "skill not found or not available for this tenant" (prompt-endpoint parity, skill missing from catalog or tenant_skills override) and "has no asset bundle — this is a prompt-only skill" (skill exists but SkillAsset row missing) — tests assert non-overlapping substrings to catch copy-paste pitfall called out in research §Pitfalls #5.
- v22.0 148-01: asset_fetch logger.info line mirrors prompt_fetch shape exactly — event name swap only, sha256 truncated to first 12 chars (no full-hash leakage to logs), same tenant_id + skill + user.sub PII boundary as prompt endpoint. No new metrics pipeline or /tmp/flywheel-asset-fetches.log file invented (research recommended against new observability pattern for single endpoint).
- v22.0 149-01: launch_browser(state_dir, headless=False) now returns (playwright_ctx, BrowserContext) from chromium.launch_persistent_context(user_data_dir=str(state_dir), ...) — NEW behavior, not a path rewrite. Research-corrected: current code uses ephemeral chromium.launch() with zero persistence. Brokers log in ONCE; profile lives at ~/.flywheel/broker/portals/<carrier>/. Caller signatures in SKILL.md prompt text adapt naturally (Claude reads new signature at exec time; no Python call sites to migrate within Phase 149).
- v22.0 149-01: portals/mapfre.py STATE_DIR = Path.home() / ".flywheel" / "broker" / "portals" / "mapfre" declared at module top with mkdir(parents=True, exist_ok=True) on import. take_screenshot piped through state_dir=STATE_DIR so artifacts centralize under <STATE_DIR>/screenshots/. _YAML_PATH = Path(__file__).parent / "mapfre.yaml" PRESERVED with inline comment documenting it as CODE ASSET (config travels with code in bundle; state goes to Path.home() — two different concerns).
- v22.0 149-01: Grep-guard (scripts/check_portal_state_paths.sh) scoped to 3 patterns: expanduser("~/.claude"), user_data_dir=...__file__ / state_path=...__file__ / _playwright_state substring, sys.path.insert.*\.claude. Deliberately NOT a broad __file__ ban — mapfre.yaml loader (Path(__file__).parent) must NOT false-positive per Pitfall 5. Smoke-tested: fails on synthetic `os.path.expanduser("~/.claude/skills/broker/_playwright_state")`, passes on current tree.
- v22.0 149-01: Broker library skill SKILL.md has NO depends_on: key — libraries never depend on libraries (single-level fanout enforced by seed.py per v22.0 147-01 decision). Library-skill injection fires on directory-name pattern, not frontmatter, so zero-depends_on libraries are forward-compatible with the injection contract.
- v22.0 149-01: All 10 broker-* consumers uniformly declare depends_on: ["broker"] — research's import-graph audit confirmed every broker-*/SKILL.md's dependencies.files: references ~/.claude/skills/broker/api_client.py (most also field_validator.py). No conditional wiring. broker-compare-quotes and broker-process-project retain their existing dependencies.skills: sibling-skill lists unchanged (those are documentation; depends_on: is ONLY for library-bundle fetch).
- v22.0 150.1-01: BYOK wire format = body-based `api_key` field (not header). POC verified body-double-read works cleanly on pinned fastapi=0.135.1 / starlette=0.52.1 — FastAPI caches request._body so a Depends reading via `await request.json()` does not consume the stream for downstream Pydantic body parameters. 50× stress + empty-body edge case both passed. Declared verbatim in _enforcement.py module docstring for Plan 02/03 consumers (Info-9).
- v22.0 150.1-01: Allowlist lives on Settings as `subsidy_allowed_skills: set[str]` (Pydantic-validated, not a module-level constant) — trivial to monkeypatch in tests and ops can override via env var in future without code change. Single source of truth: `settings.subsidy_allowed_skills == {"company-intel"}`.
- v22.0 150.1-01: Three distinct skill_executor tests cover different failure modes — `_allowlist_reads_from_settings` (monkeypatch allowlist to `{"foo"}`, prove meeting-prep raises), `_allowlist_reflects_real_settings` (prod default {"company-intel"}, prove meeting-prep raises), `_blocks_gtm_skills_without_byok` (Blocker-1 closure using real GTM names gtm-pipeline + gtm-company-fit-analyzer + gtm-outbound-messenger). Each catches a different regression shape.
- v22.0 150.1-01: meeting-prep removal safe to deploy — prod SELECT at 2026-04-18T09:58:40Z: 12 SkillRuns/30d, 0 anonymous users, 1 authenticated user WITHOUT BYOK affected. NOT a mass breakage; no rollback plan required; Plan 03 deploy gate can proceed with one-to-one DM to the 1 affected user.
- v22.0 150.1-01: 15-site leak inventory > 9-site CONTEXT estimate — audit found 13 direct `settings.flywheel_subsidy_api_key` references + 2 hidden `AsyncAnthropic()` env-var fallback shapes at solicitation_drafter.py:135 and recommendation_drafter.py:153 that the original 9-count missed. Plan 02 tests must patch at module path (not package path) because these two use `from anthropic import AsyncAnthropic` (name already bound at import time).
- v22.0 150.1-01: Two unrelated ("company-intel", "meeting-prep") tuples at api/auth.py:84 (_ONBOARDING_SKILLS for compute_lifecycle_state) and services/briefing.py:125 (_onboarding_skills for first-post-onboarding-run filter) are NOT subsidy — they're onboarding-lifecycle funnel semantics — and explicitly kept. A future phase that wants to decouple "onboarding skill" from "subsidy skill" can introduce `settings.onboarding_skills` as a separate Settings field. Not a Phase 150.1 concern per CONTEXT.md.
- v22.0 150.1-01: Inline comment in skill_executor.py allowlist gate clarifies _execute_web_run (line ~3273) is a SEPARATE BYOK-gated path that does NOT hit this subsidy fallback — prevents future reviewer from thinking web-path is a backdoor or attempting a "harmless simplification" that collapses both paths.
- v22.0 150.1-03: Branch P3 (warm + Claude Code handoff) chosen for frontend broker api.ts migration — `POST /skills/{name}/runs` EXISTS at backend/src/flywheel/api/skills.py:741 BUT rejects web_tier=3 skills with HTTP 422 ("requires the local Claude Code agent"); all broker-* skills are web_tier=3, so warm+enqueue would silently degrade at the enqueue step. Blocker-3 invariant forbids silent degradation, so frontend uses warm-only + explicit handoff. Adding a web_tier-safe enqueue endpoint was explicitly rejected as Branch P2 (consumer-side plan boundary); TODO(150.2) placed at all 4 call sites for the follow-up work.
- v22.0 150.1-03: ClaudeCommandModal (frontend/src/features/broker/components/shared/ClaudeCommandModal.tsx, NEW 143 LOC) is the reusable web-UI handoff surface for web_tier=3 skills — shows a pre-filled /broker:* slash command + copy-to-clipboard button (execCommand fallback for http contexts), Lumif.ai brand tokens (#E94D35 accent, rounded-xl 12px, Inter). Wired via onHandoff opt-in callback on 4 hooks + 2 components. Pattern reusable for any future web_tier=3 skill that needs a UI entry point.
- v22.0 150.1-03: api_client.py _post/_get/_patch helpers extended with optional extra_headers kwarg rather than duplicating fetch logic per Pattern 3a method — single source of truth for HTTP calls preserved. Threading X-Flywheel-Skill as `extra_headers={"X-Flywheel-Skill": skill_name}` on every extract_*/save_* call gives backend require_subsidy_decision a skill name on every request (13 emissions counted in file).
- v22.0 150.1-03: Broker library version jumped 1.0 → 1.2 (skipped 1.1) — plan assumed library was on v1.1 matching consumers, but on-disk SKILL.md was v1.0 (Phase 149 did NOT separately version the library; only consumers were bumped 1.0→1.1). Jumped straight to v1.2 to align library SHA bump with the consumer 1.1→1.2 step and avoid a dangling intermediate version. Documented in MIGRATION-NOTES.md narrative.
- v22.0 150.1-03: Post-checkpoint UX upgrade approved — Branch P3's initial `{status:'extract-only'}` silent return was replaced with ClaudeCommandModal after operator evaluated the dead-button UX. Principle: when Blocker-3 forces warm-only, the UX should make the constraint explicit (copy-paste command into CC) rather than look broken. `apiPost` extended with optional headers arg at the same time (frontend/src/lib/api.ts).
- v22.0 150.1-03: Destructive-prod approval gate cleared on second use of Phase 149-02 template — dry-run computed SHA 60cb34a0e32ef96b3d1aae8f9ac94c1ab5eda9ece33a8b37fe5fb655ee78ce46 / 9283 bytes (delta +2044 from 217ebdc1/7239B); operator approved option-a GO; real re-seed exit 0 with SHA byte-matching pre-computed capture exactly. Template is now proven reusable (Phase 149-02 → Phase 150.1-03); future seed/migration plans inherit it without re-derivation.
- v22.0 150.1-03: Phase 147 byte-determinism invariant (ZipInfo(date_time=(1980,1,1,0,0,0)) + sorted alphabetical walk + external_attr=0o644<<16) empirically preserved through v1.0 → v1.2 bundle upgrade — 3× consecutive in-memory runs produce identical SHA; post-seed prod SELECT byte-matches pre-seed dry-run capture. Confirms Phase 147 contract is stable across non-trivial bundle content changes (+2044 bytes of new Pattern 3a helpers in api_client.py) and safe to rely on for Phase 151 offline-cache contracts.
- v22.0 150.1-03: Consumer skills with assets:[] correctly have NO skill_assets row post-seed — 10 broker-* rows show bundle_size_bytes NULL / bundle_sha256 NULL in verification SQL. This is the intended Phase 147 assets:[] contract (consumers depend on broker library via single-level fanout, not their own bundle). Empty bundle_sha256 is CORRECT for these rows, not a regression; any future "cleanup" attempting to fill them breaks fanout.
- v22.0 150.1-03: Post-seed verification SQL re-used the Phase 149-02 pattern (./backend/.venv/bin/python + sys.path+.env shim replicating seed_skills.py bootstrap) — proven pattern for running prod SELECT when direct psql is unavailable to the agent context. Same session factory, same prod credentials, identical connection surface as the seed pipeline — guaranteed to read the same DB the seed just wrote. Future verification scripts should keep using this shim rather than requesting broadened file permissions.
- v22.0 149-01: version bump 1.0 → 1.1 (minor) signals non-breaking metadata change (added assets: + depends_on: keys, no prompt body change). Triggers seed.py's _CHANGE_FIELDS = ("version", "description", "web_tier", "system_prompt") change detector so the 10 rows get UPDATE on next prod seed.
- v22.0 149-01: scripts/validate_skills.py now walks the same tree as backend/src/flywheel/db/seed.py — SKIP_DIRS narrowed from {_archived,_shared,gtm-shared} → {_archived}, walk-loop startswith("_") filter dropped. REQUIRED_FIELDS.web_tier exempted for library skills (tags contains "library" or enabled:false). Plan step 2.4 said "don't change other constants" but this was a Rule 3 BLOCKING issue — without the web_tier exemption and startswith("_") drop, running validate_skills against the migrated tree fails on broker / _shared / gtm-shared.
- v22.0 149-01: broker bundle SHA-256 = 217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f (7239 bytes). Computed via in-process _build_bundle('skills/broker', ['*.py', 'portals/*.py', 'portals/*.yaml']) through backend venv (./backend/.venv/bin/python). Byte-identical to what Plan 02's real prod seed will write into skill_assets.bundle — verification SQL `SELECT encode(digest(bundle,'sha256'),'hex') FROM skill_assets WHERE skill_id = (SELECT id FROM skill_definitions WHERE name='broker')` must return exactly this hash. Similar captured: gtm-shared=6740d954b205/8251B, _shared=79819ec05c58/15588B.
- v22.0 149-01: System Python 3.9 (/usr/bin/python3) CANNOT run scripts/seed_skills.py — MappedAnnotationError on PEP 604 `Mapped[str | None]` in backend/src/flywheel/db/models.py. Must use ./backend/.venv/bin/python (Python 3.12, per backend/pyproject.toml requires-python >=3.12). Future dry-run invocations within this repo MUST use the backend venv; validation scripts (scripts/validate_skills.py) work fine on 3.9.
- v22.0 149-02: Prod seed via `./backend/.venv/bin/python scripts/seed_skills.py --verbose --skills-dir skills` succeeded with exit 0, Added: 3 (_shared/broker/gtm-shared), Updated: 10 (all broker-* 1.0→1.1), Unchanged: 10, Errors: 0. Single COMMIT at end of skill processing — Phase 147's "all-or-nothing per seed run" invariant held.
- v22.0 149-02: Bundle SHA-256 byte-determinism proven across machines/runs — prod skill_assets.bundle hashes match Plan 01's in-process _build_bundle capture byte-exactly (broker=217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f/7239, _shared=79819ec05c58f8c398fe5e639f6c65b536ee70fdad78ad415cf1d05511c653eb/15588, gtm-shared=6740d954b20593f7ecc947c589c214d75ee91e4b7356c7ae900524043e384c15/8251). Phase 147's ZipInfo(date_time=(1980,1,1,0,0,0)) + sorted entries + external_attr=0o644<<16 deterministic-zip invariant is confirmed stable for Phase 151 offline-cache contracts.
- v22.0 149-02: Observed side-effect — seed.py also upserts tenant_skills linkage (24+ INSERT ON CONFLICT DO NOTHING rows observed in /tmp/149-prod-seed.log) auto-wiring the 3 new library skills to every existing tenant. This is pre-existing seed.py behavior, not a Plan 02 change. Implication for Phase 150: no explicit tenant-wiring needed before the MCP tool can fetch broker bundle — catalog fetch for any tenant already includes broker/_shared/gtm-shared.
- v22.0 149-02: Consumer broker-* skills correctly have NO skill_assets row (verified: 10 rows with bundle_size=0, sha='(no bundle)' in post-seed SELECT) — Phase 147's `assets: []` → skip bundle write contract holds. Consumer invocation in Phase 150 MCP tool: fetch library bundle via depends_on: ["broker"] single-level fanout, extract to temp dir, prepend to sys.path at runtime.
- v22.0 149-02: Verification SQL ran via ./backend/.venv/bin/python + sys.path+.env shim replicating seed_skills.py bootstrap (direct psql unavailable because agent context cannot read backend/.env directly). Same session factory, same prod credentials, identical connection surface as the seed — guaranteed to read the same DB the seed just wrote. Recommended pattern for future verification scripts without expanding agent file permissions.
- v22.0 149-02: skills/broker/MIGRATION-NOTES.md placed INSIDE the library skill tree (not docs/ or .planning/) — so it ships with the bundle in Phase 150's MCP asset fetch. Research-correct: no cp -R _playwright_state snippet (pre-149 code used ephemeral chromium.launch without persistence; nothing to copy); only a defensive `mkdir -p ~/.flywheel/broker/portals/mapfre` command offered for operators who want to pre-seed the state directory.
- v22.0 149-02: Destructive-prod approval gate template established — (1) dry-run to prod with `--dry-run --verbose`, (2) extract summary to /tmp/{phase}-prod-dryrun-summary.txt, (3) compute SHA-256 diff against Plan N's capture, (4) present 3-option prompt (GO/HALT/partial) to operator, (5) wait for explicit response, (6) only then run real seed. Reusable for any future seed/migration plan touching prod Supabase — enforced by CONTEXT.md guardrail + user global memory preference ("Destructive DB ops require approval").
- v22.0 150-01: skill_definitions.depends_on ARRAY(Text) NOT NULL DEFAULT '{}'::text[] applied to prod Supabase via per-statement-commit apply_065_depends_on_column.py (Supabase PgBouncer workaround; same shape as Phase 146's apply_064). Column population: 10 broker-* consumers → depends_on=['broker'], 3 libraries (broker, _shared, gtm-shared) → depends_on=[]. alembic_version='065_depends_on_column'. ARRAY(Text) over JSONB deliberately avoids the "never .append on JSONB-bound list" SQLAlchemy mutation gotcha and matches the shape of tags/contract_reads/contract_writes.
- v22.0 150-01: seed.py Step 3 UPSERT values dict + on_conflict_do_update set_ block both include depends_on now. Write runs unconditionally via ON CONFLICT DO UPDATE — _CHANGE_FIELDS detector's "Unchanged" tally is informational only (accounting of version/description/web_tier/system_prompt drift); actual column values written every seed. Verified: post-reseed SQL confirms all 10 broker-* carry ['broker'] despite seed accounting reporting "Unchanged" for all 23 rows.
- v22.0 150-01: graphlib.TopologicalSorter (stdlib 3.9+) chosen over hand-rolled DFS for fanout walker — free CycleError with .args[1] carrying cycle nodes, 3 LOC vs ~20 for hand-rolled equivalent. Python 3.12 on backend/.venv; stdlib availability confirmed. static_order() returns deepest-first (library before consumer) which matches CONTEXT's wire-format contract.
- v22.0 150-01: _resolve_bundle_chain transitive lookups DROP the enabled=True filter — libraries are seeded enabled=false per Phase 147 contract so they'd 404 every broker consumer if the filter persisted. Inline comment in skills.py documents this as intentional non-fix: any future "cleanup" adding the filter back breaks fanout. Root-skill lookup DOES filter enabled=True (consumer must be directly invokable); only the transitive walker drops it.
- v22.0 150-01: Bundle bytes ship VERBATIM from skill_assets.bundle — no re-serialization on read path. Preserves Phase 147's deterministic-zip invariant + Phase 149's captured SHA byte-identity end-to-end (DB column → server memory → bytes() for memoryview-safe copy → hashlib.sha256 re-hash → base64.b64encode → HTTP body → client b64-decode → client SHA recompute → zip extract). Live smoke confirmed SHA=217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f (7239B) byte-matches Phase 149's in-process _build_bundle hash.
- v22.0 150-01: Server-side pre-ship SHA-256 re-hash happens in-line with bundle assembly: for each (skill_def, asset) in chain, compute hashlib.sha256(bytes(asset.bundle)).hexdigest() and compare against asset.bundle_sha256. Mismatch → logger.error with full forensic detail (tenant, root skill, offending skill, stored vs recomputed hashes, size) + HTTPException(500, "Bundle integrity check failed for '{skill_name}' on server (DB/storage corruption signal — operator incident logged)"). Forensic detail stays in logs; wire response names the skill but not the real hash (avoids leaking diagnostic data to pre-auth callers).
- v22.0 150-01: Consumer with assets:[] (like broker-parse-contract, which has no skill_assets row) is resolved in _resolve_bundle_chain for deps[] completeness but SKIPPED in the response bundles[] list via `if asset is None: continue`. Emitting an empty 0-byte consumer bundle would break byte-identity. Both n:1 (consumer asset-less) and n:2 (consumer + library both have assets — topological order: library first, consumer last) cases locked into the test suite.
- v22.0 150-01: rollup_sha = sha256("\n".join(f"{b.name}:{b.sha256}" for b in bundles).encode("ascii")).hexdigest() — enables constant-time equality checks on the whole response for Phase 152 cache layer. Single-value cache key avoids O(N) per-bundle hash comparison on repeat fetches. Deterministic test (`test_rollup_sha_deterministic_across_calls`) locks the invariant.
- v22.0 150-01: deps = [skill_def.name for skill_def, _ in chain if skill_def.name != skill.name] — derived from resolved chain (includes skills with no asset row) so deps is accurate even when the consumer itself has assets:[]. For broker-parse-contract, bundles=[broker] and deps=['broker'] — both consistent.
- v22.0 150-01: BundleEntry Pydantic model mirrors SkillAssetsResponse fields (sha256, size, format, version, updated_at, bundle_b64) but adds `name` field to tag each entry with its skill identity. Wire-format symmetry means any client that handles single-skill /assets responses can adapt to fanout /assets/bundle with minimal schema delta.
- v22.0 150-01: Protected-root short-circuit runs BEFORE _resolve_bundle_chain is invoked (matches Phase 148 DELIVER-03 pattern: no bundle bytes read for protected skills). Protected-in-chain check runs INSIDE the walker, during transitive-dep lookup — before any asset query for the protected skill. Test `test_protected_root_returns_403` asserts mock_db.execute.await_count == 2 (only override probe + root lookup; walker never runs). Test `test_protected_dep_in_chain_returns_403` asserts a 403 with both the consumer + library names in the error message.
- v22.0 150-01: limiter.reset() in EVERY TestAssetBundleEndpoint method (14 methods, 14 resets) — continues the v22.0 148-01 pattern. All tests share TEST_USER_ID via _make_user(), and pytest reordering could put test_rate_limit BEFORE success tests, leaving an exhausted bucket. Class-scoped fixture hygiene matters for rate-limited endpoint tests.
- v22.0 150-01: Rate limit stays at 10/minute (decorator @limiter.limit("10/minute")) matching Phase 148 get_skill_assets — CONTEXT DELIVER-06 parity lock. Fanout is heavier work than single-skill fetch (2-N DB queries + N SHA computations + N base64 encodes for an N-length chain), but consistency across the endpoint family matters more than micro-tuning per-endpoint limits at this rollout stage. Phase 152 cache layer can short-circuit the heavy work via rollup_sha key.
- v22.0 150-01: MockSkillDefinition.__init__ extended with depends_on kwarg (defaults to empty list) — backwards-compat for all pre-Phase-150 test methods that didn't pass the kwarg, forwards-compat for TestAssetBundleEndpoint which needs to mock a dep graph. Single-file change.
- v22.0 150-01: Destructive-prod approval gate format pattern stands up for DDL migrations too (not just seed changes) — operator was prompted before ALTER TABLE ran, GO response recorded, script then executed with pre-verify → DDL → post-verify → stamp UPDATE evidence trail captured to /tmp/150-01-apply.log. Same gate template from Phase 149's seed applied to Phase 150's schema evolution.
- v22.0 150-01: Live smoke via `cd backend && PYTHONPATH=src ./.venv/bin/python` (not from repo root) — the backend .env file lives at backend/.env and pydantic-settings `model_config = {"env_file": ".env"}` resolves relative. Running from repo root drops to localhost defaults and fails with "role 'flywheel' does not exist". Future Phase 150/151/152 verification scripts touching prod Supabase via backend venv must run from backend/ directory OR invoke scripts/seed_skills.py which has its own manual env-loader.
- v22.0 150-02: cli/flywheel_mcp/bundle.py is the locked path (NOT cli/src/flywheel/bundle.py from CONTEXT). Matches Plan 01's Path Correction + actual cli/pyproject.toml `packages = ["flywheel_cli", "flywheel_mcp"]` layout. CONTEXT.md typo flagged in Plan 02's path-correction-locked pre-flight; Plan 02's files honor REQUIREMENTS.md EXEC-01 authoritative wording.
- v22.0 150-02: Verify-before-extract ordering enforced STRUCTURALLY (not just by comment). test_integrity_check_detects_tamper monkeypatches flywheel_mcp.bundle.zipfile.ZipFile to count constructor invocations and asserts call_count == 0 after BundleIntegrityError fires on SHA mismatch. Any future refactor that reverses the order (extract-then-verify) or adds a zipfile call before hashlib.sha256 re-hash fails this test immediately — load-bearing architectural invariant.
- v22.0 150-02: _safe_extract helper pattern enforced by structural spot checks. verify step 3: `inspect.getsource(materialize_skill_bundle)` must NOT contain 'is_relative_to' (guard is helper-local). verify step 4: `inspect.getsource(_safe_extract)` MUST contain 'is_relative_to' AND '.resolve()'. Prevents future "convenience" inlining of the traversal guard that would couple it to the materializer and make audit harder.
- v22.0 150-02: Defensive backslash rejection in _safe_extract as pre-resolve check. On POSIX `..\..\evil.exe` is a safe literal filename (backslash is not a separator); on Windows the same string escapes. Rejecting any zip entry containing '\\' is fail-closed + cost-free (Phase 147 deterministic-seed pipeline uses forward slashes or no separator — legitimate bundles never ship backslash filenames). 4 parametrized tests lock all 4 attack variants including this one.
- v22.0 150-02: 401 handling is exactly ONE-SHOT per Pitfall 9. Initial _ensure_token + _do_get() → 401 → one more _ensure_token + one more _do_get() → still-401 → clear_credentials + BundleFetchError(401, "Session expired. Run `flywheel login` and retry."). Test asserts get_token called exactly 2x in fetch path (FlywheelClient.__init__ call reset to 0 first), clear_credentials exactly 1x, _client.get exactly 2x. No recursion possible.
- v22.0 150-02: BundleCacheError shipped EMPTY in Phase 150 — placeholder whose __init__(skill_name, reason) matches Phase 152's cache-layer spec. Defined at module top alongside its siblings so Phase 152 raises `BundleCacheError(name, "stale cache")` without adding a new public-API import (class already exposed in flywheel_mcp.bundle.__all__). Zero behavior in Phase 150; stable import surface for Phases 151/152.
- v22.0 150-02: TemporaryDirectory deliberately WITHOUT ignore_cleanup_errors=True. SC5 demands zero bytes remain on disk post-exit; silent cleanup failures would mask a leak. test_no_bytes_under_tmp_after_exit captures baseline `glob('/tmp/flywheel-skill-*')` set before the with-block and asserts post-exit glob is a subset (no NEW leaks; preexisting ones from parallel runs ok).
- v22.0 150-02: Bundle bytes ship VERBATIM through the client. base64.b64decode(entry["bundle_b64"]) in FlywheelClient.fetch_skill_assets_bundle returns raw zip bytes; materialize_skill_bundle re-hashes via hashlib.sha256(bundle_bytes).hexdigest() and compares to server-provided sha256. No intermediate buffer, no re-serialization — preserves Phase 147 byte-determinism / Phase 149 captured SHA end-to-end across backend → wire → client → hash check.
- v22.0 150-02: sys.path cleanup uses .remove() by value, not .pop(0). If user code mutated sys.path during the with-block (inserted more entries at index 0, or reassigned sys.path wholesale), .pop(0) would remove the wrong entry. .remove() finds the inserted path string; if absent (user replaced sys.path), ValueError is swallowed in the nested try — original exception (if any) propagates cleanly. test_sys_path_tolerates_user_mutation proves the contract.
- v22.0 150-02: Deferred FlywheelClient import inside materialize_skill_bundle's function body (NOT at module top). bundle.py is imported by api_client.py (for BundleFetchError); if bundle.py imported FlywheelClient at module load it would create a circular import. Lazy import at enter-time resolves cleanly and has zero runtime overhead after the first call (Python module cache).
- v22.0 150-02: Zero new runtime dependencies. cli/pyproject.toml unchanged. All bundle/api-client code uses stdlib (hashlib, io, sys, tempfile, zipfile, base64, time, contextlib, pathlib) + existing httpx + existing FlywheelClient. pytest was installed at dev-time via `./cli/.venv/bin/python -m ensurepip && -m pip install pytest` but is not a runtime dep.
- v22.0 150-03: flywheel_fetch_skill_assets MCP tool returns ToolResult(content=[File(data=b, format="zip", name=f"{skill}.zip")...], structured_content={skill, deps, rollup_sha, shas}) — NOT CONTEXT-locked tuple[dict, list[File]]. Rule-1 deviation pre-approved by plan critical directive #1 and justified by RESEARCH Pitfall 5 (fastmcp does not document tuple returns — verified against installed fastmcp.tools.base.ToolResult source in cli/.venv). CONTEXT intent preserved 1:1 (binary files + metadata in one tool response).
- v22.0 150-03: SHAs ride on ToolResult.structured_content, never on File attributes. RESEARCH Pitfall 4 verified against fastmcp.utilities.types.File.__init__(path, data, format, name, annotations) — no metadata kwarg exists. test_tool_source_has_no_file_metadata_kwarg uses inspect.getsource() to statically assert 'metadata=' absent between 'File(' and matching ')' in the tool's source. Belt-and-suspenders regression guard catches future editors who might want to "attach SHA to the File" — catches a class of mistake that type-checkers miss.
- v22.0 150-03: BundleError + FlywheelAPIError both caught at MCP boundary, mapped to ToolResult(content=[mt.TextContent(type="text", text=str(exc))]). Matches flywheel_fetch_skill_prompt's existing pattern of catching FlywheelAPIError and returning str(exc), adapted for the ToolResult return type. Claude sees clean actionable messages ("Skill 'bar' not found. Check the skill name for typos.") instead of Python tracebacks. FLYWHEEL_DEBUG=1 env var gates logger.exception(...) for stderr traceback — user-facing ToolResult message stays clean regardless of debug state.
- v22.0 150-03: E2E tests marker-gated via cli/tests/conftest.py with auto-skip. pytest_configure registers the e2e marker (silences "unknown marker" warnings); pytest_collection_modifyitems auto-marks every @pytest.mark.e2e test with `pytest.mark.skip(reason="...")` UNLESS the run's markexpr contains "e2e". Default `pytest cli/tests/` needs no prod creds; opt-in via `pytest cli/tests/ -m e2e`. Mirrors backend Phase 146/148 pattern of gating live-infra tests.
- v22.0 150-03: Live-prod E2E uses documented fallback routing. _resolve_live_backend() probes prod UAT DNS+TCP (uat-flywheel-backend.lumif.ai), falls back to http://localhost:8000 if unreachable, pytest.skips if neither works. From this dev machine prod UAT DNS fails (pre-existing network/ngrok hop issue, not Plan 03 scope) so tests route through local backend which connects to SAME prod Supabase. Byte-identity with Phase 149 capture preserved end-to-end because DB layer is unchanged — only the HTTP transport hop differs. All 5 Phase 150 SCs proven green via this path.
- v22.0 150-03: SC3 (tamper integrity check) tests enforce verify-before-extract ordering AT THE MCP LAYER, not just the materializer unit test (Plan 02 already did that). Test monkeypatches flywheel_mcp.bundle.zipfile.ZipFile to count constructor invocations, then calls materialize_skill_bundle with a post-fetch tampered broker bundle. Asserts BundleIntegrityError raised AND zipfile call_count == 0. Same structural invariant as Plan 02 but against live prod data — guards against any future refactor that moves SHA verification after zipfile construction.
- v22.0 150-03: SC5 uses snapshot-before/snapshot-after cache-diff pattern. Captures `{p.relative_to(cache) for p in cache.rglob('*')}` set before the with-block and again after, asserts the diff (after - before) is empty. Tolerates pre-existing cache bytes from other tools/past phases (SC5 contract is "Phase 150 writes zero new bytes", NOT "cache is globally empty" — Phase 152 adds cache legitimately). Also asserts tempdir captured post-exit does NOT exist and no sibling /tmp/flywheel-skill-* dirs.
- v22.0 150-03: SC4 import guard: field_validator.py is in the BROKER LIBRARY bundle (broker-parse-contract has assets:[] per Phase 147 contract so only the library comes back in the n:1 response). sys.modules snapshot captured pre-with-block and new modules popped in finally so subsequent tests get a clean import state. Inside the with-block, sys.path[0] == str(tmp) and importlib.import_module("field_validator") resolves to the broker library's validator module.
- v22.0 150-03: 6 E2E tests not 5 — added test_sc1_mcp_tool_returns_tool_result as belt-and-suspenders so SC1 proves BOTH the client layer (via FlywheelClient.fetch_skill_assets_bundle direct) AND the MCP-tool layer (via flywheel_fetch_skill_assets end-of-pipeline) against live prod data. Separates client-integration from MCP-surface-integration assertions. No new SC; just finer-grained SC1 coverage.
- v22.0 150-03: Zero new runtime dependencies. cli/pyproject.toml unchanged. Only added imports in server.py: `import os` (for FLYWHEEL_DEBUG env check), `from fastmcp.utilities.types import File` (already in cli venv), `from flywheel_mcp.bundle import BundleError` (shipped in Plan 02, so available at import time).
- v22.0 151-01: Cache dir perms 0o755 (default umask), NOT 0o700 — bundle bytes already travel over HTTPS; matches existing `~/.flywheel/` convention; avoids multi-user-dev friction. Explicit override of Phase 149's `~/.flywheel/` 0o700 decision because semantic differs (cache holds public bundle bytes, not credentials).
- v22.0 151-01: `?shas_only=true` branch on `/assets/bundle` added as early return AFTER fanout walk + protected-dep check but BEFORE base64 encode — preserves 403 for protected deps AND rate-limit parity while skipping bundle-bytes work; empty `bundle_b64=""` signals shas-only mode to client (schema unchanged, `bundle_b64: str` already unconstrained).
- v22.0 151-01: `BundleCache` in NEW `cli/flywheel_mcp/cache.py` module (not extending `bundle.py`) — clean separation, keeps Phase 150's verify-before-extract materializer pure and untouched. Cache refresh takes injected `fetcher` callable instead of `FlywheelClient` instance — breaks the circular import cycle (`api_client` → `cache` → `bundle` → `api_client`).
- v22.0 151-01: Correlation ID threaded through ALL retry attempts with SAME value (not regenerated per retry) — so backend logs tie N retries to a single user-visible fetch under one grep-able ID. Auto-generated via `secrets.token_hex(4)` (8 hex chars) at entry; never echoed to stdout (only stderr WARN + logger.info extra).
- v22.0 151-01: Cache write post-fetch is best-effort (try/except logs `cache_write_failed` warning) — disk full or permission error does NOT fail the user-visible fetch. Fetch succeeds regardless of cache health.
- v22.0 151-01: `BundleCacheError` raised when offline + ANY cache trace exists in index.json (even if bundle bytes are gone). Staticmethod `_has_any_cache_trace(cache, name)` peeks at `_load_index()` independently of `has_stale()` which requires usable bundle.zip. Correct semantic: "user had a cache, it's now unusable" vs "never cached this skill" (raw `BundleFetchError`).
- v22.0 152-01: Phase 152 deprecation banner placement on SKILL.md files — immediately AFTER the closing `---` of YAML frontmatter and BEFORE the first heading, wrapped in leading+trailing blank lines, so the banner renders as an isolated block quote in GitHub/Claude rendering and does not disturb any frontmatter parser (regex or YAML-AST).
- v22.0 152-01: Idempotency key for Phase 152 annotation passes = the literal substring `DEPRECATED (Phase 152`. Every banner/marker insertion script short-circuits if this substring already appears anywhere in the target file. Re-runs of the annotation scripts are safe.
- v22.0 152-01: README install-note wording is byte-identical across the two scrubbed READMEs (gtm-web-scraper-extractor + gtm-company-fit-analyzer) per the Task 2 Step B spec — avoids divergent messaging and future drift between skills.
- v22.0 152-01: skills/broker/MIGRATION-NOTES.md rewrite is minimal — only the single future-tense "After Phase 152 ... will be tagged ... archived read-only" paragraph was flipped to past tense anchored at 2026-04-19. The Phase 149/151 narrative and coexistence-window sentences were left untouched to preserve provenance.
- v22.0 152-01: SC3 grep gate coverage expanded past the plan's explicit task-list targets (Rule 3 Blocking deviation). Files surfaced by `rg -l "~/.claude/skills/" skills/ backend/src/flywheel/ cli/ | grep -v _archived | grep -v /tests/` that lacked the `DEPRECATED (Phase 152` marker received banners/markers so the plan's own success criterion passes truthfully — not by carving gate exceptions. This establishes the SC3 grep as a durable regression check for Phases 152-02/03/04.

### Pending Todos

- v18.0 Phase 132-03 awaiting final verify (committed at 387291a)
- Title matching false positives in _filter_unprepped (deferred from 66.1)
- Private import coupling in flywheel_ritual.py (tech debt)
- **Module-based skill access control**: Add `module` column to `skill_definitions` (broker/gtm/meetings) + `tenant_modules` table to gate skill access by subscription.
- **Broker frontend polish gaps** (from UX audit): comparison matrix data flow (coverage_id linkage), quote document download links, carrier logos across all views

### Roadmap Evolution

- Phase 145 added: Stage 1 intake correctness — typed upload (Requirements vs Current Coverage zones) + auto gap detection + current-policies extraction pass + orphan_policies metadata + document_misrouted safety net
- Phase 150.1 inserted after Phase 150: CC-as-Brain Enforcement (URGENT) — close backend LLM-call billing leak discovered during Phase 150 discussion. Current backend defaults to `flywheel_subsidy_api_key` fallback at 9 call sites; skill executor only gates `company-intel`+`meeting-prep`; broker endpoints bypass enforcement entirely. Phase 150.1 audits all LLM call sites + extends CC-as-Brain enforcement + routes analysis to Pattern 3a (Python returns raw data → Claude-in-conversation analyzes) where possible. Must ship BEFORE Phase 151 (Broker Dogfood) so dogfood validates a gated system.

### Blockers/Concerns

None active.

**Pre-implementation decisions for v21.0 (from spec):**
1. Deployment environment -- Docker, bare metal, or serverless? Determines conversion tool choice (LibreOffice vs Gotenberg vs Python fallback).
2. CORS test -- Can fetch() from localhost:5173 access Supabase signed URLs? If not, need proxy endpoint.
3. [DONE in 143-01] source_excerpt required -- now enforced in EXTRACTION_TOOL.input_schema.properties.coverages.items.required.

## Session Continuity

Last session: 2026-04-19
Stopped at: Completed 152-02-PLAN.md (finalization after checkpoint resolution). Phase 152 Plan 02 COMPLETE. User resolved Task 2 checkpoint:decision to option-d (defer external per-skill repo archival as documented residual task in Plan 04 PR description); Task 3 skipped per plan's `<done>` clause permitting the skipped path when Task 2 = option-d. SUMMARY.md finalized at .planning/phases/152-retirement/152-02-SUMMARY.md with Task 2/3 evidence + Residual Task section (archive `Sharan0516/claude-skill-web-scraper` via GitHub Settings → Danger Zone, owner = user Sharan, evidence format = archive timestamp + curl-verify "This repository has been archived" grep on HTML landing page; hand-off vehicle = Plan 04 retirement PR description). Per-plan commit captures SUMMARY.md + STATE.md only — tag itself was pushed during Task 1 pre-checkpoint and has no file artifact. Plan 04 now fully unblocked: SC1 (legacy-skills-final tag) satisfied via Task 1 push to both origin + lumifai remotes; SC3 + SC4 gates from Plan 03 still green; residual task cleanly documented for post-merge action. Only remaining Phase 152 work: Plan 04 (telemetry gate + retirement PR).

Previous session: 2026-04-19
Stopped at: Completed 152-03-PLAN.md. Phase 152 Plan 03 COMPLETE (commit 92e2a42, `docs(152-03): document MCP-only skill delivery in root README`). Per-plan commit strategy — single atomic commit for Tasks 1/2/3 + 2 Rule 3 deviations (SC4 denial-sentence filter + untracked-README interpretation). README.md now tracked (first time). SC3 + SC4 gates both PASS (plan-level verification block run post-commit). Full classification table (395 hits / 85 files / 10 allowed buckets) + verbatim grep evidence in .planning/phases/152-retirement/152-03-SUMMARY.md. Plan 04 (telemetry gate + PR) unblocked. Plan 02 remains checkpoint-pending (user decision on per-skill external repo archival options a/b/c/d + web-UI archive flow) — independent of Plan 03.

Previous previous session: 2026-04-19
Stopped at: Completed 152-01-PLAN.md. Phase 152 Plan 01 COMPLETE. Commit 2c4450d (`feat(152-01): annotate legacy skill surface with Phase 152 deprecation banners`). Per-plan commit strategy — single atomic commit covers Tasks 1/2/3 + the Rule 3 SC3-gate expansion. Files modified: 35 (18 canonical SKILL.md banner insertions + 1 Rule 3 SKILL.md + 2 README install-note scrubs + 4 reference-doc top-of-file banners + 7 Python deprecation comments + backend/src/flywheel/context_utils.py comment + skills/_shared/context_utils.py module-level comment + skills/broker/MIGRATION-NOTES.md past-tense rewrite + top-of-file marker + skills/MIGRATION-PLAN.md historical banner). Files deleted: 13 (all enumerated SKILL.md backup files outside skills/_archived/). Plan-level SC3 gate passes with zero unannotated hits across skills/, backend/src/flywheel/, cli/. All 8 touched Python files parse clean via ast.parse. One Rule 3 deviation (Blocking) — the plan's success criterion SC3 surfaced 9 files not in the explicit Task 1/2/3 target list; annotated all with file-appropriate banners so the gate passes truthfully. SUMMARY at .planning/phases/152-retirement/152-01-SUMMARY.md. Plan 02 (tag + archive legacy repo) receives: fully-annotated working tree ready for `git tag legacy-skills-final` capture of the final archival snapshot; zero backup cruft remains under skills/; SC3 grep is now a durable regression check for subsequent Phase 152 plans. No runtime behavior changes; archival grooming only.

Previous session: 2026-04-18
Stopped at: Completed 151-01-PLAN.md. Phase 151 Plan 01 COMPLETE. Commit 55e1800 (`feat(151-01): backend shas_only + cache module + FlywheelClient integration`). Files modified: backend/src/flywheel/api/skills.py (+56 LOC shas_only branch + correlation_id extra on 2 logger.info sites), backend/src/tests/test_skills_api.py (+180 LOC, 4 new tests in TestAssetBundleEndpoint), cli/flywheel_mcp/api_client.py (+200 LOC, fetch_skill_assets_bundle signature change + _fetch_shas_only + _has_any_cache_trace + full cache/offline integration). Files created: cli/flywheel_mcp/cache.py (644 LOC, BundleCache class + CacheEntry + RefreshResult + _atomic_write, stdlib-only), cli/tests/test_cache.py (721 LOC, 18 hermetic tests — 11 BundleCache unit + 7 FlywheelClient integration), .planning/phases/151-broker-dogfood-resilience/151-01-SUMMARY.md. Test results: backend 32/38 pass (6 pre-existing failures unchanged from Phase 150.1 baseline: MockLead/MockInvite/SSE-auth fixtures in TestListSkills+TestStartRun+TestSSEStream); cli 70/76 pass + 6 e2e skipped (opt-in only); zero new regressions. Public API additions: `FlywheelClient.fetch_skill_assets_bundle(name, *, bypass_cache=False, correlation_id=None)` signature expanded with keyword-only kwargs; `from flywheel_mcp.cache import BundleCache, CacheEntry, RefreshResult`; backend `?shas_only=true` query param on existing `/assets/bundle` endpoint; backend accepts `X-Flywheel-Correlation-ID` request header. One Rule 3 deviation (added `_has_any_cache_trace` staticmethod to classify offline errors correctly — needed because `has_stale()` requires usable bundle.zip, but BundleCacheError must fire on any cache-trace-in-index regardless of byte loadability). Plan 02 receives: `BundleCache.refresh(name?, *, fetcher)` method hook ready for `flywheel_refresh_skills` MCP tool binding; `BundleCacheError` now actually raised on offline+stale path (was placeholder in Phase 150); end-to-end correlation-id plumbing stable; backend `?shas_only=true` tested ready for Plan 03 dogfood latency measurement. Plan 02 scope: lock exact user-facing copy across all 6 error classes via `test_error_messages.py` + wire `flywheel_refresh_skills` MCP tool using injected fetcher pattern. Full Plan 02 handoff checklist in 151-01-SUMMARY.md "Next Phase Readiness" section.

Previous session: 2026-04-18
Stopped at: Completed 150-02-PLAN.md. Phase 150 Plan 02 COMPLETE. Commit 81a1110 (`feat(150-02): client materializer + exception taxonomy + 15 unit tests`). Files: cli/flywheel_mcp/bundle.py (NEW ~190 LOC — 4 exception classes + _safe_extract + materialize_skill_bundle), cli/flywheel_mcp/api_client.py (MODIFIED +123 LOC — fetch_skill_assets_bundle method + base64/time imports), cli/tests/test_bundle.py (NEW ~460 LOC — 15 hermetic tests), .planning/phases/150-mcp-tool-unpack-helper/150-02-SUMMARY.md (NEW). Test results: 15/15 pass in 0.09s; full cli/tests/ 47/47 pass zero regressions. Public API: `from flywheel_mcp.bundle import materialize_skill_bundle, BundleError, BundleFetchError, BundleIntegrityError, BundleSecurityError, BundleCacheError`. Exception hierarchy rooted at BundleError; catch-all is `except BundleError:`. Contract: `with materialize_skill_bundle(name) as tmp: ...` yields pathlib.Path, prepends sys.path, auto-cleanup on exit. Two minor Clarifications (not Deviations) logged in SUMMARY: (1) 15 tests not 14 (added exception-hierarchy sanity test as bonus persistent regression guard); (2) grep -c 'is_relative_to' returns 2 (doc ref + code use — single-source-of-truth preserved). Zero Rule 1/2/3/4 deviations; no auth gates; no spec gaps logged to SPEC-GAPS.md. Handoff to Plan 03: register flywheel_fetch_skill_assets MCP tool in cli/flywheel_mcp/server.py returning ToolResult(content=[File(data=b, format='zip', name=f'{skill_name}.zip') for (skill_name, _sha, b) in bundles], structured_content={'skill', 'deps', 'shas', 'rollup_sha'}); catch BundleError at tool boundary + re-raise as FlywheelAPIError with FLYWHEEL_DEBUG=1 respected; live prod smoke against broker-parse-contract asserting (tmp/api_client.py).exists() + (not tmp.exists() post-exit) + sys.path restored. Full Plan 03 handoff checklist in 150-02-SUMMARY.md "Next Phase Readiness" section.

Previous session: 2026-04-18
Stopped at: Completed 150-01-PLAN.md. Phase 150 Plan 01 COMPLETE. Commit 3fe18bb (`feat(150-01): depends_on column + fanout endpoint with topological walker`). Operator approved GO at destructive-prod approval gate for sub-step 1.5 ALTER TABLE + sub-step 1.6 re-seed. Sub-step 1.5: `cd backend && PYTHONPATH=src ./.venv/bin/python scripts/apply_065_depends_on_column.py` applied DDL to prod Supabase (column exists, NOT NULL, default '{}'::text[], 0 NULL rows, alembic_version='065_depends_on_column'). Sub-step 1.6: `./backend/.venv/bin/python scripts/seed_skills.py --verbose --skills-dir skills` against prod — exit 0, unchanged accounting per _CHANGE_FIELDS but actual depends_on written via unconditional ON CONFLICT DO UPDATE. Post-reseed SQL verified: 10 broker-* consumers have depends_on=['broker'], 3 libraries (broker/_shared/gtm-shared) have depends_on=[]. Task 2 shipped: backend/src/flywheel/api/skills.py +230 LOC with BundleEntry + AssetsBundleResponse Pydantic models, _resolve_bundle_chain helper (graphlib.TopologicalSorter + CycleError + missing-dep 500 + protected-in-chain 403 + NO enabled filter on library lookups per Phase 147 contract), get_skill_assets_bundle @router.get("/{skill_name}/assets/bundle") with root tenant-override + enabled=True + protected-root 403 + fanout walk + server-side SHA re-hash (defense-in-depth) + rollup_sha over ordered (name,sha) pairs. backend/src/tests/test_skills_api.py +430 LOC TestAssetBundleEndpoint class (14 methods, all pass): success n:1 + success n:2 topological order + rollup_sha determinism + client SHA b64 round-trip + protected root (with await_count==2 assertion) + protected dep in chain + missing dep + cycle + 404 unknown + 401 missing auth + tenant-override allows + tenant-override excludes + SHA mismatch + rate limit 429. MockSkillDefinition.__init__ extended with depends_on kwarg. Zero regressions vs main baseline (28/34 pass; 6 failures pre-existing from TestListSkills/TestStartRun/TestSSEStream pre-dating Phase 150). Live smoke against prod Supabase via `cd backend && PYTHONPATH=src ./.venv/bin/python`: broker-parse-contract/assets/bundle → deps=['broker'], 1 bundle, sha=217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f, size=7239B (byte-matches Phase 149 capture), rollup_sha=fcb22b9f2f61fbab02ba54e28935b071d5ff3abae0c13cbccc247c89915d4873. Client b64-decode + SHA recompute matches. n:1 contract honored. All 5 Plan 01 ROADMAP SCs GREEN (SC1 topological+per-bundle SHA via unit+smoke; SC2 server re-hash via unit+smoke; SC3 protected gates via unit; SC4 missing-dep+cycle via unit; SC5 tenant+rate+auth parity via unit). Zero plan deviations. Single commit per config.json commit_strategy=per-plan. Audit logs at /tmp/150-01-apply.log + /tmp/150-01-reseed.log + /tmp/150-01-smoke.log. SUMMARY.md at .planning/phases/150-mcp-tool-unpack-helper/150-01-SUMMARY.md. Next: Plan 02 (cli/flywheel_mcp/bundle.py materialize_skill_bundle context manager + cli/flywheel_mcp/api_client.py fetch_skill_assets_bundle method + cli/flywheel_mcp/server.py flywheel_fetch_skill_assets MCP tool); Plan 03 (end-to-end prod smoke w/ broker consumer + materialized sys.path + live endpoint extraction). Handoff doc in 150-01-SUMMARY.md's "Next Phase Readiness" section.

Previous (Phase 149 Plan 02): Completed 149-02-PLAN.md. Phase 149 Plan 02 COMPLETE. Operator approved GO (option-a) at the mandatory destructive-prod approval gate after reviewing /tmp/149-prod-dryrun-summary.txt with hash-match + row-count delta (3 new / 10 updated / 0 errors). Real prod seed via `./backend/.venv/bin/python scripts/seed_skills.py --verbose --skills-dir skills` against prod Supabase: exit 0, Added: 3 (_shared/broker/gtm-shared library skills), Updated: 10 (all 10 broker-* consumers on version 1.0→1.1 bump carrying assets:[] + depends_on:["broker"] frontmatter from Plan 01), Unchanged: 10 (other pre-existing skills), Errors: 0. Single COMMIT at end of processing (Phase 147 guarantee — mid-flight failures roll back entire seed). Seed also auto-wired 24+ tenant_skills ON CONFLICT DO NOTHING rows linking the 3 new library skills to every existing tenant (pre-existing seed.py behavior). Post-seed SELECT via ./backend/.venv/bin/python + sys.path+.env shim confirmed all 13 target rows live: 3 library with bundle_size>0 and SHA-256 byte-matching Plan 01 (broker=217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f/7239, _shared=79819ec05c58f8c398fe5e639f6c65b536ee70fdad78ad415cf1d05511c653eb/15588, gtm-shared=6740d954b20593f7ecc947c589c214d75ee91e4b7356c7ae900524043e384c15/8251), 10 broker-* consumers with bundle_size=0 and sha='(no bundle)' per assets:[] contract. Byte-determinism across Plan 01 local run + Plan 02 prod seed is proven — Phase 147's deterministic-zip invariant holds rock-solid for Phase 151 offline-cache contracts. skills/broker/MIGRATION-NOTES.md authored inline with the library tree (82 lines): coexistence window docs, first-login-after-migration messaging (research-correct: no cp -R snippet because pre-149 chromium.launch was ephemeral with zero persistence on disk — nothing to copy; only a defensive `mkdir -p ~/.flywheel/broker/portals/mapfre` offered for ops pre-seeding). Zero plan deviations. 4 tasks completed across two sessions (Tasks 1+2 pre-compact dry-run + approval; Tasks 3+4 this session). Audit artifacts: /tmp/149-prod-dryrun.log, /tmp/149-prod-seed.log, /tmp/149-verify-sql.txt, /tmp/149-verify-query.sql. Phase 149 now has all 5 ROADMAP SCs green. Next: gsd-verifier (sonnet) against Phase 149 → if passed, mark task #17 complete + update ROADMAP.md (progress row 1/1 Complete 2026-04-17) + suggest /compact before Phase 150 kickoff.

Previous (Phase 149 Plan 01): Completed 149-01-PLAN.md (commit 4359719). Phase 149 Plan 01 COMPLETE. New broker library skill at flywheel-v2/skills/broker/ with SKILL.md + api_client.py (184 LOC verbatim) + field_validator.py (51 LOC verbatim) + portals/{__init__.py verbatim, base.py rewritten for chromium.launch_persistent_context, mapfre.py with STATE_DIR=Path.home()/".flywheel"/"broker"/"portals"/"mapfre" and sys.path.insert(~/.claude) removed, mapfre.yaml 63 LOC verbatim}. 10 broker-* SKILL.md files gained assets:[] + depends_on:["broker"] + version 1.0→1.1. gtm-shared populated with gtm_utils.py (424 LOC) + parallel.py (303 LOC); SKILL.md rewritten from placeholder to final v1.0 library manifest. scripts/validate_skills.py SKIP_DIRS narrowed to {"_archived"}, Rule 3 fixes: library-skill web_tier exemption + startswith("_") walk filter drop. scripts/check_portal_state_paths.sh new executable grep-guard. Local dry-run seed via ./backend/.venv/bin/python against prod Supabase: 3 added, 10 updated, 10 unchanged, exit 0, zero UnknownDependencyError. Deterministic broker bundle SHA-256 = 217ebdc1c28416e94104845a7ac0d2e49e71fe77caa60531934d05f2be17a33f (7239 bytes) captured for Plan 02 cross-check. 3 Rule-based deviations documented (2 Rule 3 blocking + 1 Rule 1 bug cleanup). Originals at ~/.claude/skills/broker/ + ~/.claude/skills/gtm-shared/ preserved intact.

Previous (Phase 148): Completed 148-01-PLAN.md (commit 073f8dc). Phase 148 COMPLETE. GET /api/v1/skills/{name}/assets endpoint live with full auth/tenant-override/rate-limit parity to get_skill_prompt + 403 short-circuit for protected skills (bundle bytes never read from DB for server-side-execution skills). backend/src/flywheel/api/skills.py +116 LOC (base64 import, SkillAsset import, SkillAssetsResponse Pydantic model, get_skill_assets handler with 97 LOC). backend/src/tests/test_skills_api.py +241 LOC (TestAssetEndpoint class with 8 tests covering all 5 ROADMAP SCs — success round-trip, 401, 403-protected-with-await_count==2, 404 skill-missing, 404 distinct prompt-only message, tenant-override allows/excludes, rate-limit 429). One Rule 3 deviation: limiter.reset() added to EVERY test method (not just 2 per plan) to prevent class-order flake from shared TEST_USER_ID. Zero regressions vs main baseline (6 pre-existing test_skills_api failures + 2 pre-existing test_rate_limit failures verified identical via git stash). Zero new ruff warnings.

Previous (Phase 147): Completed 147-01-PLAN.md (commit 08a10e0). Phase 147 COMPLETE. Seed pipeline now produces idempotent content-addressed DEFLATE zip bundles in skill_assets for every skill declaring assets: in SKILL.md frontmatter. backend/src/flywheel/db/seed.py extended 499→709 LOC with _build_bundle helper (deterministic via ZipInfo(date_time=(1980,1,1,0,0,0))), UnknownDependencyError exception (raised pre-DB in two-pass validate), three-step per-skill write sequence (ensure skill_definitions row via ON CONFLICT DO NOTHING → UPSERT skill_assets via index_elements=['skill_id'] → UPDATE skill_definitions full fields+prompt), library-skill injection (enabled=false + tags=['library'] for _shared/gtm-shared). backend/src/tests/test_seed_bundles.py shipped (336 LOC, 9 tests: 4 pure-unit + 5 DB-backed @pytest.mark.postgres, locks all 5 SCs). skills/_shared/SKILL.md + skills/gtm-shared/SKILL.md stubs on disk (new gtm-shared/ directory). 351 pytest tests collect (342 baseline + 9 new, zero regressions). All 5 ROADMAP SCs GREEN with log+SQL evidence via prod-Supabase smoke against isolated tmp-dir tree: SC1 (bundle entries match globs, tests/ excluded, ZIP_DEFLATED compression), SC2 ('bundle skipped (sha256 match)' logged + skill_assets.updated_at unchanged on re-seed), SC3 (phase-147-smoke-_shared + phase-147-smoke-gtm-shared seeded with enabled=False, tags @> ['library']), SC4 (UnknownDependencyError(skill='phase-147-smoke-consumer-sc4', missing=['ghost-library']) raised pre-DB, zero partial writes), SC5 ('bundle updated' log line precedes 'definition upserted' within one transaction). Zero row leakage — all phase-147-smoke-* DELETEd in try/finally. Two Rule 1 bugs fixed inline during execution: (1) zipfile.ZIP_DEFLATE → ZIP_DEFLATED (plan code referenced non-existent stdlib constant); (2) constraint='uq_skill_assets_skill_id' → index_elements=['skill_id'] (Phase 146 created uniqueness as INDEX not named CONSTRAINT; ON CONFLICT ON CONSTRAINT only resolves named constraints). Evidence logs at /tmp/147-smoke-first.log + /tmp/147-smoke-second.log + /tmp/147-smoke-full.log + /tmp/147-smoke-dryrun.log. Dry-run against real skills/ tree parses 22 skills with 0 errors. --skip-assets CLI flag deferred (Claude's Discretion, optional). parse_frontmatter docstring fixed as drive-by.

Resume file: Phase 150 Plans 01 + 02 COMPLETE (commits 3fe18bb + 81a1110). Phase 149 still awaits gsd-verifier (open loop from prior session — not blocking). Next action = Plan 03 of Phase 150 — register flywheel_fetch_skill_assets MCP tool in cli/flywheel_mcp/server.py returning ToolResult per FastMCP escape-hatch pattern, catch BundleError at tool boundary + respect FLYWHEEL_DEBUG=1 env var, run live prod end-to-end smoke against broker-parse-contract proving (tmp/api_client.py).exists() + tmp auto-cleanup + sys.path restoration. Plan 02's public API is locked and MCP tool consumes it directly; full Plan 03 handoff checklist lives in 150-02-SUMMARY.md "Next Phase Readiness".

v21.0 PENDING (not blocking v22.0): 145-04-PLAN.md Task 4 human-verify CHECKPOINT -- autonomous Tasks 1-3 complete (commit 71c5b4e). Awaiting user to run Scenarios A/B/C/D and type "approved" to mark Plan 04 / Phase 145 complete.
