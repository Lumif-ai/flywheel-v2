---
phase: 68-email-to-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/035_add_email_task_fields.py
  - backend/src/flywheel/db/models.py
autonomous: true

must_haves:
  truths:
    - "Re-running the ritual for the same emails creates no duplicate tasks (email_id FK enables NOT EXISTS guard)"
    - "Source-filtered queries use indexed lookups (idx_tasks_source composite index)"
    - "Migration is backwards-compatible -- all existing tasks unaffected (columns are nullable)"
  artifacts:
    - path: "backend/alembic/versions/035_add_email_task_fields.py"
      provides: "Alembic migration adding email_id FK and resolution columns"
      contains: "add_column"
    - path: "backend/src/flywheel/db/models.py"
      provides: "Updated Task model with email_id, resolved_by, resolution_source_id, resolution_note columns"
      contains: "email_id"
  key_links:
    - from: "backend/src/flywheel/db/models.py"
      to: "backend/alembic/versions/035_add_email_task_fields.py"
      via: "ORM model matches migration columns"
      pattern: "email_id.*ForeignKey.*emails.id"
---

<objective>
Add email_id FK and resolution metadata columns to the tasks table via Alembic migration, and update the Task ORM model to match.

Purpose: ETL-05 -- schema foundation that all other plans depend on. The email_id FK enables indexed idempotency checks (no duplicate tasks per email). Resolution columns are schema prep for the future commitment ledger (Layer B).
Output: Migration file + updated ORM model
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-email-to-tasks.md
@backend/src/flywheel/db/models.py
@backend/alembic/versions/034_create_tasks_table.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration for email task fields</name>
  <files>backend/alembic/versions/035_add_email_task_fields.py</files>
  <action>
Create a new Alembic migration file `035_add_email_task_fields.py` that adds 4 columns and 2 indexes to the `tasks` table:

Columns:
1. `email_id` -- `sa.Column('email_id', postgresql.UUID(), sa.ForeignKey('emails.id', ondelete='SET NULL'), nullable=True)`
2. `resolved_by` -- `sa.Column('resolved_by', sa.Text(), nullable=True)` (allowed values: "user", "system" -- no CHECK constraint, just convention)
3. `resolution_source_id` -- `sa.Column('resolution_source_id', postgresql.UUID(), nullable=True)` (intentionally NO FK -- polymorphic reference)
4. `resolution_note` -- `sa.Column('resolution_note', sa.Text(), nullable=True)`

Indexes:
1. `idx_tasks_email` on `(email_id)` -- for idempotency lookups
2. `idx_tasks_source` on `(tenant_id, user_id, source)` -- for source-filtered queries

Follow the existing migration pattern from `034_create_tasks_table.py`. Use `op.add_column()` and `op.create_index()`. Include a proper `downgrade()` that drops indexes first, then columns.

Set the `down_revision` to the revision ID of the latest migration (check head with `alembic heads`).
  </action>
  <verify>
Run `cd backend && alembic check` to verify migration is recognized. Run `alembic upgrade head` against dev database to verify it applies cleanly. Run `alembic downgrade -1` then `alembic upgrade head` to verify reversibility.
  </verify>
  <done>Migration applies cleanly, adding 4 nullable columns and 2 indexes to tasks table. Downgrade removes them. No existing data affected.</done>
</task>

<task type="auto">
  <name>Task 2: Update Task ORM model with new columns</name>
  <files>backend/src/flywheel/db/models.py</files>
  <action>
Add 4 new mapped columns to the `Task` class (after `account_id` and before `title` for the FK, and after `metadata_` for resolution fields):

1. `email_id: Mapped[UUID | None] = mapped_column(ForeignKey("emails.id", ondelete="SET NULL"), nullable=True)` -- after `account_id`
2. `resolved_by: Mapped[str | None] = mapped_column(Text, nullable=True)` -- after `metadata_`
3. `resolution_source_id: Mapped[UUID | None] = mapped_column(postgresql.UUID(), nullable=True)` -- after `resolved_by`
4. `resolution_note: Mapped[str | None] = mapped_column(Text, nullable=True)` -- after `resolution_source_id`

Add the `idx_tasks_email` and `idx_tasks_source` indexes to `__table_args__`:
- `Index("idx_tasks_email", "email_id")`
- `Index("idx_tasks_source", "tenant_id", "user_id", "source")`

Add a relationship for the email FK:
- `email: Mapped["Email | None"] = relationship()` (alongside the existing `meeting` and `account` relationships)

Do NOT change any existing columns or indexes.
  </action>
  <verify>
Run `cd backend && python -c "from flywheel.db.models import Task; print([c.name for c in Task.__table__.columns])"` to verify new columns appear. Verify the app still starts: `cd backend && python -c "from flywheel.db.models import *; print('OK')"`.
  </verify>
  <done>Task model has email_id (FK), resolved_by, resolution_source_id, resolution_note columns. Indexes declared in __table_args__. Email relationship defined.</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` succeeds on a fresh or existing database
- Task ORM model imports without error
- New columns are nullable (backwards-compatible)
- `email_id` FK references `emails.id` with `ondelete='SET NULL'`
- Both new indexes exist after migration
</verification>

<success_criteria>
Schema is ready for email task extraction. email_id FK column enables `NOT EXISTS` idempotency checks. Resolution columns are in place for Layer B. No existing functionality broken.
</success_criteria>

<output>
After completion, create `.planning/phases/68-email-to-tasks/68-01-SUMMARY.md`
</output>
