---
phase: 140-coverage-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["140-01"]
files_modified:
  - backend/src/flywheel/engines/contract_analyzer.py
autonomous: true

must_haves:
  truths:
    - "AI extraction outputs coverage_type_key (canonical key from taxonomy) instead of free-text coverage_type"
    - "AI extraction returns limit_amount as a number (not string) and limit_currency separately"
    - "When AI encounters a genuinely new coverage type, a new CoverageType row is inserted with is_verified=false, added_by='ai_extraction'"
    - "When AI suggests an alias for an existing key, the alias is appended to that entry's aliases JSONB"
    - "Currency mismatch between AI-detected currency and project currency is stored in coverage metadata_"
  artifacts:
    - path: "backend/src/flywheel/engines/contract_analyzer.py"
      provides: "Updated extraction tool schema, dynamic prompt builder, taxonomy processing, limit validation"
      contains: "coverage_type_key"
  key_links:
    - from: "backend/src/flywheel/engines/contract_analyzer.py"
      to: "backend/src/flywheel/db/models.py"
      via: "imports CoverageType model for taxonomy loading and new type insertion"
      pattern: "from.*models import.*CoverageType"
    - from: "contract_analyzer.py dynamic prompt"
      to: "coverage_types table"
      via: "loads filtered taxonomy rows into system prompt"
      pattern: "select.*CoverageType.*where"
---

<objective>
Rewrite the contract_analyzer.py AI extraction pipeline to use the canonical taxonomy: dynamic prompt with filtered coverage types, updated tool schema with coverage_type_key + numeric limits, and backend processing for new types, alias learning, and currency mismatch detection.

Purpose: Makes AI extraction output canonical keys instead of free text, enables self-growing taxonomy, and fixes limit parsing for non-USD currencies.
Output: Updated contract_analyzer.py with taxonomy-aware extraction.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPEC-COVERAGE-TAXONOMY.md (sections 5.2, 5.5)
@.planning/phases/140-coverage-taxonomy/140-RESEARCH.md
@.planning/phases/140-coverage-taxonomy/140-01-SUMMARY.md
@backend/src/flywheel/engines/contract_analyzer.py
@backend/src/flywheel/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update extraction tool schema and prompt builder</name>
  <files>backend/src/flywheel/engines/contract_analyzer.py</files>
  <action>
**Replace the static `ANALYSIS_SYSTEM_PROMPT`** (lines ~36-63) with a `build_extraction_prompt()` function that:
1. Accepts `coverage_types: list[dict]` and `project_currency: str` as parameters
2. Builds a taxonomy block listing each type as: `- {key}: {display_name} (aliases: {aliases_joined})`
3. Returns the full system prompt string with the taxonomy embedded, following the template in SPEC section 5.5.2
4. Include instructions for: matching to existing keys, suggesting aliases, creating new types with snake_case keys, limit extraction as numbers, currency inference rules ($ in Mexican contracts = MXN)

**Replace the `EXTRACTION_TOOL` dict** (lines ~66-146) with the updated schema from SPEC section 5.5.1. Key changes:
- `coverage_type` field replaced by `coverage_type_key` (canonical key)
- Add `is_new_type` (boolean, required)
- Add `new_type_display_names` (object, required if is_new_type=true)
- Add `raw_coverage_name` (string, required — verbatim from contract)
- Add `suggested_alias` (string, optional)
- Change `limit_amount` type from string to number
- Add `limit_currency` (string, optional)
- Add "pt" to `contract_language` enum
- Required fields: coverage_type_key, is_new_type, raw_coverage_name, description, category, confidence_score

**Replace `_parse_limit_amount`** (lines ~149-171) with `_validate_limit`:
```python
def _validate_limit(amount) -> float | None:
    if amount is None:
        return None
    try:
        val = float(amount)
        return val if val > 0 else None
    except (TypeError, ValueError):
        return None
```

**Add `_load_taxonomy` async function:**
```python
async def _load_taxonomy(db: AsyncSession, country_code: str, lob: str) -> list[dict]:
```
Queries CoverageType where is_active=True AND (countries is empty OR country_code is in countries) AND (lines_of_business is empty OR lob is in lines_of_business). Returns list of dicts with key, display_name (from display_names['en']), aliases (flattened list from all languages), category.

**Add `_process_extracted_coverage` async function** following SPEC section 5.5.4:
- If `is_new_type=True`: INSERT new CoverageType with is_verified=false, added_by='ai_extraction', countries=[project.country_code], lines_of_business=[project.line_of_business]. Use `session.merge()` or try/except IntegrityError for duplicate safety.
- If `suggested_alias` present and not a new type: load existing CoverageType by key, append alias to `aliases` JSONB using reassignment pattern (NOT in-place mutation — SQLAlchemy won't detect it). Use `existing.aliases = {**updated_dict}` pattern.
- Handle currency mismatch: if `limit_currency` differs from `project.currency`, store warning in coverage's `metadata_` JSONB.
- Create and return the `ProjectCoverage` object with both `coverage_type_key` (canonical) and `coverage_type` (raw_coverage_name for backward compat), `required_limit` from `_validate_limit(limit_amount)`.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.engines.contract_analyzer import build_extraction_prompt, _validate_limit, EXTRACTION_TOOL; print('Tool keys:', list(EXTRACTION_TOOL['input_schema']['properties']['coverages']['items']['properties'].keys())); print('Validate:', _validate_limit(1000000), _validate_limit(None), _validate_limit(-5)); print('OK')"`
Should show coverage_type_key in tool properties, validate returns (1000000.0, None, None), and OK.
  </verify>
  <done>contract_analyzer.py has dynamic prompt builder with taxonomy, updated tool schema with coverage_type_key + numeric limits, _validate_limit replaces _parse_limit_amount, _process_extracted_coverage handles new types + aliases + currency mismatch</done>
</task>

<task type="auto">
  <name>Task 2: Update analyze_contract() to use taxonomy pipeline</name>
  <files>backend/src/flywheel/engines/contract_analyzer.py</files>
  <action>
Modify the `analyze_contract()` function (lines ~193-395) to integrate the taxonomy pipeline:

1. **At the start of analyze_contract:** Load taxonomy using `_load_taxonomy(db, project.country_code, project.line_of_business)`. Get project currency from `project.currency` (already exists, defaults to 'MXN').

2. **Build the system prompt dynamically:** Replace `ANALYSIS_SYSTEM_PROMPT` usage with `build_extraction_prompt(taxonomy_types, project.currency)`.

3. **In the coverage processing loop** (where `tool_use` results are parsed): Replace the old coverage creation logic with calls to `_process_extracted_coverage()` for each coverage item. This handles:
   - Setting `coverage_type_key` from AI output
   - Setting `coverage_type` (display name) from `raw_coverage_name` for backward compat
   - Creating new taxonomy entries for `is_new_type=True` items
   - Appending suggested aliases to existing entries
   - Validating limits with `_validate_limit` instead of `_parse_limit_amount`
   - Storing currency mismatch warnings in metadata_

4. **Remove old code:** Delete `_parse_limit_amount` function, delete any direct `coverage_type` string processing that's been replaced.

5. **Ensure `db` session is passed through** to analyze_contract (check existing signature — it likely already has the session via dependencies). The taxonomy loading and new type insertion both need the session.

Important: Keep the existing error handling, logging, and retry patterns. Only change the prompt construction, tool schema reference, and coverage processing logic.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.engines.contract_analyzer import analyze_contract; import inspect; sig = inspect.signature(analyze_contract); print('Params:', list(sig.parameters.keys())); print('OK')"`
Should print parameter names and OK. Also verify: `python -c "from flywheel.engines.contract_analyzer import _parse_limit_amount" 2>&1` should fail (function removed).
  </verify>
  <done>analyze_contract() loads taxonomy, builds dynamic prompt, processes AI output through _process_extracted_coverage, creates coverage rows with coverage_type_key, auto-creates new taxonomy entries, learns aliases, detects currency mismatches</done>
</task>

</tasks>

<verification>
1. `from flywheel.engines.contract_analyzer import build_extraction_prompt, _validate_limit, _load_taxonomy, _process_extracted_coverage` all succeed
2. `_parse_limit_amount` is removed (import fails)
3. EXTRACTION_TOOL schema has coverage_type_key, is_new_type, raw_coverage_name, suggested_alias, limit_amount (number), limit_currency
4. Dynamic prompt builder includes taxonomy block and currency instructions
5. No static ANALYSIS_SYSTEM_PROMPT constant (replaced by function)
</verification>

<success_criteria>
- AI extraction uses canonical coverage_type_key from taxonomy (not free text)
- limit_amount is validated as a number (AI does parsing)
- New coverage types auto-create taxonomy entries with is_verified=false
- Alias suggestions are appended to existing entries' JSONB (with reassignment pattern for SQLAlchemy change detection)
- Currency mismatches stored in coverage metadata_
- _parse_limit_amount is deleted
</success_criteria>

<output>
After completion, create `.planning/phases/140-coverage-taxonomy/140-02-SUMMARY.md`
</output>
