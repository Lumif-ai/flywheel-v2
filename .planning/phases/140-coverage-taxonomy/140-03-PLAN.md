---
phase: 140-coverage-taxonomy
plan: 03
type: execute
wave: 2
depends_on: ["140-01"]
files_modified:
  - backend/src/flywheel/api/broker/projects.py
  - backend/src/flywheel/api/broker/quotes.py
  - backend/src/flywheel/engines/quote_comparator.py
autonomous: true

must_haves:
  truths:
    - "GET /broker/coverage-types returns filtered taxonomy list with optional country and lob query params"
    - "_compute_carrier_matches uses exact coverage_type_key set intersection (no _normalize_coverage_key)"
    - "_normalize_coverage_key function is deleted"
    - "_coverage_to_dict includes coverage_type_key alongside coverage_type"
    - "Project create/update accepts country_code and line_of_business"
    - "Quote extraction passes coverage_type_key through to quote_comparator"
  artifacts:
    - path: "backend/src/flywheel/api/broker/projects.py"
      provides: "New coverage-types endpoint, updated serializers, updated carrier matching"
      contains: "coverage_type_key"
    - path: "backend/src/flywheel/api/broker/quotes.py"
      provides: "coverage_type_key in coverages_dicts"
      contains: "coverage_type_key"
    - path: "backend/src/flywheel/engines/quote_comparator.py"
      provides: "coverage_type_key passthrough"
      contains: "coverage_type_key"
  key_links:
    - from: "backend/src/flywheel/api/broker/projects.py"
      to: "backend/src/flywheel/db/models.py"
      via: "imports CoverageType for taxonomy endpoint query"
      pattern: "from.*models import.*CoverageType"
    - from: "backend/src/flywheel/api/broker/quotes.py"
      to: "backend/src/flywheel/engines/quote_comparator.py"
      via: "passes coverage_type_key in coverages_dicts to comparator"
      pattern: "coverage_type_key.*coverage_type_key"
---

<objective>
Add the coverage-types API endpoint, update carrier matching to use exact canonical key comparison, update serializers to include coverage_type_key, accept market context fields in project payloads, and pass coverage_type_key through quote extraction to the comparator.

Purpose: Exposes the taxonomy via API, fixes the core carrier matching bug (fuzzy -> exact), and ensures coverage_type_key flows through all data paths.
Output: Updated projects.py, quotes.py, quote_comparator.py.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPEC-COVERAGE-TAXONOMY.md (sections 5.6, 7)
@.planning/phases/140-coverage-taxonomy/140-RESEARCH.md
@.planning/phases/140-coverage-taxonomy/140-01-SUMMARY.md
@backend/src/flywheel/api/broker/projects.py
@backend/src/flywheel/api/broker/quotes.py
@backend/src/flywheel/engines/quote_comparator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Taxonomy endpoint + serializer + project payload updates</name>
  <files>backend/src/flywheel/api/broker/projects.py</files>
  <action>
**New endpoint: GET /broker/coverage-types** (TAX-14)

Add to `projects_router` (it already hosts misc broker endpoints like /health, /gate-counts):

```python
@projects_router.get("/coverage-types")
async def get_coverage_types(
    country: str | None = None,
    lob: str | None = None,
    db: AsyncSession = Depends(get_db),
    _user = Depends(require_module("broker")),
):
```

Query CoverageType where is_active=True. If `country` provided, filter: `countries == '{}' OR country = ANY(countries)`. If `lob` provided, filter: `lines_of_business == '{}' OR lob = ANY(lines_of_business)`. Order by sort_order, then key.

Return `{"coverage_types": [list of dicts]}` where each dict has: key, category, display_name (from display_names.get("en", key)), aliases (flattened from all locale lists), countries, lines_of_business, is_verified.

**Update `_coverage_to_dict`** (TAX-15, around line 156-194):
Add `"coverage_type_key": c.coverage_type_key` to the returned dict. Keep existing `coverage_type` for backward compatibility.

**Update `_project_to_dict`** (around line 124-153):
Add `"country_code": p.country_code` and `"line_of_business": p.line_of_business` to the returned dict.

**Update `CreateProjectBody`** (Pydantic model, around line 87-94) (TAX-16):
Add optional fields `country_code: str | None = None` and `line_of_business: str | None = None`. In the create endpoint, set these on the BrokerProject if provided.

Also update the project update endpoint to accept `country_code` and `line_of_business` in the update payload.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.broker.projects import projects_router; routes = [r.path for r in projects_router.routes]; print('coverage-types route:', '/coverage-types' in routes or '/broker/coverage-types' in [r.path for r in projects_router.routes]); print('Routes:', [r.path for r in projects_router.routes if 'coverage' in r.path]); print('OK')"`
  </verify>
  <done>GET /broker/coverage-types endpoint exists with country/lob filtering, _coverage_to_dict includes coverage_type_key, _project_to_dict includes country_code + line_of_business, create/update accept market context fields</done>
</task>

<task type="auto">
  <name>Task 2: Carrier matching rewrite + quote passthrough</name>
  <files>backend/src/flywheel/api/broker/projects.py, backend/src/flywheel/api/broker/quotes.py, backend/src/flywheel/engines/quote_comparator.py</files>
  <action>
**Rewrite `_compute_carrier_matches`** (TAX-12, projects.py around line 1185):

Replace the current implementation that uses `_normalize_coverage_key` with exact canonical key comparison:

```python
def _compute_carrier_matches(carriers, coverage_types, contract_value=None):
    project_keys = {ct for ct in coverage_types if ct}  # coverage_type_key values
    # ... for each carrier:
    carrier_keys = set(carrier.coverage_types or [])  # already canonical keys from carrier config
    matched = project_keys & carrier_keys
    match_score = len(matched) / len(project_keys) if project_keys else 0
```

At the call site (around line 1254-1261), change the coverage type extraction to use `coverage_type_key` instead of `coverage_type`:
```python
coverage_types = [c.coverage_type_key for c in project.coverages if c.coverage_type_key]
```

**Delete `_normalize_coverage_key` function** (line 1180). Remove completely.

**Update quotes.py** (TAX-17, around line 436-444):
In `extract_quote_endpoint`, where `coverages_dicts` is built for passing to the quote comparator, add `coverage_type_key` to each coverage dict:
```python
"coverage_type_key": c.coverage_type_key,
```

**Update quote_comparator.py** (around lines 67-71 and 156-163):
Pass through `coverage_type_key` alongside existing `coverage_type` in the comparison output. Add it to coverage dict structures used in comparison results.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.broker.projects import _compute_carrier_matches; print('OK')"` should succeed.
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.broker.projects import _normalize_coverage_key" 2>&1` should fail with ImportError (function deleted).
  </verify>
  <done>_compute_carrier_matches uses exact coverage_type_key set intersection, _normalize_coverage_key is deleted, quotes.py passes coverage_type_key to comparator, comparator passes it through in output</done>
</task>

</tasks>

<verification>
1. GET /broker/coverage-types endpoint exists on projects_router
2. _normalize_coverage_key is gone (ImportError on import)
3. _compute_carrier_matches uses set intersection on canonical keys
4. _coverage_to_dict includes coverage_type_key
5. Quote extraction passes coverage_type_key to comparator
</verification>

<success_criteria>
- New taxonomy endpoint returns filtered coverage types
- Carrier matching uses exact canonical key comparison (no fuzzy normalization)
- All serializers include coverage_type_key for backward-compatible migration
- Project create/update accepts country_code and line_of_business
- coverage_type_key flows through quote extraction -> comparator -> output
</success_criteria>

<output>
After completion, create `.planning/phases/140-coverage-taxonomy/140-03-SUMMARY.md`
</output>
