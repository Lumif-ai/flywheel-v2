---
phase: 140-coverage-taxonomy
plan: 04
type: execute
wave: 3
depends_on: ["140-03"]
files_modified:
  - frontend/src/features/broker/constants/coverage.ts
  - frontend/src/features/broker/components/GapAnalysis.tsx
  - frontend/src/features/broker/components/GapCoverageGrid.tsx
  - frontend/src/features/broker/components/comparison/comparison-utils.ts
  - frontend/src/features/broker/types/broker.ts
  - skills/broker-parse-policies/SKILL.md
  - skills/broker-parse-contract/SKILL.md
autonomous: true

must_haves:
  truths:
    - "INSURANCE_CATEGORIES, SURETY_CATEGORIES, GAP_STATUS, STATUS_COLORS are defined once in constants/coverage.ts"
    - "GapAnalysis.tsx, GapCoverageGrid.tsx, comparison-utils.ts import from constants/coverage.ts instead of defining locally"
    - "No duplicated category or status arrays exist in any component file"
    - "ProjectCoverage TypeScript type includes coverage_type_key optional string"
    - "broker-parse-policies skill uses taxonomy API instead of hardcoded translation map"
    - "broker-parse-contract skill uses 'category' (not 'coverage_category')"
  artifacts:
    - path: "frontend/src/features/broker/constants/coverage.ts"
      provides: "Single source of truth for coverage categories, gap statuses, status colors"
      exports: ["INSURANCE_CATEGORIES", "SURETY_CATEGORIES", "ALL_CATEGORIES", "GAP_STATUS", "STATUS_COLORS"]
    - path: "frontend/src/features/broker/types/broker.ts"
      provides: "Updated ProjectCoverage type"
      contains: "coverage_type_key"
    - path: "skills/broker-parse-policies/SKILL.md"
      provides: "Taxonomy-aware policy parsing skill"
      contains: "coverage-types"
  key_links:
    - from: "frontend/src/features/broker/components/GapAnalysis.tsx"
      to: "frontend/src/features/broker/constants/coverage.ts"
      via: "import { INSURANCE_CATEGORIES, SURETY_CATEGORIES }"
      pattern: "import.*from.*constants/coverage"
    - from: "frontend/src/features/broker/components/GapCoverageGrid.tsx"
      to: "frontend/src/features/broker/constants/coverage.ts"
      via: "import { INSURANCE_CATEGORIES, SURETY_CATEGORIES }"
      pattern: "import.*from.*constants/coverage"
    - from: "frontend/src/features/broker/components/comparison/comparison-utils.ts"
      to: "frontend/src/features/broker/constants/coverage.ts"
      via: "import and re-export from constants/coverage"
      pattern: "import.*from.*constants/coverage"
---

<objective>
Create the single-source-of-truth frontend constants file, remove all duplicated category/status arrays from component files, add coverage_type_key to the TypeScript type, and update broker skills to use the taxonomy API.

Purpose: Eliminates frontend constant duplication (DRY), adds type support for canonical keys, and makes skills taxonomy-aware.
Output: New constants file, updated components (import changes only), updated types, updated skills.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SPEC-COVERAGE-TAXONOMY.md (sections 5.9, 8)
@.planning/phases/140-coverage-taxonomy/140-RESEARCH.md
@frontend/src/features/broker/components/GapAnalysis.tsx
@frontend/src/features/broker/components/GapCoverageGrid.tsx
@frontend/src/features/broker/components/comparison/comparison-utils.ts
@frontend/src/features/broker/types/broker.ts
@skills/broker-parse-policies/SKILL.md
@skills/broker-parse-contract/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend constants file + remove duplications + type update</name>
  <files>frontend/src/features/broker/constants/coverage.ts, frontend/src/features/broker/components/GapAnalysis.tsx, frontend/src/features/broker/components/GapCoverageGrid.tsx, frontend/src/features/broker/components/comparison/comparison-utils.ts, frontend/src/features/broker/types/broker.ts</files>
  <action>
**Create `frontend/src/features/broker/constants/coverage.ts`** (TAX-18):

```typescript
export const INSURANCE_CATEGORIES = ['liability', 'property', 'auto', 'workers_comp', 'specialty'] as const
export const SURETY_CATEGORIES = ['surety'] as const
export const ALL_CATEGORIES = [...INSURANCE_CATEGORIES, ...SURETY_CATEGORIES] as const

export type CoverageCategory = typeof ALL_CATEGORIES[number]

export const GAP_STATUS = ['covered', 'missing', 'insufficient', 'unknown'] as const
export type GapStatus = typeof GAP_STATUS[number]

export const STATUS_COLORS: Record<GapStatus, { bg: string; text: string }> = {
    covered: { bg: '#DCFCE7', text: '#15803D' },
    missing: { bg: '#FEE2E2', text: '#B91C1C' },
    insufficient: { bg: '#FEF3C7', text: '#A16207' },
    unknown: { bg: '#F3F4F6', text: '#6B7280' },
}
```

**Update `GapAnalysis.tsx`** (TAX-19):
- Remove the local `const INSURANCE_CATEGORIES = [...]` and `const SURETY_CATEGORIES = [...]` declarations (lines 12-13)
- Add import: `import { INSURANCE_CATEGORIES, SURETY_CATEGORIES } from '../constants/coverage'`

**Update `GapCoverageGrid.tsx`** (TAX-19):
- Remove the local `const INSURANCE_CATEGORIES = [...]` and `const SURETY_CATEGORIES = [...]` declarations (lines 9-10)
- Add import: `import { INSURANCE_CATEGORIES, SURETY_CATEGORIES } from '../constants/coverage'`
- If there are any local STATUS_COLORS or gap status definitions, remove those too and import from coverage.ts

**Update `comparison-utils.ts`** (TAX-19):
- Remove the local `export const INSURANCE_CATEGORIES = [...]` and `export const SURETY_CATEGORIES = [...]` declarations (lines 3-4)
- Replace with re-export: `export { INSURANCE_CATEGORIES, SURETY_CATEGORIES } from '../../constants/coverage'`
- This preserves backward compatibility for ComparisonGrid.tsx and ComparisonTabs.tsx which import from comparison-utils

**Update `broker.ts` types** (TAX-20):
- Add `coverage_type_key?: string` to the `ProjectCoverage` interface

**Verify no other files define these constants.** Search for any remaining `INSURANCE_CATEGORIES` or `SURETY_CATEGORIES` declarations (not imports) in the broker feature directory.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2 && npx tsc --noEmit 2>&1 | head -20` — should compile without errors related to coverage constants.
Run: `grep -rn "const INSURANCE_CATEGORIES" frontend/src/features/broker/ --include="*.ts" --include="*.tsx"` — should show ONLY the constants/coverage.ts definition, not GapAnalysis, GapCoverageGrid, or comparison-utils.
  </verify>
  <done>Single constants/coverage.ts file is the only definition of INSURANCE_CATEGORIES, SURETY_CATEGORIES, GAP_STATUS, STATUS_COLORS. All component files import from it. ProjectCoverage type includes coverage_type_key.</done>
</task>

<task type="auto">
  <name>Task 2: Skill updates</name>
  <files>skills/broker-parse-policies/SKILL.md, skills/broker-parse-contract/SKILL.md</files>
  <action>
**Update `broker-parse-policies/SKILL.md`** (TAX-21):
Find the hardcoded Spanish-to-English coverage translation map (around lines 155-165 in the skill). Replace it with instructions to call the taxonomy API:

1. At the start of the skill execution, add a step: "Call GET /broker/coverage-types?country={project.country_code}&lob={project.line_of_business} to load canonical coverage types with aliases"
2. Replace the hardcoded map with instructions to use the taxonomy response for matching policy coverage names to canonical keys
3. The AI should use taxonomy aliases (which include contract-language terms like Spanish names) to match policy coverage names to canonical keys
4. Remove any hardcoded coverage type translation dictionaries

**Update `broker-parse-contract/SKILL.md`** (TAX-22):
Find the line that references `coverage_category` (around line 177 or wherever `c.get("coverage_category", "unknown")` appears). Change `coverage_category` to `category` to match the actual API field name.
  </action>
  <verify>
Run: `grep -n "coverage_category" skills/broker-parse-contract/SKILL.md` — should return no matches (all replaced with "category").
Run: `grep -n "coverage-types" skills/broker-parse-policies/SKILL.md` — should show the taxonomy API call instruction.
  </verify>
  <done>broker-parse-policies uses taxonomy API for coverage matching (no hardcoded translation map), broker-parse-contract uses 'category' field name (not 'coverage_category')</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — no TypeScript errors from constant imports or type changes
2. `grep -rn "const INSURANCE_CATEGORIES" frontend/src/features/broker/` shows only constants/coverage.ts
3. `grep -rn "const SURETY_CATEGORIES" frontend/src/features/broker/` shows only constants/coverage.ts
4. broker.ts ProjectCoverage type includes coverage_type_key
5. broker-parse-contract has no "coverage_category" references
6. broker-parse-policies references taxonomy API
</verification>

<success_criteria>
- Single source of truth: constants/coverage.ts defines INSURANCE_CATEGORIES, SURETY_CATEGORIES, GAP_STATUS, STATUS_COLORS
- Zero duplicated category/status arrays in component files
- ProjectCoverage TypeScript type includes optional coverage_type_key
- broker-parse-policies skill uses taxonomy API instead of hardcoded translation map
- broker-parse-contract skill uses correct field name 'category'
- Frontend compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/140-coverage-taxonomy/140-04-SUMMARY.md`
</output>
