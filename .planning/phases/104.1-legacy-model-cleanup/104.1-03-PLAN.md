---
phase: 104.1-legacy-model-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["104.1-01"]
files_modified:
  - backend/src/flywheel/api/signals.py
  - backend/src/flywheel/services/synthesis_engine.py
autonomous: true

must_haves:
  truths:
    - "Signals endpoint returns badge counts from pipeline_entries and activities tables"
    - "Synthesis engine rate-limits using PipelineEntry.ai_summary_updated_at"
    - "Synthesis engine generates/asks against PipelineEntry, not Account"
  artifacts:
    - path: "backend/src/flywheel/api/signals.py"
      provides: "Signals API querying new tables"
      contains: "pipeline_entries"
    - path: "backend/src/flywheel/services/synthesis_engine.py"
      provides: "Synthesis engine using PipelineEntry"
      contains: "PipelineEntry"
  key_links:
    - from: "backend/src/flywheel/api/signals.py"
      to: "pipeline_entries table"
      via: "raw SQL FROM clause"
      pattern: "FROM pipeline_entries"
    - from: "backend/src/flywheel/services/synthesis_engine.py"
      to: "PipelineEntry model"
      via: "type hints and attribute access"
      pattern: "entry\\.ai_summary_updated_at"
---

<objective>
Rewire signals.py (raw SQL table/column references) and synthesis_engine.py (ORM type references) from legacy models to PipelineEntry/Activity.

Purpose: Fix the signals sidebar badge counts and AI synthesis to query live pipeline data instead of stale legacy tables.
Output: signals.py queries pipeline_entries/activities, synthesis_engine.py uses PipelineEntry type.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104.1-legacy-model-cleanup/104.1-RESEARCH.md
@backend/src/flywheel/api/signals.py
@backend/src/flywheel/services/synthesis_engine.py
@backend/src/flywheel/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire signals.py raw SQL from legacy to pipeline tables</name>
  <files>backend/src/flywheel/api/signals.py</files>
  <action>
1. Read the full file to understand the two SQL constants: `_ACCOUNT_SIGNALS_SQL` and `_REPLY_SIGNALS_SQL`.

2. Rewrite `_ACCOUNT_SIGNALS_SQL`:
   - `FROM accounts a` -> `FROM pipeline_entries a`
   - `a.graduated_at IS NOT NULL` -> `a.retired_at IS NULL AND a.stage NOT IN ('identified')` (show entries that are actively in pipeline, not retired or just identified)
   - `a.last_interaction_at` -> `a.last_activity_at`
   - `a.next_action_due` -> `a.next_action_date` (note: type changed from TIMESTAMP to DATE, adjust any date arithmetic accordingly)
   - `a.next_action_type` -> `a.next_action_note`
   - Check for any other Account column references and map per the research column mapping table

3. Rewrite `_REPLY_SIGNALS_SQL`:
   - `outreach_activities` -> `activities`
   - `oa.account_id` -> `oa.pipeline_entry_id`
   - `oa.created_at` -> `oa.occurred_at` (for recency checks)
   - `oa.status = 'replied'` -> check Activity status enum values. In the new model, status might be 'completed' or similar. Read the Activity model to confirm valid status values. If 'replied' doesn't exist, use the equivalent.
   - Any JOIN to `accounts` -> JOIN to `pipeline_entries`
   - Same column mapping as above for account fields

4. Update any ORM imports at the top of the file. If it imports `Account` or `OutreachActivity`, change to `PipelineEntry` and `Activity`. If it only uses raw SQL (no ORM imports), just fix the SQL strings.

5. Search the entire file for ALL occurrences of: `accounts`, `account_contacts`, `outreach_activities`, `graduated_at`, `last_interaction_at`, `next_action_due`, `next_action_type`, `account_id` (in SQL context), `sent_at`, `from_email`. Fix every occurrence.
  </action>
  <verify>
    - `grep -n "accounts\|outreach_activities\|account_contacts\|graduated_at\|last_interaction_at\|next_action_due\|next_action_type" backend/src/flywheel/api/signals.py` returns zero matches (except possibly in comments documenting the migration)
    - `cd backend && python -c "from flywheel.api.signals import router"` succeeds
    - No syntax errors in the SQL strings (Python can parse the module)
  </verify>
  <done>signals.py SQL queries reference pipeline_entries and activities tables with correct column names. No legacy table/column references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire synthesis_engine.py from Account to PipelineEntry</name>
  <files>backend/src/flywheel/services/synthesis_engine.py</files>
  <action>
1. Read the full file. Find the import of `Account` from `flywheel.db.models`.

2. Change the import from `Account` to `PipelineEntry`.

3. Update the `enforce_rate_limit` method:
   - Change type hint from `account: Account` to `entry: PipelineEntry`
   - Change `account.ai_summary_updated_at` to `entry.ai_summary_updated_at` (this column exists after Plan 01's migration)
   - Change `account.updated_at` to `entry.updated_at`
   - Rename all `account` parameter references to `entry`

4. Update the `generate` method:
   - Change type hint from `account` to `entry` (or whatever parameter name is used)
   - `account.id` -> `entry.id`
   - `account.ai_summary` -> `entry.ai_summary`
   - `account.ai_summary_updated_at` -> `entry.ai_summary_updated_at`
   - Any attribute access that differs per column mapping (check research)

5. Update the `ask` method similarly.

6. Check all callers of SynthesisEngine methods. The primary caller is relationships.py (Plan 04 will rewire that). If there are other callers, note them. The type change from Account to PipelineEntry means callers must also pass PipelineEntry objects -- this is fine since Plan 04 will rewire relationships.py.

7. Search for ANY remaining reference to `Account` (as import, type hint, or variable name) and fix.
  </action>
  <verify>
    - `grep -n "Account" backend/src/flywheel/services/synthesis_engine.py` returns zero matches (except possibly in comments)
    - `grep -n "from flywheel.db.models import.*Account" backend/src/flywheel/services/synthesis_engine.py` returns zero matches
    - `cd backend && python -c "from flywheel.services.synthesis_engine import SynthesisEngine"` succeeds
  </verify>
  <done>synthesis_engine.py uses PipelineEntry type throughout. Rate limiting, generate, and ask methods all reference PipelineEntry attributes including ai_summary_updated_at.</done>
</task>

</tasks>

<verification>
- signals.py has zero references to legacy table names or column names
- synthesis_engine.py has zero references to Account model
- Both files import successfully
- SQL in signals.py is syntactically valid
</verification>

<success_criteria>
- Signals endpoint queries pipeline_entries and activities (not accounts and outreach_activities)
- Synthesis engine accepts PipelineEntry objects and uses ai_summary_updated_at for rate limiting
- Zero legacy model references in either file
</success_criteria>

<output>
After completion, create `.planning/phases/104.1-legacy-model-cleanup/104.1-03-SUMMARY.md`
</output>
