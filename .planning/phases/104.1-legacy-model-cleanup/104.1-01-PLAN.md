---
phase: 104.1-legacy-model-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/052_add_ai_summary_updated_at_to_pipeline_entries.py
  - backend/src/flywheel/db/models.py
  - backend/src/flywheel/api/meetings.py
  - backend/src/flywheel/services/skill_executor.py
autonomous: true

must_haves:
  truths:
    - "PipelineEntry model has ai_summary_updated_at column available for synthesis rate-limiting"
    - "Meeting prep endpoint resolves account name via PipelineEntry, not Account"
    - "Skill executor looks up account name/domain via PipelineEntry, not Account"
  artifacts:
    - path: "backend/alembic/versions/052_add_ai_summary_updated_at_to_pipeline_entries.py"
      provides: "Migration adding ai_summary_updated_at to pipeline_entries"
      contains: "ai_summary_updated_at"
    - path: "backend/src/flywheel/db/models.py"
      provides: "PipelineEntry with ai_summary_updated_at column"
      contains: "ai_summary_updated_at"
    - path: "backend/src/flywheel/api/meetings.py"
      provides: "Meeting prep using PipelineEntry"
      contains: "PipelineEntry"
    - path: "backend/src/flywheel/services/skill_executor.py"
      provides: "Skill executor using PipelineEntry"
      contains: "PipelineEntry"
  key_links:
    - from: "backend/src/flywheel/db/models.py"
      to: "pipeline_entries table"
      via: "ORM mapped_column"
      pattern: "ai_summary_updated_at.*mapped_column"
---

<objective>
Add the missing ai_summary_updated_at column to PipelineEntry (via migration) and rewire the two LOW-complexity files (meetings.py, skill_executor.py) from Account to PipelineEntry.

Purpose: Unblocks synthesis rate-limiting on new model and fixes the two easiest files as quick wins.
Output: Migration file, updated PipelineEntry model, rewired meetings.py and skill_executor.py.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104.1-legacy-model-cleanup/104.1-RESEARCH.md
@backend/src/flywheel/db/models.py
@backend/src/flywheel/api/meetings.py
@backend/src/flywheel/services/skill_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ai_summary_updated_at column to PipelineEntry</name>
  <files>
    backend/alembic/versions/052_add_ai_summary_updated_at_to_pipeline_entries.py
    backend/src/flywheel/db/models.py
  </files>
  <action>
1. Add `ai_summary_updated_at` column to the PipelineEntry ORM model in models.py. Add it near the existing `ai_summary` field:
   ```python
   ai_summary_updated_at: Mapped[datetime.datetime | None] = mapped_column(
       DateTime(timezone=True), nullable=True
   )
   ```
2. Create Alembic migration file `052_add_ai_summary_updated_at_to_pipeline_entries.py`. Use the standard Alembic template with:
   - `op.add_column('pipeline_entries', sa.Column('ai_summary_updated_at', sa.DateTime(timezone=True), nullable=True))`
   - Downgrade: `op.drop_column('pipeline_entries', 'ai_summary_updated_at')`
   - Set `down_revision` to the latest existing migration (check `backend/alembic/versions/` for the current head)
3. IMPORTANT: Due to Supabase PgBouncer DDL constraint, this migration must be applied via SQL Editor or individual commits, NOT via `alembic upgrade`. Add a comment at the top of the migration file noting this.
4. Check if the latest migration revision number is actually 051 or different -- use whatever the actual head is as down_revision.
  </action>
  <verify>
    - `python -c "from flywheel.db.models import PipelineEntry; assert hasattr(PipelineEntry, 'ai_summary_updated_at')"` succeeds
    - Migration file exists and has correct up/down operations
    - `cd backend && python -m alembic check` shows no conflicts (or at minimum the migration is syntactically valid)
  </verify>
  <done>PipelineEntry ORM model has ai_summary_updated_at column. Migration file ready to apply.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire meetings.py from Account to PipelineEntry</name>
  <files>backend/src/flywheel/api/meetings.py</files>
  <action>
1. Find the import of `Account` from `flywheel.db.models` (should be in the imports section). Replace `Account` with `PipelineEntry` in that import line. Keep all other imports (Integration, Meeting, SkillRun, etc.) unchanged.
2. Find the `prep_meeting()` endpoint (around line 546-550) where it does `select(Account).where(Account.id == account_id)`. Change to `select(PipelineEntry).where(PipelineEntry.id == account_id)`.
3. The result variable (likely `account`) references `.name` -- this maps directly since PipelineEntry also has `.name`. Rename the variable from `account` to `entry` for clarity.
4. Search the entire file for any other references to `Account` to ensure nothing is missed.
5. Do NOT change any other logic in the file -- this is a targeted import/query swap only.
  </action>
  <verify>
    - `grep -n "Account" backend/src/flywheel/api/meetings.py` returns zero matches (only PipelineEntry references remain)
    - `cd backend && python -c "from flywheel.api.meetings import router"` succeeds without import errors
  </verify>
  <done>meetings.py imports PipelineEntry instead of Account. The prep_meeting endpoint queries pipeline_entries table.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire skill_executor.py from Account to PipelineEntry</name>
  <files>backend/src/flywheel/services/skill_executor.py</files>
  <action>
1. Find both locations where Account is imported and used (around lines 1564 and 1890 per research). There may be two separate `from flywheel.db.models import Account` statements (local imports inside functions).
2. At each location, change `from flywheel.db.models import Account` to `from flywheel.db.models import PipelineEntry`.
3. Change the query from `select(Account).where(Account.id == account_id)` to `select(PipelineEntry).where(PipelineEntry.id == account_id)`.
4. The result accesses `.name` and `.domain` -- both exist on PipelineEntry with the same semantics. Rename variable from `account` to `entry` for clarity.
5. Search the entire file for any remaining references to `Account` (as model, not as string in comments/logs) and fix them.
6. Do NOT change the `_ensure_pipeline_entry()` helper -- it already uses PipelineEntry correctly.
  </action>
  <verify>
    - `grep -n "from flywheel.db.models import Account" backend/src/flywheel/services/skill_executor.py` returns zero matches
    - `grep -n "select(Account)" backend/src/flywheel/services/skill_executor.py` returns zero matches
    - `cd backend && python -c "from flywheel.services.skill_executor import SkillExecutor"` succeeds
  </verify>
  <done>skill_executor.py uses PipelineEntry for all account lookups. No remaining Account imports.</done>
</task>

</tasks>

<verification>
- No file in this plan imports Account, AccountContact, or OutreachActivity
- PipelineEntry model has ai_summary_updated_at field
- Migration file is syntactically valid
- All modified files import successfully
</verification>

<success_criteria>
- PipelineEntry.ai_summary_updated_at exists in ORM and migration is ready to apply
- meetings.py uses PipelineEntry for account lookup
- skill_executor.py uses PipelineEntry for both account lookups
- Zero remaining Account/AccountContact/OutreachActivity imports in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/104.1-legacy-model-cleanup/104.1-01-SUMMARY.md`
</output>
