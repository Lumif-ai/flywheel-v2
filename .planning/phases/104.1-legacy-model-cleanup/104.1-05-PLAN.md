---
phase: 104.1-legacy-model-cleanup
plan: 05
type: execute
wave: 2
depends_on: ["104.1-01"]
files_modified:
  - backend/src/flywheel/engines/meeting_processor_web.py
  - backend/src/flywheel/seed_crm.py
autonomous: true

must_haves:
  truths:
    - "Meeting processor creates PipelineEntry objects (not Account) when auto-creating prospects"
    - "Meeting processor creates Contact objects (not AccountContact) when upserting contacts"
    - "Meeting processor queries PipelineEntry/Contact for attendee matching and account linking"
    - "Seed CRM tool creates pipeline_entries, contacts, and activities instead of legacy objects"
  artifacts:
    - path: "backend/src/flywheel/engines/meeting_processor_web.py"
      provides: "Meeting intelligence pipeline using new models"
      contains: "PipelineEntry"
    - path: "backend/src/flywheel/seed_crm.py"
      provides: "Dev seed tool using new models"
      contains: "PipelineEntry"
  key_links:
    - from: "backend/src/flywheel/engines/meeting_processor_web.py"
      to: "PipelineEntry model"
      via: "ORM object creation"
      pattern: "PipelineEntry\\("
    - from: "backend/src/flywheel/engines/meeting_processor_web.py"
      to: "Contact model"
      via: "ORM object creation"
      pattern: "Contact\\("
---

<objective>
Rewire meeting_processor_web.py (HIGH complexity -- creates objects, complex domain logic) and seed_crm.py (dev tool) from legacy models to PipelineEntry/Contact/Activity.

Purpose: The meeting processor is a core engine that auto-creates prospects and contacts from meetings. Currently inserting into legacy tables. Seed CRM is a dev tool that also needs updating.
Output: Both files create and query PipelineEntry/Contact/Activity objects.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104.1-legacy-model-cleanup/104.1-RESEARCH.md
@backend/src/flywheel/engines/meeting_processor_web.py
@backend/src/flywheel/seed_crm.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/services/pipeline_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire meeting_processor_web.py from legacy to pipeline models</name>
  <files>backend/src/flywheel/engines/meeting_processor_web.py</files>
  <action>
Read the full file first. This file has 4 key functions that need rewiring:

**Import changes:**
- `Account` -> `PipelineEntry`
- `AccountContact` -> `Contact`
- `OutreachActivity` -> `Activity` (if imported)

**1. classify_meeting() -- attendee matching:**
- `AccountContact` -> `Contact`
- `AccountContact.account_id` -> `Contact.pipeline_entry_id`
- `Account` -> `PipelineEntry`
- Any join on `AccountContact.account_id == Account.id` -> `Contact.pipeline_entry_id == PipelineEntry.id`
- Variable names: `account` -> `entry`, `account_contact` -> `contact`

**2. auto_create_prospect() -- creates new pipeline entries:**
- This currently creates `Account(...)` objects. Change to create `PipelineEntry(...)` objects.
- Column mapping for creation:
  - `Account(name=..., domain=..., tenant_id=..., status='prospect')` -> `PipelineEntry(name=..., domain=..., tenant_id=..., stage='identified', entity_type='company')`
  - `source` stays same
  - `normalized_name` stays same
  - Do NOT set `graduated_at` (doesn't exist on PipelineEntry)
  - Do NOT set `relationship_status` (doesn't exist)
- ALTERNATIVE: Consider using `PipelineService.upsert_from_source()` if it handles dedup by domain/name. Check pipeline_service.py for this method. If it exists and handles the same logic (dedup by normalized name), use it instead of raw object creation. This is preferable because it handles edge cases.
- If using PipelineService, the pattern from skill_executor.py `_ensure_pipeline_entry()` (line ~1555) is a good reference.

**3. upsert_account_contacts() -- creates/queries contacts:**
- `AccountContact` -> `Contact`
- `AccountContact.account_id` -> `Contact.pipeline_entry_id`
- `AccountContact(account_id=..., name=..., email=..., tenant_id=...)` -> `Contact(pipeline_entry_id=..., name=..., email=..., tenant_id=...)`
- Remove `source` field from creation (not on Contact model)
- `AccountContact.role_in_deal` -> `Contact.role`
- Add `is_primary=False` as default for new contacts (new field on Contact)

**4. auto_link_meeting_to_account() -- queries for account matching:**
- `Account` -> `PipelineEntry`
- `AccountContact` -> `Contact`
- `Account.domain` -> `PipelineEntry.domain` (same)
- `AccountContact.account_id` -> `Contact.pipeline_entry_id`
- COUNT queries on AccountContact -> COUNT on Contact
- Result: `meeting.account_id = found_account.id` -> check if Meeting has `pipeline_entry_id` field and set that instead. If Meeting has both `account_id` and `pipeline_entry_id`, set `pipeline_entry_id`.

**5. Variable naming throughout:**
- `account` -> `entry` or `pipeline_entry`
- `account_id` in local variables -> `pipeline_entry_id` (but be careful -- Meeting.account_id may still exist as a column)
- `account_contacts` -> `contacts`

**6. Search the entire file for ANY remaining references to old model names, old column names, old table names.**
  </action>
  <verify>
    - `grep -n "from flywheel.db.models import.*Account[^C]" backend/src/flywheel/engines/meeting_processor_web.py` returns zero matches (no Account import, AccountContact handled separately)
    - `grep -n "AccountContact\|OutreachActivity" backend/src/flywheel/engines/meeting_processor_web.py` returns zero matches
    - `grep -n "graduated_at\|role_in_deal\|account_id.*=.*account" backend/src/flywheel/engines/meeting_processor_web.py` returns zero matches for old patterns
    - `cd backend && python -c "from flywheel.engines.meeting_processor_web import MeetingProcessorWeb"` succeeds
  </verify>
  <done>Meeting processor creates PipelineEntry and Contact objects. All queries use new model names and column names. Attendee matching, prospect creation, contact upsert, and account linking all use pipeline models.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire seed_crm.py from legacy to pipeline models</name>
  <files>backend/src/flywheel/seed_crm.py</files>
  <action>
Read the full file. This is a dev-only CLI tool that seeds CRM data.

**Import changes:**
- `Account` -> `PipelineEntry`
- `AccountContact` -> `Contact`
- `OutreachActivity` -> `Activity`

**Object creation changes:**

For Account -> PipelineEntry creation:
- `Account(name=..., domain=..., status=..., tenant_id=...)` -> `PipelineEntry(name=..., domain=..., stage='identified', tenant_id=..., entity_type='company')`
- `fit_score`, `fit_tier`, `intel`, `source`, `relationship_type` stay same names
- `entity_level` -> `entity_type`
- Remove `graduated_at`, `relationship_status`, `pipeline_stage` (use `stage` instead)
- `last_interaction_at` -> `last_activity_at`
- `next_action_due` -> `next_action_date` (change type if needed -- DATE not TIMESTAMP)
- `next_action_type` -> `next_action_note`

For AccountContact -> Contact creation:
- `AccountContact(account_id=..., ...)` -> `Contact(pipeline_entry_id=..., ...)`
- `role_in_deal` -> `role`
- Remove `source` field
- Add `is_primary=False` default

For OutreachActivity -> Activity creation:
- `OutreachActivity(account_id=..., ...)` -> `Activity(pipeline_entry_id=..., ...)`
- `sent_at` -> `occurred_at`
- Remove `from_email` field
- Add `type` field (required on Activity -- e.g., 'email', 'meeting', 'call')
- `status` values may differ -- map appropriately

**Query changes:**
- Any `select(Account)` -> `select(PipelineEntry)`, same for Contact and Activity
- Any `Account.id` -> `PipelineEntry.id`, etc.

**Also update any cleanup/delete logic** to target the new table objects.
  </action>
  <verify>
    - `grep -n "Account\|AccountContact\|OutreachActivity" backend/src/flywheel/seed_crm.py | grep -v "#\|comment\|pipeline"` returns zero matches
    - `cd backend && python -c "from flywheel.seed_crm import main"` succeeds (or whatever the entry point is)
  </verify>
  <done>seed_crm.py creates PipelineEntry, Contact, and Activity objects. Seeds data into live pipeline tables instead of legacy tables.</done>
</task>

</tasks>

<verification>
- meeting_processor_web.py has zero references to Account, AccountContact, OutreachActivity
- seed_crm.py has zero references to legacy model names
- Both files import successfully
- Object creation uses correct column names for new models
</verification>

<success_criteria>
- Meeting processor auto-creates PipelineEntry (not Account) for new prospects
- Meeting processor upserts Contact (not AccountContact) for meeting attendees
- Meeting processor links meetings via pipeline_entry_id
- Seed tool creates pipeline_entries, contacts, activities in live tables
- Zero legacy model references in either file
</success_criteria>

<output>
After completion, create `.planning/phases/104.1-legacy-model-cleanup/104.1-05-SUMMARY.md`
</output>
