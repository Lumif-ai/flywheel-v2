---
phase: 104.1-legacy-model-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/api/outreach.py
  - backend/src/flywheel/api/timeline.py
  - backend/src/flywheel/main.py
autonomous: true

must_haves:
  truths:
    - "No dead endpoints referencing legacy models are registered in the app"
    - "The outreach router is removed from app startup"
    - "The timeline router is removed from app startup"
  artifacts:
    - path: "backend/src/flywheel/main.py"
      provides: "App without outreach_router and timeline_router"
  key_links:
    - from: "backend/src/flywheel/main.py"
      to: "api/outreach.py"
      via: "router import removed"
      pattern: "outreach_router"
---

<objective>
Delete dead backend endpoints (outreach.py entirely, timeline.py entirely) and unregister their routers from the app. These endpoints are superseded by the pipeline API and have no frontend callers.

Purpose: Remove dead code that references legacy models, reducing the surface area of legacy references.
Output: Two files deleted or gutted, main.py cleaned up.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104.1-legacy-model-cleanup/104.1-RESEARCH.md
@backend/src/flywheel/main.py
@backend/src/flywheel/api/outreach.py
@backend/src/flywheel/api/timeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete outreach.py and timeline.py, unregister routers</name>
  <files>
    backend/src/flywheel/api/outreach.py
    backend/src/flywheel/api/timeline.py
    backend/src/flywheel/main.py
  </files>
  <action>
1. **Verify dead endpoints before deleting.** Run a grep across the entire frontend for:
   - `/outreach` (API calls to outreach endpoints)
   - `/accounts/.*/outreach` patterns
   - `/accounts/.*/graduate` patterns
   - `/accounts/.*/timeline` patterns
   - `/pulse` patterns
   If ANY active frontend caller is found (not in dead/unused files), STOP and rewire instead of deleting.

2. **Also verify no backend callers.** Grep backend code for imports from `flywheel.api.outreach` and `flywheel.api.timeline` (besides main.py). Check if any service or engine calls these endpoints internally.

3. **Delete the files:**
   - `git rm backend/src/flywheel/api/outreach.py`
   - `git rm backend/src/flywheel/api/timeline.py`

4. **Clean up main.py:**
   - Remove `from flywheel.api.outreach import router as outreach_router` (around line 49)
   - Remove `from flywheel.api.timeline import router as timeline_router` (around line 51)
   - Remove `app.include_router(outreach_router, prefix="/api/v1")` (around line 223)
   - Remove `app.include_router(timeline_router, prefix="/api/v1")` (around line 225)
   - Keep all other routers intact.

5. **Check for dead frontend code too** -- the `frontend/src/features/accounts/` directory likely has dead components (AccountDetailPage, TimelineFeed, useTimeline hook) that called these endpoints. Note them in the SUMMARY but do NOT delete frontend code in this plan (backend-only scope).
  </action>
  <verify>
    - `ls backend/src/flywheel/api/outreach.py 2>/dev/null` returns nothing (file deleted)
    - `ls backend/src/flywheel/api/timeline.py 2>/dev/null` returns nothing (file deleted)
    - `grep -n "outreach_router\|timeline_router" backend/src/flywheel/main.py` returns zero matches
    - `cd backend && python -c "from flywheel.main import create_app; app = create_app()"` succeeds (app starts without the removed routers)
  </verify>
  <done>outreach.py and timeline.py are deleted. Their routers are removed from main.py. The app starts cleanly without them.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no import breakage across backend</name>
  <files></files>
  <action>
1. Run a comprehensive grep across the entire backend for any remaining references to the deleted files:
   - `grep -rn "from flywheel.api.outreach" backend/src/`
   - `grep -rn "from flywheel.api.timeline" backend/src/`
   - `grep -rn "outreach_router" backend/src/`
   - `grep -rn "timeline_router" backend/src/`
2. If any references are found, fix them (likely just removing dead imports).
3. Run `cd backend && python -c "import flywheel.main"` to verify no import chain is broken.
4. Also check if `signals_router` (from signals.py) is still registered in main.py -- it should be, since signals.py is being rewired in Plan 03, not deleted.
  </action>
  <verify>
    - Zero grep hits for outreach_router or timeline_router outside of git history
    - `cd backend && python -c "import flywheel.main"` exits 0
  </verify>
  <done>No dangling imports reference the deleted files. Backend module graph is clean.</done>
</task>

</tasks>

<verification>
- outreach.py and timeline.py no longer exist on disk
- main.py has no references to deleted routers
- Backend starts without errors
- No module imports the deleted files
</verification>

<success_criteria>
- Two dead API files deleted
- main.py cleaned of their router registrations
- Backend app starts successfully
- Zero dangling references to deleted files
</success_criteria>

<output>
After completion, create `.planning/phases/104.1-legacy-model-cleanup/104.1-02-SUMMARY.md`
</output>
