---
phase: 104.1-legacy-model-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["104.1-01"]
files_modified:
  - backend/src/flywheel/api/relationships.py
autonomous: true

must_haves:
  truths:
    - "All 8 relationship endpoints query PipelineEntry instead of Account"
    - "Graduated_at partition predicate is replaced with stage-based filter"
    - "Contact fields use new column names (pipeline_entry_id, role instead of account_id, role_in_deal)"
    - "Synthesis and ask endpoints pass PipelineEntry objects to SynthesisEngine"
  artifacts:
    - path: "backend/src/flywheel/api/relationships.py"
      provides: "All relationship endpoints using PipelineEntry/Contact"
      contains: "PipelineEntry"
  key_links:
    - from: "backend/src/flywheel/api/relationships.py"
      to: "PipelineEntry model"
      via: "ORM select queries"
      pattern: "select\\(PipelineEntry\\)"
    - from: "backend/src/flywheel/api/relationships.py"
      to: "backend/src/flywheel/services/synthesis_engine.py"
      via: "SynthesisEngine.generate/ask calls"
      pattern: "SynthesisEngine"
    - from: "backend/src/flywheel/api/relationships.py"
      to: "context_entries.pipeline_entry_id"
      via: "context query FK"
      pattern: "pipeline_entry_id"
---

<objective>
Rewire relationships.py -- the most complex file (8 endpoints) -- from Account/AccountContact to PipelineEntry/Contact. This is the most actively-used legacy-model file.

Purpose: The relationships page is heavily used by the frontend. Every endpoint currently hits legacy tables and returns wrong/no results.
Output: All relationship endpoints query PipelineEntry/Contact with correct column mapping.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104.1-legacy-model-cleanup/104.1-RESEARCH.md
@backend/src/flywheel/api/relationships.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/api/pipeline.py
@backend/src/flywheel/services/synthesis_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire relationships.py imports, queries, and serialization</name>
  <files>backend/src/flywheel/api/relationships.py</files>
  <action>
This is the most complex rewire. Read the entire file first, then apply changes systematically.

**Import changes:**
- `Account` -> `PipelineEntry`
- `AccountContact` -> `Contact`
- Remove any `OutreachActivity` import if present

**Global query pattern changes (apply to ALL endpoints):**
- Every `select(Account)` -> `select(PipelineEntry)`
- Every `Account.id` -> `PipelineEntry.id`
- Every `Account.tenant_id` -> `PipelineEntry.tenant_id`
- Every `Account.graduated_at.isnot(None)` -> `PipelineEntry.retired_at.is_(None), PipelineEntry.stage.notin_(['identified'])` (show active pipeline entries, not retired or raw identified)
- Every `Account.name` -> `PipelineEntry.name`
- Every `Account.domain` -> `PipelineEntry.domain`
- Every `Account.status` -> `PipelineEntry.stage` (note: values differ -- prospect/engaged -> identified/qualified/engaged/active/won/lost/retired)
- Every `Account.entity_level` -> `PipelineEntry.entity_type`
- Every `Account.relationship_type` -> `PipelineEntry.relationship_type` (same name)
- Every `Account.relationship_status` -> REMOVE (no equivalent; derive from stage if needed in serialization)
- Every `Account.fit_score` -> `PipelineEntry.fit_score` (same)
- Every `Account.fit_tier` -> `PipelineEntry.fit_tier` (same)
- Every `Account.intel` -> `PipelineEntry.intel` (same)
- Every `Account.source` -> `PipelineEntry.source` (same)
- Every `Account.ai_summary` -> `PipelineEntry.ai_summary` (same)
- Every `Account.ai_summary_updated_at` -> `PipelineEntry.ai_summary_updated_at` (added by Plan 01)
- Every `Account.last_interaction_at` -> `PipelineEntry.last_activity_at`
- Every `Account.next_action_due` -> `PipelineEntry.next_action_date`
- Every `Account.next_action_type` -> `PipelineEntry.next_action_note`
- Every `Account.pipeline_stage` -> `PipelineEntry.stage`
- Every `Account.contacts` -> `PipelineEntry.contacts` (same relationship name)
- Every `Account.outreach_activities` -> `PipelineEntry.activities`

**Contact model changes:**
- `AccountContact` -> `Contact`
- `AccountContact.account_id` -> `Contact.pipeline_entry_id`
- `AccountContact.role_in_deal` -> `Contact.role`
- `AccountContact.name` -> `Contact.name` (same)
- `AccountContact.email` -> `Contact.email` (same)
- `AccountContact.source` -> REMOVE (not on Contact model)

**ContextEntry FK change:**
- `ContextEntry.account_id` -> `ContextEntry.pipeline_entry_id` (for context/notes queries)

**Meeting FK change:**
- If any queries join Meeting on account_id, change to `Meeting.pipeline_entry_id`

**Serialization dict changes:**
- In response dicts where `entity_level` is returned, change key or value source to `entity_type`
- Where `relationship_status` is returned, either remove or derive from `stage`
- Where `last_interaction_at` is returned, source from `last_activity_at`
- Where `next_action_due` is returned, source from `next_action_date`
- Where `next_action_type` is returned, source from `next_action_note`
- Where `pipeline_stage` key is returned, source from `stage`

**SynthesisEngine calls:**
- `SynthesisEngine.enforce_rate_limit(account)` -> `SynthesisEngine.enforce_rate_limit(entry)` (type now PipelineEntry after Plan 03)
- `SynthesisEngine.generate(db, account)` -> `SynthesisEngine.generate(db, entry)`
- `SynthesisEngine.ask(db, account, question)` -> `SynthesisEngine.ask(db, entry, question)`

**Variable naming:**
- Rename `account` variables to `entry` throughout for clarity
- Rename `accounts` to `entries`

**CRITICAL: Check every endpoint systematically.** The research lists these active endpoints:
1. GET /relationships/ (list)
2. GET /relationships/{id} (detail)
3. PATCH /relationships/{id}/type (update relationship type)
4. POST /relationships/{id}/notes (add note)
5. POST /relationships/{id}/synthesize (AI synthesis)
6. POST /relationships/{id}/ask (AI Q&A)
7. POST /relationships/{id}/prep (meeting prep)
8. Any others found in the file

For reference patterns, look at `api/pipeline.py` which shows the canonical way to query PipelineEntry.
  </action>
  <verify>
    - `grep -n "Account\b" backend/src/flywheel/api/relationships.py | grep -v "#\|comment"` returns zero matches (no Account references except comments)
    - `grep -n "AccountContact" backend/src/flywheel/api/relationships.py` returns zero matches
    - `grep -n "graduated_at" backend/src/flywheel/api/relationships.py` returns zero matches
    - `grep -n "role_in_deal\|entity_level\|last_interaction_at\|next_action_due\|next_action_type\|relationship_status\|outreach_activities" backend/src/flywheel/api/relationships.py` returns zero matches
    - `cd backend && python -c "from flywheel.api.relationships import router"` succeeds
  </verify>
  <done>All 8+ relationship endpoints query PipelineEntry/Contact instead of Account/AccountContact. Stage-based filtering replaces graduated_at. All column names use new schema. SynthesisEngine receives PipelineEntry objects.</done>
</task>

</tasks>

<verification>
- Zero references to Account, AccountContact, graduated_at, role_in_deal, entity_level, last_interaction_at in relationships.py
- File imports successfully
- Response serialization uses new column names
- SynthesisEngine calls pass PipelineEntry objects
</verification>

<success_criteria>
- Every relationship endpoint queries pipeline_entries table
- Contact queries use pipeline_entry_id FK
- Context queries use ContextEntry.pipeline_entry_id
- Graduated_at predicate replaced with stage-based filter
- All serialization dicts use new column names
</success_criteria>

<output>
After completion, create `.planning/phases/104.1-legacy-model-cleanup/104.1-04-SUMMARY.md`
</output>
