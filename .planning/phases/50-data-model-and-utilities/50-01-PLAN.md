---
phase: 50-data-model-and-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/027_crm_tables.py
autonomous: true

must_haves:
  truths:
    - "Running alembic upgrade head creates accounts, account_contacts, and outreach_activities tables"
    - "All three new tables have RLS policies enforcing tenant isolation"
    - "context_entries table gains a nullable account_id FK column"
    - "Appropriate indexes exist for tenant-scoped queries and deduplication"
  artifacts:
    - path: "backend/alembic/versions/027_crm_tables.py"
      provides: "Alembic migration for CRM schema"
      contains: "op.create_table"
  key_links:
    - from: "accounts.tenant_id"
      to: "tenants.id"
      via: "FK constraint"
      pattern: "ForeignKey.*tenants\\.id"
    - from: "account_contacts.account_id"
      to: "accounts.id"
      via: "FK with CASCADE"
      pattern: "ForeignKey.*accounts\\.id.*ondelete.*CASCADE"
    - from: "outreach_activities.account_id"
      to: "accounts.id"
      via: "FK with CASCADE"
      pattern: "ForeignKey.*accounts\\.id.*ondelete.*CASCADE"
    - from: "context_entries.account_id"
      to: "accounts.id"
      via: "nullable FK"
      pattern: "account_id.*accounts\\.id"
---

<objective>
Create the Alembic migration that adds the three CRM tables (accounts, account_contacts, outreach_activities) with full RLS policies, indexes, and adds a nullable account_id FK to context_entries.

Purpose: DATA-01 -- the database schema is the foundation for all CRM functionality in phases 51-53.
Output: Single migration file 027_crm_tables.py
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/alembic/versions/026_documents_storage_path_nullable.py
@backend/alembic/versions/010_row_level_security.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Alembic migration 027_crm_tables</name>
  <files>backend/alembic/versions/027_crm_tables.py</files>
  <action>
Create migration file `027_crm_tables.py` with `revision = "027_crm_tables"` and `down_revision = "026_docs_storage_nullable"`.

**upgrade() must:**

1. Create `accounts` table:
   - `id` UUID PK, server_default gen_random_uuid()
   - `tenant_id` UUID NOT NULL FK to tenants.id
   - `name` Text NOT NULL (display name)
   - `normalized_name` Text NOT NULL (for dedup lookups)
   - `domain` Text nullable (company website domain)
   - `status` Text server_default 'prospect' (prospect/scored/sent/awaiting/replied/engaged/churned)
   - `fit_score` Numeric nullable
   - `fit_tier` Text nullable (Strong/Moderate/Weak)
   - `intel` JSONB server_default '{}'
   - `source` Text NOT NULL (how account was created)
   - `last_interaction_at` TIMESTAMPTZ nullable
   - `next_action_due` TIMESTAMPTZ nullable
   - `next_action_type` Text nullable
   - `created_at` TIMESTAMPTZ server_default now()
   - `updated_at` TIMESTAMPTZ server_default now()

   Indexes:
   - UniqueConstraint on (tenant_id, normalized_name) named `uq_account_tenant_normalized`
   - Index on (tenant_id, status) named `idx_account_tenant_status`
   - Index on (tenant_id, next_action_due) named `idx_account_next_action` WHERE next_action_due IS NOT NULL

2. Create `account_contacts` table:
   - `id` UUID PK, server_default gen_random_uuid()
   - `tenant_id` UUID NOT NULL FK to tenants.id
   - `account_id` UUID NOT NULL FK to accounts.id ON DELETE CASCADE
   - `name` Text NOT NULL
   - `email` Text nullable
   - `title` Text nullable (job title)
   - `role_in_deal` Text nullable (champion/blocker/decision-maker/influencer/end-user)
   - `linkedin_url` Text nullable
   - `notes` Text nullable
   - `source` Text NOT NULL
   - `created_at` TIMESTAMPTZ server_default now()
   - `updated_at` TIMESTAMPTZ server_default now()

   Indexes:
   - Index on (account_id) named `idx_contact_account`
   - Index on (tenant_id, email) named `idx_contact_tenant_email` WHERE email IS NOT NULL

3. Create `outreach_activities` table:
   - `id` UUID PK, server_default gen_random_uuid()
   - `tenant_id` UUID NOT NULL FK to tenants.id
   - `account_id` UUID NOT NULL FK to accounts.id ON DELETE CASCADE
   - `contact_id` UUID nullable FK to account_contacts.id ON DELETE SET NULL
   - `channel` Text NOT NULL (email/linkedin/phone)
   - `direction` Text NOT NULL (outbound/inbound)
   - `status` Text server_default 'sent' (draft/sent/delivered/opened/replied/bounced)
   - `subject` Text nullable
   - `body_preview` Text nullable (first 200 chars)
   - `sent_at` TIMESTAMPTZ nullable
   - `metadata` JSONB server_default '{}'
   - `created_at` TIMESTAMPTZ server_default now()

   Indexes:
   - Index on (account_id) named `idx_outreach_account`
   - Index on (tenant_id, sent_at DESC) named `idx_outreach_tenant_sent`

4. Add column `account_id` to `context_entries` -- UUID nullable FK to accounts.id ON DELETE SET NULL, with index `idx_context_account` on (account_id) WHERE account_id IS NOT NULL.

5. Enable RLS on all three new tables following the exact pattern from migration 010:
   - ALTER TABLE {t} ENABLE ROW LEVEL SECURITY
   - ALTER TABLE {t} FORCE ROW LEVEL SECURITY
   - GRANT SELECT, INSERT, UPDATE, DELETE ON {t} TO app_user
   - CREATE POLICY tenant_isolation_select (FOR SELECT USING tenant_id = current_setting('app.tenant_id', true)::uuid)
   - CREATE POLICY tenant_isolation_insert (FOR INSERT WITH CHECK ...)
   - CREATE POLICY tenant_isolation_update (FOR UPDATE USING ... WITH CHECK ...)
   - CREATE POLICY tenant_isolation_delete (FOR DELETE USING ...)

   Use a loop over ["accounts", "account_contacts", "outreach_activities"].

**downgrade() must:**
1. Drop column account_id from context_entries
2. Drop tables outreach_activities, account_contacts, accounts (in that order for FK deps)
   RLS policies are dropped automatically with the tables.

Use `sa.` prefix for SQLAlchemy types (import sqlalchemy as sa). Use `from sqlalchemy.dialects.postgresql import JSONB, TIMESTAMP` for PG types. Match the style of existing migrations (e.g., 026).
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'alembic/versions/027_crm_tables.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); print('revision:', mod.revision, 'down:', mod.down_revision)"` to confirm the migration file parses without syntax errors and has correct revision chain.
  </verify>
  <done>
Migration file exists at backend/alembic/versions/027_crm_tables.py with correct revision chain (down_revision = "026_docs_storage_nullable"). The upgrade function creates all three tables with correct columns, types, indexes, constraints, RLS policies, and adds account_id FK to context_entries. The downgrade function reverses everything cleanly.
  </done>
</task>

</tasks>

<verification>
1. Migration file parses without Python syntax errors
2. revision = "027_crm_tables" and down_revision = "026_docs_storage_nullable"
3. All three tables created with correct column types
4. UniqueConstraint on (tenant_id, normalized_name) for accounts
5. CASCADE deletes on account_contacts.account_id and outreach_activities.account_id
6. SET NULL on outreach_activities.contact_id and context_entries.account_id
7. RLS policies for all three new tables (12 policies total: 4 per table)
8. account_id nullable FK added to context_entries
</verification>

<success_criteria>
The migration file is syntactically valid Python, has correct Alembic revision chain, and contains DDL for all three CRM tables with RLS policies, plus the account_id column addition to context_entries.
</success_criteria>

<output>
After completion, create `.planning/phases/50-data-model-and-utilities/50-01-SUMMARY.md`
</output>
