---
phase: 05-review-api-and-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/api/email.py
  - frontend/src/lib/api.ts
autonomous: true

must_haves:
  truths:
    - "GET /api/v1/email/threads returns paginated thread summaries sorted by priority tier"
    - "GET /api/v1/email/threads/:threadId returns messages with scores, reasoning, context refs, and pending draft"
    - "POST /api/v1/email/sync triggers background Gmail sync and returns immediately"
    - "Frontend api client has a put() method for draft editing"
  artifacts:
    - path: "backend/src/flywheel/api/email.py"
      provides: "GET threads, GET thread detail, GET digest, POST sync endpoints plus existing approve/dismiss/edit"
      exports: ["router"]
    - path: "frontend/src/lib/api.ts"
      provides: "HTTP client with get/post/patch/put/delete methods"
      contains: "put:"
  key_links:
    - from: "backend/src/flywheel/api/email.py"
      to: "flywheel.db.models.Email, EmailScore, EmailDraft"
      via: "SQLAlchemy LEFT JOIN query"
      pattern: "outerjoin.*EmailScore|outerjoin.*EmailDraft"
    - from: "backend/src/flywheel/api/email.py"
      to: "flywheel.services.gmail_sync.sync_gmail"
      via: "BackgroundTasks callback"
      pattern: "background_tasks.add_task"
---

<objective>
Add the read-side API endpoints to api/email.py (GET threads, GET thread detail, GET digest, POST manual sync) and add the missing `put` method to the frontend API client.

Purpose: The frontend (Plan 02) needs these endpoints to render the scored inbox. The existing api/email.py only has draft lifecycle endpoints (approve/dismiss/edit). This plan adds the read endpoints without touching the existing ones.

Output: Extended api/email.py with 4 new endpoints; api.ts with put method.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-review-api-and-frontend/05-RESEARCH.md
@backend/src/flywheel/api/email.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/services/gmail_sync.py
@backend/src/flywheel/api/briefing.py
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /email/threads and GET /email/threads/{thread_id} endpoints</name>
  <files>backend/src/flywheel/api/email.py</files>
  <action>
Extend the existing api/email.py file (do NOT delete or modify the existing approve/dismiss/edit endpoints). Add at the top of the endpoints section (before the draft lifecycle endpoints):

1. Add new Pydantic response models above the existing ones:
   - `ThreadSummary`: thread_id (str), subject (str|None), sender_name (str|None), sender_email (str), latest_received_at (datetime), message_count (int), max_priority (int|None), priority_tier (str), has_pending_draft (bool), draft_id (str|None), is_read (bool)
   - `ThreadListResponse`: threads (list[ThreadSummary]), total (int), offset (int), limit (int)
   - `MessageScore`: priority (int), category (str), reasoning (str|None), suggested_action (str|None), context_refs (list[dict])
   - `MessageDetail`: id (str), gmail_message_id (str), sender_email (str), sender_name (str|None), subject (str|None), snippet (str|None), received_at (datetime), is_read (bool), is_replied (bool), score (MessageScore|None)
   - `DraftDetail`: id (str), status (str), draft_body (str|None), user_edits (str|None)
   - `ThreadDetailResponse`: thread_id (str), subject (str|None), messages (list[MessageDetail]), draft (DraftDetail|None), max_priority (int|None), priority_tier (str)
   - Helper function `priority_to_tier(p: int|None) -> str` mapping 5=critical, 4=high, 3=medium, 1-2=low, None=unscored

2. `GET /email/threads` endpoint:
   - Query params: priority_min (int|None), offset (int=0), limit (int=50)
   - Single query: `select(Email, EmailScore, EmailDraft).outerjoin(EmailScore, EmailScore.email_id == Email.id).outerjoin(EmailDraft, (EmailDraft.email_id == Email.id) & (EmailDraft.status == "pending")).where(Email.tenant_id == user.tenant_id).order_by(Email.received_at.desc())`
   - Group in Python by `gmail_thread_id` using a dict (not defaultdict). For each group, compute: max_priority as MAX(score.priority) where is_replied=False, has_pending_draft, draft_id, message_count, latest_received_at, subject/sender from most recent message.
   - If `priority_min` is set, filter threads where max_priority >= priority_min (exclude None-priority threads).
   - Sort: priority DESC (None treated as 0), then latest_received_at DESC.
   - Slice with offset/limit. Count total threads before slicing.
   - Return ThreadListResponse.
   - IMPORTANT: Do NOT call get_thread_priority() per thread (N+1). Inline the MAX computation during grouping.

3. `GET /email/threads/{thread_id}` endpoint:
   - Path param: thread_id (str) — this is gmail_thread_id.
   - Query emails where gmail_thread_id == thread_id AND tenant_id == user.tenant_id, with LEFT JOIN on EmailScore and EmailDraft.
   - Build MessageDetail list from the results. For each email, attach its score if present.
   - Find the pending draft (if any) across all emails in the thread.
   - Compute max_priority same as thread list.
   - Return ThreadDetailResponse.
   - If no emails found for that thread_id, raise 404.

Add necessary imports at top: `from collections import defaultdict` (if needed), `from datetime import datetime` (already imported), `from fastapi import BackgroundTasks, Query`.
  </action>
  <verify>Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.email import router; print([r.path for r in router.routes])"` — should list /threads, /threads/{thread_id}, plus existing draft endpoints.</verify>
  <done>GET /email/threads returns ThreadListResponse with priority-sorted thread summaries. GET /email/threads/{thread_id} returns ThreadDetailResponse with messages, scores, and draft. Both use single JOIN queries with Python-side grouping.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /email/sync, GET /email/digest endpoints and api.ts put method</name>
  <files>backend/src/flywheel/api/email.py, frontend/src/lib/api.ts</files>
  <action>
1. Add to api/email.py (after the thread endpoints, before draft lifecycle endpoints):

   **POST /email/sync:**
   - Pydantic model `SyncResponse`: message (str), syncing (bool)
   - Depends on `require_tenant`, `get_tenant_db`, and `BackgroundTasks` (from fastapi).
   - Query for gmail-read Integration with status="connected" for the tenant. If not found, raise 400.
   - Define an inner async function `_run_sync()` that imports `sync_gmail` from `flywheel.services.gmail_sync` and `get_session_factory, tenant_session` from `flywheel.db.session`. Creates a fresh tenant session via `async with tenant_session(get_session_factory(), str(user.tenant_id), str(user.id)) as sync_db:` and calls `await sync_gmail(sync_db, integration)`. This matches the exact pattern used by `_sync_one_integration()` in `gmail_sync.py`.
   - Add the task via `background_tasks.add_task(_run_sync)`.
   - Return SyncResponse(message="Sync triggered", syncing=True).
   - Add import at top of file: `from flywheel.db.session import get_session_factory, tenant_session`.

   **GET /email/digest:**
   - Pydantic model `DigestThread`: thread_id (str), subject (str|None), sender_email (str), category (str|None), priority (int|None), message_count (int)
   - Pydantic model `DigestResponse`: date (str), threads (list[DigestThread]), total (int)
   - Query emails received today (received_at >= start of today UTC) that have an EmailScore with priority <= 2.
   - Group by gmail_thread_id, return one DigestThread per thread with category from the score.
   - Return DigestResponse with today's date as ISO string.

2. Add `put` method to `frontend/src/lib/api.ts`:
   - In the `api` object, add after the `post` method:
   ```
   put: <T>(path: string, body?: unknown) =>
     request<T>(path, { method: 'PUT', body: body ? JSON.stringify(body) : undefined }),
   ```
  </action>
  <verify>Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.email import router; print([r.path for r in router.routes])"` — should list /sync, /digest, /threads, /threads/{thread_id}, and draft endpoints. Then verify api.ts: `grep -c 'put:' frontend/src/lib/api.ts` should return 1.</verify>
  <done>POST /email/sync triggers background Gmail sync. GET /email/digest returns today's low-priority threads grouped by category. Frontend api.ts has put() method for draft editing.</done>
</task>

</tasks>

<verification>
- All 4 new endpoints importable: `from flywheel.api.email import router`
- Existing approve/dismiss/edit endpoints unchanged (verify approve endpoint signature still exists)
- No N+1 queries — thread list uses single JOIN + Python grouping
- api.ts exports get, post, patch, put, delete methods
</verification>

<success_criteria>
- GET /email/threads returns ThreadListResponse sorted by priority tier with pagination
- GET /email/threads/{thread_id} returns full thread detail with messages, scores, and pending draft
- POST /email/sync triggers background sync and returns immediately
- GET /email/digest returns today's low-priority email summary
- api.ts put method exists for draft editing
- Zero changes to existing approve/dismiss/edit endpoint logic
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-api-and-frontend/05-01-SUMMARY.md`
</output>
