---
phase: 64-unified-meetings
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - backend/src/flywheel/api/meetings.py
  - backend/src/flywheel/services/calendar_sync.py
autonomous: true

must_haves:
  truths:
    - "GET /meetings/?time=upcoming returns meetings with meeting_date >= now, sorted soonest first"
    - "GET /meetings/?time=past returns meetings with meeting_date < now, sorted most recent first"
    - "POST /meetings/{id}/prep auto-links account if needed and returns run_id + stream_url"
    - "get_meeting_prep_suggestions() queries Meeting table (not WorkItems) for scheduled meetings within 48 hours"
  artifacts:
    - path: "backend/src/flywheel/api/meetings.py"
      provides: "time param on list_meetings, POST /{id}/prep endpoint"
      contains: "prep_meeting"
    - path: "backend/src/flywheel/services/calendar_sync.py"
      provides: "get_meeting_prep_suggestions rewritten to query Meeting table"
      contains: "Meeting.processing_status == \"scheduled\""
  key_links:
    - from: "backend/src/flywheel/api/meetings.py (prep_meeting)"
      to: "backend/src/flywheel/engines/meeting_processor_web.py"
      via: "auto_link_meeting_to_account called with get_session_factory() as factory arg"
      pattern: "auto_link_meeting_to_account\\(get_session_factory\\(\\)"
    - from: "backend/src/flywheel/api/meetings.py (prep_meeting)"
      to: "backend/src/flywheel/db/models.py"
      via: "SkillRun creation with Account-ID prefix"
      pattern: "Account-ID:"
---

<objective>
Add time-based listing, meeting prep endpoint, and migrate prep suggestions to the unified meetings table.

Purpose: Complete the backend API surface for the unified meetings experience -- users can browse upcoming/past meetings, trigger prep from any scheduled meeting, and get suggestions from the meetings table.
Output: Updated meetings.py with time param + prep endpoint, rewritten get_meeting_prep_suggestions().
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-unified-meetings/64-RESEARCH.md
@.planning/phases/64-unified-meetings/64-01-SUMMARY.md
@backend/src/flywheel/api/meetings.py
@backend/src/flywheel/api/relationships.py (lines 848-902, prep_relationship pattern)
@backend/src/flywheel/services/calendar_sync.py
@backend/src/flywheel/engines/meeting_processor_web.py (auto_link_meeting_to_account)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Time param on GET /meetings/ and POST /meetings/{id}/prep endpoint</name>
  <files>backend/src/flywheel/api/meetings.py</files>
  <action>
**1. Add `time` query param to `list_meetings()`:**

Add `time: str | None = None` parameter (values: "upcoming" | "past" | None).

Logic:
- `time == "upcoming"`: add `.where(Meeting.meeting_date >= now)` and `.order_by(Meeting.meeting_date.asc())` (soonest first)
- `time == "past"`: add `.where(Meeting.meeting_date < now)` and `.order_by(Meeting.meeting_date.desc())` (most recent first)
- `time is None`: keep existing default `.order_by(Meeting.meeting_date.desc())`

Apply the same time filter to the count query so pagination totals are accurate. Import `datetime, timezone` if not already imported (they are).

Also add `provider` to the list item serialization dict (needed by frontend to distinguish google-calendar vs granola). Add `location` and `calendar_event_id` to the serialization so the frontend can show location and identify calendar-sourced meetings.

**2. Add `POST /meetings/{id}/prep` endpoint:**

Follow the exact pattern from `POST /relationships/{id}/prep` in relationships.py:

```python
@router.post("/{meeting_id}/prep", status_code=202)
async def prep_meeting(
    meeting_id: UUID,
    user: TokenPayload = Depends(require_tenant),
    db: AsyncSession = Depends(get_tenant_db),
) -> dict:
```

Implementation:
1. Load Meeting by id + tenant_id + deleted_at IS NULL. Return 404 if not found.
2. If `meeting.account_id` is set, use it directly. Skip to step 5.
3. If `meeting.account_id` is NULL, call `auto_link_meeting_to_account`. **CRITICAL: This function requires an `async_sessionmaker` factory as its first argument -- it opens its own sessions internally and cannot reuse the FastAPI `db` session.** Call it as:
   ```python
   from flywheel.db.session import get_session_factory

   linked_id = await auto_link_meeting_to_account(
       get_session_factory(),
       tenant_id=user.tenant_id,
       attendees=meeting.attendees or [],
       user_id=user.user_id,
       meeting_title=meeting.title or "",
   )
   ```
   If it returns an account_id, **update the meeting row on the existing `db` session and commit before proceeding:**
   ```python
   if linked_id:
       meeting.account_id = linked_id
       await db.commit()
       await db.refresh(meeting)
   ```
   This commit MUST happen before SkillRun creation so the account_id is persisted regardless of what happens next.
4. If still no account_id after auto-link attempt, raise 400 with detail "No account could be linked to this meeting. Add attendees with company email addresses."
5. Build `input_text` with the Account-ID dispatch prefix (same format as relationships prep):
   ```
   Account-ID: {account_id}
   Account-Name: {account_name}
   Meeting-ID: {meeting_id}
   ```
   Load the Account name from DB (query Account by account_id) for the Account-Name line.
6. Create SkillRun with `skill_name="meeting-prep"`, `input_text`, `status="pending"`.
7. Return `{"run_id": str(run.id), "stream_url": f"/api/v1/skills/runs/{run.id}/stream"}`.

Import `Account` from models. Import `auto_link_meeting_to_account` from `flywheel.engines.meeting_processor_web`. Import `get_session_factory` from `flywheel.db.session`. Import `status` from fastapi for HTTP_202_ACCEPTED.
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.meetings import prep_meeting; print('OK')"` to verify the endpoint imports. Run `grep -c "time.*upcoming\|time.*past" src/flywheel/api/meetings.py` to confirm time param handling. Run `grep "get_session_factory" src/flywheel/api/meetings.py` to confirm factory import is present. Run `grep "await db.commit" src/flywheel/api/meetings.py` to confirm explicit commit after auto-link.
  </verify>
  <done>GET /meetings/ accepts time=upcoming|past with correct sort order. POST /meetings/{id}/prep calls auto_link_meeting_to_account with get_session_factory() as factory arg, commits meeting.account_id to DB before SkillRun creation, and returns run_id + stream_url for SSE consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate get_meeting_prep_suggestions() to Meeting table</name>
  <files>backend/src/flywheel/services/calendar_sync.py</files>
  <action>
Rewrite `get_meeting_prep_suggestions()` to query the Meeting table instead of WorkItem:

1. Change imports at top of file: the function now needs `Meeting` (already imported from Task 1 in Plan 01) and `SuggestionDismissal` (keep this import -- dismissals still work the same way).

2. Replace the query:
   - Old: `WorkItem` with `type == "meeting"`, `status == "upcoming"`, `scheduled_at` within 48 hours
   - New: `Meeting` with `processing_status == "scheduled"`, `meeting_date` within 48 hours, `deleted_at IS NULL`

3. Replace the external attendees check:
   - Old: `(meeting.data or {}).get("has_external_attendees", False)`
   - New: Check `meeting.attendees` directly -- `any(a.get("is_external", False) for a in (meeting.attendees or []))`. The Meeting table stores attendees as JSONB `[{"email": ..., "name": ..., "is_external": ...}]`.

4. Update the suggestion dict:
   - Change `"work_item_id"` key to `"meeting_id"` (string UUID)
   - Change `"scheduled_at"` to use `meeting.meeting_date` (not `scheduled_at`)
   - Change `"attendees"` to use `meeting.attendees` directly (already in correct format, not nested in `data`)
   - Add `"account_id"` field: `str(meeting.account_id) if meeting.account_id else None`

5. Update the dismissal check: change `suggestion_key == str(meeting.id)` (Meeting.id instead of WorkItem.id). This is the same UUID type so the pattern works identically.

6. Remove `WorkItem` and `MeetingClassification` from imports if they are no longer used anywhere in the file (they should not be after Plan 01 Task 2 replaced upsert_meeting_work_item). Keep `SuggestionDismissal` since it is still used.

7. Remove `from flywheel.services.meeting_classifier import classify_meeting` if it was not already removed in Plan 01 (it should have been).
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.services.calendar_sync import get_meeting_prep_suggestions; print('OK')"`. Run `grep -c "WorkItem" src/flywheel/services/calendar_sync.py` -- should return 0.
  </verify>
  <done>get_meeting_prep_suggestions() queries Meeting table for scheduled meetings with external attendees within 48 hours. No WorkItem references remain in calendar_sync.py.</done>
</task>

</tasks>

<verification>
1. `grep "time.*str.*None" backend/src/flywheel/api/meetings.py` confirms time param exists
2. `grep "prep_meeting" backend/src/flywheel/api/meetings.py` confirms prep endpoint exists
3. `grep "get_session_factory" backend/src/flywheel/api/meetings.py` confirms factory import for auto_link
4. `grep "await db.commit" backend/src/flywheel/api/meetings.py` confirms explicit commit after auto-link
5. `grep "auto_link_meeting_to_account" backend/src/flywheel/api/meetings.py` confirms account linking
6. `grep "Meeting.processing_status.*scheduled" backend/src/flywheel/services/calendar_sync.py` confirms suggestions migration
7. `grep -c "WorkItem" backend/src/flywheel/services/calendar_sync.py` returns 0
</verification>

<success_criteria>
- GET /meetings/?time=upcoming returns future meetings sorted ascending
- GET /meetings/?time=past returns past meetings sorted descending
- POST /meetings/{id}/prep calls auto_link_meeting_to_account with get_session_factory() factory arg (not the FastAPI db session), commits meeting.account_id before SkillRun creation, returns run_id + stream_url
- get_meeting_prep_suggestions() queries Meeting table with processing_status='scheduled' and meeting_date within 48 hours
- Zero WorkItem references remain in calendar_sync.py
</success_criteria>

<output>
After completion, create `.planning/phases/64-unified-meetings/64-02-SUMMARY.md`
</output>
