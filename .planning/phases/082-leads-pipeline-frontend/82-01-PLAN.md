---
phase: 082-leads-pipeline-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/leads/types/lead.ts
  - frontend/src/features/leads/api.ts
  - frontend/src/features/leads/hooks/useLeads.ts
  - frontend/src/features/leads/hooks/useLeadsPipeline.ts
  - frontend/src/features/leads/hooks/useLeadDetail.ts
  - frontend/src/features/leads/hooks/useLeadGraduate.ts
  - frontend/src/features/leads/hooks/useLeadsColumns.ts
  - frontend/src/features/leads/components/cell-renderers/StageBadge.tsx
  - frontend/src/features/leads/components/cell-renderers/PurposePills.tsx
  - frontend/src/features/leads/components/cell-renderers/LeadGraduateButton.tsx
  - frontend/src/features/leads/components/cell-renderers/LeadCompanyCell.tsx
autonomous: true

must_haves:
  truths:
    - "Lead types match backend serializer shapes exactly (Lead, LeadContact, LeadMessage, LeadsResponse, PipelineFunnel, LeadParams)"
    - "API functions call correct endpoints (/leads/, /leads/pipeline, /leads/{id}, /leads/{id}/graduate)"
    - "React Query hooks provide paginated leads, funnel counts, detail, and graduation mutation"
    - "Column hook defines 8 columns with localStorage persistence"
    - "Cell renderers display stage badge, purpose pills, company name+domain, and graduate button"
  artifacts:
    - path: "frontend/src/features/leads/types/lead.ts"
      provides: "Lead, LeadContact, LeadMessage, LeadsResponse, PipelineFunnel, LeadParams types"
      exports: ["Lead", "LeadContact", "LeadMessage", "LeadsResponse", "PipelineFunnel", "LeadParams"]
    - path: "frontend/src/features/leads/api.ts"
      provides: "fetchLeads, fetchLeadsPipeline, fetchLeadDetail, graduateLead API functions"
      exports: ["fetchLeads", "fetchLeadsPipeline", "fetchLeadDetail", "graduateLead"]
    - path: "frontend/src/features/leads/hooks/useLeads.ts"
      provides: "Paginated leads query with placeholderData"
      exports: ["useLeads"]
    - path: "frontend/src/features/leads/hooks/useLeadsPipeline.ts"
      provides: "Funnel counts query"
      exports: ["useLeadsPipeline"]
    - path: "frontend/src/features/leads/hooks/useLeadDetail.ts"
      provides: "Lead detail query, enabled when id set"
      exports: ["useLeadDetail"]
    - path: "frontend/src/features/leads/hooks/useLeadGraduate.ts"
      provides: "Graduation mutation with invalidation"
      exports: ["useLeadGraduate"]
    - path: "frontend/src/features/leads/hooks/useLeadsColumns.ts"
      provides: "8-column ag-grid column defs with localStorage persistence"
      exports: ["useLeadsColumns"]
    - path: "frontend/src/features/leads/components/cell-renderers/StageBadge.tsx"
      provides: "Colored stage pill for ag-grid cells"
    - path: "frontend/src/features/leads/components/cell-renderers/PurposePills.tsx"
      provides: "Purpose tag pills with +N overflow"
    - path: "frontend/src/features/leads/components/cell-renderers/LeadGraduateButton.tsx"
      provides: "Graduate action cell with stopPropagation"
    - path: "frontend/src/features/leads/components/cell-renderers/LeadCompanyCell.tsx"
      provides: "Company name + domain subtitle cell"
  key_links:
    - from: "frontend/src/features/leads/api.ts"
      to: "@/lib/api"
      via: "import api"
      pattern: "import.*api.*from.*@/lib/api"
    - from: "frontend/src/features/leads/hooks/useLeads.ts"
      to: "frontend/src/features/leads/api.ts"
      via: "fetchLeads in queryFn"
      pattern: "fetchLeads"
    - from: "frontend/src/features/leads/hooks/useLeadGraduate.ts"
      to: "frontend/src/features/leads/api.ts"
      via: "graduateLead in mutationFn"
      pattern: "graduateLead"
    - from: "frontend/src/features/leads/hooks/useLeadsColumns.ts"
      to: "cell-renderers"
      via: "cellRenderer imports"
      pattern: "StageBadge|PurposePills|LeadGraduateButton|LeadCompanyCell"
---

<objective>
Create the entire data layer and cell renderers for the leads pipeline feature: TypeScript types, API functions, React Query hooks, ag-grid column definitions, and 4 cell renderer components.

Purpose: Establish the foundation that the page orchestrator and side panel will consume. All data fetching, mutation, and table rendering logic is isolated here.
Output: 11 files — types, API, 5 hooks, 4 cell renderers
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/082-leads-pipeline-frontend/82-RESEARCH.md
@frontend/src/features/leads/DESIGN-BRIEF.md
@frontend/src/features/pipeline/api.ts
@frontend/src/features/pipeline/types/pipeline.ts
@frontend/src/features/pipeline/hooks/usePipeline.ts
@frontend/src/features/pipeline/hooks/usePipelineColumns.ts
@frontend/src/features/pipeline/hooks/useGraduate.ts
@frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx
@frontend/src/features/pipeline/components/cell-renderers/CompanyCell.tsx
@frontend/src/features/pipeline/components/cell-renderers/GraduateButton.tsx
@frontend/src/features/pipeline/components/cell-renderers/DaysSinceCell.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/design-tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API functions, and React Query hooks</name>
  <files>
    frontend/src/features/leads/types/lead.ts
    frontend/src/features/leads/api.ts
    frontend/src/features/leads/hooks/useLeads.ts
    frontend/src/features/leads/hooks/useLeadsPipeline.ts
    frontend/src/features/leads/hooks/useLeadDetail.ts
    frontend/src/features/leads/hooks/useLeadGraduate.ts
  </files>
  <action>
Create the data layer following pipeline patterns exactly:

1. **types/lead.ts** — Define all 6 interfaces from the research code examples section: Lead, LeadContact, LeadMessage, LeadsResponse, PipelineFunnel, LeadParams. Also export STAGE_COLORS map and STAGE_ORDER array. These are derived directly from backend serializers in leads.py.

2. **api.ts** — 4 functions following `features/pipeline/api.ts` pattern:
   - `fetchLeads(params: LeadParams)` — GET /leads/ with clean param filtering (strip undefined/null)
   - `fetchLeadsPipeline()` — GET /leads/pipeline
   - `fetchLeadDetail(id: string)` — GET /leads/{id}
   - `graduateLead(id: string)` — POST /leads/{id}/graduate
   All use `api` from `@/lib/api`.

3. **hooks/useLeads.ts** — `useLeads(params: LeadParams)` wrapping useQuery with queryKey `['leads', params]` and `placeholderData: (prev) => prev` for smooth pagination (same as usePipeline.ts).

4. **hooks/useLeadsPipeline.ts** — `useLeadsPipeline()` wrapping useQuery with queryKey `['leads-pipeline']`. This is a separate, independently cached query for funnel counts.

5. **hooks/useLeadDetail.ts** — `useLeadDetail(id: string | null)` wrapping useQuery with queryKey `['lead-detail', id]` and `enabled: !!id` so it only fires when a lead is selected.

6. **hooks/useLeadGraduate.ts** — `useLeadGraduate()` wrapping useMutation. On success: invalidate `['leads']`, `['leads-pipeline']`, `['accounts']`, fire `toast.success('{account_name} graduated to accounts')`. On error: `toast.error('Failed to graduate lead')`. Follow useGraduate.ts pattern exactly.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — no type errors in leads files. Verify all exports are importable.</verify>
  <done>6 type interfaces, 4 API functions, and 4 React Query hooks exist with correct query keys, endpoint paths, and invalidation patterns</done>
</task>

<task type="auto">
  <name>Task 2: Column definitions hook and 4 cell renderers</name>
  <files>
    frontend/src/features/leads/hooks/useLeadsColumns.ts
    frontend/src/features/leads/components/cell-renderers/StageBadge.tsx
    frontend/src/features/leads/components/cell-renderers/PurposePills.tsx
    frontend/src/features/leads/components/cell-renderers/LeadGraduateButton.tsx
    frontend/src/features/leads/components/cell-renderers/LeadCompanyCell.tsx
  </files>
  <action>
Create 4 cell renderers and the column definitions hook:

1. **StageBadge.tsx** — ag-grid cell renderer. Pill shape (rounded-full, px-2.5 py-0.5) with 6px colored dot + stage label text. Uses STAGE_COLORS map from types/lead.ts. Background = stage color at 10% opacity. Text + dot = stage color at full saturation. Font: 11px, weight 500. Add aria-label="{stage} stage". Must be typed as ICellRendererParams<Lead>.

2. **PurposePills.tsx** — ag-grid cell renderer. Renders purpose[] array as pills. Max 2 visible, with "+N" overflow pill. Each pill: rounded-full, px-2 py-px, font 10px weight 500. Use purpose-specific subtle tint colors from design brief (sales=coral tint, fundraising=green tint, advisors=blue tint, partnerships=purple tint). Gap 4px. Must handle empty array gracefully.

3. **LeadGraduateButton.tsx** — ag-grid cell renderer. Shows "Graduate" text button (12px, weight 500, brand-coral color, hover brand-tint bg, rounded-lg, px-2.5 py-1). Hidden if `data.graduated_at` is set. On click: calls `event.stopPropagation()` then calls `context.onGraduate(data.id)` passed via ag-grid context prop. aria-label="Graduate {data.name} to account".

4. **LeadCompanyCell.tsx** — ag-grid cell renderer. Company name (14px, weight 600, headingText) + domain subtitle below (12px, secondaryText). Domain is plain text in table, not clickable. Handle null domain gracefully (show name only).

5. **useLeadsColumns.ts** — Hook returning column defs array and column state handlers. 8 columns per design brief table:
   - Company: cellRenderer LeadCompanyCell, flex 1.2, minWidth 180, pinned 'left', sortable
   - Stage: cellRenderer StageBadge, width 110
   - Fit: cellRenderer FitTierBadge (create a leads-specific copy here or inline — do NOT import from pipeline since it's typed to PipelineItem). Width 110. Use the same badges.fitTier color map from design-tokens.
   - Contacts: field contact_count, width 80, right-aligned, tabular-nums
   - Purpose: cellRenderer PurposePills, width 140, not sortable
   - Source: field source, width 100, capitalize via valueFormatter
   - Added: field created_at, width 100, relative time formatter (copy formatRelativeTime from usePipelineColumns.ts lines 13-27)
   - Action: cellRenderer LeadGraduateButton, width 90, pinned 'right', not sortable

   Column state persistence: localStorage key `'flywheel:leads:columnState'`, same getInitialState() and onColumnStateChanged() pattern as usePipelineColumns.ts.

For FitTierBadge: create a leads-specific copy as a simple inline renderer within useLeadsColumns.ts (or as a separate file). Use the same `badges.fitTier` color map from `@/lib/design-tokens`. This avoids importing from pipeline which is typed to PipelineItem. The badge is small (~20 lines).
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — no type errors. Verify useLeadsColumns exports column defs array and state handlers.</verify>
  <done>4 cell renderers render correct visual output for their data types. useLeadsColumns returns 8 column definitions with correct renderers, widths, pinning, and localStorage persistence.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors in all 11 new files
- All exports are correctly typed and importable
- No imports from pipeline feature (all leads-specific)
- STAGE_COLORS and STAGE_ORDER exported from types/lead.ts
- Query keys match: ['leads', params], ['leads-pipeline'], ['lead-detail', id]
- Graduation mutation invalidates ['leads'], ['leads-pipeline'], ['accounts']
</verification>

<success_criteria>
The complete data layer for the leads feature exists: types match backend serializers, API functions hit correct endpoints, hooks manage server state with React Query, column definitions configure 8 ag-grid columns with cell renderers, and all cell renderers match the design brief specs.
</success_criteria>

<output>
After completion, create `.planning/phases/082-leads-pipeline-frontend/82-01-SUMMARY.md`
</output>
