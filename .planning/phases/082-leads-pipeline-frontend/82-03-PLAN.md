---
phase: 082-leads-pipeline-frontend
plan: 03
type: execute
wave: 3
depends_on: ["82-02"]
files_modified:
  - frontend/src/features/leads/components/ContactCard.tsx
  - frontend/src/features/leads/components/MessageThread.tsx
  - frontend/src/features/leads/components/LeadSidePanel.tsx
  - frontend/src/features/leads/components/LeadsPage.tsx
  - frontend/src/app/routes.tsx
  - frontend/src/features/navigation/components/AppSidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking a table row opens a side panel showing company info, contacts, and message threads"
    - "Side panel closes on backdrop click, X button, or Escape key"
    - "Contacts display as an accordion (one expanded at a time) with message timeline inside"
    - "Graduate button in table and panel opens confirmation dialog, then graduates lead with animation and toast"
    - "/leads route is registered and lazy-loaded"
    - "Leads nav item appears in sidebar above Pipeline"
  artifacts:
    - path: "frontend/src/features/leads/components/ContactCard.tsx"
      provides: "Expandable contact accordion item with details and messages"
      min_lines: 50
    - path: "frontend/src/features/leads/components/MessageThread.tsx"
      provides: "Vertical timeline of outreach messages"
      min_lines: 40
    - path: "frontend/src/features/leads/components/LeadSidePanel.tsx"
      provides: "Right detail panel with company info, contacts, graduate button"
      min_lines: 80
  key_links:
    - from: "frontend/src/features/leads/components/LeadsPage.tsx"
      to: "frontend/src/features/leads/components/LeadSidePanel.tsx"
      via: "conditional render when selectedLead is set"
      pattern: "selectedLead.*LeadSidePanel"
    - from: "frontend/src/features/leads/components/LeadSidePanel.tsx"
      to: "frontend/src/features/leads/hooks/useLeadDetail.ts"
      via: "useLeadDetail(lead.id) for full data"
      pattern: "useLeadDetail"
    - from: "frontend/src/features/leads/components/LeadSidePanel.tsx"
      to: "frontend/src/features/leads/components/ContactCard.tsx"
      via: "maps over contacts array"
      pattern: "ContactCard"
    - from: "frontend/src/features/leads/components/ContactCard.tsx"
      to: "frontend/src/features/leads/components/MessageThread.tsx"
      via: "renders messages when expanded"
      pattern: "MessageThread"
    - from: "frontend/src/app/routes.tsx"
      to: "frontend/src/features/leads/components/LeadsPage.tsx"
      via: "lazy import and Route element"
      pattern: "LeadsPage"
    - from: "frontend/src/features/leads/components/LeadsPage.tsx"
      to: "frontend/src/features/leads/hooks/useLeadGraduate.ts"
      via: "useLeadGraduate mutation for graduation flow"
      pattern: "useLeadGraduate"
---

<objective>
Complete the leads pipeline feature: side panel with contacts and message threads, graduation flow with confirmation dialog, and route/navigation registration. After this plan, the entire feature is functional end-to-end.

Purpose: Deliver the drill-in and graduation experiences — a founder can click a lead to see contacts and messages, then graduate the lead to a full account.
Output: 3 new components + updates to LeadsPage, routes.tsx, AppSidebar.tsx
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/082-leads-pipeline-frontend/82-RESEARCH.md
@.planning/phases/082-leads-pipeline-frontend/82-01-SUMMARY.md
@.planning/phases/082-leads-pipeline-frontend/82-02-SUMMARY.md
@frontend/src/features/leads/DESIGN-BRIEF.md
@frontend/src/features/pipeline/components/PipelineSidePanel.tsx
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/components/GraduationModal.tsx
@frontend/src/app/routes.tsx
@frontend/src/features/navigation/components/AppSidebar.tsx
@frontend/src/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContactCard, MessageThread, and LeadSidePanel</name>
  <files>
    frontend/src/features/leads/components/ContactCard.tsx
    frontend/src/features/leads/components/MessageThread.tsx
    frontend/src/features/leads/components/LeadSidePanel.tsx
  </files>
  <action>
**MessageThread.tsx** — Vertical timeline of outreach messages:

Props: `messages: LeadMessage[]`.

Layout: vertical timeline with connected steps.
- Left edge: numbered circles (20px diameter, 1px border in status color, white bg, number centered 11px weight 600)
- Vertical line connecting circles: 1px solid subtle-border, positioned between circles
- Right of each node: message card

Message card (collapsed): flex row — channel icon (Mail or Linkedin from lucide, 14px, secondaryText) + status dot (6px, rounded-full, status color from design brief status colors table) + subject (13px, weight 500, truncate) + timestamp (12px, secondaryText, right-aligned). Cursor pointer, hover bg rgba(0,0,0,0.02).

Message card (expanded): subject (14px, weight 600), body (13px, weight 400, whitespace pre-wrap, max-h-[200px] overflow-auto), timestamp row showing "Drafted: Mar 28" / "Sent: Mar 29" / "Replied: Apr 1" as applicable (12px, secondaryText). Background rgba(0,0,0,0.02) rounded-lg px-3 py-2.

Status colors: drafted=#d97706, sent=#0284c7, delivered=#2563eb, replied=#16a34a, bounced=#dc2626.

State: `expandedId: string | null` — track which message is expanded. Click toggles.

Accessibility: Each message is a button with aria-expanded. Enter/Space to toggle.

Motion: height via CSS grid trick (grid-template-rows: 0fr to 1fr), 150ms ease. Respect prefers-reduced-motion.

Handle empty messages array: render nothing (parent ContactCard handles "no messages" text).

**ContactCard.tsx** — Expandable contact accordion item:

Props: `contact: LeadContact`, `isExpanded: boolean`, `onToggle: () => void`.

Collapsed layout: flex row, items-center, gap-3, py-3 px-4.
- Left: 36px avatar circle with initials (bg brand-tint, text brand-coral, 13px weight 600)
- Center: name (14px, weight 600, headingText), title below (12px, secondaryText), role badge pill (10px) if role present
- Right: StageBadge (per-contact pipeline_stage — import from cell-renderers/) + ChevronDown icon (rotates 180deg when expanded)
- Bottom border 1px solid subtle-border (except last child via CSS :last-child)
- Cursor pointer, hover bg rgba(0,0,0,0.02)

Expanded layout (below header): contact details row + MessageThread.
- Contact details: email with Mail icon + mailto link, linkedin with Linkedin icon + external link (target _blank). 12px, info color. Only show if email/linkedin_url present.
- Gap 12px between details and messages.
- MessageThread with contact.messages (if present and non-empty).
- If no messages: muted text "No outreach messages yet" (13px, secondaryText).

Accessibility: button role on header, aria-expanded, Enter/Space to toggle, aria-label="{name}, {title}, click to expand".

Motion: expand via CSS grid trick, 150ms ease. Chevron rotation 200ms ease.

**LeadSidePanel.tsx** — Right detail panel:

Props: `lead: Lead`, `onClose: () => void`, `onGraduate: (id: string) => void`.

Uses `useLeadDetail(lead.id)` to fetch full detail with contacts and messages.

Layout: fixed right, top 0, bottom 0, w-[440px], z-40. Border-left 1px solid subtle-border. Box-shadow: 0 8px 30px rgba(0,0,0,0.12). Flex-col, h-screen, bg card-bg.

Sections:
1. Header (flex-shrink-0, px-6 py-4, border-bottom): company name (18px, weight 600) + close X button (top-right, icon-only button with aria-label="Close panel").

2. Company info (px-6 py-4, border-bottom): domain with Globe icon + link (if domain present), row with StageBadge + FitTierBadge (inline, same leads-specific FitTierBadge, or a simple styled span using badges.fitTier colors from design-tokens) + purpose pills. Fit rationale below (13px, secondaryText, italic, mt-2) if present.

3. Contacts section (flex-1, overflow-y-auto, px-6 py-4): "Contacts" header + count badge (pill). List of ContactCard components. Accordion behavior: track `expandedContactId`, only one open at a time (clicking one closes the other).

4. Footer (flex-shrink-0, px-6 py-4, border-top): Graduate button — full-width, bg coral gradient (from-[#E94D35] to-[#D4432E]), white text, rounded-xl, h-10. Disabled if lead.graduated_at is set (opacity 0.5, cursor not-allowed). onClick calls onGraduate(lead.id).

States:
- Loading (detail fetching): shimmer skeleton — header bar + 3 contact blocks
- No contacts: "No contacts added yet" muted centered text
- Error: "Failed to load lead details" + retry button (call refetch)

Escape key: useEffect with keydown listener for Escape, calls onClose. Cleanup on unmount.

Focus trap: on mount, focus the panel container. On Escape or close, return focus to previously focused element (save document.activeElement on mount, restore on unmount).

Motion: enter translateX(100%) to translateX(0), 200ms cubic-bezier(0.16, 1, 0.3, 1). Exit: reverse, 150ms ease-in. Use CSS transition on transform. Respect prefers-reduced-motion.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — all three components compile. Verify ContactCard renders with expand/collapse. Verify MessageThread renders timeline. Verify LeadSidePanel renders all sections.</verify>
  <done>Side panel shows company info, contacts accordion with message timelines, and graduate button. Panel opens/closes with animation. Contacts expand one at a time. Messages expand to show body text.</done>
</task>

<task type="auto">
  <name>Task 2: Wire side panel and graduation into LeadsPage, register route and sidebar</name>
  <files>
    frontend/src/features/leads/components/LeadsPage.tsx
    frontend/src/app/routes.tsx
    frontend/src/features/navigation/components/AppSidebar.tsx
  </files>
  <action>
**LeadsPage.tsx updates** — Add side panel rendering and graduation flow:

1. Import LeadSidePanel, Dialog components, and useLeadGraduate hook.

2. Add graduation state and mutation:
   - `const graduate = useLeadGraduate()`
   - When graduatingId is set (from table button context.onGraduate or panel button), show confirmation dialog.

3. Confirmation dialog (using Dialog from components/ui/dialog.tsx):
   - Title: "Graduate lead?"
   - Body: "This will promote {name} to a full account with all contacts and outreach history."
   - Cancel button (outline variant) + Graduate button (coral bg, white text)
   - On cancel: setGraduatingId(null)
   - On confirm: call graduate.mutate(graduatingId). In onSuccess callback: setGraduatingId(null), if selectedLead?.id === graduatingId then setSelectedLead(null). The hook already handles invalidation and toast.

4. Side panel rendering (below the table, conditionally):
   ```
   {selectedLead && (
     <>
       <div className="fixed inset-0 z-30" style={{ background: 'rgba(0,0,0,0.2)' }}
            onClick={() => setSelectedLead(null)} />
       <LeadSidePanel
         lead={selectedLead}
         onClose={() => setSelectedLead(null)}
         onGraduate={(id) => setGraduatingId(id)}
       />
     </>
   )}
   ```

5. Row graduation animation in ag-grid: update getRowStyle callback — when `params.data?.id === graduatingId`, return style with transform translateX(100%), opacity 0, transition 'all 300ms ease-out'. After mutation success, use setTimeout(300ms) to allow animation to complete before query refetch visually takes effect.

6. Update onRowClicked: `setSelectedLead(e.data)` — already exists from plan 02 but verify it works with the panel now rendering.

**routes.tsx update** — Add /leads route:

Add lazy import (same pattern as PipelinePage):
```typescript
const LeadsPage = lazy(() =>
  import('@/features/leads/components/LeadsPage').then((m) => ({ default: m.LeadsPage }))
)
```

Add route after the /pipeline route:
```typescript
<Route path="/leads" element={<Suspense fallback={null}><LeadsPage /></Suspense>} />
```

**AppSidebar.tsx update** — Add Leads nav item:

Add a "Leads" item immediately above the Pipeline item in the same sidebar group. Use the Users icon (already imported in AppSidebar). Follow the exact same SidebarMenuItem/SidebarMenuButton pattern as Pipeline:
```typescript
<SidebarMenuItem>
  <SidebarMenuButton
    isActive={location.pathname === '/leads'}
    render={<NavLink to="/leads" />}
    tooltip="Leads"
  >
    <Users className="size-4" />
    <span>Leads</span>
  </SidebarMenuButton>
</SidebarMenuItem>
```

Place this ABOVE the Pipeline SidebarMenuItem (Leads is pre-pipeline in the outbound flow).
  </action>
  <verify>
1. `npx tsc --noEmit` passes with zero errors
2. Navigate to localhost:5173/leads — page renders with funnel, filters, table
3. Click a table row — side panel slides in showing company info, contacts, messages
4. Click backdrop or X or press Escape — panel closes
5. Expand a contact — see message timeline, other contacts collapse
6. Click Graduate in table — confirmation dialog appears, does NOT open side panel
7. Click Graduate in panel — confirmation dialog appears
8. Confirm graduation — row animates out, toast fires, funnel counts update
9. Sidebar shows "Leads" above "Pipeline" with Users icon
10. Direct navigation to /leads works (route registered)
  </verify>
  <done>Complete leads pipeline feature is functional: funnel visualization, filterable table, side panel with contacts and messages, graduation flow with confirmation and animation, route registered at /leads, sidebar navigation added.</done>
</task>

</tasks>

<verification>
Full end-to-end verification of all 5 success criteria:
1. /leads route renders funnel with clickable stage segments showing counts
2. ag-grid table displays 8 columns with server-side pagination
3. Row click opens side panel with company info, contacts accordion, message threads
4. Funnel clicks, filter dropdowns, and search all sync and filter the table
5. Graduate button (table + panel) promotes lead via POST /leads/{id}/graduate with confirmation, animation, and toast
</verification>

<success_criteria>
The complete leads pipeline feature is functional and accessible. A founder can browse leads in a funnel, filter by stage/fit/purpose/search, drill into any lead to see contacts and messages, and graduate leads to accounts. The feature is navigable from the sidebar and works at /leads.
</success_criteria>

<output>
After completion, create `.planning/phases/082-leads-pipeline-frontend/82-03-SUMMARY.md`
</output>
