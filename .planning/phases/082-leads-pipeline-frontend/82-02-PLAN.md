---
phase: 082-leads-pipeline-frontend
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - frontend/src/features/leads/components/LeadsFunnel.tsx
  - frontend/src/features/leads/components/LeadsFilterBar.tsx
  - frontend/src/features/leads/components/LeadsPage.tsx
autonomous: true

must_haves:
  truths:
    - "Funnel shows 6 stage segments with counts, clickable to filter table"
    - "Filter bar has search (300ms debounce) + 3 single-select dropdowns (Stage, Fit Tier, Purpose)"
    - "Funnel click and Stage dropdown stay synced (both control activeStage state)"
    - "ag-grid table renders leads with 8 columns, server-side pagination, and custom pagination controls"
    - "Page resets to page 0 when any filter changes"
    - "Loading state shows funnel shimmer + table shimmer"
    - "Empty state shows EmptyState component with Users icon"
  artifacts:
    - path: "frontend/src/features/leads/components/LeadsFunnel.tsx"
      provides: "Horizontal funnel bar visualization"
      min_lines: 60
    - path: "frontend/src/features/leads/components/LeadsFilterBar.tsx"
      provides: "Search input + 3 single-select filter dropdowns"
      min_lines: 80
    - path: "frontend/src/features/leads/components/LeadsPage.tsx"
      provides: "Page orchestrator with state, hooks, layout, table, pagination"
      min_lines: 150
  key_links:
    - from: "frontend/src/features/leads/components/LeadsPage.tsx"
      to: "frontend/src/features/leads/hooks/useLeads.ts"
      via: "useLeads hook call"
      pattern: "useLeads"
    - from: "frontend/src/features/leads/components/LeadsPage.tsx"
      to: "frontend/src/features/leads/hooks/useLeadsPipeline.ts"
      via: "useLeadsPipeline hook call"
      pattern: "useLeadsPipeline"
    - from: "frontend/src/features/leads/components/LeadsPage.tsx"
      to: "frontend/src/features/leads/hooks/useLeadsColumns.ts"
      via: "useLeadsColumns hook call"
      pattern: "useLeadsColumns"
    - from: "frontend/src/features/leads/components/LeadsFunnel.tsx"
      to: "frontend/src/features/leads/types/lead.ts"
      via: "STAGE_COLORS and STAGE_ORDER imports"
      pattern: "STAGE_COLORS|STAGE_ORDER"
    - from: "frontend/src/features/leads/components/LeadsFilterBar.tsx"
      to: "frontend/src/features/leads/components/LeadsPage.tsx"
      via: "props: activeStage, fitTier, purpose, search + onChange handlers"
      pattern: "onStageChange|onFitTierChange|onPurposeChange|onSearchChange"
---

<objective>
Build the three main visible components of the leads page: the funnel visualization, the filter bar, and the page orchestrator with ag-grid table and pagination. After this plan, the /leads page renders a working funnel + filters + table (without side panel or graduation flow).

Purpose: Deliver the primary data browsing experience — a founder can see leads in a funnel, filter them, and browse the table.
Output: 3 components — LeadsFunnel, LeadsFilterBar, LeadsPage
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/082-leads-pipeline-frontend/82-RESEARCH.md
@.planning/phases/082-leads-pipeline-frontend/82-01-SUMMARY.md
@frontend/src/features/leads/DESIGN-BRIEF.md
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/components/PipelineFilterBar.tsx
@frontend/src/features/pipeline/components/PipelineViewTabs.tsx
@frontend/src/lib/design-tokens.ts
@frontend/src/components/ui/empty-state.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LeadsFunnel and LeadsFilterBar components</name>
  <files>
    frontend/src/features/leads/components/LeadsFunnel.tsx
    frontend/src/features/leads/components/LeadsFilterBar.tsx
  </files>
  <action>
**LeadsFunnel.tsx** — Horizontal pipeline funnel (novel component, no pipeline equivalent):

Props: `funnel: Record<string, number>`, `total: number`, `activeStage: string | null`, `onStageChange: (stage: string | null) => void`, `isLoading: boolean`.

Layout:
- Container: full-width, h-16, rounded-xl, overflow-hidden, flex row, gap-0.5 (2px)
- 6 segments in STAGE_ORDER: scraped, scored, researched, drafted, sent, replied
- Each segment: flex-grow proportional to count (use `flex: Math.max(count, 1)`), min-w-[56px]
- First segment: rounded-l-xl. Last: rounded-r-xl.
- Background: STAGE_COLORS[stage] at 12% opacity
- Content: vertically centered column — stage label (11px, weight 500, uppercase, letter-spacing 0.04em) + count (18px, weight 700, tabular-nums)
- Text color: STAGE_COLORS[stage] at full saturation
- Cursor: pointer

States:
- Hover: brightness(1.08), transition 200ms
- Active (stage === activeStage): 2px solid bottom border in stage color, font-weight 800 on count
- Click: toggle — if already active, call onStageChange(null), else onStageChange(stage)
- Loading: single shimmer div, full width, h-16, rounded-xl (use ShimmerSkeleton)
- Empty (all zeros): show segments with "0", opacity 0.5

Accessibility: role="tablist" on container. Each segment: role="tab", aria-selected={activeStage === stage}, aria-label="Filter by {stage}: {count} leads". Keyboard: onKeyDown for ArrowLeft/ArrowRight to navigate segments, Enter/Space to select.

**LeadsFilterBar.tsx** — Search + 3 single-select dropdowns:

Props: `search: string`, `onSearchChange: (s: string) => void`, `activeStage: string | null`, `onStageChange: (s: string | null) => void`, `fitTier: string | null`, `onFitTierChange: (s: string | null) => void`, `purpose: string | null`, `onPurposeChange: (s: string | null) => void`.

Layout: flex row, items-center, gap-3.

Search input (left): max-w-sm, h-10, rounded-xl, Search icon prefix (lucide), X clear button when non-empty. Placeholder "Search leads...". Focus ring: brand-coral 2px. Debounce is handled by parent (LeadsPage), so this just calls onSearchChange on every keystroke.

Vertical divider: 1px w, h-6, subtle-border color.

3 single-select dropdowns (custom, not native select). Each dropdown:
- Trigger button: rounded-lg, px-3 py-1.5, text 13px weight 500, secondaryText color. ChevronDown icon (14px). When filter active: bg brand-tint, text brand-coral.
- Dropdown menu: absolute, rounded-xl, border, shadow-lg, py-1, max-h-60 overflow-auto, z-50.
- Options: px-3 py-2, hover bg rgba(0,0,0,0.04), checkmark icon when selected.
- First option always "All" (clears filter → null).
- Stage dropdown options: STAGE_ORDER (scraped through replied) — synced with funnel's activeStage.
- Fit Tier options: Strong, Good, Moderate, Weak, No Fit.
- Purpose options: sales, fundraising, advisors, partnerships.
- Click outside or Escape closes dropdown (use a useEffect with document click listener and ref).
- Only one dropdown open at a time (track `openDropdown: 'stage' | 'fit' | 'purpose' | null`).

Active filter chips (far right): when any filter is active, show pill (rounded-full, px-2.5 py-1, bg brand-tint, text brand-coral, 12px weight 500) with label + X button to clear.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — no type errors. Visually inspect both components render without crashes by temporarily mounting in LeadsPage.</verify>
  <done>LeadsFunnel renders 6 proportional segments with counts and handles click-to-filter toggling. LeadsFilterBar renders search + 3 dropdowns with proper sync and active states.</done>
</task>

<task type="auto">
  <name>Task 2: LeadsPage orchestrator with ag-grid table and pagination</name>
  <files>
    frontend/src/features/leads/components/LeadsPage.tsx
  </files>
  <action>
Create the page orchestrator following PipelinePage.tsx pattern exactly:

**State (all in LeadsPage, no context/reducer):**
- `activeStage: string | null` (funnel + stage dropdown filter)
- `fitTier: string | null`
- `purpose: string | null`
- `search: string` (raw input)
- `debouncedSearch: string` (debounced 300ms via useEffect + setTimeout, same pattern as PipelineFilterBar)
- `page: number` (0-indexed)
- `pageSize: number` (default 50)
- `selectedLead: Lead | null` (for side panel — wired in plan 03)
- `graduatingId: string | null` (for row animation — wired in plan 03)

**Hooks:**
- `useLeads({ offset: page * pageSize, limit: pageSize, pipeline_stage: activeStage, fit_tier: fitTier, purpose, search: debouncedSearch })`
- `useLeadsPipeline()`
- `useLeadsColumns()` — destructure colDefs, getInitialState, onColumnStateChanged

**Effects:**
- Reset page to 0 when activeStage, fitTier, purpose, debouncedSearch, or pageSize change
- Debounce search: `useEffect` with 300ms setTimeout on `search`, setting `debouncedSearch`

**Layout (max-w-[1120px] mx-auto, px-12 py-8):**

1. Title: "Leads" (28px, weight 700, headingText) + total count in parens (28px, weight 400, secondaryText). Total from pipeline.total.

2. LeadsFunnel: pass pipeline.funnel, pipeline.total, activeStage, onStageChange, isLoading.

3. LeadsFilterBar: pass search, onSearchChange, activeStage, onStageChange (SAME handler as funnel — this is how they sync), fitTier, onFitTierChange, purpose, onPurposeChange.

4. ag-grid table:
   - Import AllCommunityModule from ag-grid-community, pass as modules={[AllCommunityModule]}
   - Theme: themeQuartz.withParams({...}) — identical config from research code examples section
   - rowData: leads.items
   - columnDefs: from useLeadsColumns
   - initialState: getInitialState()
   - onColumnStateChanged: handler from useLeadsColumns
   - onRowClicked: `(e) => setSelectedLead(e.data)` (side panel opens in plan 03)
   - context: `{ onGraduate: (id: string) => setGraduatingId(id) }` (graduation wired in plan 03)
   - rowHeight: 44, headerHeight: 36
   - suppressCellFocus: true, suppressRowClickSelection: true
   - getRowStyle: if graduatingId matches, apply slide-out animation (CSS: transform translateX(100%) + opacity 0, transition 300ms)
   - domLayout: 'normal', set container height to fill available space

5. Pagination footer (below table, border-top):
   - Left: "1-50 of 309" format using `page * pageSize + 1` through `Math.min((page + 1) * pageSize, total)`
   - Right: pageSize select (25, 50, 100) + ChevronLeft prev button (disabled at page 0) + "1/7" page indicator + ChevronRight next button (disabled at last page)
   - Follow PipelinePage lines 254-306 pattern exactly

**Rendering flow:**
- isLoading (no data yet): funnel shimmer + 8 skeleton rows in table area
- items.length === 0 + not loading: EmptyState with Users icon, "No leads in pipeline", "Run a GTM pipeline to start discovering prospects"
- items.length > 0: ag-grid table

Export as named export `LeadsPage` (not default — matches project convention for lazy loading).

Note: Side panel rendering and graduation confirmation dialog will be added in plan 03. For now, `selectedLead` and `graduatingId` states exist but the panel/dialog JSX is not yet rendered.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/. The page compiles. Temporarily add a route in routes.tsx to verify the page renders at /leads with mock or real data (the backend is already built). Verify funnel displays, filters work, table populates, pagination controls function.</verify>
  <done>LeadsPage renders funnel + filter bar + ag-grid table + pagination. Funnel clicks filter the table. Search debounces at 300ms. Filter dropdowns update the table. Page resets on filter change. Loading and empty states display correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- LeadsFunnel renders 6 segments with correct proportional widths and stage colors
- Clicking a funnel segment filters the table AND updates the Stage dropdown
- Clicking the same segment again clears the filter
- Search debounces and filters server-side
- Pagination shows correct "X-Y of Z" and prev/next work
- Empty state appears when no leads match filters
- Loading skeleton appears on initial load
</verification>

<success_criteria>
The /leads page displays a working funnel visualization, filter bar with search and 3 dropdowns, ag-grid table with 8 columns, and pagination controls. All filters sync correctly and trigger server-side filtering via React Query. The page handles loading, empty, and populated states.
</success_criteria>

<output>
After completion, create `.planning/phases/082-leads-pipeline-frontend/82-02-SUMMARY.md`
</output>
