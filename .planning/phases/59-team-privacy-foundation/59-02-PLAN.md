---
phase: 59-team-privacy-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/api/email.py
  - backend/src/flywheel/api/integrations.py
  - backend/src/flywheel/api/skills.py
autonomous: true

must_haves:
  truths:
    - "Email list/thread/digest endpoints filter by user_id at the query level"
    - "Draft approve/dismiss/edit endpoints verify parent email ownership before acting"
    - "Email sync endpoint filters integration lookup by user_id"
    - "Integration DELETE and sync endpoints include user_id in WHERE clause"
    - "Integration list endpoint filters by user_id"
    - "Skill runs list/detail/attribution/trace endpoints filter by user_id"
  artifacts:
    - path: "backend/src/flywheel/api/email.py"
      provides: "User-scoped email queries and draft ownership checks"
      contains: "Email.user_id == user.sub"
    - path: "backend/src/flywheel/api/integrations.py"
      provides: "User-scoped integration queries"
      contains: "Integration.user_id == user.sub"
    - path: "backend/src/flywheel/api/skills.py"
      provides: "User-scoped skill run queries"
      contains: "SkillRun.user_id == user.sub"
  key_links:
    - from: "backend/src/flywheel/api/email.py"
      to: "user.sub (TokenPayload)"
      via: "Email.user_id == user.sub in WHERE clauses"
      pattern: "Email\\.user_id == user\\.sub"
    - from: "backend/src/flywheel/api/integrations.py"
      to: "user.sub (TokenPayload)"
      via: "Integration.user_id == user.sub in WHERE clauses"
      pattern: "Integration\\.user_id == user\\.sub"
    - from: "backend/src/flywheel/api/skills.py"
      to: "user.sub (TokenPayload)"
      via: "SkillRun.user_id == user.sub in WHERE clauses"
      pattern: "SkillRun\\.user_id == user\\.sub"
---

<objective>
Add defense-in-depth user_id filters to all API endpoints that handle personal data: email threads/digest/drafts, integrations list/delete/sync, and skill runs list/detail/attribution/trace.

Purpose: While RLS (Plan 01) enforces user isolation at the database level, explicit API-layer filters provide defense-in-depth, clearer error messages, and protect against RLS misconfiguration. This covers PRIV-05, PRIV-06, and PRIV-07.

Output: Three modified API files with user_id filters on all personal data endpoints.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-team-privacy-foundation.md
@.planning/phases/59-team-privacy-foundation/59-RESEARCH.md
@backend/src/flywheel/api/email.py
@backend/src/flywheel/api/integrations.py
@backend/src/flywheel/api/skills.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user_id filters to email API endpoints (PRIV-05)</name>
  <files>backend/src/flywheel/api/email.py</files>
  <action>
Modify `backend/src/flywheel/api/email.py` to add user-level scoping to 7 endpoints. Read the file first to confirm current line numbers (research noted specific lines but they may have shifted).

**List endpoints — add `Email.user_id == user.sub` to existing WHERE clauses:**

1. `list_threads()`: Find `.where(Email.tenant_id == user.tenant_id)` and add `Email.user_id == user.sub` as additional filter argument.

2. `get_thread()`: Find the `and_()` clause with `Email.gmail_thread_id == thread_id, Email.tenant_id == user.tenant_id` and add `Email.user_id == user.sub` to the `and_()`.

3. `get_digest()`: Find the `and_()` clause with `Email.tenant_id == user.tenant_id` and add `Email.user_id == user.sub`.

**Draft endpoints — add parent email ownership check after loading draft:**

4. `approve_draft()`: After the existing code that loads the parent email (for Message-ID header), add ownership verification. If the code already loads the parent email, add `if email is None or email.user_id != user.sub: raise HTTPException(status_code=404, detail="Draft not found")`. If the parent email is NOT already loaded at that point, add the load + check.

5. `dismiss_draft()`: After loading the draft, load parent email and verify ownership:
```python
email_result = await db.execute(select(Email).where(Email.id == draft.email_id))
email = email_result.scalar_one_or_none()
if email is None or email.user_id != user.sub:
    raise HTTPException(status_code=404, detail="Draft not found")
```

6. `edit_draft()`: Same parent email ownership check pattern as dismiss_draft.

**Sync endpoint — add user_id to integration lookup:**

7. `trigger_sync()`: Find the integration lookup query (uses `Integration.tenant_id`, `Integration.provider`, `Integration.status`) and add `Integration.user_id == user.sub` to the WHERE clause.

IMPORTANT: Use `user.sub` (not `user.id` — TokenPayload has `.sub` as the UUID field). Do NOT change the response status code patterns — ownership failures should return 404 (not 403) to avoid leaking resource existence.
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.email import router; print('Import OK')"` to verify no syntax errors.

Then grep for the added filters:
- `grep -n "Email.user_id == user.sub" src/flywheel/api/email.py` — should show at least 3 occurrences (list_threads, get_thread, get_digest)
- `grep -n "email.user_id != user.sub" src/flywheel/api/email.py` — should show occurrences in draft endpoints (approve, dismiss, edit)
- `grep -n "Integration.user_id == user.sub" src/flywheel/api/email.py` — should show 1 occurrence in trigger_sync
  </verify>
  <done>
All 7 email.py endpoints have user_id filtering: 3 list/query endpoints filter by Email.user_id == user.sub, 3 draft endpoints verify parent email ownership (404 on mismatch), 1 sync endpoint filters integration by user_id.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add user_id filters to integrations and skills API endpoints (PRIV-06, PRIV-07)</name>
  <files>backend/src/flywheel/api/integrations.py, backend/src/flywheel/api/skills.py</files>
  <action>
**integrations.py (PRIV-06) — 3 endpoints:**

Read `backend/src/flywheel/api/integrations.py` first to confirm current structure.

1. `list_integrations()`: Find `select(Integration)` with no filter (RLS handles it). Add `.where(Integration.user_id == user.sub)` for defense-in-depth.

2. `disconnect_integration()`: Find where integration is loaded by `Integration.id`. Change to:
```python
integration = (
    await db.execute(
        select(Integration).where(
            Integration.id == integration_id,
            Integration.user_id == user.sub,
        )
    )
).scalar_one_or_none()
if integration is None:
    raise HTTPException(status_code=404, detail="Integration not found")
```

3. `sync_integration()`: Same pattern as disconnect — add `Integration.user_id == user.sub` to the WHERE clause on the integration lookup.

Do NOT modify OAuth authorize or callback endpoints — they are already correct per research.

**skills.py (PRIV-07) — 4 endpoints:**

Read `backend/src/flywheel/api/skills.py` first to confirm current structure.

1. `list_runs()`: Add `.where(SkillRun.user_id == user.sub)` to BOTH `base` query AND `count_q` query. Both must be filtered — otherwise pagination math breaks (count includes all tenant runs but items are user-scoped).

2. `get_run()`: Add `SkillRun.user_id == user.sub` to the WHERE clause alongside `SkillRun.id == run_id`.

3. `get_run_attribution()`: Same — add `SkillRun.user_id == user.sub` to WHERE.

4. `get_run_trace()`: Same — add `SkillRun.user_id == user.sub` to WHERE.

Do NOT modify `stream_run()` — it already uses `get_tenant_session` which sets `app.user_id`, and RLS (PRIV-04) handles enforcement.

IMPORTANT: Use `user.sub` consistently (not `user.id`). Return 404 (not 403) on ownership mismatches.
  </action>
  <verify>
Run both import checks:
- `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.integrations import router; print('Import OK')"`
- `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.skills import router; print('Import OK')"`

Then grep for filters:
- `grep -n "Integration.user_id == user.sub" src/flywheel/api/integrations.py` — should show 3 occurrences (list, disconnect, sync)
- `grep -n "SkillRun.user_id == user.sub" src/flywheel/api/skills.py` — should show at least 5 occurrences (base, count_q, get_run, attribution, trace)
  </verify>
  <done>
integrations.py has user_id filters on list (1), disconnect (1), sync (1) endpoints. skills.py has user_id filters on list_runs base + count_q (2), get_run (1), get_run_attribution (1), get_run_trace (1). All use user.sub, all return 404 on ownership mismatch. stream_run left unchanged (RLS handles it).
  </done>
</task>

</tasks>

<verification>
- All 3 API files import without errors
- email.py: 7 endpoints have user_id filtering (3 list + 3 draft ownership + 1 sync)
- integrations.py: 3 endpoints have user_id filtering
- skills.py: 5 query locations have user_id filtering (base + count_q + 3 detail endpoints)
- No 403 status codes introduced (all ownership failures return 404)
- user.sub used consistently (not user.id)
</verification>

<success_criteria>
All personal data API endpoints include explicit user_id filters as defense-in-depth. Combined with Plan 01's RLS policies, user data is isolated at both database and application layers.
</success_criteria>

<output>
After completion, create `.planning/phases/59-team-privacy-foundation/59-02-SUMMARY.md`
</output>
