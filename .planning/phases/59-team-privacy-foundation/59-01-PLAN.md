---
phase: 59-team-privacy-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/031_user_level_rls.py
autonomous: true

must_haves:
  truths:
    - "User B in the same tenant calling any email query gets zero of User A's emails"
    - "User B querying integrations sees only their own integrations"
    - "User B querying skill_runs sees only their own runs (plus tenant-shared NULL-user runs)"
    - "User B querying work_items sees only their own items (plus tenant-shared NULL-user items)"
    - "All 7 tables have user_isolation RLS policies replacing tenant_isolation policies"
  artifacts:
    - path: "backend/alembic/versions/031_user_level_rls.py"
      provides: "Alembic migration with user-level RLS policies on 7 tables"
      contains: "CREATE POLICY user_isolation"
  key_links:
    - from: "backend/alembic/versions/031_user_level_rls.py"
      to: "app.user_id session variable"
      via: "current_setting('app.user_id', true)::uuid in USING/WITH CHECK clauses"
      pattern: "current_setting\\('app\\.user_id'"
---

<objective>
Create Alembic migration that replaces tenant-only RLS policies with user-level RLS policies on 7 tables: emails, email_scores, email_drafts, email_voice_profiles, integrations, work_items, skill_runs.

Purpose: This is the core database-level enforcement that prevents users from seeing each other's personal data within the same tenant. The session layer already sets app.user_id on every request — the policies just need to check it.

Output: Single Alembic migration file (031_user_level_rls.py) with upgrade and downgrade functions.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-team-privacy-foundation.md
@.planning/phases/59-team-privacy-foundation/59-RESEARCH.md
@backend/alembic/versions/030_graduated_at.py
@backend/alembic/versions/020_email_models.py
@backend/alembic/versions/002_enable_rls_policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create user-level RLS migration for all 7 tables</name>
  <files>backend/alembic/versions/031_user_level_rls.py</files>
  <action>
Create Alembic migration file `backend/alembic/versions/031_user_level_rls.py` with:

- `revision = "031_user_level_rls"` (must be <=32 chars)
- `down_revision = "030_grad_at"` (matches the revision ID in 030_graduated_at.py, NOT the filename)

**upgrade() function — 7 tables, 3 patterns:**

Pattern 1 — Direct user_id (emails, email_voice_profiles, integrations):
- emails: DROP 4 per-operation policies (`tenant_isolation_select`, `tenant_isolation_insert`, `tenant_isolation_update`, `tenant_isolation_delete`), CREATE single `user_isolation` policy with `tenant_id + user_id` check
- email_voice_profiles: Same DROP 4 per-operation pattern, same CREATE
- integrations: DROP single `tenant_isolation` policy, CREATE `user_isolation` with `tenant_id + user_id`

Pattern 2 — Nullable user_id (work_items, skill_runs):
- work_items: DROP single `tenant_isolation`, CREATE `user_isolation` with `tenant_id AND (user_id IS NULL OR user_id = current_setting(...))`
- skill_runs: Same nullable pattern

Pattern 3 — Subquery via FK (email_scores, email_drafts):
- email_scores: DROP 4 per-operation policies, CREATE `user_isolation` with `tenant_id AND email_id IN (SELECT id FROM emails WHERE user_id = current_setting(...))`
- email_drafts: Same subquery pattern

Use the EXACT SQL from the SPEC (PRIV-01 through PRIV-04). Use `op.execute()` for all SQL statements.

**downgrade() function — restore original policies:**

For email-family tables (emails, email_scores, email_drafts, email_voice_profiles):
- DROP `user_isolation` policy
- Recreate 4 per-operation `tenant_isolation_*` policies (SELECT, INSERT, UPDATE, DELETE) that check only `tenant_id = current_setting('app.tenant_id', true)::uuid`
- Match the original policy structure from migration 020_email_models.py

For 002-migration tables (integrations, work_items, skill_runs):
- DROP `user_isolation` policy
- Recreate single `tenant_isolation` policy that checks only `tenant_id`
- Match the original policy structure from migration 002_enable_rls_policies.py

IMPORTANT: The email tables use per-operation policy names (4 each). The other 3 tables use a single `tenant_isolation` policy. The DROP statements must match the CORRECT names per table. Getting this wrong fails the migration.
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "import ast; ast.parse(open('alembic/versions/031_user_level_rls.py').read()); print('Syntax OK')"` to verify valid Python.

Then verify content:
- grep for `CREATE POLICY user_isolation` — should appear 7 times (once per table)
- grep for `DROP POLICY IF EXISTS tenant_isolation_select` — should appear 4 times (emails, email_scores, email_drafts, email_voice_profiles)
- grep for `DROP POLICY IF EXISTS tenant_isolation ON` — should appear 3 times (integrations, work_items, skill_runs)
- grep for `user_id IS NULL` — should appear in work_items and skill_runs policies (2 occurrences in USING + 2 in WITH CHECK = 4 total)
- grep for `email_id IN` — should appear in email_scores and email_drafts policies (2 tables x USING + WITH CHECK = 4 total)
- Verify `down_revision = "030_grad_at"` (not "030_graduated_at")
  </verify>
  <done>
Migration file exists with: correct revision chain (031 -> 030), 7 user_isolation CREATE POLICY statements matching SPEC SQL exactly, correct DROP statements per table (4 per-operation for email-family, 1 single for others), complete downgrade restoring original policies, valid Python syntax.
  </done>
</task>

</tasks>

<verification>
- Migration file parses as valid Python
- All 7 tables have user_isolation policies in upgrade()
- DROP statements use correct policy names per table
- downgrade() restores original policies with correct names
- Revision chain: 031_user_level_rls -> 030_grad_at
</verification>

<success_criteria>
Single migration file creates user-level RLS policies on all 7 tables. The SQL matches the SPEC exactly. Downgrade correctly restores the original tenant-only policies.
</success_criteria>

<output>
After completion, create `.planning/phases/59-team-privacy-foundation/59-01-SUMMARY.md`
</output>
