---
phase: 57-relationship-surfaces
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - frontend/src/features/relationships/components/RelationshipListPage.tsx
  - frontend/src/features/relationships/components/RelationshipCard.tsx
autonomous: true

must_haves:
  truths:
    - "Each relationship type list page renders as a card grid (3-col desktop, 2-col tablet, 1-col mobile)"
    - "Cards are sorted by urgency (signal_count descending, then last_interaction_at descending)"
    - "Warm tint background is visible on all relationship list pages"
    - "Empty state with type-specific illustration and CTA appears when no relationships of that type exist"
  artifacts:
    - path: "frontend/src/features/relationships/components/RelationshipListPage.tsx"
      provides: "Card grid page for all 4 relationship types"
      exports: ["RelationshipListPage"]
      min_lines: 60
    - path: "frontend/src/features/relationships/components/RelationshipCard.tsx"
      provides: "Individual relationship card with urgency border"
      exports: ["RelationshipCard"]
      min_lines: 40
  key_links:
    - from: "frontend/src/features/relationships/components/RelationshipListPage.tsx"
      to: "frontend/src/features/relationships/hooks/useRelationships.ts"
      via: "useRelationships(type) call"
      pattern: "useRelationships"
    - from: "frontend/src/features/relationships/components/RelationshipCard.tsx"
      to: "/relationships/:id?fromType="
      via: "navigate with fromType param"
      pattern: "fromType"
---

<objective>
Build the relationship list pages as responsive card grids with urgency sorting, warm background register, and type-specific empty states.

Purpose: This is the primary browsing surface for relationships — founders see all prospects, customers, advisors, or investors at a glance with visual urgency cues.

Output: RelationshipListPage.tsx (shared across all 4 types via `type` prop) and RelationshipCard.tsx.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-relationship-surfaces/57-RESEARCH.md
@.planning/phases/57-relationship-surfaces/57-01-SUMMARY.md

@frontend/src/features/relationships/api.ts
@frontend/src/features/relationships/types/relationships.ts
@frontend/src/features/relationships/hooks/useRelationships.ts
@frontend/src/lib/design-tokens.ts
@frontend/src/components/ui/branded-card.tsx
@frontend/src/components/ui/empty-state.tsx
@frontend/src/components/ui/avatar.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RelationshipCard component</name>
  <files>frontend/src/features/relationships/components/RelationshipCard.tsx</files>
  <action>
    Create `RelationshipCard.tsx` that renders an individual relationship in the grid:

    Props: `{ item: RelationshipListItem; type: RelationshipType }`

    Layout (using BrandedCard):
    - Variant derived from signal_count: `item.signal_count > 0 ? 'action' : 'info'` (action = coral left border for urgent items)
    - onClick navigates to `/relationships/${item.id}?fromType=${type}` — CRITICAL: must include `?fromType=` so the detail page knows which tabs to show and where the back link goes
    - Card content:
      1. Top row: Avatar (size default — initials from item.name) + name (font-weight 600, font-size 15px) + domain (secondary text, truncated)
      2. Middle row: relationship_status badge using badge-translucent class with inline coral rgba styles + entity_level text (secondary, capitalize)
      3. Bottom row: primary_contact_name (if exists, with small Users icon), last_interaction_at formatted as relative time ("3 days ago"), signal_count badge if > 0
    - Use `transition-interactive` class for hover animation
    - ai_summary: if not null, show first 120 chars as a muted paragraph below the middle row (truncated with ellipsis)

    For relative time: create inline helper `formatTimeAgo(dateStr: string): string` that returns "Xd ago", "Xh ago", or "just now" — copy the pattern from AccountsPage.tsx if one exists, otherwise implement simply with Date math.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — RelationshipCard.tsx compiles without errors.</verify>
  <done>RelationshipCard renders name, avatar, status badge, contact, time ago, signal count, and navigates to detail with fromType param on click.</done>
</task>

<task type="auto">
  <name>Task 2: RelationshipListPage with card grid, sorting, loading, and empty state</name>
  <files>frontend/src/features/relationships/components/RelationshipListPage.tsx</files>
  <action>
    Replace the placeholder in `RelationshipListPage.tsx` with the full implementation:

    Props: `{ type: RelationshipType }` (passed from routes.tsx)

    Structure:
    1. Page wrapper div with `style={{ background: registers.relationship.background }}` and `className="min-h-dvh"` — this is the warm tint register (per research Pattern 2, Pitfall 6)
    2. Content container: `className="mx-auto w-full"` with `style={{ maxWidth: spacing.maxGrid, padding: '${spacing.section} ${spacing.pageDesktop}' }}`
    3. Page title: capitalize type + "s" (e.g., "Prospects", "Customers") using typography.pageTitle styles
    4. Grid container: `className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-6"`

    Data:
    - Call `useRelationships(type)` to get items
    - Sort items client-side by urgency: first by signal_count descending, then by last_interaction_at descending (most recent first), nulls last
    - Map sorted items to `<RelationshipCard key={item.id} item={item} type={type} />`

    Loading state:
    - When `isLoading`, render a grid of 6 `<ShimmerSkeleton className="h-40 rounded-xl" />` elements in the same 3-col grid layout

    Empty state:
    - When items is empty and not loading, render `<EmptyState>` component:
      - Icon: type-specific from lucide-react (prospect=Users, customer=TrendingUp, advisor=Briefcase, investor=DollarSign)
      - Title: "No {type}s yet"
      - Description: type-specific message (e.g., "Graduate accounts from your Pipeline to see them here")
      - CTA button: "Go to Pipeline" linking to /pipeline

    Error state:
    - If error, show a simple error message with retry option
  </action>
  <verify>Run `npx tsc --noEmit`. Start dev server, navigate to /relationships/prospects — page loads with warm background, shows shimmer then cards (or empty state if no data). Verify /relationships/customers, /advisors, /investors all work. Check 3-col grid on wide viewport, verify responsive collapse to 2-col and 1-col by resizing browser.</verify>
  <done>All 4 relationship list pages render card grids with warm tint background, urgency sorting, loading skeletons, and type-specific empty states. Cards navigate to detail with fromType param.</done>
</task>

</tasks>

<verification>
- All 4 list URLs render card grids with warm tint background
- Cards show name, avatar, status, contact, time ago, signal badge
- Cards with signal_count > 0 have coral left border
- Clicking a card navigates to /relationships/:id?fromType=prospect (etc.)
- Empty state shows with correct type-specific icon and CTA
- Grid is 3-col on desktop, 2-col on tablet, 1-col on mobile
- Loading state shows 6 shimmer skeletons in grid layout
</verification>

<success_criteria>
- RelationshipListPage.tsx and RelationshipCard.tsx compile and render
- Urgency sort: high-signal items appear first
- Warm tint background distinguishes relationship pages from pipeline
- fromType URL param propagated on every card click
</success_criteria>

<output>
After completion, create `.planning/phases/57-relationship-surfaces/57-02-SUMMARY.md`
</output>
