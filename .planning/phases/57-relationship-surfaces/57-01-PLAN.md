---
phase: 57-relationship-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/relationships/api.ts
  - frontend/src/features/relationships/types/relationships.ts
  - frontend/src/features/relationships/hooks/useRelationships.ts
  - frontend/src/features/relationships/hooks/useRelationshipDetail.ts
  - frontend/src/features/relationships/hooks/useSignals.ts
  - frontend/src/features/relationships/hooks/useCreateNote.ts
  - frontend/src/features/relationships/hooks/useSynthesize.ts
  - frontend/src/features/relationships/hooks/useAsk.ts
  - frontend/src/features/navigation/components/AppSidebar.tsx
  - frontend/src/app/routes.tsx
autonomous: true

must_haves:
  truths:
    - "Sidebar shows RELATIONSHIPS section with Prospects, Customers, Advisors, Investors links with coral badge counts"
    - "Pipeline appears below the four relationship links in sidebar"
    - "Navigating to /relationships/prospects loads a page (even if placeholder) without 404"
    - "Navigating to /relationships/:id loads a page without 404"
  artifacts:
    - path: "frontend/src/features/relationships/api.ts"
      provides: "API functions for all relationship endpoints"
      exports: ["fetchRelationships", "fetchRelationshipDetail", "fetchSignals", "createNote", "synthesize", "askRelationship"]
    - path: "frontend/src/features/relationships/types/relationships.ts"
      provides: "TypeScript types matching backend Pydantic schemas"
      contains: "RelationshipListItem"
    - path: "frontend/src/features/relationships/hooks/useSignals.ts"
      provides: "Signal badge count query for sidebar"
      exports: ["useSignals"]
    - path: "frontend/src/features/navigation/components/AppSidebar.tsx"
      provides: "RELATIONSHIPS section with badge counts above Pipeline"
      contains: "RELATIONSHIPS"
    - path: "frontend/src/app/routes.tsx"
      provides: "Lazy routes for /relationships/* pages"
      contains: "relationships"
  key_links:
    - from: "frontend/src/features/relationships/hooks/useSignals.ts"
      to: "frontend/src/features/relationships/api.ts"
      via: "useQuery calling fetchSignals"
      pattern: "fetchSignals"
    - from: "frontend/src/features/navigation/components/AppSidebar.tsx"
      to: "frontend/src/features/relationships/hooks/useSignals.ts"
      via: "useSignals() call for badge counts"
      pattern: "useSignals"
    - from: "frontend/src/app/routes.tsx"
      to: "frontend/src/features/relationships/components/RelationshipListPage"
      via: "lazy import"
      pattern: "relationships/components/RelationshipListPage"
---

<objective>
Create the entire data layer (API functions, TypeScript types, React Query hooks), register all routes, and redesign the sidebar with a RELATIONSHIPS section showing coral badge counts above Pipeline.

Purpose: Every subsequent plan (02-05) depends on this foundation layer. Without types, hooks, API functions, and routes, no component can render real data or navigate correctly.

Output: Complete features/relationships/ data layer, 5 lazy routes in routes.tsx, sidebar with RELATIONSHIPS section + badge counts.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-relationship-surfaces/57-RESEARCH.md
@.planning/phases/56-pipeline-grid/56-01-SUMMARY.md
@.planning/phases/56-pipeline-grid/56-03-SUMMARY.md

@frontend/src/features/navigation/components/AppSidebar.tsx
@frontend/src/app/routes.tsx
@frontend/src/lib/design-tokens.ts
@frontend/src/features/pipeline/hooks/useGraduate.ts
@frontend/src/features/pipeline/api.ts
@frontend/src/features/accounts/hooks/useAccountDetail.ts
@backend/src/flywheel/api/relationships.py
@backend/src/flywheel/api/signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: API layer, types, and React Query hooks</name>
  <files>
    frontend/src/features/relationships/api.ts
    frontend/src/features/relationships/types/relationships.ts
    frontend/src/features/relationships/hooks/useRelationships.ts
    frontend/src/features/relationships/hooks/useRelationshipDetail.ts
    frontend/src/features/relationships/hooks/useSignals.ts
    frontend/src/features/relationships/hooks/useCreateNote.ts
    frontend/src/features/relationships/hooks/useSynthesize.ts
    frontend/src/features/relationships/hooks/useAsk.ts
  </files>
  <action>
    Create `frontend/src/features/relationships/types/relationships.ts`:
    - `RelationshipType = 'prospect' | 'customer' | 'advisor' | 'investor'`
    - `RelationshipListItem` matching backend schema: id, name, domain, relationship_type (string[]), entity_level, relationship_status, ai_summary, signal_count, primary_contact_name, last_interaction_at, created_at
    - `ContactItem`: id, name, title, email, linkedin_url, role, last_contacted_at
    - `TimelineItem`: id, source, content, created_at, direction (string|null), contact_name (string|null)
    - `RelationshipDetailItem` extending RelationshipListItem with: ai_summary_updated_at, contacts (ContactItem[]), recent_timeline (TimelineItem[]), commitments (unknown[]), intel (Record<string, unknown>)
    - `TypeBadge`: type, label, total_signals, counts (reply_received, followup_overdue, commitment_due, stale_relationship)
    - `SignalsResponse`: types (TypeBadge[]), total
    - `AskResponse`: answer, sources (Array<{source, content, date}>), insufficient_context

    Create `frontend/src/features/relationships/api.ts`:
    - Import `api` from `@/lib/api`
    - `queryKeys` object matching the pattern from 57-RESEARCH.md: relationships.all, relationships.list(type), relationships.detail(id), signals.all
    - `fetchRelationships(type: string)` -> GET /relationships/ with params {type, limit: 100}
    - `fetchRelationshipDetail(id: string)` -> GET /relationships/{id}
    - `fetchSignals()` -> GET /signals/
    - `createNote(id: string, content: string)` -> POST /relationships/{id}/notes with body {content}
    - `synthesize(id: string)` -> POST /relationships/{id}/synthesize
    - `askRelationship(id: string, question: string)` -> POST /relationships/{id}/ask with body {question}

    Create hooks following existing patterns (see pipeline/hooks/ and accounts/hooks/):
    - `useRelationships(type: string)`: useQuery with key queryKeys.relationships.list(type), calls fetchRelationships, enabled: !!user (from useAuthStore)
    - `useRelationshipDetail(id: string)`: useQuery with key queryKeys.relationships.detail(id), calls fetchRelationshipDetail, enabled: !!id && !!user
    - `useSignals()`: useQuery with key queryKeys.signals.all, calls fetchSignals, enabled: !!user, staleTime: 30_000 (per research — sidebar does not need realtime precision)
    - `useCreateNote()`: useMutation calling createNote, onSuccess invalidates queryKeys.relationships.detail(id)
    - `useSynthesize()`: useMutation calling synthesize, onSuccess invalidates queryKeys.relationships.detail(id). In onError, check if `error.code === 429` and show `toast.error('AI summary was refreshed recently. Try again in a few minutes.')` using sonner
    - `useAsk()`: useMutation calling askRelationship — does NOT invalidate (stateless Q&A)
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — all new files compile without type errors. Verify all 8 files exist: `ls frontend/src/features/relationships/api.ts frontend/src/features/relationships/types/relationships.ts frontend/src/features/relationships/hooks/*.ts`</verify>
  <done>All 8 files exist, export the correct functions/types, and compile cleanly. queryKeys exported from api.ts. All hooks use `enabled: !!user` guard.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar redesign with RELATIONSHIPS section + route registration</name>
  <files>
    frontend/src/features/navigation/components/AppSidebar.tsx
    frontend/src/app/routes.tsx
  </files>
  <action>
    Modify `AppSidebar.tsx`:
    - Import `useSignals` from `@/features/relationships/hooks/useSignals`
    - Import icons: `Users, Briefcase, DollarSign` from lucide-react (TrendingUp already imported)
    - Add `useSignals()` hook call at top of component (alongside existing hooks)
    - Create helper: `const signalByType = (type: string) => signals?.types.find(t => t.type === type)?.total_signals ?? 0`
    - RESTRUCTURE the SidebarContent: the existing SidebarGroup that contains Briefing/Company Profile/Library/Accounts/Pipeline/Email needs to be split:
      1. First SidebarGroup: Briefing, Company Profile, Library, Email (general nav items)
      2. NEW SidebarGroup with SidebarGroupLabel "Relationships" (styled: fontSize 11px, fontWeight 600, textTransform uppercase, letterSpacing 0.06em, color var(--secondary-text)):
         - Prospects (icon: Users, path: /relationships/prospects)
         - Customers (icon: TrendingUp, path: /relationships/customers)
         - Advisors (icon: Briefcase, path: /relationships/advisors)
         - Investors (icon: DollarSign, path: /relationships/investors)
         Each with coral badge: `badge > 0 && <span className="badge-translucent ml-auto" style={{ background: 'rgba(233,77,53,0.1)', color: 'var(--brand-coral)' }}>{badge}</span>`
         Use `isActive={location.pathname.startsWith(path)}` for active state
      3. Pipeline item moves to after the Relationships group (still in its own SidebarGroup or inline)
    - Keep Accounts link for backward compatibility (it still works with the old /accounts routes)

    Modify `routes.tsx`:
    - Add lazy imports for `RelationshipListPage` and `RelationshipDetail` from `@/features/relationships/components/`
    - Add 5 routes after `/pipeline`:
      ```
      /relationships/prospects  -> <RelationshipListPage type="prospect" />
      /relationships/customers  -> <RelationshipListPage type="customer" />
      /relationships/advisors   -> <RelationshipListPage type="advisor" />
      /relationships/investors  -> <RelationshipListPage type="investor" />
      /relationships/:id        -> <RelationshipDetail />
      ```
    - All wrapped in `<Suspense fallback={null}>`
    - NOTE: The actual components don't exist yet — the lazy() imports will resolve once Plans 02/03 create them. The routes are registered now so the sidebar links work immediately once components land.

    Create minimal placeholder components so the app doesn't crash:
    - `frontend/src/features/relationships/components/RelationshipListPage.tsx`: export function RelationshipListPage({ type }: { type: string }) — renders `<div>Loading {type}...</div>` as placeholder
    - `frontend/src/features/relationships/components/RelationshipDetail.tsx`: export function RelationshipDetail() — renders `<div>Loading detail...</div>` as placeholder
    These placeholders will be fully replaced in Plans 02 and 03.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/. Start dev server `npm run dev` and verify: sidebar shows RELATIONSHIPS section with 4 links and Pipeline below them. Clicking Prospects navigates to /relationships/prospects without crash. Check browser console for no 401 errors on auth pages (useSignals enabled guard).</verify>
  <done>Sidebar displays RELATIONSHIPS header with Prospects/Customers/Advisors/Investors links showing coral badge counts (when signals exist). Pipeline appears below the relationship links. All 5 /relationships/* routes load placeholder pages without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Sidebar visually shows RELATIONSHIPS section header above Pipeline
- Each relationship link shows a coral badge count (or no badge if 0 signals)
- Clicking each of the 4 type links navigates correctly
- Navigating to /relationships/some-uuid loads the detail placeholder
- No console errors on /onboarding or /auth routes (useSignals enabled guard works)
</verification>

<success_criteria>
- Complete data layer: 1 api.ts, 1 types file, 6 hooks — all type-safe and compiling
- Sidebar redesigned with RELATIONSHIPS section + 4 badge links + Pipeline repositioned
- 5 routes registered and navigable
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/57-relationship-surfaces/57-01-SUMMARY.md`
</output>
