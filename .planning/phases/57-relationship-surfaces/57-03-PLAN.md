---
phase: 57-relationship-surfaces
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - frontend/src/features/relationships/components/RelationshipDetail.tsx
  - frontend/src/features/relationships/components/RelationshipHeader.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking a card opens the detail page with left AI panel slot (320px) and main content area"
    - "Detail page tab set is type-driven: Prospects and Customers show Intelligence tab; Advisors and Investors do not"
    - "Back link navigates to the correct relationship type list page based on fromType URL param"
    - "Header shows avatar, name, domain, type badges (clickable)"
  artifacts:
    - path: "frontend/src/features/relationships/components/RelationshipDetail.tsx"
      provides: "Shared detail page with AI panel slot + tab navigation"
      exports: ["RelationshipDetail"]
      min_lines: 80
    - path: "frontend/src/features/relationships/components/RelationshipHeader.tsx"
      provides: "Header card with avatar, name, type badges"
      exports: ["RelationshipHeader"]
      min_lines: 30
  key_links:
    - from: "frontend/src/features/relationships/components/RelationshipDetail.tsx"
      to: "useSearchParams"
      via: "reads fromType URL param for tab config"
      pattern: "fromType"
    - from: "frontend/src/features/relationships/components/RelationshipDetail.tsx"
      to: "frontend/src/features/relationships/hooks/useRelationshipDetail.ts"
      via: "useRelationshipDetail(id) for data"
      pattern: "useRelationshipDetail"
---

<objective>
Build the shared RelationshipDetail page skeleton — the two-panel layout with AI panel slot on the left (320px) and main content (header + tabs) on the right. Tab navigation is type-driven via the fromType URL param.

Purpose: This is the structural shell that Plans 04 (tab bodies) and 05 (AI panel) fill in. The layout, header, tab navigation, and back-link must work correctly before content lands.

Output: RelationshipDetail.tsx (two-panel layout + tabs) and RelationshipHeader.tsx (avatar + badges).
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-relationship-surfaces/57-RESEARCH.md
@.planning/phases/57-relationship-surfaces/57-01-SUMMARY.md

@frontend/src/features/relationships/api.ts
@frontend/src/features/relationships/types/relationships.ts
@frontend/src/features/relationships/hooks/useRelationshipDetail.ts
@frontend/src/lib/design-tokens.ts
@frontend/src/components/ui/tabs.tsx
@frontend/src/components/ui/avatar.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/features/accounts/components/AccountDetailPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RelationshipHeader component</name>
  <files>frontend/src/features/relationships/components/RelationshipHeader.tsx</files>
  <action>
    Create `RelationshipHeader.tsx`:

    Props: `{ account: RelationshipDetailItem; fromType: RelationshipType }`

    Layout:
    1. Avatar (size="xl" = 48px) with initials from account.name, using brand-tint background + brand-coral text (matching sidebar avatar style)
    2. Name: typography.pageTitle styles
    3. Domain: if exists, render as external link with Globe icon (same pattern as AccountDetailPage.tsx)
    4. Type badges row: map `account.relationship_type` array to badge-translucent spans with inline coral rgba styles. Each badge is wrapped in a `<Link>` to `/relationships/{typePlural}` (e.g., clicking "advisor" badge navigates to /relationships/advisors). The currently active fromType badge gets a slightly stronger background (rgba(233,77,53,0.2) instead of 0.1).
    5. Entity level: small secondary text showing "Company" or "Person"
    6. Relationship status: badge-translucent with appropriate color (per existing status patterns)

    The header is a flex row on desktop (avatar left, text right) and stacks vertically on mobile.
  </action>
  <verify>Run `npx tsc --noEmit` from frontend/ — RelationshipHeader compiles without errors.</verify>
  <done>RelationshipHeader renders avatar, name, domain link, clickable type badges, entity level, and status.</done>
</task>

<task type="auto">
  <name>Task 2: RelationshipDetail page with two-panel layout and type-driven tabs</name>
  <files>frontend/src/features/relationships/components/RelationshipDetail.tsx</files>
  <action>
    Replace the placeholder `RelationshipDetail.tsx` with the full implementation:

    URL params:
    - `id` from `useParams<{ id: string }>()`
    - `fromType` from `useSearchParams()` — `searchParams.get('fromType') as RelationshipType` with fallback to 'prospect'
    - CRITICAL: Always derive type from URL param, NOT from account.relationship_type (see Pitfall 2 in research)

    TAB_CONFIG constant (define at module level, not inside component):
    ```
    const TAB_CONFIG: Record<RelationshipType, Array<{ key: string; label: string }>> = {
      prospect:  [{ key: 'timeline', label: 'Timeline' }, { key: 'people', label: 'People' }, { key: 'intelligence', label: 'Intelligence' }, { key: 'commitments', label: 'Commitments' }],
      customer:  [{ key: 'timeline', label: 'Timeline' }, { key: 'people', label: 'People' }, { key: 'intelligence', label: 'Intelligence' }, { key: 'commitments', label: 'Commitments' }],
      advisor:   [{ key: 'timeline', label: 'Timeline' }, { key: 'people', label: 'People' }, { key: 'commitments', label: 'Commitments' }],
      investor:  [{ key: 'timeline', label: 'Timeline' }, { key: 'people', label: 'People' }, { key: 'commitments', label: 'Commitments' }],
    }
    ```

    Layout:
    1. Warm tint background: `style={{ background: registers.relationship.background }}` on outermost wrapper with `className="min-h-dvh"`
    2. Content wrapper: maxWidth spacing.maxGrid, padding section/pageDesktop
    3. Back link: `<Link to={'/relationships/' + fromType + 's'}>` with ArrowLeft icon, text shows capitalize(fromType + 's') (e.g., "Prospects")
    4. `<RelationshipHeader account={account} fromType={fromType} />`
    5. Two-panel layout: `className="flex flex-col lg:flex-row gap-6 mt-6"`
       - Left panel (AI slot): `className="w-full lg:w-[320px] lg:shrink-0"` — render a placeholder div with ShimmerSkeleton or a simple "AI Panel" text box. Plan 05 will replace this with the full AskPanel component. Use a comment: `{/* AskPanel slot — replaced in Plan 05 */}`
       - Right panel (main): `className="flex-1 min-w-0"`
         - Tabs using the existing `<Tabs>` / `<TabsList variant="line">` / `<TabsTrigger>` / `<TabsContent>` from `@/components/ui/tabs`
         - Map TAB_CONFIG[fromType] to TabsTrigger elements
         - Each TabsContent renders a placeholder `<div className="py-4 text-muted-foreground">Coming soon...</div>` — Plan 04 replaces with real tab components
         - defaultValue="timeline"

    Loading state:
    - When isLoading, render a skeleton layout matching the two-panel structure (left: 320px skeleton block, right: header skeleton + tabs skeleton)

    Error state:
    - If error or !account, render back link + "Relationship not found" message

    Mobile layout:
    - On mobile (< lg), the flex-col makes the AI panel stack above the tabs. This is acceptable — the AI panel becomes a collapsible section on mobile (Plan 05 handles collapse behavior).
  </action>
  <verify>Run `npx tsc --noEmit`. Navigate to a relationship detail URL (e.g., /relationships/some-id?fromType=prospect) — page renders with two-panel layout, header with avatar and badges, tab navigation showing Timeline/People/Intelligence/Commitments. Navigate with ?fromType=advisor — Intelligence tab should NOT appear. Back link navigates to correct list page.</verify>
  <done>RelationshipDetail renders two-panel layout with header, type-driven tabs (Intelligence shown for prospect/customer only), back link driven by fromType, loading skeleton, and placeholder content slots for Plans 04 and 05.</done>
</task>

</tasks>

<verification>
- Detail page renders with 320px left panel + main content area
- Tab set for prospect/customer includes Intelligence; advisor/investor does not
- Back link says "Prospects" and navigates to /relationships/prospects when fromType=prospect
- Header shows 48px avatar, name, domain link, type badges
- Clicking a type badge navigates to that type's list page
- Loading skeleton renders during data fetch
- Mobile layout stacks panels vertically
</verification>

<success_criteria>
- TAB_CONFIG is a single authoritative constant driving all tab rendering
- fromType always comes from URL params, never from account.relationship_type
- Two-panel layout with explicit 320px left panel width
- All placeholder slots clearly marked for Plans 04 and 05
</success_criteria>

<output>
After completion, create `.planning/phases/57-relationship-surfaces/57-03-SUMMARY.md`
</output>
