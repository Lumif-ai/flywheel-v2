---
phase: 72-draft-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/engines/email_drafter.py
  - backend/src/flywheel/api/email.py
autonomous: true

must_haves:
  truths:
    - "Every new draft stores a voice_snapshot dict inside context_used JSONB with all 10 voice profile fields"
    - "POST /email/drafts/{draft_id}/regenerate with action 'shorter' returns a regenerated draft body that is shorter"
    - "POST /email/drafts/{draft_id}/regenerate with action 'more_casual' returns a regenerated draft with casual tone — persistent voice profile is unchanged"
    - "POST /email/drafts/{draft_id}/regenerate with custom_instructions returns a draft incorporating the free-form instruction"
    - "Regeneration of a non-pending draft returns 400"
    - "GET thread detail response includes voice_snapshot extracted from context_used for each draft"
  artifacts:
    - path: "backend/src/flywheel/api/email.py"
      provides: "POST /drafts/{draft_id}/regenerate endpoint with RegenerateRequest model"
      contains: "regenerate_draft"
    - path: "backend/src/flywheel/engines/email_drafter.py"
      provides: "Voice snapshot storage in context_used and regenerate_draft_with_overrides helper"
      contains: "voice_snapshot"
  key_links:
    - from: "email.py regenerate endpoint"
      to: "email_drafter.regenerate_draft_with_overrides"
      via: "Calls drafter helper with merged voice overrides"
      pattern: "regenerate_draft_with_overrides"
    - from: "email_drafter.draft_email"
      to: "EmailDraft.context_used"
      via: "Appends voice_snapshot dict to context_used list"
      pattern: "voice_snapshot"
---

<objective>
Add voice snapshot persistence to the draft creation pipeline and build a POST /email/drafts/{draft_id}/regenerate endpoint that re-drafts with one-time voice overrides (shorter, longer, more casual, more formal, or custom instructions) without modifying the persistent voice profile.

Purpose: Backend foundation for draft regeneration with quick voice adjustments and voice annotation display.
Output: Updated `email_drafter.py` with snapshot storage + regeneration helper, and new regenerate endpoint in `email.py`.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-draft-enhancements/72-RESEARCH.md
@backend/src/flywheel/engines/email_drafter.py
@backend/src/flywheel/api/email.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/services/gmail_sync.py
@backend/src/flywheel/api/deps.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store voice snapshot in context_used during draft creation</name>
  <files>backend/src/flywheel/engines/email_drafter.py</files>
  <action>
In `draft_email()`, after the voice profile is loaded and formatted into the system prompt, create a `voice_snapshot` dict containing all 10 voice profile fields (tone, avg_length, sign_off, characteristic_phrases, formality_level, greeting_style, question_style, paragraph_pattern, emoji_usage, avg_sentences) and append it to the `context_used` list with `{"type": "voice_snapshot", ...fields}`.

The snapshot should be created from the loaded `EmailVoiceProfile` row's column values. Include it in the context_used list passed to `_upsert_email_draft()` alongside existing context_ref entries.

Extract the core drafting logic (prompt building + Claude API call) into a reusable helper function `_generate_draft_body(db, tenant_id, email, voice_profile_dict, context_refs, custom_instructions=None)` that:
1. Accepts a voice profile as a plain dict (not ORM object) so overrides can be merged before calling
2. Fetches email body on-demand using existing `_fetch_body_with_fallback` pattern
3. Builds the system prompt with all 10 voice fields
4. If `custom_instructions` is provided, appends it as an additional instruction block after the voice section: "Additional instructions for this draft: {custom_instructions}"
5. Calls Claude and returns the draft body string

Also add a `regenerate_draft_with_overrides(db, tenant_id, draft_id, overrides=None, custom_instructions=None)` function that:
1. Loads the existing EmailDraft by id + tenant check
2. Verifies status is "pending" — raises ValueError if not
3. Loads the parent Email row
4. Loads the current EmailVoiceProfile
5. Builds a merged voice dict: start with all 10 profile fields, then overlay any overrides dict entries
6. Calls `_generate_draft_body` with the merged voice dict
7. Updates the draft row: set `draft_body` to new body, set `user_edits` to None, update `context_used` replacing the old voice_snapshot entry with the new merged snapshot
8. Returns the updated draft row

Define the quick action override mappings as a module-level constant:
```python
QUICK_ACTION_OVERRIDES = {
    "shorter": {"avg_length": 40, "avg_sentences": 2, "paragraph_pattern": "short single-line responses"},
    "longer": {"avg_length": 150, "avg_sentences": 6, "paragraph_pattern": "2-3 sentence paragraph blocks"},
    "more_casual": {"formality_level": "casual", "tone": "friendly and relaxed", "emoji_usage": "occasional"},
    "more_formal": {"formality_level": "formal", "tone": "professional and polished", "emoji_usage": "never"},
}
```
  </action>
  <verify>
Read email_drafter.py and confirm:
1. `voice_snapshot` dict is appended to context_used in `draft_email()`
2. `_generate_draft_body` helper exists and accepts voice profile as dict
3. `regenerate_draft_with_overrides` function exists with status check, voice merge, and draft update
4. `QUICK_ACTION_OVERRIDES` constant is defined with all 4 actions
5. No import errors: `cd backend && python -c "from flywheel.engines.email_drafter import regenerate_draft_with_overrides, QUICK_ACTION_OVERRIDES; print('OK')"`
  </verify>
  <done>draft_email() persists voice snapshot in context_used, and regenerate_draft_with_overrides() can re-draft with merged overrides without touching the persistent voice profile</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /email/drafts/{draft_id}/regenerate endpoint and expose voice_snapshot in DraftDetail</name>
  <files>backend/src/flywheel/api/email.py</files>
  <action>
Add Pydantic request/response models (after existing email Pydantic models):

```python
class RegenerateRequest(BaseModel):
    action: str | None = None  # "shorter", "longer", "more_casual", "more_formal"
    custom_instructions: str | None = None  # Free-form override text

class RegenerateDraftResponse(BaseModel):
    id: UUID
    draft_body: str
    voice_snapshot: dict | None
    message: str
```

Add validation to RegenerateRequest: at least one of `action` or `custom_instructions` must be provided. If `action` is provided, it must be one of the four valid values.

Add the endpoint following the same pattern as `approve_draft` and `dismiss_draft`:

```python
@router.post("/drafts/{draft_id}/regenerate", response_model=RegenerateDraftResponse)
async def regenerate_draft(draft_id: UUID, body: RegenerateRequest, user=Depends(current_user), db=Depends(get_db)):
```

Implementation:
1. Import `regenerate_draft_with_overrides` and `QUICK_ACTION_OVERRIDES` from `flywheel.engines.email_drafter`
2. Resolve overrides: if `body.action`, look up `QUICK_ACTION_OVERRIDES[body.action]`; if not found, return 400
3. Call `regenerate_draft_with_overrides(db, user.tenant_id, draft_id, overrides=overrides, custom_instructions=body.custom_instructions)`
4. Catch ValueError (non-pending draft) and return 400
5. Catch draft not found and return 404
6. Extract voice_snapshot from updated draft's context_used (find entry with type=="voice_snapshot")
7. Return RegenerateDraftResponse with the new draft body, voice snapshot, and success message

Also update the existing DraftDetail Pydantic model (or the thread detail response construction) to include a `voice_snapshot: dict | None` field. In the thread detail endpoint, extract the voice_snapshot from each draft's `context_used` array (find the dict where `type == "voice_snapshot"`) and include it in the response. If no voice_snapshot exists (legacy drafts), return None.
  </action>
  <verify>
1. Read email.py and confirm RegenerateRequest, RegenerateDraftResponse models exist
2. Confirm regenerate_draft endpoint is registered on the router
3. Confirm DraftDetail includes voice_snapshot field
4. No import errors: `cd backend && python -c "from flywheel.api.email import router; print('OK')"`
  </verify>
  <done>POST /email/drafts/{draft_id}/regenerate endpoint accepts action or custom_instructions, regenerates draft with overrides, returns new body + voice snapshot. Thread detail response includes voice_snapshot for each draft.</done>
</task>

</tasks>

<verification>
1. Import check passes for both email_drafter.py and email.py
2. Voice snapshot dict is stored in context_used during normal draft creation
3. Regenerate endpoint validates action values and rejects invalid ones with 400
4. Regenerate endpoint rejects non-pending drafts with 400
5. DraftDetail response includes voice_snapshot field (None for legacy drafts)
</verification>

<success_criteria>
- New drafts created by the sync pipeline include a voice_snapshot in context_used
- POST /email/drafts/{draft_id}/regenerate with action="more_casual" produces a regenerated draft with casual overrides applied
- POST /email/drafts/{draft_id}/regenerate with custom_instructions="be more empathetic" produces a draft incorporating that instruction
- The persistent EmailVoiceProfile row is never modified by regeneration
- Thread detail API response includes voice_snapshot for drafts that have one
</success_criteria>

<output>
After completion, create `.planning/phases/72-draft-enhancements/72-01-SUMMARY.md`
</output>
