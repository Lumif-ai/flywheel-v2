---
phase: 81-platform-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/flywheel_mcp/templates/CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md template exists with context routing rules referencing flywheel_read_context and flywheel_write_context"
    - "CLAUDE.md template includes skill-first lookup rules referencing flywheel_fetch_skills and flywheel_fetch_skill_prompt"
    - "CLAUDE.md template includes output saving rules referencing flywheel_save_document and flywheel_save_meeting_summary"
    - "Template is under 60 lines, uses imperative tone, and lists concrete business intelligence examples"
  artifacts:
    - path: "cli/flywheel_mcp/templates/CLAUDE.md"
      provides: "CLAUDE.md integration rules template for founders"
      contains: "flywheel_write_context"
  key_links:
    - from: "cli/flywheel_mcp/templates/CLAUDE.md"
      to: "cli/flywheel_mcp/server.py"
      via: "MCP tool name references"
      pattern: "flywheel_(read_context|write_context|fetch_skills|fetch_skill_prompt|save_document|save_meeting_summary)"
---

<objective>
Create the CLAUDE.md template that teaches Claude Code how to use Flywheel -- context routing, skill-first lookup, and output saving.

Purpose: When a founder installs Flywheel, this template gets added to their project CLAUDE.md so Claude Code automatically routes business intel to the context store, checks Flywheel skills first, and saves outputs to the library.
Output: `cli/flywheel_mcp/templates/CLAUDE.md`
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-platform-setup/81-RESEARCH.md
@cli/flywheel_mcp/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create templates directory and CLAUDE.md template</name>
  <files>cli/flywheel_mcp/templates/CLAUDE.md</files>
  <action>
Create directory `cli/flywheel_mcp/templates/` and write the CLAUDE.md template file.

The template has three sections matching SETUP-01 requirements. Use the research-validated content from 81-RESEARCH.md as the baseline, with these rules:

**Section 1: Context Store -- Business Intelligence Routing**
- IMPORTANT prefix for the routing rule
- Business intel -> `flywheel_write_context` (list concrete examples: contacts, company details, competitive intel, pain points, positioning insights, pricing signals, market signals, meeting outcomes, deal context, relationship notes)
- Read-before-write rule: call `flywheel_read_context` first to enrich rather than duplicate
- Non-business data (code, personal notes, scratch work) stays local

**Section 2: Skill Discovery -- Flywheel First**
- ALWAYS check Flywheel first when user asks for a business task
- Flow: `flywheel_fetch_skills` -> match by triggers -> `flywheel_fetch_skill_prompt` -> execute following loaded prompt
- Fall back to general Claude Code only if no skill matches
- Do NOT mention `flywheel_run_skill` (that is the cron/scheduled path, not interactive)

**Section 3: Output -- Save to Library**
- Deliverables (documents, briefings, reports, collateral) -> `flywheel_save_document`
- Meeting summaries -> `flywheel_save_meeting_summary`
- Both Flywheel AND local copies (not either/or)

Format rules:
- Target ~50 lines total (under 60 max)
- Imperative tone ("Save deliverables via..." not "You might want to...")
- Reference exact MCP tool names with backtick formatting
- Use bullet points, not paragraphs
- Header: `# Flywheel Integration` (so it can be appended to existing CLAUDE.md)
- No explanations of WHY rules exist -- only WHAT to do
  </action>
  <verify>
1. File exists: `ls cli/flywheel_mcp/templates/CLAUDE.md`
2. Line count under 60: `wc -l cli/flywheel_mcp/templates/CLAUDE.md`
3. Contains all 6 required tool names: `grep -c 'flywheel_read_context\|flywheel_write_context\|flywheel_fetch_skills\|flywheel_fetch_skill_prompt\|flywheel_save_document\|flywheel_save_meeting_summary' cli/flywheel_mcp/templates/CLAUDE.md` should return 6+
4. Does NOT contain flywheel_run_skill: `grep -c 'flywheel_run_skill' cli/flywheel_mcp/templates/CLAUDE.md` should return 0
5. Has all three section headers: Context Store, Skill Discovery, Output
  </verify>
  <done>
CLAUDE.md template exists at cli/flywheel_mcp/templates/CLAUDE.md with all three rule sections (context routing, skill-first lookup, output saving), references exact MCP tool names, is under 60 lines, uses imperative tone, and lists concrete business intelligence examples
  </done>
</task>

</tasks>

<verification>
1. Template file exists at `cli/flywheel_mcp/templates/CLAUDE.md`
2. All 6 MCP tool names referenced correctly (flywheel_read_context, flywheel_write_context, flywheel_fetch_skills, flywheel_fetch_skill_prompt, flywheel_save_document, flywheel_save_meeting_summary)
3. flywheel_run_skill is NOT referenced (interactive path only)
4. Template is under 60 lines
5. Three sections present: Context Store, Skill Discovery, Output
6. Concrete business intel examples listed (contacts, companies, competitive intel, etc.)
7. Read-before-write rule included
</verification>

<success_criteria>
A founder can copy the contents of cli/flywheel_mcp/templates/CLAUDE.md into their project's CLAUDE.md file, and Claude Code will automatically: (1) route business intelligence to the context store, (2) check Flywheel skills before falling back to general capabilities, and (3) save deliverables and meeting summaries to the Flywheel library.
</success_criteria>

<output>
After completion, create `.planning/phases/81-platform-setup/81-01-SUMMARY.md`
</output>
