---
phase: 80-frontend-feature-flags
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/feature-flags.ts
  - frontend/src/vite-env.d.ts
  - frontend/src/app/routes.tsx
  - frontend/src/features/navigation/components/AppSidebar.tsx
  - frontend/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "VITE_FEATURE_EMAIL=false removes email route from nav and redirects /email to /"
    - "VITE_FEATURE_TASKS=false removes tasks route from nav and redirects /tasks to /"
    - "With flags unset (default), all features remain visible and functional"
    - "Email flag=false prevents useEmailThreads() hook from firing globally"
  artifacts:
    - path: "frontend/src/lib/feature-flags.ts"
      provides: "Centralized boolean constants for compile-time feature gating"
      exports: ["FEATURE_EMAIL", "FEATURE_TASKS"]
    - path: "frontend/src/vite-env.d.ts"
      provides: "TypeScript declarations for VITE_FEATURE_EMAIL and VITE_FEATURE_TASKS"
      contains: "VITE_FEATURE_EMAIL"
    - path: "frontend/src/app/routes.tsx"
      provides: "Conditional route rendering with redirect fallback for gated features"
      contains: "FEATURE_EMAIL"
    - path: "frontend/src/features/navigation/components/AppSidebar.tsx"
      provides: "Conditional nav items gated by feature flags"
      contains: "FEATURE_EMAIL"
    - path: "frontend/src/app/layout.tsx"
      provides: "AuthenticatedAlerts gated behind FEATURE_EMAIL to prevent unnecessary API calls"
      contains: "FEATURE_EMAIL"
  key_links:
    - from: "frontend/src/lib/feature-flags.ts"
      to: "import.meta.env.VITE_FEATURE_EMAIL"
      via: "compile-time env var evaluation"
      pattern: "import\\.meta\\.env\\.VITE_FEATURE"
    - from: "frontend/src/app/routes.tsx"
      to: "frontend/src/lib/feature-flags.ts"
      via: "import of FEATURE_EMAIL, FEATURE_TASKS constants"
      pattern: "from.*feature-flags"
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/lib/feature-flags.ts"
      via: "FEATURE_EMAIL gates AuthenticatedAlerts rendering and hook call"
      pattern: "FEATURE_EMAIL"
---

<objective>
Implement compile-time feature flags that hide email and tasks features from design partners.

Purpose: Design partners should only see polished features. Email and tasks routes must be invisible and inaccessible when their respective VITE_FEATURE_* env vars are set to "false".
Output: Feature flag module + gated routes, nav items, and global email alerts.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-frontend-feature-flags/80-RESEARCH.md

@frontend/src/app/routes.tsx
@frontend/src/app/layout.tsx
@frontend/src/features/navigation/components/AppSidebar.tsx
@frontend/src/vite-env.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feature-flags module and update TypeScript declarations</name>
  <files>frontend/src/lib/feature-flags.ts, frontend/src/vite-env.d.ts</files>
  <action>
    Create `frontend/src/lib/feature-flags.ts` exporting two boolean constants:
    - `FEATURE_EMAIL`: `import.meta.env.VITE_FEATURE_EMAIL !== 'false'`
    - `FEATURE_TASKS`: `import.meta.env.VITE_FEATURE_TASKS !== 'false'`

    Default-to-enabled pattern (`!== 'false'`) so developers without env vars get all features. Only explicitly setting `=false` disables.

    Add a JSDoc comment block explaining: compile-time evaluation, how to disable, default behavior.

    Update `frontend/src/vite-env.d.ts` to extend `ImportMetaEnv` with optional string declarations for `VITE_FEATURE_EMAIL` and `VITE_FEATURE_TASKS`. Keep the existing `/// <reference types="vite/client" />` line. Add the `ImportMeta` interface as well for full type coverage.
  </action>
  <verify>Run `cd frontend && npx tsc --noEmit` to confirm no TypeScript errors. Verify feature-flags.ts exports both constants.</verify>
  <done>feature-flags.ts exists with FEATURE_EMAIL and FEATURE_TASKS exports. vite-env.d.ts declares both env vars. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Gate routes, sidebar nav, and global email alerts behind feature flags</name>
  <files>frontend/src/app/routes.tsx, frontend/src/features/navigation/components/AppSidebar.tsx, frontend/src/app/layout.tsx</files>
  <action>
    **routes.tsx:**
    Import `FEATURE_EMAIL, FEATURE_TASKS` from `@/lib/feature-flags`.

    For `/email` route (line 99): wrap in `{FEATURE_EMAIL && (...)}`. Add a redirect fallback: `{!FEATURE_EMAIL && <Route path="/email" element={<Navigate to="/" replace />} />}`.

    For `/tasks` route (line 109): wrap in `{FEATURE_TASKS && (...)}`. Add a redirect fallback: `{!FEATURE_TASKS && <Route path="/tasks" element={<Navigate to="/" replace />} />}`.

    Leave the lazy import declarations at top of file as-is -- they are already code-split into separate chunks and only fetched on navigation, so gating them is unnecessary.

    **AppSidebar.tsx:**
    Import `FEATURE_EMAIL, FEATURE_TASKS` from `@/lib/feature-flags`.

    Wrap the Email `SidebarMenuItem` (lines 141-150) in `{FEATURE_EMAIL && (...)}`.
    Wrap the Tasks `SidebarMenuItem` (lines 161-170) in `{FEATURE_TASKS && (...)}`.

    **layout.tsx:**
    Import `FEATURE_EMAIL` from `@/lib/feature-flags`.

    Refactor `AuthenticatedAlerts` to gate the email hook call entirely (not just the render). Use the two-component pattern from research:
    - `AuthenticatedAlerts` checks `if (!FEATURE_EMAIL) return null`, then renders `<EmailAlertInner />`
    - `EmailAlertInner` calls `useEmailThreads()` and renders `CriticalEmailAlert`

    This ensures `useEmailThreads()` never fires when email is disabled, avoiding unnecessary API calls to `/api/email/threads`.
  </action>
  <verify>
    Run `cd frontend && npx tsc --noEmit` to confirm no TypeScript errors.
    Run `cd frontend && npm run build` to confirm production build succeeds.
    Verify by reading the modified files that: (1) email route is gated with redirect, (2) tasks route is gated with redirect, (3) sidebar items are conditionally rendered, (4) AuthenticatedAlerts uses the two-component pattern with early return.
  </verify>
  <done>
    With default env (no flags set): all routes, nav items, and email alerts render normally -- zero behavior change.
    With VITE_FEATURE_EMAIL=false: /email redirects to /, Email nav item hidden, no useEmailThreads() calls on any page.
    With VITE_FEATURE_TASKS=false: /tasks redirects to /, Tasks nav item hidden.
    Both flags can be set independently.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript compiles without errors
2. `cd frontend && npm run build` -- production build succeeds
3. Grep for `FEATURE_EMAIL` in routes.tsx, AppSidebar.tsx, layout.tsx -- all three files reference the flag
4. Grep for `FEATURE_TASKS` in routes.tsx, AppSidebar.tsx -- both files reference the flag
5. Verify routes.tsx contains `Navigate to="/" replace` fallbacks for both gated routes
6. Verify layout.tsx `AuthenticatedAlerts` returns null when FEATURE_EMAIL is false (before any hook call)
</verification>

<success_criteria>
- VITE_FEATURE_EMAIL=false and VITE_FEATURE_TASKS=false removes routes from nav and route table at build time
- Direct navigation to gated route redirects to home
- Default behavior (no env vars) is unchanged -- all features visible
- No unnecessary email API calls when email feature is disabled
</success_criteria>

<output>
After completion, create `.planning/phases/80-frontend-feature-flags/80-01-SUMMARY.md`
</output>
