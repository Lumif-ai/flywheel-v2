---
phase: 094-mcp-contact-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/flywheel_mcp/api_client.py
  - cli/flywheel_mcp/server.py
autonomous: true

must_haves:
  truths:
    - "Claude Code can list pipeline contacts with outreach status via MCP tool"
    - "Claude Code can filter contacts by company, status, channel, and variant"
    - "Claude Code can create a new outreach step for a contact via MCP tool"
    - "Created outreach step has correct step_number, channel, subject, body, and variant"
    - "Contact IDs from list tool can be passed directly to create tool for batch workflows"
  artifacts:
    - path: "cli/flywheel_mcp/api_client.py"
      provides: "list_pipeline_contacts client method"
      contains: "def list_pipeline_contacts"
    - path: "cli/flywheel_mcp/server.py"
      provides: "flywheel_list_pipeline_contacts MCP tool"
      contains: "def flywheel_list_pipeline_contacts"
    - path: "cli/flywheel_mcp/server.py"
      provides: "flywheel_create_outreach_step MCP tool"
      contains: "def flywheel_create_outreach_step"
  key_links:
    - from: "cli/flywheel_mcp/server.py (flywheel_list_pipeline_contacts)"
      to: "cli/flywheel_mcp/api_client.py (list_pipeline_contacts)"
      via: "client.list_pipeline_contacts() call"
      pattern: "client\\.list_pipeline_contacts"
    - from: "cli/flywheel_mcp/server.py (flywheel_create_outreach_step)"
      to: "cli/flywheel_mcp/api_client.py (create_pipeline_activity)"
      via: "client.create_pipeline_activity() call with metadata containing step_number"
      pattern: "client\\.create_pipeline_activity"
    - from: "cli/flywheel_mcp/api_client.py (list_pipeline_contacts)"
      to: "GET /api/v1/pipeline/contacts/"
      via: "self._request('get', '/api/v1/pipeline/contacts/')"
      pattern: "_request.*pipeline/contacts"
---

<objective>
Add two MCP tools for contact-centric outreach operations: listing contacts with status and creating outreach steps.

Purpose: Enable Claude Code to perform batch outreach operations like "generate step 2 for all variant A contacts" by providing ID-based contact listing and activity creation tools.
Output: Two new MCP tools in server.py, one new client method in api_client.py.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/094-mcp-contact-tools/094-RESEARCH.md
@cli/flywheel_mcp/server.py
@cli/flywheel_mcp/api_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list_pipeline_contacts client method</name>
  <files>cli/flywheel_mcp/api_client.py</files>
  <action>
Add a `list_pipeline_contacts` method to `FlywheelClient` in the "Pipeline (unified CRM)" section, after the existing `create_pipeline_activity` method.

```python
def list_pipeline_contacts(self, **params) -> dict:
    """GET /api/v1/pipeline/contacts/ with filter params."""
    clean = {k: v for k, v in params.items() if v is not None}
    return self._request("get", "/api/v1/pipeline/contacts/", params=clean)
```

This follows the exact pattern of `list_pipeline` and `list_leads` — filter None values, pass as query params. No new patterns needed.
  </action>
  <verify>Run `python3 -c "from flywheel_mcp.api_client import FlywheelClient; print(hasattr(FlywheelClient, 'list_pipeline_contacts'))"` from the `cli/` directory — should print True.</verify>
  <done>FlywheelClient has a list_pipeline_contacts method that calls GET /api/v1/pipeline/contacts/ with filtered params.</done>
</task>

<task type="auto">
  <name>Task 2: Add flywheel_list_pipeline_contacts and flywheel_create_outreach_step MCP tools</name>
  <files>cli/flywheel_mcp/server.py</files>
  <action>
Add two new MCP tool functions to server.py. Place them after the existing pipeline-related tools (near the end of the file, before any deprecated tools section if one exists).

**Tool 1: flywheel_list_pipeline_contacts**

Follow the exact code from the research document (094-RESEARCH.md "Code Examples" section). Key details:
- Parameters: company, status, channel, variant (all str, default ""), limit (int, default 50)
- Use truthy checks (`if company:`) to filter empty strings before passing to client
- Format output as numbered list with: name, company, email, status, channel, variant, step, next_step, and IDs (contact_id and pipeline_entry_id)
- The IDs line is critical — it enables batch workflows where output feeds into flywheel_create_outreach_step
- Docstring must describe batch use cases (this is the discovery mechanism for Claude Code)

**Tool 2: flywheel_create_outreach_step**

Follow the exact code from 094-RESEARCH.md. Key details:
- Parameters: pipeline_entry_id (str), contact_id (str), step_number (int), channel (str, default "email"), subject (str, default ""), body (str, default ""), variant (str, default ""), status (str, default "drafted")
- Use existing `client.create_pipeline_activity()` — do NOT create a new client method
- Pass `type="outreach"` (not "message")
- Put step_number, variant, cadence_days, and body in the `metadata` dict (not `metadata_` — the JSON key is `metadata` without underscore, per how existing flywheel_draft_pipeline_message works)
- Pass `body_preview=(body[:200] if body else None)` as a top-level field
- Docstring must explain the batch workflow: list contacts first, then create steps for each

Both tools must follow the established error handling pattern:
```python
except FlywheelAPIError as exc:
    return str(exc)
except Exception as exc:
    return f"Error ...: {exc}"
```
  </action>
  <verify>
1. Run `cd /Users/sharan/Projects/flywheel-v2/cli && python3 -c "from flywheel_mcp.server import mcp; tools = [t.name for t in mcp._tool_manager.list_tools()]; print('list_contacts' in str(tools), 'outreach_step' in str(tools))"` — should print True True.
2. Grep for both tool names: `grep -n "def flywheel_list_pipeline_contacts\|def flywheel_create_outreach_step" cli/flywheel_mcp/server.py` — should show both functions.
  </verify>
  <done>Both MCP tools are registered and discoverable: flywheel_list_pipeline_contacts returns formatted contact list with IDs, flywheel_create_outreach_step creates an outreach activity with step_number in metadata.</done>
</task>

</tasks>

<verification>
1. Both tools import and register without errors: `cd cli && python3 -c "from flywheel_mcp.server import mcp; print('OK')"`
2. Client method exists: `python3 -c "from flywheel_mcp.api_client import FlywheelClient; c = FlywheelClient.__dict__; print('list_pipeline_contacts' in c)"`
3. Tool docstrings contain batch workflow description: `grep -A5 "flywheel_list_pipeline_contacts\|flywheel_create_outreach_step" cli/flywheel_mcp/server.py | grep -i batch`
</verification>

<success_criteria>
- flywheel_list_pipeline_contacts tool returns formatted contact list with outreach status, filterable by company/status/channel/variant
- flywheel_create_outreach_step tool creates an outreach activity with correct step_number, channel, subject, body in metadata
- Both tools follow established FlywheelClient + FlywheelAPIError patterns
- Contact IDs from list tool output can be copy-pasted into create tool parameters
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/094-mcp-contact-tools/094-01-SUMMARY.md`
</output>
