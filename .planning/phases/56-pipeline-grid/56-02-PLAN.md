---
phase: 56-pipeline-grid
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - frontend/src/features/pipeline/components/PipelinePage.tsx
  - frontend/src/features/pipeline/components/cell-renderers/CompanyCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx
  - frontend/src/features/pipeline/components/cell-renderers/OutreachDot.tsx
  - frontend/src/features/pipeline/hooks/usePipelineColumns.ts
  - frontend/src/features/pipeline/types/pipeline.ts
  - backend/src/flywheel/api/outreach.py
autonomous: true

must_haves:
  truths:
    - "Pipeline page renders an AG Grid with 8 columns at 56px row height"
    - "Columns are resizable by dragging column borders"
    - "Columns are reorderable by dragging column headers"
    - "Column widths and order persist in localStorage across page navigations"
    - "Grid uses app design tokens for colors via themeQuartz.withParams()"
    - "Company column shows avatar initials + company name + domain"
    - "Contact column shows primary contact name + title"
    - "Pipeline endpoint returns primary contact data without N+1 queries"
  artifacts:
    - path: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      provides: "AG Grid pipeline page replacing HTML table"
      contains: "AgGridReact"
    - path: "frontend/src/features/pipeline/hooks/usePipelineColumns.ts"
      provides: "Column definitions and localStorage persistence"
      contains: "localStorage"
      exports: ["usePipelineColumns"]
    - path: "frontend/src/features/pipeline/components/cell-renderers/CompanyCell.tsx"
      provides: "Avatar + company name cell renderer"
      exports: ["CompanyCell"]
    - path: "frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx"
      provides: "Contact name + title cell renderer"
      exports: ["ContactCell"]
    - path: "frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx"
      provides: "Translucent badge cell renderer for fit tier"
      exports: ["FitTierBadge"]
    - path: "frontend/src/features/pipeline/components/cell-renderers/OutreachDot.tsx"
      provides: "8px status dot cell renderer"
      exports: ["OutreachDot"]
    - path: "backend/src/flywheel/api/outreach.py"
      provides: "Pipeline endpoint extended with primary contact LEFT JOIN"
      contains: "primary_contact"
  key_links:
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipelineColumns.ts"
      via: "hook providing columnDefs + initialState + onColumnStateChanged"
      pattern: "usePipelineColumns"
    - from: "frontend/src/features/pipeline/hooks/usePipelineColumns.ts"
      to: "localStorage"
      via: "getItem/setItem with key flywheel:pipeline:columnState"
      pattern: "flywheel:pipeline:columnState"
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "ag-grid-react"
      via: "AgGridReact component with theme prop"
      pattern: "AgGridReact"
---

<objective>
Replace the current HTML table pipeline page with an AG Grid (Community v35) data grid featuring 8 columns, custom cell renderers, column resize/reorder, and localStorage state persistence.

Purpose: GRID-01 requires an Airtable-style data grid. AG Grid Community provides resize, reorder, visibility, and virtualization out of the box. The backend must also be extended to return primary contact data for the Contact, Email, and LinkedIn columns.
Output: Fully functional AG Grid pipeline replacing the HTML table, with column state persisting across navigations.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-pipeline-grid/56-RESEARCH.md
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/hooks/usePipeline.ts
@frontend/src/features/pipeline/types/pipeline.ts
@frontend/src/features/pipeline/api.ts
@backend/src/flywheel/api/outreach.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AG Grid, extend backend pipeline endpoint with contact data, update types</name>
  <files>frontend/src/features/pipeline/types/pipeline.ts, backend/src/flywheel/api/outreach.py</files>
  <action>
  **Install AG Grid:**
  ```bash
  cd frontend && npm install ag-grid-community ag-grid-react
  ```

  **Backend — extend GET /pipeline/ in outreach.py:**

  Add a LEFT JOIN LATERAL (or LEFT JOIN on a subquery) to fetch the primary contact for each account. The `AccountContact` model has fields: `name`, `title`, `email`, `linkedin_url`. The primary contact is the first contact by `created_at` (or just LIMIT 1).

  Add a subquery before the main query:
  ```python
  # Primary contact subquery (first contact per account)
  primary_contact_sq = (
      select(
          AccountContact.account_id,
          AccountContact.name.label("contact_name"),
          AccountContact.title.label("contact_title"),
          AccountContact.email.label("contact_email"),
          AccountContact.linkedin_url.label("contact_linkedin"),
      )
      .distinct(AccountContact.account_id)
      .order_by(AccountContact.account_id, AccountContact.created_at.asc())
      .subquery("primary_contact")
  )
  ```

  LEFT JOIN this subquery in the main stmt:
  ```python
  .outerjoin(primary_contact_sq, primary_contact_sq.c.account_id == Account.id)
  ```

  Add the four contact fields to the SELECT columns. Update the item building loop to include them in the PipelineItem dict.

  Add these four fields to the response dict:
  - `primary_contact_name`: str | None
  - `primary_contact_title`: str | None
  - `primary_contact_email`: str | None
  - `primary_contact_linkedin`: str | None

  **Frontend types — update pipeline.ts:**

  Add to `PipelineItem`:
  ```typescript
  primary_contact_name: string | null
  primary_contact_title: string | null
  primary_contact_email: string | null
  primary_contact_linkedin: string | null
  ```

  Also add `search` to `PipelineParams`:
  ```typescript
  search?: string
  ```
  </action>
  <verify>Run backend: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.outreach import router; print('OK')"`. Run frontend: `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit`. Verify AG Grid is in package.json: `grep ag-grid frontend/package.json`.</verify>
  <done>AG Grid installed. Pipeline endpoint returns primary_contact_name, primary_contact_title, primary_contact_email, primary_contact_linkedin for each row. Frontend PipelineItem type includes all four contact fields.</done>
</task>

<task type="auto">
  <name>Task 2: AG Grid PipelinePage with cell renderers and localStorage column persistence</name>
  <files>frontend/src/features/pipeline/components/PipelinePage.tsx, frontend/src/features/pipeline/components/cell-renderers/CompanyCell.tsx, frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx, frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx, frontend/src/features/pipeline/components/cell-renderers/OutreachDot.tsx, frontend/src/features/pipeline/hooks/usePipelineColumns.ts</files>
  <action>
  **Create cell-renderers/ directory and four cell renderer components:**

  **CompanyCell.tsx:** Renders Avatar (size="default", 32px) with initial from company name in brand-light bg + brand-coral text, company name (font-weight 500, heading-text), and domain below (caption size, secondary-text). Use the Avatar and AvatarFallback from `@/components/ui/avatar`. The cell renderer receives `{ data }` as props where data is a `PipelineItem`.

  **ContactCell.tsx:** Renders primary_contact_name (body text) and primary_contact_title below (caption, secondary-text). If no contact, show em dash in secondary-text.

  **FitTierBadge.tsx:** Translucent badge per research Pattern (opacity-10 background + full-opacity text). Palette: strong/excellent -> green, good -> blue, fair -> amber, weak/poor -> red. Pill shape (border-radius 9999px), 12px font, 500 weight. Null shows em dash.

  **OutreachDot.tsx:** 8px colored circle + capitalized status text. Colors: sent -> blue (#3B82F6), opened -> orange (#F97316), replied -> green (#22C55E), bounced -> red (#EF4444). Null shows gray dot + "None".

  **Create usePipelineColumns.ts hook:**

  This hook returns: `{ columnDefs, initialState, onColumnStateChanged, gridApiRef }`

  - `gridApiRef` = `useRef<GridApi>(null)`
  - `columnDefs`: Array of 8 ColDef objects:
    1. Company: field='name', cellRenderer=CompanyCell, minWidth=200, flex=1.5
    2. Contact: field='primary_contact_name', cellRenderer=ContactCell, minWidth=160, flex=1
    3. Email: field='primary_contact_email', minWidth=140, cellRenderer that shows mailto link or em dash
    4. LinkedIn: field='primary_contact_linkedin', minWidth=80, width=80, cellRenderer that shows external link icon or em dash
    5. Fit Tier: field='fit_tier', cellRenderer=FitTierBadge, width=120
    6. Outreach: field='last_outreach_status', cellRenderer=OutreachDot, width=130
    7. Last Action: field='last_interaction_at', valueFormatter=formatRelativeTime, width=140
    8. Days Stale: field='days_since_last_outreach', width=100, cellRenderer=DaysSinceCell (color-coded: green <7d, amber 7-14d, red >14d)

  All columns: `resizable: true`, `movable: true` (AG Grid built-in, do NOT hand-roll).
  Sortable by default. Email and LinkedIn columns: `sortable: false`.

  - `initialState`: Read from localStorage key `flywheel:pipeline:columnState`. Parse JSON. Return `{ columnState: parsed }` or undefined.
  - `onColumnStateChanged`: Callback that reads `gridApiRef.current?.getColumnState()` and writes to localStorage. Use `useCallback`.

  **Rewrite PipelinePage.tsx:**

  Replace the entire HTML table with AG Grid. Keep the page header (h1 "Pipeline", subtitle). Keep the filter selects for now (they will be upgraded in Plan 03). Remove the manual pagination (AG Grid handles virtualization; Plan 03 adds the proper page size selector).

  Create the theme object using `themeQuartz.withParams()` per research:
  ```typescript
  import { AllCommunityModule, themeQuartz } from 'ag-grid-community'
  import { AgGridReact } from 'ag-grid-react'

  const pipelineTheme = themeQuartz.withParams({
    backgroundColor: 'var(--card-bg)',
    foregroundColor: 'var(--body-text)',
    headerBackgroundColor: 'var(--card-bg)',
    headerTextColor: 'var(--secondary-text)',
    borderColor: 'var(--subtle-border)',
    accentColor: 'var(--brand-coral)',
    rowHoverColor: 'rgba(0,0,0,0.02)',
    fontSize: 14,
    rowHeight: 56,
    headerHeight: 40,
    fontFamily: "'Geist Variable', ui-sans-serif, system-ui, sans-serif",
  })
  ```

  Do NOT import any CSS files from ag-grid-community/styles/. Use only the programmatic theme API.

  Wrap AgGridReact in a div with `style={{ height: 'calc(100vh - 220px)', width: '100%' }}` — AG Grid requires a fixed-height container.

  Pass props:
  - `modules={[AllCommunityModule]}`
  - `theme={pipelineTheme}`
  - `rowData={items}` (from usePipeline hook)
  - `columnDefs` from usePipelineColumns
  - `initialState` from usePipelineColumns
  - `onColumnResized={onColumnStateChanged}`
  - `onColumnMoved={onColumnStateChanged}`
  - `onColumnVisible={onColumnStateChanged}`
  - `onGridReady={(e) => { gridApiRef.current = e.api }}`
  - `defaultColDef={{ resizable: true, sortable: true }}`

  Show loading state: while `isLoading`, show a container with 8 ShimmerSkeleton rows (each 56px height) mimicking the grid shape. Once data loads, show the AgGridReact.

  Show empty state: when `items.length === 0 && !isLoading`, render EmptyState with Inbox icon, "No prospects in pipeline" title, "Add accounts to start building your pipeline" description.

  Keep the existing filter selects (fitTier, outreachStatus) and pass them to usePipeline. Plan 03 will replace them with the proper filter bar.

  Increase default page size from 20 to 50 (the grid virtualizes so rendering 50-100 rows is fine).
  </action>
  <verify>Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit`. Verify all cell renderer files exist. Verify AG Grid import works: `grep "AgGridReact" frontend/src/features/pipeline/components/PipelinePage.tsx`. Verify localStorage key: `grep "flywheel:pipeline:columnState" frontend/src/features/pipeline/hooks/usePipelineColumns.ts`.</verify>
  <done>Pipeline page renders AG Grid with 8 columns at 56px row height. Columns resize and reorder via AG Grid built-in. Column state persists in localStorage. Company column shows avatar + name, Contact shows name + title, Fit Tier shows translucent badge, Outreach shows 8px dot. Grid uses app design tokens via themeQuartz.withParams(). No CSS imports from ag-grid-community/styles/.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep "AgGridReact" frontend/src/features/pipeline/components/PipelinePage.tsx` matches
- `grep "themeQuartz" frontend/src/features/pipeline/components/PipelinePage.tsx` matches
- `grep "flywheel:pipeline:columnState" frontend/src/features/pipeline/hooks/usePipelineColumns.ts` matches
- `ls frontend/src/features/pipeline/components/cell-renderers/` lists 4 files
- `grep "primary_contact" backend/src/flywheel/api/outreach.py` matches (contact LEFT JOIN)
- `grep "ag-grid" frontend/package.json` shows ag-grid-community and ag-grid-react
</verification>

<success_criteria>
1. AG Grid Community v35 installed and rendering on Pipeline page
2. 8 columns visible: Company (avatar), Contact (name+title), Email (mailto), LinkedIn (icon), Fit Tier (translucent badge), Outreach (dot), Last Action (relative time), Days Stale (color-coded)
3. Columns resizable by dragging borders, reorderable by dragging headers
4. Column state persists in localStorage — survives page navigation
5. Grid themed via themeQuartz.withParams() using app CSS custom properties
6. Backend returns primary contact data per row (no N+1)
7. Loading state shows shimmer skeletons, empty state shows EmptyState component
8. TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/56-pipeline-grid/56-02-SUMMARY.md`
</output>
