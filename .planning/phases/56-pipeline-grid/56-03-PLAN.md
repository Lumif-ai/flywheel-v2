---
phase: 56-pipeline-grid
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - frontend/src/features/pipeline/components/PipelineFilterBar.tsx
  - frontend/src/features/pipeline/components/PipelineViewTabs.tsx
  - frontend/src/features/pipeline/components/GraduationModal.tsx
  - frontend/src/features/pipeline/components/PipelinePage.tsx
  - frontend/src/features/pipeline/hooks/useGraduate.ts
  - frontend/src/features/pipeline/api.ts
  - backend/src/flywheel/api/outreach.py
autonomous: true

must_haves:
  truths:
    - "Fit Tier multi-select and Outreach Status multi-select narrow grid rows in real time"
    - "Text search filters grid within 300ms debounce"
    - "Saved view tabs (All, Strong Fit, Needs Follow-up, Stale) filter the grid with URL persistence"
    - "Stale rows (>14 days) render with warm tint background without filter interaction"
    - "Replied rows float to top with coral accent without filter interaction"
    - "Page size selector offers 25/50/100 options"
    - "Graduate button opens type-selection modal with Customer/Advisor/Investor checkboxes"
    - "Submitting graduation modal calls POST /relationships/{id}/graduate (not old /accounts/ endpoint)"
    - "Graduated row slides out with animation and sidebar badge count increments"
    - "Backend pipeline endpoint filters by fit_tier, outreach_status, and search text"
  artifacts:
    - path: "frontend/src/features/pipeline/components/PipelineFilterBar.tsx"
      provides: "Filter bar with multi-select dropdowns and search input"
      exports: ["PipelineFilterBar"]
    - path: "frontend/src/features/pipeline/components/PipelineViewTabs.tsx"
      provides: "Saved view tabs with URL query param persistence"
      exports: ["PipelineViewTabs"]
    - path: "frontend/src/features/pipeline/components/GraduationModal.tsx"
      provides: "Type-selection dialog for graduation flow"
      exports: ["GraduationModal"]
    - path: "frontend/src/features/pipeline/hooks/useGraduate.ts"
      provides: "Mutation hook calling POST /relationships/{id}/graduate"
      contains: "relationships"
    - path: "frontend/src/features/pipeline/api.ts"
      provides: "Updated graduateAccount calling relationships endpoint"
      contains: "/relationships/"
    - path: "backend/src/flywheel/api/outreach.py"
      provides: "Pipeline endpoint with fit_tier, outreach_status, search Query params"
      contains: "fit_tier"
  key_links:
    - from: "frontend/src/features/pipeline/hooks/useGraduate.ts"
      to: "POST /relationships/{id}/graduate"
      via: "api.post call"
      pattern: "relationships.*graduate"
    - from: "frontend/src/features/pipeline/components/PipelineViewTabs.tsx"
      to: "URL query params"
      via: "useSearchParams"
      pattern: "searchParams.*view"
    - from: "frontend/src/features/pipeline/components/PipelineFilterBar.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipeline.ts"
      via: "filter state passed as params"
      pattern: "fit_tier|outreach_status|search"
    - from: "frontend/src/features/pipeline/components/GraduationModal.tsx"
      to: "frontend/src/features/pipeline/hooks/useGraduate.ts"
      via: "graduate.mutate with types array"
      pattern: "mutate.*types"
---

<objective>
Add filter bar with multi-select and search, saved view tabs, pagination controls, stale/reply row styling, and the graduation modal with slide-out animation.

Purpose: GRID-02 through GRID-05 complete the pipeline's interactive features — filtering, saved views, visual signals for stale/replied rows, and the graduation flow that bridges Pipeline to Relationship surfaces.
Output: Fully interactive pipeline with filters, tabs, row styling, page size control, and working graduation flow using the correct API endpoint.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-pipeline-grid/56-RESEARCH.md
@.planning/phases/56-pipeline-grid/56-02-SUMMARY.md
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/hooks/useGraduate.ts
@frontend/src/features/pipeline/api.ts
@backend/src/flywheel/api/outreach.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend filter support + graduation endpoint fix + filter bar + view tabs</name>
  <files>backend/src/flywheel/api/outreach.py, frontend/src/features/pipeline/components/PipelineFilterBar.tsx, frontend/src/features/pipeline/components/PipelineViewTabs.tsx, frontend/src/features/pipeline/api.ts, frontend/src/features/pipeline/hooks/useGraduate.ts</files>
  <action>
  **Backend — add filter params to GET /pipeline/ in outreach.py:**

  Add three new Query parameters to `get_pipeline()`:
  ```python
  fit_tier: str | None = Query(None),
  outreach_status: str | None = Query(None),
  search: str | None = Query(None),
  ```

  After the existing `.where(Account.status == "prospect")`, add:
  ```python
  if fit_tier is not None:
      stmt = stmt.where(Account.fit_tier == fit_tier)
  if outreach_status is not None:
      stmt = stmt.where(last_status_sq.c.last_status == outreach_status)
  if search is not None:
      pattern = f"%{search}%"
      stmt = stmt.where(
          Account.name.ilike(pattern) | Account.domain.ilike(pattern)
      )
  ```

  IMPORTANT: Also apply the same filters to the count query. The current count query is just `select(func.count()).select_from(Account).where(Account.status == "prospect")`. It must be updated to apply the same fit_tier, outreach_status, and search filters, otherwise pagination counts will be wrong. For outreach_status filter on count, LEFT JOIN the `last_status_sq` subquery into the count query as well.

  **Frontend — api.ts:**

  Update `graduateAccount` to call the relationships endpoint:
  ```typescript
  export interface GraduatePayload {
    id: string
    types: string[]
    entity_level?: string
  }

  export function graduateAccount(payload: GraduatePayload): Promise<unknown> {
    return api.post(`/relationships/${payload.id}/graduate`, {
      types: payload.types,
      entity_level: payload.entity_level,
    })
  }
  ```

  Add `search` to the PipelineParams interface if not already there.

  **Frontend — useGraduate.ts:**

  Update to accept `GraduatePayload` instead of a string. Import it from api.ts. On success, invalidate `['pipeline']`, `['relationships']`, `['signals']`, and `['accounts']` query keys. Toast: "Account graduated successfully".

  **Frontend — PipelineFilterBar.tsx:**

  Create a filter bar component that receives and controls filter state. Props:
  ```typescript
  interface PipelineFilterBarProps {
    fitTier: string | undefined
    onFitTierChange: (value: string | undefined) => void
    outreachStatus: string | undefined
    onOutreachStatusChange: (value: string | undefined) => void
    search: string
    onSearchChange: (value: string) => void
  }
  ```

  Render:
  - Text search Input (left, flex-1, max-w-xs) with Search icon, placeholder "Search companies..."
  - Fit Tier dropdown: use a `<select>` styled to match design tokens (border-radius 8px, subtle-border, card-bg background). Options: All Tiers, Excellent, Good, Fair, Poor.
  - Outreach Status dropdown: same style. Options: All Statuses, Sent, Opened, Replied, Bounced.
  - All elements in a flex row with gap-3.

  Apply 300ms debounce on the search input using the existing `useEffect` + `setTimeout` pattern from AccountsPage.tsx.

  **Frontend — PipelineViewTabs.tsx:**

  Create saved view tabs. Props:
  ```typescript
  interface PipelineViewTabsProps {
    activeView: string
    onViewChange: (view: string) => void
    staleCounts?: { all: number; strongFit: number; needsFollowUp: number; stale: number }
  }
  ```

  Tabs: All, Strong Fit, Needs Follow-up, Stale. Use URL query params for persistence:
  ```typescript
  import { useSearchParams } from 'react-router'
  ```

  Style: horizontal pill tabs. Active tab has coral underline (2px border-bottom with brand-coral). Inactive tabs have secondary-text color. 150ms transition on color and border.

  Tab filter logic (applied in PipelinePage):
  - "All": no additional filter
  - "Strong Fit": `fitTier = 'Excellent'` (or 'Strong' depending on data — use the Excellent tier value)
  - "Needs Follow-up": `outreachStatus = 'sent'` (sent but not replied)
  - "Stale": client-side filter — show only rows where `days_since_last_outreach > 14`

  Note: "Stale" tab applies client-side filtering on the already-loaded data since the staleness field is already present in each row. The other tabs set server-side filter params.
  </action>
  <verify>Run `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.outreach import router; print('OK')"`. Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit`. Verify backend filters: `grep "fit_tier" backend/src/flywheel/api/outreach.py`. Verify graduation endpoint: `grep "relationships" frontend/src/features/pipeline/api.ts`.</verify>
  <done>Backend pipeline endpoint accepts fit_tier, outreach_status, search params with correct count queries. Frontend filter bar renders multi-selects and debounced search. View tabs persist in URL. useGraduate calls POST /relationships/{id}/graduate with types array and invalidates pipeline+relationships+signals.</done>
</task>

<task type="auto">
  <name>Task 2: Stale/reply row styling, pagination, graduation modal, wire into PipelinePage</name>
  <files>frontend/src/features/pipeline/components/GraduationModal.tsx, frontend/src/features/pipeline/components/PipelinePage.tsx</files>
  <action>
  **GraduationModal.tsx:**

  Create a modal using the existing Dialog component from `@/components/ui/dialog`. Props:
  ```typescript
  interface GraduationModalProps {
    accountId: string
    accountName: string
    open: boolean
    onClose: () => void
  }
  ```

  Content:
  - Title: "Graduate {accountName}"
  - Subtitle: "Select relationship type(s) for this account"
  - Three checkboxes: Customer, Advisor, Investor (NOT Prospect — graduating out of prospect state)
  - Each checkbox styled as a card-like selectable option: rounded-lg border, padding 12px 16px, flex row with checkbox + label + description. Selected state: brand-light background, brand-coral border.
    - Customer: "A paying or potential paying client"
    - Advisor: "Someone who provides guidance and expertise"
    - Investor: "A financial backer or funding source"
  - Entity level auto-detection: if only "Advisor" or "Investor" is selected, set `entity_level: 'person'`. Otherwise default to `'company'`.
  - "Graduate" button (primary, disabled when no types selected or mutation pending). Shows Loader2 spinner when pending.
  - On success: close modal, the row will be removed by query invalidation.

  Use the `useGraduate` hook (updated in Task 1) to call `graduate.mutate({ id: accountId, types: selected })`.

  **Row slide-out animation:** After successful graduation, before closing the modal, add the `animate-slide-out-right` class to the graduated row. Implementation: in useGraduate onSuccess, use the AG Grid API to get the row node and add a CSS class, then after 300ms (matching the animation duration), invalidate queries. Use `gridApiRef` passed via a React context or callback:

  Simpler approach — in PipelinePage, track `graduatingId` state. When graduation succeeds, set `graduatingId` to the account ID. Use AG Grid's `getRowStyle` to apply the slide-out animation class to that row. After 300ms timeout, clear `graduatingId` and let the query invalidation remove the row.

  **PipelinePage.tsx — wire everything together:**

  1. Add `PipelineFilterBar` above the grid
  2. Add `PipelineViewTabs` above PipelineFilterBar
  3. Add `GraduationModal` (controlled by `graduatingAccount` state: `{ id: string, name: string } | null`)

  **Stale row styling:** In `getRowStyle` prop on AgGridReact:
  - If `days_since_last_outreach > 14`: `{ background: 'var(--brand-tint-warmest)' }` (warm tint)
  - If `graduatingId === data.id`: `{ animation: 'slide-out-right 300ms ease-out forwards' }`

  **Reply float-to-top:** Use AG Grid's `postSortRows` prop to push rows with `last_outreach_status === 'replied'` to the top. Apply a subtle coral left border to these rows: in `getRowStyle`, if status is 'replied', add `{ borderLeft: '3px solid var(--brand-coral)' }`.

  **Pagination with page size selector:**
  Below the grid, add a footer with:
  - Left: "Showing X-Y of Z" text
  - Center: Page size selector (select with options 25, 50, 100)
  - Right: Previous / Page N of M / Next buttons

  Store `pageSize` in state (default 50). Pass to `usePipeline` as `limit`. The grid does NOT use AG Grid's built-in pagination (we control it via API params).

  **Graduate button in grid:**
  The existing "Graduate" column cell renderer should open the graduation modal instead of directly calling the API. Update the column's cellRenderer to call `setGraduatingAccount({ id: data.id, name: data.name })` instead of `graduate.mutate(accountId)`. Pass this setter to the cell renderer via `context` prop on AgGridReact:
  ```typescript
  <AgGridReact context={{ onGraduate: (id: string, name: string) => setGraduatingAccount({ id, name }) }} />
  ```
  The GraduateButton cell renderer reads from `props.context.onGraduate`.

  **View tab logic in PipelinePage:**
  - Use `useSearchParams` to read/write `?view=xxx`
  - When view changes, update filter state accordingly:
    - "all": clear fitTier and outreachStatus
    - "strong-fit": set fitTier='Excellent'
    - "needs-followup": set outreachStatus='sent'
    - "stale": no server filter change; apply client-side filter via AG Grid's `isExternalFilterPresent` + `doesExternalFilterPass` props (filter rows where `days_since_last_outreach > 14`)
  </action>
  <verify>Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit`. Verify modal exists: `grep "GraduationModal" frontend/src/features/pipeline/components/GraduationModal.tsx`. Verify view tabs: `grep "useSearchParams" frontend/src/features/pipeline/components/PipelinePage.tsx`. Verify stale styling: `grep "brand-tint-warmest" frontend/src/features/pipeline/components/PipelinePage.tsx`. Verify page size: `grep "pageSize" frontend/src/features/pipeline/components/PipelinePage.tsx`.</verify>
  <done>Stale rows show warm tint. Replied rows float to top with coral accent. Page size selector offers 25/50/100. Graduation modal opens with type selection, calls correct API, row slides out, sidebar badges update via query invalidation. View tabs persist in URL. All GRID-01 through GRID-05 requirements met.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep "fit_tier.*Query" backend/src/flywheel/api/outreach.py` matches (backend filter params)
- `grep "relationships" frontend/src/features/pipeline/api.ts` matches (correct graduate endpoint)
- `grep "useSearchParams" frontend/src/features/pipeline/components/PipelinePage.tsx` matches (URL persistence)
- `grep "brand-tint-warmest" frontend/src/features/pipeline/components/PipelinePage.tsx` matches (stale row tint)
- `grep "GraduationModal" frontend/src/features/pipeline/components/GraduationModal.tsx` matches
- `grep "pageSize" frontend/src/features/pipeline/components/PipelinePage.tsx` matches
- `ls frontend/src/features/pipeline/components/` shows PipelineFilterBar.tsx, PipelineViewTabs.tsx, GraduationModal.tsx
</verification>

<success_criteria>
1. Backend GET /pipeline/ accepts fit_tier, outreach_status, search params and applies them to both data and count queries
2. Filter bar renders multi-select dropdowns and debounced search input
3. Saved view tabs (All, Strong Fit, Needs Follow-up, Stale) persist active tab in URL ?view= param
4. Stale rows (>14 days) render with warm tint background
5. Replied rows float to top with coral left border accent
6. Page size selector offers 25/50/100 with correct pagination controls
7. Graduate button opens modal with Customer/Advisor/Investor checkboxes
8. Modal submits to POST /relationships/{id}/graduate with types array
9. Graduated row slides out with animation, pipeline+relationships+signals queries invalidated
10. TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/56-pipeline-grid/56-03-SUMMARY.md`
</output>
