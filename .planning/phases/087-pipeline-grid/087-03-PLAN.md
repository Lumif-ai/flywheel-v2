---
phase: 087-pipeline-grid
plan: 03
type: execute
wave: 3
depends_on: ["087-01", "087-02"]
files_modified:
  - frontend/src/features/pipeline/components/PipelineToolbar.tsx
  - frontend/src/features/pipeline/components/PipelineFilterBar.tsx
  - frontend/src/features/pipeline/components/PipelineViewTabs.tsx
  - frontend/src/features/pipeline/components/QuickAddRow.tsx
  - frontend/src/features/pipeline/components/PipelineSidePanel.tsx
  - frontend/src/features/pipeline/components/PipelinePage.tsx
autonomous: true

must_haves:
  truths:
    - "View tabs show All / Needs Action / Replied / Stale (not old All/Hot/Stale/Replied)"
    - "Filter bar has multi-select facets for stage, fit tier, relationship type, source"
    - "Search input in toolbar filters across name, domain, contact names"
    - "Toolbar has Grid/Board toggle with Board disabled"
    - "Quick-add row at bottom creates entries with dedup toast feedback"
    - "Side panel stub opens on Enter key / row click, closes on Escape"
  artifacts:
    - path: "frontend/src/features/pipeline/components/PipelineViewTabs.tsx"
      provides: "All / Needs Action / Replied / Stale tabs"
      contains: "needs_action"
    - path: "frontend/src/features/pipeline/components/PipelineToolbar.tsx"
      provides: "Toolbar with Grid/Board toggle, search"
      contains: "Board"
    - path: "frontend/src/features/pipeline/components/PipelineFilterBar.tsx"
      provides: "Multi-select facet filters"
      contains: "stage.*fit_tier.*relationship_type"
    - path: "frontend/src/features/pipeline/components/QuickAddRow.tsx"
      provides: "Quick-add input at grid bottom"
      contains: "onKeyDown.*Enter"
    - path: "frontend/src/features/pipeline/components/PipelineSidePanel.tsx"
      provides: "Minimal side panel stub"
      contains: "Phase 88"
  key_links:
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/components/PipelineViewTabs.tsx"
      via: "view tab selection callback"
      pattern: "PipelineViewTabs"
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipelineCreate.ts"
      via: "usePipelineCreate hook called in PipelinePage, result passed to QuickAddRow via onAdd prop"
      pattern: "usePipelineCreate"
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/components/PipelineSidePanel.tsx"
      via: "selectedItem state"
      pattern: "selectedItem.*PipelineSidePanel"
---

<objective>
Toolbar, filter bar, view tabs, quick-add row, and side panel stub surrounding the grid.

Purpose: The grid (Plan 02) shows data; this plan adds the controls for filtering, searching, switching views, quick-adding entries, and previewing entries via side panel. These are the interaction surfaces that make the grid usable.

Output: Complete surrounding UI wired into PipelinePage.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/087-pipeline-grid/087-RESEARCH.md
@.planning/phases/087-pipeline-grid/087-01-SUMMARY.md
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/components/PipelineFilterBar.tsx
@frontend/src/features/pipeline/components/PipelineViewTabs.tsx
@frontend/src/features/pipeline/components/PipelineSidePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolbar, view tabs, filter bar, and quick-add row</name>
  <files>
    frontend/src/features/pipeline/components/PipelineToolbar.tsx
    frontend/src/features/pipeline/components/PipelineFilterBar.tsx
    frontend/src/features/pipeline/components/PipelineViewTabs.tsx
    frontend/src/features/pipeline/components/QuickAddRow.tsx
  </files>
  <action>
**Rewrite `PipelineViewTabs.tsx`** (GRID-05):
- Export `ViewTab` type (or import from types/pipeline.ts if already exported there)
- Tabs: `{ id: 'all', label: 'All' }`, `{ id: 'needs_action', label: 'Needs Action' }`, `{ id: 'replied', label: 'Replied' }`, `{ id: 'stale', label: 'Stale' }`
- Props: `activeView: ViewTab`, `onViewChange: (view: ViewTab) => void`
- Render as horizontal inline tabs (same style as current implementation): each tab is a button, active tab has border-bottom 2px #E94D35 + color #121212, inactive tabs color #9CA3AF hover #6B7280
- Font: 13px weight 500
- No "Hot" tab. These are the v9.0 tabs per GRID-05.

**Create `PipelineToolbar.tsx`** (GRID-08, GRID-13):
- Props: `search: string`, `onSearchChange: (v: string) => void`, `onToggleFilters: () => void`, `filtersVisible: boolean`
- Layout: horizontal bar between view tabs and grid
- Left side: Search input with magnifying glass icon (Search from lucide-react). Style: border 1px #E5E7EB, borderRadius 8px, padding 6px 10px 6px 32px (icon inside), width 280px, fontSize 13px, placeholder "Search pipeline..."
- Right side: 
  - Filter button (Sliders icon from lucide-react) — toggles filter bar visibility. Show active state (accent color bg) when filtersVisible is true.
  - Sort button (ArrowUpDown icon) — placeholder, no-op for now (sorting is via column headers)
  - Grid/Board toggle: two icon buttons (LayoutGrid + Kanban icons). Grid active by default (accent border). Board button has `disabled` attribute and `title="Board view coming soon"` per GRID-13.
- All buttons: 32px height, borderRadius 6px, border 1px #E5E7EB, hover bg #F3F4F6

**Rewrite `PipelineFilterBar.tsx`** (GRID-04):
- Props: `stage: string[]`, `onStageChange`, `fitTier: string[]`, `onFitTierChange`, `relationshipType: string[]`, `onRelationshipTypeChange`, `source: string`, `onSourceChange`, `onClear: () => void`
- Render as a horizontal bar with multi-select dropdown filters:
  - Stage: multi-select with values ['identified', 'contacted', 'engaged', 'qualified', 'committed', 'closed'], labels capitalized
  - Fit Tier: multi-select with values ['Strong', 'Medium', 'Weak']
  - Relationship Type: multi-select with values ['prospect', 'customer', 'partner', 'investor', 'advisor']
  - Source: single-select with values ['manual', 'meeting', 'email', 'gtm_scrape']
- Each filter: button showing filter name + selected count badge. On click, show a dropdown with checkboxes. Use a simple local state for open/close. Style: button with border 1px #E5E7EB, borderRadius 6px, padding 4px 10px, fontSize 12px. Active filter (has selections): bg #FEF2F0, border-color #E94D35, text #E94D35.
- Clear all button at end: "Clear" text button in #E94D35 when any filters active, hidden when no filters
- Keep it simple — don't import external dropdown libraries. Use a div positioned absolutely below the button with checkboxes.

**Create `QuickAddRow.tsx`** (GRID-11):
- Props: `onAdd: (name: string) => void`
- Simple input below the grid: border-top 1px #F3F4F6, padding 8px 12px
- Input: fontSize 13px, color #121212, full width, no border, placeholder "+ Add company or person..."
- On Enter with non-empty value: call `onAdd(value.trim())`, clear input
- On Escape: clear input and blur
- Follow the exact pattern from research QuickAddRow example
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify all 4 component files exist and export their components. Verify PipelineViewTabs uses 'needs_action' not 'hot'.
  </verify>
  <done>
View tabs show All/Needs Action/Replied/Stale. Toolbar has search input and Grid/Board toggle (Board disabled). Filter bar has multi-select facets for stage, fit_tier, relationship_type, source with clear button. QuickAddRow captures Enter to create entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Side panel stub and PipelinePage wiring</name>
  <files>
    frontend/src/features/pipeline/components/PipelineSidePanel.tsx
    frontend/src/features/pipeline/components/PipelinePage.tsx
  </files>
  <action>
**Rewrite `PipelineSidePanel.tsx`** (GRID-09): Minimal stub for Phase 87.
- Props: `item: PipelineListItem`, `onClose: () => void`
- Fixed position right panel: width 400px, height 100vh, right 0, top 0, z-index 40, bg white, border-left 1px #E5E7EB, box-shadow -4px 0 12px rgba(0,0,0,0.05)
- Slide-in animation: CSS transition transform 200ms ease-out (translateX(100%) -> translateX(0))
- Header: item.name (font 16px weight 600), entity_type badge (company/person), close button (X icon from lucide-react) at top-right
- Below header: stage as StagePill, fit_tier as FitTierBadge
- Domain if available: text with external link icon
- Primary contact if available: name + email
- Footer message: "Full profile view coming in Phase 88" in italic gray text (#9CA3AF, fontSize 12px)
- This is intentionally minimal — Phase 88 builds the full panel with tabs, timeline, contacts list, etc.

**Update `PipelinePage.tsx`** — Wire in all Plan 03 components.

Read the current PipelinePage (as rebuilt by Plan 02) and add:

1. **Import and wire PipelineViewTabs**: Replace placeholder with `<PipelineViewTabs activeView={activeView} onViewChange={handleViewChange} />` in the header area. `handleViewChange` sets activeView, resets page to 0, and clears conflicting filters.

2. **Import and wire PipelineToolbar**: Add between view tabs and grid. Wire search/onSearchChange props to the search state. Wire onToggleFilters to a `showFilters: boolean` state.

3. **Import and wire PipelineFilterBar**: Show conditionally when showFilters is true. Wire all filter state props (stage/fitTier/relationshipType/source) and their setters. Wire onClear to reset all filter state.

4. **Import and wire QuickAddRow**: Add below the grid div, above pagination. Use `usePipelineCreate` hook: `const createMutation = usePipelineCreate()`. Wire onAdd to `createMutation.mutate({ name, entity_type: 'company' })`. Note: usePipelineCreate is called in PipelinePage, NOT in QuickAddRow — QuickAddRow only receives an `onAdd` callback prop.

5. **Import and wire PipelineSidePanel**: Show when `selectedItem` is not null. Render with backdrop overlay (same pattern as current file: fixed inset-0 z-30, bg rgba(0,0,0,0.1), onClick closes). Wire onClose to `setSelectedItem(null)`.

6. **Update URL params**: Sync `activeView` and `search` to URL search params using `useSearchParams`. On mount, read `view` and `q` from URL. On change, update URL (replace, not push).

Ensure the filter state flows correctly:
- activeView === 'all' -> no view param to API
- activeView === 'needs_action' | 'replied' | 'stale' -> pass view param
- Manual filter selections (stage, fitTier, etc.) go as separate params alongside view
- Changing view tab resets manual filters to avoid conflicting states
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify PipelinePage imports PipelineToolbar, PipelineFilterBar, PipelineViewTabs, QuickAddRow, PipelineSidePanel. Verify no old imports remain (GraduationModal, useGraduate, fetchPipelineIndustries).
  </verify>
  <done>
PipelinePage wires all surrounding components: view tabs change activeView and pass view param to API, toolbar shows search and filter toggle, filter bar shows multi-select facets, quick-add creates entries with dedup feedback, side panel stub opens on selectedItem and closes on Escape/backdrop click. URL params sync view and search state.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -r "needs_action" frontend/src/features/pipeline/components/PipelineViewTabs.tsx` returns hits
- `grep -r "Board view coming soon" frontend/src/features/pipeline/components/PipelineToolbar.tsx` returns hits
- PipelinePage imports all 5 surrounding components
- No references to 'hot' view tab, GraduationModal, or graduation anywhere in pipeline feature
</verification>

<success_criteria>
Complete pipeline page at /pipeline with view tabs (All/Needs Action/Replied/Stale), search toolbar with Grid/Board toggle, multi-select filter bar, quick-add row with dedup feedback, and minimal side panel stub. All wired into PipelinePage with URL param sync.
</success_criteria>

<output>
After completion, create `.planning/phases/087-pipeline-grid/087-03-SUMMARY.md`
</output>
