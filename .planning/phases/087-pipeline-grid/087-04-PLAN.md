---
phase: 087-pipeline-grid
plan: 04
type: execute
wave: 4
depends_on: ["087-02", "087-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can see unified pipeline grid at /pipeline"
    - "User can edit stage and fit tier inline"
    - "User can edit next action date inline via date picker"
    - "User can filter by view tabs and facets"
    - "User can search across pipeline entries"
    - "User can quick-add an entry with dedup feedback"
    - "Keyboard Enter opens side panel, Escape closes it"
    - "Row numbers display on the left of each row (GRID-01)"
    - "Arrow keys navigate between cells (GRID-09)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete pipeline grid.

Purpose: Plans 01-03 built the full pipeline grid feature. This checkpoint verifies the integrated result works end-to-end before marking the phase complete.

Output: Confirmed working pipeline grid or list of issues to fix.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/087-pipeline-grid/087-01-SUMMARY.md
@.planning/phases/087-pipeline-grid/087-02-SUMMARY.md
@.planning/phases/087-pipeline-grid/087-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and import check</name>
  <files>N/A — verification only, no files created or modified</files>
  <action>
Run the frontend build to ensure no compile errors:
```
cd frontend && npm run build 2>&1 | tail -20
```

Then verify no broken imports or missing references:
```
cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -30
```

If any errors, fix them before proceeding to the visual checkpoint.
  </action>
  <verify>
`npm run build` succeeds with exit code 0. `npx tsc --noEmit` succeeds with no errors.
  </verify>
  <done>Frontend builds cleanly with all pipeline grid components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete pipeline grid</name>
  <files>N/A — human verification only</files>
  <action>
Present the verification checklist to the user and wait for approval. The user will test the following at http://localhost:5173/pipeline:
- Row numbers visible on the left of each row (GRID-01)
- Grid display with all 10 data columns (GRID-01, GRID-02)
- Entity type visual distinction (GRID-12)
- Inline editing for stage and fit tier via dropdowns (GRID-03, GRID-10)
- Inline editing for next action date via date picker (GRID-10)
- View tabs: All, Needs Action, Replied, Stale (GRID-05)
- Filter bar with multi-select facets (GRID-04)
- Search across pipeline entries (GRID-08)
- Column sorting via header click (GRID-06)
- Pagination with 25/50/100 options (GRID-07)
- Quick-add row with dedup feedback (GRID-11)
- Keyboard navigation: Arrow keys move between cells, Enter opens panel, Escape closes (GRID-09)
- Toolbar with Grid/Board toggle, Board disabled (GRID-13)
  </action>
  <verify>User confirms all GRID requirements work correctly.</verify>
  <done>User types "approved" confirming the pipeline grid is working end-to-end.</done>
</task>

</tasks>

<verification>
Frontend builds without errors. All GRID requirements verified visually.
</verification>

<success_criteria>
User confirms the pipeline grid works end-to-end: row numbers visible, displays all columns, inline editing saves (stage, fit_tier, next_action_date), view tabs filter correctly, search works, pagination works, quick-add creates entries, arrow keys navigate cells, keyboard Enter/Escape opens/closes side panel.
</success_criteria>

<output>
After completion, create `.planning/phases/087-pipeline-grid/087-04-SUMMARY.md`
</output>
