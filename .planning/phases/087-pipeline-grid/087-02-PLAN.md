---
phase: 087-pipeline-grid
plan: 02
type: execute
wave: 2
depends_on: ["087-01"]
files_modified:
  - frontend/src/features/pipeline/components/cell-renderers/NameCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/StagePill.tsx
  - frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx
  - frontend/src/features/pipeline/components/cell-renderers/ChannelsCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/AiInsightCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/DateCell.tsx
  - frontend/src/features/pipeline/components/cell-renderers/DatePickerEditor.tsx
  - frontend/src/features/pipeline/hooks/usePipelineColumns.ts
  - frontend/src/features/pipeline/components/PipelinePage.tsx
autonomous: true

must_haves:
  truths:
    - "Grid displays pinned row number column plus 10 data columns: Name, Domain, Primary Contact, Stage, Fit, Channels, Next Outreach, AI Insight, Next Action, Last Active (GRID-01)"
    - "Stage column is editable inline via dropdown with 6 stage values"
    - "Fit tier column is editable inline via dropdown with Strong/Medium/Weak values"
    - "Next Action column is editable inline via date picker for next_action_date (GRID-10)"
    - "Person entries show circular avatar, company entries show square avatar"
    - "Stage shows as colored pill, fit tier as colored badge"
    - "Grid has server-side pagination with 25/50/100 options"
    - "Column sorting triggers server-side sort via API"
    - "Column reordering and resizing persist to localStorage"
    - "Arrow key navigation works across cells without suppressKeyboardEvent blocking it (GRID-09)"
  artifacts:
    - path: "frontend/src/features/pipeline/components/cell-renderers/NameCell.tsx"
      provides: "Name cell with entity-type-aware avatar shape"
      contains: "entity_type"
    - path: "frontend/src/features/pipeline/components/cell-renderers/StagePill.tsx"
      provides: "Colored stage pill renderer"
      contains: "identified.*contacted.*engaged"
    - path: "frontend/src/features/pipeline/components/cell-renderers/DatePickerEditor.tsx"
      provides: "Custom AG Grid cell editor with native date input for next_action_date"
      contains: "ICellEditorParams"
    - path: "frontend/src/features/pipeline/hooks/usePipelineColumns.ts"
      provides: "Column definitions with row number column and inline editors including DatePickerEditor"
      contains: "DatePickerEditor"
    - path: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      provides: "Main grid page with AG Grid, pagination, sort sync"
      contains: "AgGridReact"
  key_links:
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipeline.ts"
      via: "usePipeline hook call"
      pattern: "usePipeline\\("
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipelineMutation.ts"
      via: "onCellValueChanged handler"
      pattern: "usePipelineMutation\\("
    - from: "frontend/src/features/pipeline/hooks/usePipelineColumns.ts"
      to: "frontend/src/features/pipeline/components/cell-renderers/"
      via: "cellRenderer and cellEditor imports"
      pattern: "import.*from.*cell-renderers"
---

<objective>
Cell renderers, column definitions (including DatePickerEditor for GRID-10), and the main PipelinePage grid rebuild.

Purpose: This is the core visual grid -- the AG Grid with all 10 columns, cell renderers for rich display, inline editing via select editors and date picker, pagination, and sort sync. This is what the user sees when they open /pipeline.

Output: Working AG Grid showing unified pipeline entries with inline editing (stage, fit_tier, next_action_date) and pagination.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/087-pipeline-grid/087-RESEARCH.md
@.planning/phases/087-pipeline-grid/087-01-SUMMARY.md
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/hooks/usePipelineColumns.ts
@frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx
@frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cell renderers and DatePickerEditor for all grid columns</name>
  <files>
    frontend/src/features/pipeline/components/cell-renderers/NameCell.tsx
    frontend/src/features/pipeline/components/cell-renderers/StagePill.tsx
    frontend/src/features/pipeline/components/cell-renderers/FitTierBadge.tsx
    frontend/src/features/pipeline/components/cell-renderers/ChannelsCell.tsx
    frontend/src/features/pipeline/components/cell-renderers/AiInsightCell.tsx
    frontend/src/features/pipeline/components/cell-renderers/DateCell.tsx
    frontend/src/features/pipeline/components/cell-renderers/ContactCell.tsx
    frontend/src/features/pipeline/components/cell-renderers/DatePickerEditor.tsx
  </files>
  <action>
Create/rewrite 7 cell renderers plus 1 custom cell editor. All renderers use `ICellRendererParams` from `ag-grid-community`. All import `PipelineListItem` from `../../types/pipeline`.

**Delete old renderers** that are no longer needed: `CompanyCell.tsx`, `DaysSinceCell.tsx`, `GraduateButton.tsx`, `OutreachDot.tsx`, `OutreachSnippet.tsx`, `OutreachStatusCell.tsx`. These are all leads-centric and replaced.

**NameCell.tsx** (GRID-01, GRID-12): Entity-type-aware name with avatar.
- Person entries (`entity_type === 'person'`): circular avatar (borderRadius: 50%), no domain shown
- Company entries: square avatar (borderRadius: 6px), domain shown below name in smaller gray text
- Avatar: 28x28px, bg #F3F4F6, shows first letter uppercase, font 11px weight 600 color #6B7280
- Name: font 13px weight 600 color #121212
- Domain: font 11px color #9CA3AF
- Layout: flex row with gap-2, items centered, min-w-0 on text container for truncation
- Follow the exact pattern from research code example for NameCell

**StagePill.tsx** (GRID-02, GRID-03): Colored pill for stage values.
- Stage values: identified, contacted, engaged, qualified, committed, closed
- Color map (bg/text pairs): 
  - identified: bg #F3F4F6, text #6B7280
  - contacted: bg #DBEAFE, text #2563EB
  - engaged: bg #FEF3C7, text #D97706
  - qualified: bg #D1FAE5, text #059669
  - committed: bg #EDE9FE, text #7C3AED
  - closed: bg #E94D35 at 10% opacity, text #E94D35
- Pill style: px-2 py-0.5, borderRadius 9999px, fontSize 11px, fontWeight 600, text-transform capitalize
- Display the stage value with first letter capitalized

**FitTierBadge.tsx** (GRID-02): Update existing badge for Strong/Medium/Weak.
- Rewrite to use new tier values: Strong, Medium, Weak
- Color map: Strong -> bg #D1FAE5 text #059669, Medium -> bg #FEF3C7 text #D97706, Weak -> bg #FEE2E2 text #DC2626
- Same pill style as StagePill but slightly smaller (fontSize 11px)
- If fit_tier is null, render a dash in gray

**ContactCell.tsx**: Rewrite for unified schema.
- Display `primary_contact.name` if available, otherwise dash
- Below name: show `primary_contact.title` in 11px #9CA3AF if available
- If no primary_contact, show `contact_count` contacts as "{N} contacts" in gray, or dash if 0

**ChannelsCell.tsx** (GRID-02): Channel icons with count.
- Import icons from lucide-react: Mail, Linkedin, Phone, Globe, MessageSquare
- Map channel strings to icons: 'email' -> Mail, 'linkedin' -> Linkedin, 'phone' -> Phone, 'web' -> Globe, 'chat' -> MessageSquare
- Show first 3 channel icons (size 14px, color #6B7280) in a row with gap-1
- If more than 3, show "+N" badge after icons
- If no channels, show dash

**AiInsightCell.tsx** (GRID-02): Truncated AI summary text.
- Show `ai_summary` truncated to single line with ellipsis (text-overflow: ellipsis, white-space: nowrap, overflow: hidden)
- Font: 12px, color #6B7280
- If null, show dash in lighter gray (#D1D5DB)
- Max width from column definition (not hardcoded here)

**DateCell.tsx** (reusable): Relative time display.
- Import `formatDistanceToNow` from `date-fns`
- Show relative time like "2d ago", "1w ago" for dates within 30 days
- Show "Mon DD" format for dates older than 30 days (use `format` from date-fns)
- If null value, show dash
- Font: 12px, color #6B7280
- This renderer is reused for "Last Active" and "Next Outreach" columns (both display dates)

**DatePickerEditor.tsx** (GRID-10): Custom AG Grid cell editor for next_action_date.
- Implement as a custom AG Grid cell editor using `ICellEditorParams` from `ag-grid-community`
- Use `forwardRef` with `useImperativeHandle` to expose `getValue()` and `isPopup()` (return true for popup behavior)
- Render a native `<input type="date">` styled to match the grid theme: border 1px #E5E7EB, borderRadius 6px, padding 4px 8px, fontSize 13px
- On mount (`useEffect`), auto-focus the input and open the native date picker via `input.showPicker()` (wrapped in try/catch for browser compat)
- getValue() returns the input value as ISO date string (YYYY-MM-DD) or null if empty
- The input should be pre-filled with the current cell value if it exists
- Include a small "clear" button (X icon, 16px) next to the input that sets value to null
- Overall container: flex row, gap-1, bg white, padding 4px, border 1px #E5E7EB, borderRadius 8px, box-shadow 0 2px 8px rgba(0,0,0,0.1)
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit --pretty 2>&1 | head -30` to check for type errors. Verify old renderers are deleted: `ls frontend/src/features/pipeline/components/cell-renderers/` should show NameCell, StagePill, FitTierBadge, ChannelsCell, AiInsightCell, DateCell, ContactCell, DatePickerEditor (8 files).
  </verify>
  <done>
7 cell renderers plus DatePickerEditor exist, all typed against PipelineListItem/AG Grid interfaces, old leads-centric renderers deleted. NameCell distinguishes person (circle) vs company (square). StagePill shows colored pills for 6 stages. FitTierBadge uses Strong/Medium/Weak. ChannelsCell shows icons. AiInsightCell truncates summary. DateCell shows relative time. DatePickerEditor provides inline date input for next_action_date.
  </done>
</task>

<task type="auto">
  <name>Task 2: Column definitions hook and PipelinePage grid rebuild</name>
  <files>
    frontend/src/features/pipeline/hooks/usePipelineColumns.ts
    frontend/src/features/pipeline/components/PipelinePage.tsx
  </files>
  <action>
**Rewrite `usePipelineColumns.ts`** — Complete rewrite with a row number column plus 10 data columns:

Column definitions array (preserve existing localStorage persistence pattern for column state):

0. **Row #** (GRID-01) — NO field (use valueGetter), headerName: '' (empty header), width: 48, pinned: 'left', sortable: false, resizable: false, suppressMovable: true, lockPosition: 'left', cellStyle: { color: '#9CA3AF', fontSize: '11px', textAlign: 'center' }. valueGetter: `(params) => params.node ? params.node.rowIndex + 1 : ''`. This shows 1-based row numbers. Note: for paginated grids, if you want global row numbers across pages, use `params.node.rowIndex + offset + 1` where offset comes from the current page offset passed via context. For now, use simple `rowIndex + 1` (page-local numbering is acceptable).
1. **Name** — field: 'name', width: 200, pinned: 'left', cellRenderer: NameCell, NOT editable, sortable
2. **Domain** — field: 'domain', width: 140, sortable (plain text, no renderer needed)
3. **Primary Contact** — field: 'primary_contact', width: 160, cellRenderer: ContactCell, NOT editable, NOT sortable (nested object)
4. **Stage** — field: 'stage', width: 130, cellRenderer: StagePill, editable: true, cellEditor: 'agSelectCellEditor', cellEditorParams: { values: ['identified', 'contacted', 'engaged', 'qualified', 'committed', 'closed'] }
5. **Fit** — field: 'fit_tier', width: 100, cellRenderer: FitTierBadge, editable: true, cellEditor: 'agSelectCellEditor', cellEditorParams: { values: ['Strong', 'Medium', 'Weak'] }
6. **Channels** — field: 'channels', width: 120, cellRenderer: ChannelsCell, NOT editable, NOT sortable
7. **Next Outreach** — field: 'last_activity_at', headerName: 'Next Outreach', width: 120, cellRenderer: DateCell. This displays the last activity as a proxy for outreach timing (no separate next_outreach field exists). NOT editable.
8. **AI Insight** — field: 'ai_summary', headerName: 'AI Insight', width: 200, cellRenderer: AiInsightCell, NOT editable, NOT sortable
9. **Next Action** — field: 'next_action_date', headerName: 'Next Action', width: 150, cellRenderer: DateCell (shows the date), editable: true, cellEditor: DatePickerEditor (the custom date picker editor from Task 1). Import DatePickerEditor from cell-renderers. When no date is set, the DateCell renderer shows a dash. Sorting enabled on this column.
10. **Last Active** — field: 'last_activity_at', headerName: 'Last Active', width: 110, cellRenderer: DateCell, sortable

Default column def: `{ resizable: true, sortable: true }` (override per column as needed above).

**IMPORTANT (GRID-09):** Do NOT set `suppressKeyboardEvent` globally on the default column def or on individual columns (unless specifically needed for an editor). AG Grid's built-in arrow key cell navigation works by default and must not be suppressed. Only the Enter and Escape keys need custom handling via `onCellKeyDown`.

Keep the existing pattern from the old hook: store column state in localStorage key `'pipeline-col-state'`, restore on mount via `initialState`, update on `onColumnResized`/`onColumnMoved`/`onColumnVisible` via `api.getColumnState()`. Return `{ columnDefs, initialState, onColumnStateChanged, gridApiRef }`.

**Rewrite `PipelinePage.tsx`** — Complete rebuild. Remove ALL graduation-related code, outreach-centric logic, old type references. Preserve the overall page structure (header, filter area, grid, pagination footer).

Core state:
- `activeView: ViewTab` from URL param `?view=` (default 'all')
- `search: string` from URL param `?q=`
- `page: number` (0-indexed)
- `pageSize: number` (default 50, options 25/50/100)
- `sortModel: { colId: string; sort: 'asc' | 'desc' } | null`
- `selectedItem: PipelineListItem | null` (for side panel)
- Filter state: `fitTier: string[]`, `stage: string[]`, `relationshipType: string[]`, `source: string`

Data fetching via `usePipeline({ offset: page * pageSize, limit: pageSize, search, sort_by: sortModel?.colId, sort_dir: sortModel?.sort, stage, fit_tier: fitTier, relationship_type: relationshipType, source, view: activeView === 'all' ? undefined : activeView })`.

Inline editing via `usePipelineMutation()`: Wire `onCellValueChanged` callback — when AG Grid fires this event, call `mutation.mutate({ id: data.id, field: colDef.field, value: newValue })`. Skip if `newValue === oldValue`. This handles stage (dropdown), fit_tier (dropdown), and next_action_date (date picker) edits uniformly — all go through the same PATCH mutation.

Sort sync: Wire `onSortChanged` callback — read `api.getColumnState()`, find column with sort, update `sortModel` state. This ensures sort params go to the API for server-side sorting. Use `sortingOrder: ['asc', 'desc', null]` on AG Grid to allow clearing sort.

Preserve the existing `pipelineTheme` from the current file (themeQuartz.withParams with the brand colors).

Page layout (preserve current structure):
- Header: "Pipeline" title + PipelineViewTabs inline (from Plan 03, render placeholder div for now)
- Filter bar area: PipelineFilterBar placeholder (from Plan 03, render placeholder div for now)  
- Grid: AgGridReact with modules={[AllCommunityModule]}, theme={pipelineTheme}, rowData/columnDefs/etc.
- Pagination footer: same pattern as current — "{start}-{end} of {total}" + page size select + prev/next buttons

For the view tabs and filter bar, import them if they exist, otherwise render simple placeholder divs that Plan 03 will replace. Use conditional imports or just leave TODO comments — Plan 03 will update PipelinePage to wire in the real components.

Remove: GraduationModal import/usage, graduatingAccount/graduatingId state, postSortRows hack, getRowStyle for graduation animation, fetchPipelineIndustries query, PipelineSidePanel import (Plan 03 rebuilds the stub).

Keyboard nav (GRID-09): Add `onCellKeyDown` handler that:
- On Enter (non-editable column): `setSelectedItem(e.data)` 
- On Escape: `setSelectedItem(null)`
This wires the open/close for the side panel stub that Plan 03 creates. Arrow key navigation between cells is handled natively by AG Grid — do NOT add suppressKeyboardEvent or navigateToNextCell overrides. Confirm arrow keys work during verify step.

Delete `GraduationModal.tsx` — graduation concept eliminated.
Delete `hooks/useGraduate.ts` — graduation concept eliminated.
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/frontend && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify no imports of GraduationModal, useGraduate, or old PipelineItem type remain. Check that `PipelinePage.tsx` imports `usePipeline`, `usePipelineMutation`, and `usePipelineColumns`. Check that `usePipelineColumns.ts` imports `DatePickerEditor`. Verify row number column exists: `grep -n "rowIndex" frontend/src/features/pipeline/hooks/usePipelineColumns.ts` should return a hit. Verify no global suppressKeyboardEvent: `grep -n "suppressKeyboardEvent" frontend/src/features/pipeline/hooks/usePipelineColumns.ts` should return NO hits (arrow keys must not be blocked).
  </verify>
  <done>
PipelinePage renders AG Grid with pinned row number column (GRID-01) plus 10 data columns matching GRID-02 spec. Row numbers show 1-based index, pinned left, 48px wide, no header. Stage and fit_tier are inline-editable via agSelectCellEditor. Next Action (next_action_date) is inline-editable via DatePickerEditor (GRID-10). Sort changes sync to API params for server-side sorting. Pagination works with 25/50/100 page sizes. Column state persists to localStorage. Keyboard Enter/Escape sets selectedItem for side panel. Arrow keys navigate between cells natively (GRID-09, no suppressKeyboardEvent set). Old graduation code fully removed.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `GraduationModal.tsx` and `useGraduate.ts` deleted
- Old cell renderers (CompanyCell, DaysSinceCell, GraduateButton, OutreachDot, OutreachSnippet, OutreachStatusCell) deleted
- `grep -r "agSelectCellEditor" frontend/src/features/pipeline/` returns hits in usePipelineColumns
- `grep -r "DatePickerEditor" frontend/src/features/pipeline/` returns hits in usePipelineColumns and cell-renderers
- `grep -r "rowIndex" frontend/src/features/pipeline/hooks/usePipelineColumns.ts` returns hit (row number column)
- `grep -r "suppressKeyboardEvent" frontend/src/features/pipeline/hooks/usePipelineColumns.ts` returns NO hits
- PipelinePage imports usePipeline, usePipelineMutation, usePipelineColumns
</verification>

<success_criteria>
AG Grid at /pipeline shows row number column plus all 10 data columns, inline editing for stage/fit_tier via dropdowns and next_action_date via date picker (GRID-10), server-side sort/pagination, arrow key cell navigation (GRID-09), and keyboard nav hooks for Enter/Escape. All leads-centric code removed.
</success_criteria>

<output>
After completion, create `.planning/phases/087-pipeline-grid/087-02-SUMMARY.md`
</output>
