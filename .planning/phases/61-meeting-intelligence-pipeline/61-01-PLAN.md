---
phase: 61-meeting-intelligence-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/engines/meeting_processor_web.py
  - backend/src/flywheel/services/skill_executor.py
  - backend/src/flywheel/api/meetings.py
autonomous: true

must_haves:
  truths:
    - "POST /meetings/{id}/process creates a SkillRun and the job_queue_loop picks it up within 5s"
    - "Meeting transcript is fetched from Granola and uploaded to Supabase Storage at uploads/transcripts/{tenant_id}/{meeting_id}.txt"
    - "Meeting is classified into one of 8 types using 3-layer classification (contact match, internal check, Haiku LLM)"
    - "7 context file types are extracted from transcript via Sonnet and written as ContextEntry rows with account_id set"
    - "meeting.summary JSONB is populated with tldr, key_decisions, action_items, meeting_type after processing"
    - "SSE stage events stream during processing (fetching, storing, classifying, extracting, linking, writing, done)"
    - "CONTEXT_FILE_MAP aligns with spec MPP-04: competitive-intel, pain-points, icp-profiles, contacts, insights, action-items, product-feedback"
  artifacts:
    - path: "backend/src/flywheel/engines/meeting_processor_web.py"
      provides: "classify_meeting, extract_intelligence, upload_transcript, write_context_entries helpers"
      min_lines: 150
    - path: "backend/src/flywheel/services/skill_executor.py"
      provides: "_execute_meeting_processor() 7-stage pipeline + dispatch wiring"
      contains: "is_meeting_processor"
    - path: "backend/src/flywheel/api/meetings.py"
      provides: "POST /meetings/{id}/process endpoint"
      contains: "process_meeting"
  key_links:
    - from: "backend/src/flywheel/api/meetings.py"
      to: "SkillRun(skill_name='meeting-processor')"
      via: "POST /meetings/{id}/process creates SkillRun row"
      pattern: "skill_name.*meeting-processor"
    - from: "backend/src/flywheel/services/skill_executor.py"
      to: "backend/src/flywheel/engines/meeting_processor_web.py"
      via: "import classify_meeting, extract_intelligence"
      pattern: "from flywheel.engines.meeting_processor_web import"
    - from: "backend/src/flywheel/engines/meeting_processor_web.py"
      to: "backend/src/flywheel/services/granola_adapter.py"
      via: "get_meeting_content() for transcript fetch"
      pattern: "get_meeting_content"
---

<objective>
Build the meeting intelligence pipeline: a 7-stage engine that fetches transcripts from Granola, uploads to Supabase Storage, classifies meeting type via 3-layer detection (contact match -> internal check -> Haiku LLM), extracts 7 context file types via Sonnet, and writes structured intelligence to ContextEntry rows. Includes the POST /meetings/{id}/process API endpoint and skill_executor dispatch wiring.

Purpose: This is the intelligence brain that transforms raw meeting data (from Phase 60) into structured CRM intelligence. Without this, meetings are just metadata rows with no extracted value.

Output: Working meeting processing pipeline triggered via POST endpoint, dispatched through job_queue, producing ContextEntry rows and meeting.summary JSONB.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-intelligence-flywheel.md
@.planning/phases/61-meeting-intelligence-pipeline/61-RESEARCH.md
@.planning/phases/60-meeting-data-model-and-granola-adapter/60-01-SUMMARY.md
@.planning/phases/60-meeting-data-model-and-granola-adapter/60-02-SUMMARY.md
@.planning/phases/60-meeting-data-model-and-granola-adapter/60-03-SUMMARY.md
@backend/src/flywheel/services/skill_executor.py
@backend/src/flywheel/engines/meeting_processor_web.py
@backend/src/flywheel/api/meetings.py
@backend/src/flywheel/services/granola_adapter.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/storage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meeting processor web engine module with classify, extract, upload, and context write helpers</name>
  <files>backend/src/flywheel/engines/meeting_processor_web.py</files>
  <action>
Create `backend/src/flywheel/engines/meeting_processor_web.py` with these functions:

1. **classify_meeting()** — 3-layer classification returning one of 8 types (discovery, expert, prospect, advisor, investor-pitch, internal, customer-feedback, team-meeting):
   - Layer 1: Query `AccountContact` for attendee emails. If found, derive type from parent account's `relationship_type` array (e.g., advisor account -> "advisor" type). Use `factory()` session with RLS context set.
   - Layer 2: If all attendee emails share the tenant domain -> classify as internal (<=3 attendees) or team-meeting (>3). Query `Tenant.domain` from DB. If `tenant.domain` is NULL, skip Layer 2 entirely (log debug warning).
   - Layer 3: Haiku LLM call with title + attendees + first 500 chars of transcript. Model string: `"claude-haiku-4-5-20251001"`. Use `anthropic.Anthropic()` sync client wrapped in `asyncio.get_event_loop().run_in_executor(None, lambda: ...)` to avoid blocking event loop. Prompt returns ONLY the classification code.
   - Accept `api_key: str | None` param — pass to `anthropic.Anthropic(api_key=api_key)` if provided.

2. **extract_intelligence()** — Sonnet extraction into structured JSON dict matching the 7 MPP-04 context files:
   - Model string: `"claude-sonnet-4-20250514"`. Sync client in run_in_executor.
   - System prompt instructs extraction of these 7 keys (aligned with MPP-04 spec context file names):
     - `competitive_intel` — competitor mentions, pricing, switching signals, speaker attribution
     - `pain_points` — problems with severity, speaker attribution, painkiller/vitamin classification
     - `icp_profiles` — company segments, buying signals, decision-maker info, budget signals, champion indicators
     - `contacts` — per-person: name, title, company, role, meeting history, notes
     - `insights` — strategic takeaways, quotable moments, cross-call patterns, next steps
     - `action_items` — commitments with owners, due dates, team vs external
     - `product_feedback` — feature requests, reactions, demo feedback
   - Also extract `tldr` and `key_decisions` as top-level summary fields (these go into meeting.summary JSONB, NOT into context files).
   - User prompt includes: full transcript, provider's ai_summary (if available), meeting_type, existing account context (if any).
   - Parse JSON response (strip ```json fences if present).
   - Return dict with all keys (empty lists for missing keys).

3. **upload_transcript()** — Upload transcript text to Supabase Storage:
   - Path: `transcripts/{tenant_id}/{meeting_id}.txt` in the `uploads` bucket.
   - URL: `{SUPABASE_URL}/storage/v1/object/uploads/transcripts/{tenant_id}/{meeting_id}.txt`
   - Use httpx async client with `Authorization: Bearer {SUPABASE_SERVICE_ROLE_KEY}`, `Content-Type: text/plain`.
   - Return the storage path string.

4. **write_context_entries()** — Write extracted intelligence to ContextEntry rows:
   - Accept: factory, tenant_id, user_id, account_id (may be None), meeting_date, meeting_slug, extracted dict.
   - Use `CONTEXT_FILE_MAP` to map extracted keys to ContextEntry file_names (aligned with spec MPP-04):
     ```python
     CONTEXT_FILE_MAP = {
         "competitive_intel": "competitive-intel",
         "pain_points": "pain-points",
         "icp_profiles": "icp-profiles",
         "contacts": "contacts",
         "insights": "insights",
         "action_items": "action-items",
         "product_feedback": "product-feedback",
     }
     ```
   - For each non-empty extracted key that has a mapping in CONTEXT_FILE_MAP, create a ContextEntry ORM object directly:
     - `source="ctx-meeting-processor"`, `detail=meeting_slug`, `confidence="medium"`
     - `account_id` set directly on ORM object
     - `date` = meeting_date as datetime.date
   - Do NOT write `tldr` or `key_decisions` as context entries — those are summary-only fields.
   - Set RLS context (both app.tenant_id and app.user_id) in session before writes.
   - Dedup: before inserting, check if ContextEntry with same (file_name, source, detail, tenant_id) exists. Skip if so.
   - meeting_slug format: `meeting-{YYYY-MM-DD}-{title_slug}` where title_slug = first 20 chars of title, lowercased, non-alphanumeric replaced with hyphens.

Constants at module level:
- `_HAIKU_MODEL = "claude-haiku-4-5-20251001"`
- `_SONNET_MODEL = "claude-sonnet-4-20250514"`
- `EXTRACTION_SYSTEM_PROMPT` and `EXTRACTION_USER_PROMPT` as formatted string templates.
- `CONTEXT_FILE_MAP` dict mapping extracted keys to ContextEntry file_names (7 entries, matching MPP-04 exactly).

Do NOT import from the existing CLI `engines/meeting_processor.py` — it uses sync storage_backend. The two implementations coexist permanently.
  </action>
  <verify>
1. `python -c "from flywheel.engines.meeting_processor_web import classify_meeting, extract_intelligence, upload_transcript, write_context_entries, CONTEXT_FILE_MAP; print(sorted(CONTEXT_FILE_MAP.values()))"` succeeds and prints `['action-items', 'competitive-intel', 'contacts', 'icp-profiles', 'insights', 'pain-points', 'product-feedback']`.
2. `grep -c "buying-signals\|competitor-intel\|key-decisions\|budget-signals\|champion-indicators" backend/src/flywheel/engines/meeting_processor_web.py` returns 0 (old names NOT present).
  </verify>
  <done>
Four async helper functions exist in meeting_processor_web.py: classify_meeting (3-layer), extract_intelligence (Sonnet, 7 context keys + tldr/key_decisions), upload_transcript (Supabase Storage), write_context_entries (direct ContextEntry ORM creation with account_id and dedup). CONTEXT_FILE_MAP uses the 7 spec-aligned names: competitive-intel, pain-points, icp-profiles, contacts, insights, action-items, product-feedback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire _execute_meeting_processor() 7-stage pipeline into skill_executor.py and add POST /meetings/{id}/process endpoint</name>
  <files>backend/src/flywheel/services/skill_executor.py, backend/src/flywheel/api/meetings.py</files>
  <action>
**In skill_executor.py:**

1. Add dispatch flag near line 572-574 (alongside existing is_company_intel, is_meeting_prep, is_email_scorer):
   ```python
   is_meeting_processor = run.skill_name == "meeting-processor"
   ```
   Update the `if has_engine or ...` condition to include `or is_meeting_processor`.

2. Add `elif is_meeting_processor:` dispatch block calling `_execute_meeting_processor()` with params: factory, run_id, tenant_id, user_id, meeting_id=UUID(run.input_text), api_key.

3. Add "meeting-processor" to the subsidy API key allowlist (~line 506): `if run.skill_name in ("company-intel", "meeting-prep", "email-scorer", "meeting-processor")`.

4. Add `_execute_meeting_processor()` async function (place near `_execute_company_intel()`):

   Signature: `async def _execute_meeting_processor(factory, run_id, tenant_id, user_id, meeting_id, api_key=None) -> tuple[str, dict, list]`

   7-stage pipeline:
   - **Stage 1 "fetching"**: Emit stage event via `_append_event_atomic()`. Load Meeting row from DB. Load Integration row (provider="granola") to get encrypted API key. Decrypt via `decrypt_api_key()`. Call `get_meeting_content(decrypted_key, meeting.external_id)` from granola_adapter. If no content, mark meeting as "failed", return early.
   - **Stage 2 "storing"**: Emit stage event. Call `upload_transcript(tenant_id, meeting_id, content.transcript)` from meeting_processor_web. Store returned path on `meeting.transcript_url`. Also update `meeting.attendees` from content.attendees (Phase 60 may have stored `[]`).
   - **Stage 3 "classifying"**: Emit stage event. Call `classify_meeting(factory, tenant_id, meeting.title, content.attendees, content.transcript, meeting.ai_summary, api_key)`.
   - **Stage 4 "extracting"**: Emit stage event. Call `extract_intelligence(content.transcript, meeting.ai_summary, meeting_type, existing_context_str, api_key)`. For existing_context_str: query ContextEntry rows for the meeting's account_id (if known from Stage 5 of a prior run) to provide as context to the LLM. If no account_id yet, pass empty string.
   - **Stage 5 "linking"**: Emit stage event. Set `account_id = None` for now (Plan 02 replaces this with auto_link_meeting_to_account call). Log: "Account linking deferred to Plan 02".
   - **Stage 6 "writing"**: Emit stage event. Generate meeting_slug from meeting date + title. Call `write_context_entries(factory, tenant_id, user_id, account_id, meeting_date, meeting_slug, extracted)`.
   - **Stage 7 "done"**: Update meeting row: `summary` JSONB (tldr, key_decisions, action_items from extracted dict, attendee_roles from content.attendees, meeting_type, pain_points), `processing_status="complete"`, `processed_at=now()`, `meeting_type=meeting_type`, `account_id=account_id`, `skill_run_id=run_id`. Emit done event.

   Error handling: wrap in try/except. On failure: update meeting.processing_status="failed", re-raise. Use `_append_event_atomic()` for error event.

   Return: (output_summary_str, token_usage_dict, tool_calls_list).

**In meetings.py:**

Add `POST /{meeting_id}/process` endpoint:
- Requires `require_tenant` auth.
- Load meeting by id + tenant_id. 404 if not found.
- Check `processing_status in ("pending", "failed")`. 409 if already processing/complete.
- Set `meeting.processing_status = "processing"` immediately (race condition guard).
- Create `SkillRun(tenant_id=user.tenant_id, user_id=user.sub, skill_name="meeting-processor", input_text=str(meeting_id), status="pending")`.
- Update `meeting.skill_run_id = run.id`.
- Commit.
- Return `{"run_id": str(run.id), "meeting_id": str(meeting_id)}`.

Import `SkillRun` from db.models for the endpoint.
  </action>
  <verify>
1. `grep -n "is_meeting_processor" backend/src/flywheel/services/skill_executor.py` shows the dispatch flag and elif block.
2. `grep -n "meeting-processor" backend/src/flywheel/services/skill_executor.py` shows subsidy allowlist entry.
3. `grep -n "process_meeting" backend/src/flywheel/api/meetings.py` shows the POST endpoint.
4. `python -c "from flywheel.services.skill_executor import _execute_meeting_processor"` succeeds.
  </verify>
  <done>
_execute_meeting_processor() exists with 7-stage pipeline (fetching, storing, classifying, extracting, linking-placeholder, writing, done). POST /meetings/{id}/process endpoint creates SkillRun and guards against duplicate processing. Dispatch wiring routes "meeting-processor" skill runs to the engine. Subsidy API key allowlist includes "meeting-processor".
  </done>
</task>

</tasks>

<verification>
1. Import check: `cd backend && python -c "from flywheel.engines.meeting_processor_web import classify_meeting, extract_intelligence, upload_transcript, write_context_entries, CONTEXT_FILE_MAP; from flywheel.services.skill_executor import _execute_meeting_processor; print('All imports OK')"`
2. Context file names correct: `cd backend && python -c "from flywheel.engines.meeting_processor_web import CONTEXT_FILE_MAP; assert sorted(CONTEXT_FILE_MAP.values()) == ['action-items', 'competitive-intel', 'contacts', 'icp-profiles', 'insights', 'pain-points', 'product-feedback'], 'CONTEXT_FILE_MAP mismatch'; print('MPP-04 aligned')"`
3. Endpoint registered: `grep -n "process_meeting\|/{meeting_id}/process" backend/src/flywheel/api/meetings.py`
4. Dispatch wired: `grep -n "meeting-processor" backend/src/flywheel/services/skill_executor.py`
5. No CLI engine imports: `grep -rn "from flywheel.engines.meeting_processor import" backend/src/flywheel/services/` should return nothing
6. No old context file names: `grep -c "buying-signals\|competitor-intel\|key-decisions\|budget-signals\|champion-indicators" backend/src/flywheel/engines/meeting_processor_web.py` returns 0
</verification>

<success_criteria>
- meeting_processor_web.py exists with 4 exported functions (classify, extract, upload, write)
- CONTEXT_FILE_MAP has exactly 7 entries matching MPP-04: competitive-intel, pain-points, icp-profiles, contacts, insights, action-items, product-feedback
- extract_intelligence() returns keys matching CONTEXT_FILE_MAP plus tldr/key_decisions for summary
- _execute_meeting_processor() handles all 7 stages with SSE events
- POST /meetings/{id}/process creates SkillRun with correct guards
- "meeting-processor" is in both dispatch chain and subsidy allowlist
- Stage 5 (linking) is a placeholder that Plan 02 will replace
- No imports from the CLI meeting_processor.py engine
</success_criteria>

<output>
After completion, create `.planning/phases/61-meeting-intelligence-pipeline/61-01-SUMMARY.md`
</output>