---
phase: 66.1-flywheel-stabilization
plan: 03
subsystem: api
tags: [flywheel, postgres, coalesce, optimistic-locking, mcp, polling]

requires:
  - phase: 66.1-flywheel-stabilization
    plan: 02
    provides: "Corrected flywheel engine with UTC timezone, batch query, Stage 4 guards"
provides:
  - "NULL-safe _append_event_atomic via COALESCE"
  - "Execution caps: Stage 2 (20 meetings), Stage 3 (15 meetings)"
  - "Optimistic lock on Stage 4 task status update"
  - "Extended MCP polling timeout (~10 min)"
affects: [flywheel-runtime, mcp-cli]

tech-stack:
  added: []
  patterns:
    - "COALESCE NULL guard pattern for JSONB array append"
    - "Execution cap pattern: constant + slice + informational event log"
    - "Optimistic lock pattern: WHERE status = expected_value + rowcount check"

key-files:
  created: []
  modified:
    - "backend/src/flywheel/services/skill_executor.py"
    - "backend/src/flywheel/engines/flywheel_ritual.py"
    - "cli/flywheel_mcp/server.py"

key-decisions:
  - "MAX_MEETINGS_TO_PROCESS = 20, MAX_MEETINGS_TO_PREP = 15 -- empirical caps balancing completeness vs timeout risk"
  - "Optimistic lock uses Task.status == 'confirmed' WHERE clause (not SELECT FOR UPDATE) -- simpler, sufficient for this use case"
  - "Private import coupling documented as tech debt -- too risky to refactor during stabilization"
  - "SKILL.md engine field already present (Issue 17 confirmed, no change needed)"

patterns-established:
  - "Execution cap pattern: define constant, log event with counts, slice list"
  - "Optimistic lock pattern: add expected-state WHERE clause, check rowcount == 0 for race detection"

duration: 2min
completed: 2026-03-29
---

# Phase 66.1 Plan 03: Robustness Hardening Summary

**NULL-safe event logging via COALESCE, execution caps (20/15 meetings), optimistic lock on task status, and 10-min MCP polling window**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-29T04:13:55Z
- **Completed:** 2026-03-29T04:16:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- _append_event_atomic now handles NULL events_log via COALESCE instead of silently dropping events
- Stage 2 capped at 20 meetings, Stage 3 capped at 15 meetings per run -- prevents MCP timeout on large backlogs
- Stage 4 task status update uses optimistic lock (WHERE status = 'confirmed') to prevent race conditions between concurrent runs
- MCP polling window extended from ~5 min to ~10 min to accommodate larger runs
- Private import coupling between flywheel_ritual.py and skill_executor.py documented as tech debt
- SKILL.md engine field confirmed present (Issue 17 verified -- no change needed)

## Task Commits

Per-plan commit strategy (single commit for all tasks):

1. **All tasks** - `0fb91b0` (feat)

## Files Created/Modified
- `backend/src/flywheel/services/skill_executor.py` - COALESCE NULL guard on _append_event_atomic
- `backend/src/flywheel/engines/flywheel_ritual.py` - Execution caps, optimistic lock, tech debt comment
- `cli/flywheel_mcp/server.py` - Extended polling interval from 27 to 57 iterations (~10 min)

## Decisions Made
- MAX_MEETINGS_TO_PROCESS = 20 and MAX_MEETINGS_TO_PREP = 15 chosen as empirical caps that balance completeness with timeout risk
- Optimistic lock uses simple WHERE clause (not SELECT FOR UPDATE) -- sufficient for preventing double-execution without lock contention
- Private import coupling documented but not refactored -- stabilization phase is wrong time for that structural change
- SKILL.md already had `engine: flywheel_ritual` at line 9 -- Issue 17 confirmed resolved, no code change needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 66.1 stabilization complete (all 3 plans done)
- Flywheel engine now has: infra blockers fixed (Plan 01), correctness fixes (Plan 02), robustness hardening (Plan 03)
- No blockers

---
*Phase: 66.1-flywheel-stabilization*
*Completed: 2026-03-29*
