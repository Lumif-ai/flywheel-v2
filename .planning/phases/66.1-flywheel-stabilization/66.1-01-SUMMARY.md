---
phase: 66.1-flywheel-stabilization
plan: 01
subsystem: infra, database
tags: [pydantic, supabase, alembic, env-vars, migration]

# Dependency graph
requires:
  - phase: 65-01
    provides: tasks table schema design
  - phase: 55-03
    provides: Supabase Storage upload pattern
provides:
  - Settings singleton with AliasChoices for SUPABASE_SERVICE_KEY / SUPABASE_SERVICE_ROLE_KEY
  - Clean migration 034 (no profiles FK, updated_at trigger)
  - All Supabase Storage operations use centralized config
affects: [66.1-02, 66.1-03, flywheel-engine, meeting-processor, tasks-api]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "AliasChoices on Settings field for env var backward compatibility"
    - "Local import of settings inside function body (relationships.py, meeting_processor_web.py) to avoid circular imports"

key-files:
  created: []
  modified:
    - backend/src/flywheel/config.py
    - backend/src/flywheel/services/document_storage.py
    - backend/src/flywheel/engines/meeting_processor_web.py
    - backend/src/flywheel/api/relationships.py
    - backend/alembic/versions/034_create_tasks_table.py

key-decisions:
  - "AliasChoices accepts 3 variants: SUPABASE_SERVICE_KEY, SUPABASE_SERVICE_ROLE_KEY, supabase_service_key"
  - "Local imports in meeting_processor_web.py and relationships.py to avoid circular import risk"
  - "Removed unused os import from document_storage.py, meeting_processor_web.py, relationships.py"

patterns-established:
  - "AliasChoices pattern: when env var naming conflicts exist, use validation_alias with AliasChoices to accept all variants"

# Metrics
duration: 2min
completed: 2026-03-29
---

# Phase 66.1 Plan 01: Infrastructure Blockers Summary

**Supabase env var access consolidated through settings singleton with AliasChoices, migration 034 fixed (profiles FK removed, updated_at trigger added)**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-29T04:09:38Z
- **Completed:** 2026-03-29T04:11:52Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- All Supabase Storage operations (document_storage.py, meeting_processor_web.py, relationships.py) now use `settings.supabase_service_key` instead of raw `os.environ` lookups
- Config.py accepts both `SUPABASE_SERVICE_KEY` and `SUPABASE_SERVICE_ROLE_KEY` via Pydantic AliasChoices -- no more KeyError on env var mismatch
- Migration 034 creates tasks table without FK to profiles.id (matching meetings table pattern)
- Migration 034 includes updated_at trigger with proper create/teardown in upgrade/downgrade

## Task Commits

Single plan-level commit (per-plan strategy):

1. **Task 1: Consolidate Supabase env var access** - `4fd1acf` (fix)
2. **Task 2: Fix migration 034** - `4fd1acf` (fix)

## Files Created/Modified
- `backend/src/flywheel/config.py` - Added AliasChoices import and validation_alias on supabase_service_key field
- `backend/src/flywheel/services/document_storage.py` - Replaced os.environ with settings singleton (2 locations), removed os import
- `backend/src/flywheel/engines/meeting_processor_web.py` - Replaced os.environ.get with settings singleton (upload_transcript), removed os import
- `backend/src/flywheel/api/relationships.py` - Replaced os.environ with settings singleton (file upload), removed os import
- `backend/alembic/versions/034_create_tasks_table.py` - Removed profiles FK, added updated_at trigger function and trigger

## Decisions Made
- Used local imports (`from flywheel.config import settings`) inside function bodies in meeting_processor_web.py and relationships.py to avoid circular import risk -- these files are imported early in the module graph
- Removed `import os` from all three fixed files since no other os.environ usage remained
- AliasChoices includes lowercase `supabase_service_key` variant for .env file compatibility (pydantic-settings lowercases env vars by default)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Supabase Storage operations are now resilient to env var naming -- ready for E2E testing
- Migration 034 can be applied cleanly after 033 -- tasks table ready for creation
- Plan 02 (Stage 4 crash fix) and Plan 03 (remaining issues) can proceed

---
*Phase: 66.1-flywheel-stabilization*
*Completed: 2026-03-29*
