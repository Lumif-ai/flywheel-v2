---
phase: 95-skill-ip-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/db/models.py
  - backend/src/flywheel/db/seed.py
  - backend/alembic/versions/050_add_skill_protected.py
autonomous: true

must_haves:
  truths:
    - "skill_definitions table has a protected BOOLEAN column defaulting to TRUE"
    - "Seed pipeline reads public: true from SKILL.md frontmatter and maps to protected=false in upsert"
    - "Alembic migration file exists for documentation even though DDL is applied via SQL Editor"
  artifacts:
    - path: "backend/src/flywheel/db/models.py"
      provides: "protected column on SkillDefinition"
      contains: "protected.*Mapped.*bool"
    - path: "backend/src/flywheel/db/seed.py"
      provides: "frontmatter parsing for public flag and upsert with protected field"
      contains: "protected"
    - path: "backend/alembic/versions/050_add_skill_protected.py"
      provides: "Migration documentation for protected column"
      contains: "ADD COLUMN protected"
  key_links:
    - from: "backend/src/flywheel/db/seed.py"
      to: "backend/src/flywheel/db/models.py"
      via: "SkillData.protected field mapped to SkillDefinition.protected column"
      pattern: "protected.*stmt\\.excluded\\.protected"
---

<objective>
Add the `protected` column to skill_definitions and extend the seed pipeline to populate it from SKILL.md frontmatter.

Purpose: Establishes the data layer that Plan 02 uses to gate prompt access. Default TRUE (fail closed) means all skills are protected unless explicitly marked `public: true` in frontmatter.
Output: DB model with protected column, seed script that parses and upserts the flag, Alembic migration file for documentation.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-skill-ip-protection/95-RESEARCH.md
@backend/src/flywheel/db/models.py
@backend/src/flywheel/db/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protected column to SkillDefinition model and create migration</name>
  <files>backend/src/flywheel/db/models.py, backend/alembic/versions/050_add_skill_protected.py</files>
  <action>
1. In `backend/src/flywheel/db/models.py`, add a `protected` column to the `SkillDefinition` class after the `enabled` column (line 845):
   ```python
   protected: Mapped[bool] = mapped_column(
       Boolean, nullable=False, server_default=text("true")
   )
   ```
   This defaults to TRUE (fail closed) — new skills are protected unless explicitly marked public.

2. Create `backend/alembic/versions/050_add_skill_protected.py` migration file following the pattern from `049_add_saved_views.py`. The upgrade function should contain the SQL as documentation:
   ```sql
   ALTER TABLE skill_definitions ADD COLUMN protected BOOLEAN NOT NULL DEFAULT TRUE;
   CREATE INDEX idx_skill_defs_protected ON skill_definitions (protected) WHERE protected = true;
   ```
   Include a comment block at the top: "NOTE: Due to Supabase PgBouncer DDL rollback issue, apply these statements individually via Supabase SQL Editor, then run: alembic stamp 050_add_skill_protected"

3. The downgrade function should contain:
   ```sql
   DROP INDEX IF EXISTS idx_skill_defs_protected;
   ALTER TABLE skill_definitions DROP COLUMN protected;
   ```
  </action>
  <verify>
  - `python -c "from flywheel.db.models import SkillDefinition; print(hasattr(SkillDefinition, 'protected'))"` returns True
  - Migration file exists at `backend/alembic/versions/050_add_skill_protected.py`
  </verify>
  <done>SkillDefinition model has `protected` boolean column with server_default=true. Migration file documents the DDL with a note about Supabase SQL Editor application.</done>
</task>

<task type="auto">
  <name>Task 2: Extend seed pipeline to parse public flag and upsert protected field</name>
  <files>backend/src/flywheel/db/seed.py</files>
  <action>
1. Add `protected` field to the `SkillData` dataclass (after `enabled`, line 64):
   ```python
   protected: bool = True
   ```

2. In `scan_skills()` function, after the `enabled` parsing block (around line 297), add parsing for the `public` frontmatter flag:
   ```python
   # Read public flag from frontmatter (default False = protected by default)
   is_public = data.get("public", False)
   if not isinstance(is_public, bool):
       is_public = False
   protected = not is_public
   ```

3. Add `protected=protected` to the `SkillData(...)` constructor call (around line 312).

4. In `seed_skills()`, add `"protected": skill.protected` to the `values` dict (around line 436).

5. CRITICAL: Add `"protected": stmt.excluded.protected` to the `set_` dict in `on_conflict_do_update` (around line 453). Without this, the column never gets updated from frontmatter on subsequent seeds. This is Pitfall 2 from research.
  </action>
  <verify>
  Run seed in dry-run or check parse output:
  - `cd backend && python -c "from flywheel.db.seed import scan_skills; skills, errs = scan_skills('skills'); print([(s.name, s.protected) for s in skills[:3]])"` — should show skills with their protected status
  </verify>
  <done>Seed pipeline reads `public: true` from SKILL.md frontmatter, maps it to `protected=false`, and includes the field in both INSERT values and ON CONFLICT UPDATE set_ clause.</done>
</task>

</tasks>

<verification>
- SkillDefinition.protected column exists in ORM model with server_default=true
- SkillData dataclass has protected field defaulting to True
- scan_skills() parses `public` from frontmatter and inverts to `protected`
- Upsert statement includes `protected` in both values and set_ clause
- Migration file exists with correct DDL and Supabase workaround note
</verification>

<success_criteria>
The data layer is complete: protected column defined in model, migration documented, and seed pipeline will correctly populate the flag from frontmatter. Plan 02 can now build the API and executor security on top of this.
</success_criteria>

<output>
After completion, create `.planning/phases/95-skill-ip-protection/95-01-SUMMARY.md`
</output>
