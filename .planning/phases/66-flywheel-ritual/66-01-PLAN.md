---
phase: 66-flywheel-ritual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/api/meetings.py
  - backend/src/flywheel/services/meeting_sync.py
  - skills/flywheel/SKILL.md
  - backend/src/flywheel/services/skill_executor.py
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "POST /meetings/sync endpoint still works identically after refactor (same response shape)"
    - "sync_granola_meetings() is callable with async_sessionmaker (engine pattern)"
    - "SKILL.md has engine: flywheel_ritual frontmatter, no allowed-tools, no curl"
    - "skill_executor.py dispatches skill_name=='flywheel' to execute_flywheel_ritual"
    - "flywheel is in subsidy key allowlist"
    - "REQUIREMENTS.md FLY-01 through FLY-06 marked as superseded by backend-engine rearchitect"
  artifacts:
    - path: "backend/src/flywheel/services/meeting_sync.py"
      provides: "Shared sync_granola_meetings(factory, tenant_id, user_id) function"
      exports: ["sync_granola_meetings"]
    - path: "skills/flywheel/SKILL.md"
      provides: "Backend-engine skill definition with engine: flywheel_ritual"
      contains: "engine: flywheel_ritual"
    - path: "backend/src/flywheel/services/skill_executor.py"
      provides: "Dispatch wiring for flywheel engine + subsidy allowlist"
      contains: "is_flywheel"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability for FLY-01 through FLY-06"
      contains: "Superseded"
  key_links:
    - from: "backend/src/flywheel/api/meetings.py"
      to: "backend/src/flywheel/services/meeting_sync.py"
      via: "import and call sync_granola_meetings"
      pattern: "from flywheel\\.services\\.meeting_sync import sync_granola_meetings"
    - from: "backend/src/flywheel/services/skill_executor.py"
      to: "backend/src/flywheel/engines/flywheel_ritual.py"
      via: "lazy import in dispatch"
      pattern: "from flywheel\\.engines\\.flywheel_ritual import execute_flywheel_ritual"
---

<objective>
Extract Granola sync logic into a shared function, replace the curl-based SKILL.md with engine frontmatter, wire the flywheel dispatch in skill_executor.py, and update REQUIREMENTS.md to reflect the rearchitect.

Purpose: Foundation for the flywheel engine — shared sync function usable by both the API endpoint and the engine, correct SKILL.md for seed.py, dispatch so the job queue routes to the engine, and accurate requirements tracking.
Output: meeting_sync.py, updated SKILL.md, updated skill_executor.py, updated REQUIREMENTS.md
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-flywheel-ritual-rearchitect.md
@.planning/phases/66-flywheel-ritual/66-RESEARCH.md
@backend/src/flywheel/api/meetings.py
@backend/src/flywheel/services/skill_executor.py
@skills/flywheel/SKILL.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract sync logic into shared meeting_sync.py and refactor endpoint</name>
  <files>backend/src/flywheel/services/meeting_sync.py, backend/src/flywheel/api/meetings.py</files>
  <action>
Create `backend/src/flywheel/services/meeting_sync.py` with a shared function:

```python
async def sync_granola_meetings(
    factory: async_sessionmaker,
    tenant_id: UUID,
    user_id: UUID,
) -> dict:
```

Extract the following from `meetings.py` lines 142-253 into this function:
1. Move `_apply_processing_rules(raw, rules)` (lines 37-75) into meeting_sync.py as a module-level helper (pure function, no DB).
2. Move `_find_matching_scheduled(db, tenant_id, raw)` (lines 78-134) into meeting_sync.py. Change its signature from `(db: AsyncSession, ...)` to work inside the shared function's session context (it will use the session opened by `sync_granola_meetings`).
3. The shared function opens its own session via `async with factory() as session:`, sets RLS context (`SELECT set_config('app.tenant_id', :tid, true)`), then runs the exact same sync logic currently inline in the endpoint (steps 1-7 from lines 161-253).
4. Key imports needed: `Integration`, `Meeting` from `flywheel.db.models`, `decrypt_api_key` from `flywheel.auth.encryption`, `list_meetings as granola_list_meetings` from `flywheel.services.granola_adapter`, `RawMeeting` from same.
5. Returns `{"synced": int, "skipped": int, "already_seen": int, "total_from_provider": int}`.

Refactor `meetings.py`:
1. Remove the inline sync logic from the `POST /sync` endpoint.
2. Remove `_apply_processing_rules` and `_find_matching_scheduled` functions (they live in meeting_sync.py now).
3. Replace endpoint body with:
```python
@router.post("/sync")
async def sync_meetings(
    user: TokenPayload = Depends(require_tenant),
) -> dict:
    factory = get_session_factory()
    return await sync_granola_meetings(factory, user.tenant_id, user.sub)
```
4. Remove the `db: AsyncSession = Depends(get_tenant_db)` dependency from sync_meetings since sync_granola_meetings manages its own sessions.
5. Keep all other endpoints unchanged.
6. Add `from flywheel.services.meeting_sync import sync_granola_meetings` import.

IMPORTANT: The `_find_matching_scheduled` helper currently takes `AsyncSession`. In meeting_sync.py, make it a nested function inside `sync_granola_meetings` that uses the session from the `async with factory() as session:` context, OR keep it module-level but pass the session explicitly. Either approach works — just ensure it uses the factory-opened session.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.services.meeting_sync import sync_granola_meetings; print('OK')"` — should print OK.
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.meetings import router; print('OK')"` — should print OK (endpoint still importable).
Grep: `grep -c "sync_granola_meetings" backend/src/flywheel/api/meetings.py` — should be >= 1 (import + call).
Grep: `grep "async def sync_granola_meetings" backend/src/flywheel/services/meeting_sync.py` — should match.
  </verify>
  <done>
- `sync_granola_meetings(factory, tenant_id, user_id)` exists in meeting_sync.py with async_sessionmaker pattern
- `POST /meetings/sync` endpoint calls sync_granola_meetings instead of inline logic
- `_apply_processing_rules` and `_find_matching_scheduled` live in meeting_sync.py
- No logic duplication between endpoint and shared function
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace SKILL.md, delete api-reference.md, wire dispatch + subsidy</name>
  <files>skills/flywheel/SKILL.md, backend/src/flywheel/services/skill_executor.py</files>
  <action>
**Replace `skills/flywheel/SKILL.md`** — completely overwrite with:

```yaml
---
name: flywheel
version: "2.0"
description: >
  Daily operating ritual. One command syncs meetings from Granola, processes
  unprocessed recordings into intelligence, prepares briefings for upcoming
  external meetings, executes confirmed tasks via appropriate skills, and
  produces an HTML daily brief. Invoked via MCP flywheel_run_skill("flywheel").
engine: flywheel_ritual
web_tier: 1
contract_reads:
  - contacts
  - company-intel
  - competitive-intel
  - positioning
contract_writes:
  - contacts
  - company-intel
---

# Flywheel Ritual

You are the task execution agent for the Flywheel daily ritual (Stage 4).

When invoked with a task to execute, you receive:
- Task title and description
- Relevant context from the context store (account intel, contacts, positioning)
- Meeting context if the task originated from a meeting

Your job: produce a high-quality deliverable for the suggested skill. Write in the
founder's voice. Use web_search to gather current information when needed.

## Output Format

Return well-structured HTML. Use inline styles with Inter font, #E94D35 for accents,
#121212 headings, #6B7280 body text.

## Constraints

- Never auto-send emails or messages — produce drafts for review
- Never fabricate specific numbers, quotes, or data points
- Cite sources when using web research
```

**Delete `skills/flywheel/references/api-reference.md`:**
Remove the file. Also remove the `references/` directory if it's now empty.

**Update `skill_executor.py` dispatch** (around lines 567-634):
1. Add `is_flywheel = run.skill_name == "flywheel"` after the existing `is_meeting_processor` line (~line 580).
2. Add `or is_flywheel` to the dispatch condition at line 581:
   ```python
   if has_engine or is_company_intel or is_meeting_prep or is_email_scorer or is_meeting_processor or is_flywheel:
   ```
3. Add the flywheel dispatch case BEFORE the final `else` (the "has no engine dispatch" error), AFTER the `is_meeting_processor` block:
   ```python
   elif is_flywheel:
       from flywheel.engines.flywheel_ritual import execute_flywheel_ritual
       output, token_usage, tool_calls = await execute_flywheel_ritual(
           factory=factory,
           run_id=run.id,
           tenant_id=run.tenant_id,
           user_id=run.user_id,
           api_key=api_key,
       )
   ```

4. **Subsidy allowlist** (line 506): Add `"flywheel"` to the tuple:
   ```python
   if run.skill_name in ("company-intel", "meeting-prep", "email-scorer", "meeting-processor", "flywheel") and settings.flywheel_subsidy_api_key:
   ```

5. **HTML rendering path** (line 677): Add `is_flywheel` to the meeting-prep HTML check:
   ```python
   if is_meeting_prep or is_account_meeting_prep or is_flywheel:
       rendered_html = output
   ```

These are 4 surgical edits to skill_executor.py. Do NOT restructure or refactor any other code.
  </action>
  <verify>
Run: `cat skills/flywheel/SKILL.md | head -5` — should show `name: flywheel` and NOT contain `allowed-tools`.
Run: `ls skills/flywheel/references/api-reference.md 2>/dev/null && echo EXISTS || echo DELETED` — should print DELETED.
Run: `grep "is_flywheel" backend/src/flywheel/services/skill_executor.py | wc -l` — should be >= 3.
Run: `grep '"flywheel"' backend/src/flywheel/services/skill_executor.py | head -3` — should show subsidy tuple and dispatch.
Run: `grep "allowed-tools" skills/flywheel/SKILL.md | wc -l` — should be 0.
Run: `grep "curl" skills/flywheel/SKILL.md | wc -l` — should be 0.
  </verify>
  <done>
- SKILL.md has `engine: flywheel_ritual`, `web_tier: 1`, contract_reads/writes, NO `allowed-tools`
- `skills/flywheel/references/api-reference.md` deleted
- skill_executor.py routes `flywheel` to `execute_flywheel_ritual` via lazy import
- `flywheel` added to subsidy key allowlist
- `flywheel` added to HTML rendering path (rendered_html = output)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md to mark FLY-01 through FLY-06 as superseded</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/REQUIREMENTS.md` to reflect that the flywheel ritual has been rearchitected from a CLI/curl-based skill to a backend engine.

**In the "Phase C: /flywheel CLI Ritual" section**, update the requirement descriptions:
- FLY-01: Change from `/flywheel` Claude Code skill description to: "Superseded by backend-engine rearchitect (SPEC-flywheel-ritual-rearchitect.md). Now ORCH-01/ORCH-06."
- FLY-02: Change to: "Superseded — now Stage 1 of flywheel engine (ORCH-03)."
- FLY-03: Change to: "Superseded — now Stage 4 of flywheel engine (ORCH-12)."
- FLY-04: Change to: "Superseded — now Stage 3 of flywheel engine (ORCH-05)."
- FLY-05: Change to: "Superseded — now Stage 2 of flywheel engine (ORCH-04)."
- FLY-06: Change to: "Superseded — auth via MCP JWT session, no FLYWHEEL_API_TOKEN needed."

Mark each with `[~]` (superseded) instead of `[ ]` (pending).

**In the Traceability table**, update FLY-01 through FLY-06:
- Change Status from `Pending` to `Superseded (Phase 66 rearchitect)`

Add a note after the FLY entries: `*FLY-01 through FLY-06 superseded by backend-engine rearchitect. See SPEC-flywheel-ritual-rearchitect.md for replacement requirements ORCH-01 through ORCH-12.*`
  </action>
  <verify>
Grep: `grep "Superseded" .planning/REQUIREMENTS.md | wc -l` — should be >= 6 (FLY-01 through FLY-06).
Grep: `grep "SPEC-flywheel-ritual-rearchitect" .planning/REQUIREMENTS.md` — should match.
Grep: `grep "\[~\].*FLY-0" .planning/REQUIREMENTS.md | wc -l` — should be 6.
  </verify>
  <done>
- FLY-01 through FLY-06 marked as superseded with `[~]` checkboxes
- Each FLY requirement points to its replacement ORCH requirement
- Traceability table updated to show "Superseded (Phase 66 rearchitect)"
- Reference to SPEC-flywheel-ritual-rearchitect.md included
  </done>
</task>

</tasks>

<verification>
- `python -c "from flywheel.services.meeting_sync import sync_granola_meetings"` imports cleanly
- `python -c "from flywheel.api.meetings import router"` imports cleanly
- SKILL.md frontmatter has `engine: flywheel_ritual` (seed.py reads this)
- No references to `FLYWHEEL_API_TOKEN`, `curl`, `Bash`, or `allowed-tools` in `skills/flywheel/`
- skill_executor.py has flywheel dispatch, subsidy, and HTML rendering wiring
- REQUIREMENTS.md FLY-01 through FLY-06 marked as superseded with ORCH cross-references
</verification>

<success_criteria>
- Shared `sync_granola_meetings()` function exists and is used by the API endpoint
- SKILL.md is a clean backend-engine definition (not a curl-based Claude Code skill)
- skill_executor.py dispatches `flywheel` to the engine (which Plan 02 will create)
- All existing endpoints still work (sync, process-pending, list, detail, prep)
- REQUIREMENTS.md accurately reflects the rearchitect
</success_criteria>

<output>
After completion, create `.planning/phases/66-flywheel-ritual/66-01-SUMMARY.md`
</output>
