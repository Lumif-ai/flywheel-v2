---
phase: 65-task-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - backend/src/flywheel/engines/meeting_processor_web.py
  - backend/src/flywheel/services/skill_executor.py
autonomous: true

must_haves:
  truths:
    - "Processing a meeting with commitments creates Task rows in the database"
    - "Founder commitment 'we will send a one-pager' produces task with suggested_skill='sales-collateral' and trust_level='review'"
    - "Other party commitment 'I will send requirements' produces task with commitment_direction='theirs'"
    - "Email-related tasks always have trust_level='confirm'"
    - "Task extraction emits SSE 'extracting_tasks' stage event before 'done'"
  artifacts:
    - path: "backend/src/flywheel/engines/meeting_processor_web.py"
      provides: "extract_tasks() and write_task_rows() async helpers"
      contains: "extract_tasks"
    - path: "backend/src/flywheel/services/skill_executor.py"
      provides: "Stage 7 task extraction in meeting processor pipeline"
      contains: "extracting_tasks"
  key_links:
    - from: "backend/src/flywheel/services/skill_executor.py"
      to: "backend/src/flywheel/engines/meeting_processor_web.py"
      via: "imports extract_tasks, write_task_rows"
      pattern: "from.*meeting_processor_web.*import.*extract_tasks"
    - from: "backend/src/flywheel/engines/meeting_processor_web.py"
      to: "backend/src/flywheel/db/models.py"
      via: "writes Task rows to database"
      pattern: "Task\\("
---

<objective>
Add Stage 7 "Task Extraction" to the meeting processor pipeline that uses Haiku to classify commitments from transcripts and create Task rows.

Purpose: This is the core intelligence feature — turning conversation into structured tasks with commitment classification and skill mapping.
Output: extract_tasks() helper in meeting_processor_web.py, Stage 7 insertion in skill_executor.py pipeline.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-task-intelligence/65-RESEARCH.md
@.planning/phases/65-task-intelligence/65-01-SUMMARY.md

@backend/src/flywheel/services/skill_executor.py
@backend/src/flywheel/engines/meeting_processor_web.py
@backend/src/flywheel/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extract_tasks() and write_task_rows() helpers in meeting_processor_web.py</name>
  <files>backend/src/flywheel/engines/meeting_processor_web.py</files>
  <action>
Add two new async functions to meeting_processor_web.py:

**1. extract_tasks(transcript, extracted, meeting_type, api_key) -> list[dict]:**

Build a prompt using TASK_EXTRACTION_PROMPT (define as module-level constant — see research for full prompt text). The prompt receives:
- The transcript text
- The extracted intelligence from Stage 4 (JSON-serialized)
- The meeting type classification

Call Haiku via the sync-in-executor pattern already used in this file (see _call_haiku pattern at lines ~248-259):
- def _call_haiku() with anthropic.Anthropic(api_key=api_key)
- model="claude-haiku-4-5-20251001", max_tokens=2048
- Wrap in asyncio.get_event_loop().run_in_executor(None, _call_haiku)

Parse the JSON response using the same cleanup pattern from extract_intelligence() (strip markdown code fences, then json.loads). Return empty list on parse failure (log warning, don't crash pipeline).

Post-processing enforcement: iterate through returned tasks and force trust_level='confirm' for any task where suggested_skill contains "email". This is a HARD safety rule per success criteria #5.

**2. write_task_rows(factory, tenant_id, user_id, meeting_id, account_id, tasks) -> int:**

For each task dict from extract_tasks():
- Create a Task ORM instance with:
  - tenant_id, user_id, meeting_id, account_id from params
  - source="meeting-processor"
  - All fields from the dict (title, description, task_type, commitment_direction, suggested_skill, skill_context, trust_level, priority, due_date)
  - Parse due_date string to datetime if present (use dateutil.parser.isoparse or datetime.fromisoformat)
- Add to session, flush

Use factory() to get a session. CRITICAL per research Pitfall 2: the session MUST have both app.tenant_id and app.user_id set via set_config for RLS. Check how other write functions in this file handle session creation — follow the same pattern.

Return count of tasks created.
  </action>
  <verify>
Run: cd /Users/sharan/Projects/flywheel-v2/backend && python -c "
from flywheel.engines.meeting_processor_web import extract_tasks, write_task_rows
print('extract_tasks and write_task_rows imported successfully')
" && python -c "
with open('src/flywheel/engines/meeting_processor_web.py') as f:
    content = f.read()
assert 'TASK_EXTRACTION_PROMPT' in content, 'Missing prompt constant'
assert 'trust_level' in content and 'confirm' in content, 'Missing email trust_level enforcement'
assert 'email' in content.lower(), 'Missing email skill check'
assert 'meeting-processor' in content, 'Missing source field'
print('All helper checks passed')
"
  </verify>
  <done>extract_tasks() calls Haiku with structured prompt and returns parsed task dicts. write_task_rows() creates Task ORM instances with proper RLS context. Email tasks are forced to trust_level='confirm'.</done>
</task>

<task type="auto">
  <name>Task 2: Insert Stage 7 task extraction into meeting processor pipeline</name>
  <files>backend/src/flywheel/services/skill_executor.py</files>
  <action>
In _execute_meeting_processor() in skill_executor.py, insert a new stage between the current final processing stage and the "done" stage:

1. Import extract_tasks and write_task_rows from meeting_processor_web at the top of the function (local import to avoid circular — follow the same pattern as other local imports in this function).

2. After the current Stage 6 (context writing) and BEFORE the "done" stage, insert:

```
# Stage 7: Task extraction
await _append_event_atomic(factory, run_id, {
    "event": "stage",
    "data": {"stage": "extracting_tasks", "message": "Extracting commitments and tasks..."},
})

tasks = await extract_tasks(
    transcript=content.transcript,  # the transcript text from Stage 1
    extracted=extracted,             # the extracted intelligence dict from Stage 4
    meeting_type=meeting_type,       # from Stage 3 classification
    api_key=api_key,                 # the Anthropic API key
)

tasks_created = 0
if tasks:
    tasks_created = await write_task_rows(
        factory=factory,
        tenant_id=tenant_id,
        user_id=user_id or meeting.user_id,
        meeting_id=meeting_id,
        account_id=account_id,
        tasks=tasks,
    )

# Include task count in stage event
await _append_event_atomic(factory, run_id, {
    "event": "stage",
    "data": {"stage": "extracting_tasks", "message": f"Extracted {tasks_created} task(s)"},
})
```

3. The existing "done" stage becomes Stage 8 (but stage names are strings, so no renaming needed — just ensure "done" event fires AFTER task extraction).

4. Read the actual variable names used in the pipeline for transcript, extracted intelligence, meeting_type, api_key, tenant_id, user_id, meeting_id, account_id — they may differ from the names used above. Match them exactly.

5. Wrap the task extraction in try/except so failures don't crash the entire meeting pipeline. Log errors but continue to "done" stage. Task extraction is best-effort — a meeting should still process successfully even if task extraction fails.
  </action>
  <verify>
Run: cd /Users/sharan/Projects/flywheel-v2/backend && python -c "
with open('src/flywheel/services/skill_executor.py') as f:
    content = f.read()
assert 'extracting_tasks' in content, 'Missing extracting_tasks stage'
assert 'extract_tasks' in content, 'Missing extract_tasks call'
assert 'write_task_rows' in content, 'Missing write_task_rows call'
# Verify extracting_tasks appears inside _execute_meeting_processor and before its done stage
# Find the function boundary
func_start = content.index('async def _execute_meeting_processor')
# Find the next top-level async def after the function (or end of file)
next_func = content.find('async def ', func_start + 50)
func_body = content[func_start:next_func] if next_func != -1 else content[func_start:]
# Within that function body, verify ordering
extract_pos = func_body.index('extracting_tasks')
done_pos = func_body.rindex('Processing complete')
assert done_pos > extract_pos, 'Done stage must come after task extraction within _execute_meeting_processor'
print('Pipeline insertion checks passed')
"
  </verify>
  <done>Stage 7 "extracting_tasks" inserted into meeting processor pipeline before "done" stage. Task extraction is wrapped in try/except for resilience. Pipeline emits SSE events for task extraction progress.</done>
</task>

</tasks>

<verification>
1. extract_tasks() is importable from meeting_processor_web
2. write_task_rows() is importable from meeting_processor_web
3. skill_executor.py pipeline has "extracting_tasks" stage before "done"
4. Email tasks forced to trust_level='confirm' in extract_tasks post-processing
5. Task extraction failure does not crash the meeting pipeline
</verification>

<success_criteria>
- Meeting processing pipeline now has 8 stages (task extraction as Stage 7, done as Stage 8)
- Haiku classifies commitments into 5 categories (yours/theirs/mutual/signal/speculation)
- Task rows created with suggested_skill, skill_context, trust_level, due_date
- Email-related tasks always get trust_level='confirm'
- SSE events include "extracting_tasks" stage
</success_criteria>

<output>
After completion, create `.planning/phases/65-task-intelligence/65-02-SUMMARY.md`
</output>
