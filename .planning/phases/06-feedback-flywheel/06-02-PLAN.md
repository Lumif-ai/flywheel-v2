---
phase: 06-feedback-flywheel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/services/gmail_sync.py
autonomous: true

must_haves:
  truths:
    - "When a new message arrives in an existing thread via incremental sync, _score_new_emails is called for that message"
    - "Thread priority (MAX query) automatically reflects the newly scored message without additional code"
    - "Re-scoring of new thread messages counts against the daily cap as a new EmailScore row (not a double-count)"
    - "FEED-03 behavior is documented inline in gmail_sync.py for future maintainability"
  artifacts:
    - path: "backend/src/flywheel/services/gmail_sync.py"
      provides: "FEED-03 documentation comments in _score_new_emails, sync_gmail, and _full_sync"
  key_links:
    - from: "backend/src/flywheel/services/gmail_sync.py sync_gmail"
      to: "backend/src/flywheel/services/gmail_sync.py _score_new_emails"
      via: "new_email_ids list includes new messages in existing threads"
      pattern: "new_email_ids\\.append"
---

<objective>
Document the existing FEED-03 thread re-scoring behavior in gmail_sync.py.

Purpose: FEED-03 (re-score threads when new messages arrive) is already implemented by the existing sync loop architecture. New messages in existing threads are scored via _score_new_emails, and thread priority auto-updates via the MAX query in get_thread_priority. This plan documents that behavior for maintainability and confirms FEED-03 is satisfied without new code.
Output: Documentation comments in gmail_sync.py confirming FEED-03 behavior.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-feedback-flywheel/06-RESEARCH.md
@backend/src/flywheel/services/gmail_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document FEED-03 re-scoring behavior in gmail_sync.py</name>
  <files>
    backend/src/flywheel/services/gmail_sync.py
  </files>
  <action>
Add documentation comments to `backend/src/flywheel/services/gmail_sync.py` to confirm and explain the FEED-03 behavior. These are documentation-only changes — no logic modifications.

1. In `_score_new_emails` docstring, add a FEED-03 section after the existing docstring content:
   ```
   FEED-03 (Thread Re-scoring): New messages in existing threads are automatically
   handled here. When incremental sync picks up a messagesAdded event for an existing
   thread, the new email_id is included in the email_ids list passed to this function.
   The new message gets its own EmailScore row. Thread-level priority auto-updates
   because get_thread_priority() is a read-time MAX query (SCORE-07) — no additional
   re-scoring trigger is needed.
   ```

2. In `sync_gmail` incremental sync section, add an inline comment right before or after the `new_email_ids.append(email_id)` line inside the `messagesAdded` loop:
   ```
   # FEED-03: New messages in existing threads arrive as messagesAdded events.
   # Their email_ids go into new_email_ids -> _score_new_emails -> individual EmailScore rows.
   # Thread priority auto-updates via get_thread_priority() MAX query at read time.
   ```

3. In `_full_sync`, add an inline comment near the `new_email_ids.append(email_id)` line:
   ```
   # FEED-03: Both new threads and new messages in existing threads get scored.
   ```
  </action>
  <verify>
Run `cd /Users/sharan/Projects/flywheel-v2/backend && PYTHONPATH=src python3 -c "from flywheel.services.gmail_sync import _score_new_emails, sync_gmail; print('imports OK')"` — should succeed (no logic changes, just comments).
Run `cd /Users/sharan/Projects/flywheel-v2/backend && grep -c 'FEED-03' src/flywheel/services/gmail_sync.py` — should return 3 or more (one in each of the three locations).
  </verify>
  <done>
gmail_sync.py has FEED-03 documentation comments in _score_new_emails docstring, sync_gmail incremental section, and _full_sync. No behavioral changes — documentation confirms the requirement is already satisfied by existing architecture.
  </done>
</task>

</tasks>

<verification>
Phase 6 Plan 02 verification:

1. **FEED-03 documentation:** `_score_new_emails`, `sync_gmail`, and `_full_sync` have inline comments documenting the re-scoring path.
2. **No logic changes:** gmail_sync.py changes are comments only — no behavioral modification.
3. **Import chain:** All modified files import cleanly.
4. **grep confirmation:** `grep 'FEED-03' gmail_sync.py` returns 3+ matches.
</verification>

<success_criteria>
- gmail_sync.py has FEED-03 documentation comments in _score_new_emails docstring, sync_gmail incremental section, and _full_sync
- No behavioral changes to gmail_sync.py
- All imports resolve cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-feedback-flywheel/06-02-SUMMARY.md`
</output>
