---
phase: 60-meeting-data-model-and-granola-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/032_create_meetings_table.py
  - backend/src/flywheel/db/models.py
autonomous: true

must_haves:
  truths:
    - "meetings table exists with all columns from MDE-01"
    - "Dedup unique index prevents duplicate external_id per tenant+provider"
    - "RLS enforces tenant-read (SELECT for all members) and owner-write (INSERT/UPDATE/DELETE for user_id match)"
    - "Meeting ORM model is importable and matches table schema"
  artifacts:
    - path: "backend/alembic/versions/032_create_meetings_table.py"
      provides: "Alembic migration creating meetings table, indexes, RLS policies, GRANT"
      contains: "CREATE POLICY meetings_tenant_read"
    - path: "backend/src/flywheel/db/models.py"
      provides: "Meeting ORM class in CRM TABLES section"
      contains: "class Meeting(Base)"
  key_links:
    - from: "backend/alembic/versions/032_create_meetings_table.py"
      to: "031_user_level_rls"
      via: "down_revision chain"
      pattern: "down_revision.*031_user_level_rls"
    - from: "backend/src/flywheel/db/models.py"
      to: "accounts, skill_runs"
      via: "ForeignKey references"
      pattern: 'ForeignKey\("accounts.id"'
---

<objective>
Create the meetings table via Alembic migration with split-visibility RLS, and add the Meeting ORM model to models.py.

Purpose: Data foundation for meeting ingestion -- all subsequent plans (adapter, sync) depend on this table existing.
Output: Migration file `032_create_meetings_table.py` and `Meeting` class in `db/models.py`.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/SPEC-intelligence-flywheel.md (MDE-01, MDE-02 sections)
@.planning/phases/60-meeting-data-model-and-granola-adapter/60-RESEARCH.md
@backend/alembic/versions/031_user_level_rls.py (current head — down_revision reference)
@backend/alembic/versions/027_crm_tables.py (table creation + RLS + GRANT pattern)
@backend/src/flywheel/db/models.py (CRM TABLES section, lines ~1087-1243)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration for meetings table with split-visibility RLS</name>
  <files>backend/alembic/versions/032_create_meetings_table.py</files>
  <action>
Create migration file `032_create_meetings_table.py` with:
- `revision = "032_create_meetings_table"` (25 chars, under 32 limit)
- `down_revision = "031_user_level_rls"`

**upgrade():**
1. `op.create_table("meetings", ...)` with all columns from MDE-01:
   - id (UUID, PK, gen_random_uuid default)
   - tenant_id (UUID, NOT NULL, FK tenants.id)
   - user_id (UUID, NOT NULL, FK profiles.id)
   - provider (Text, NOT NULL)
   - external_id (Text, nullable)
   - title (Text, nullable)
   - meeting_date (TIMESTAMP with timezone, NOT NULL)
   - duration_mins (Integer, nullable)
   - attendees (JSONB, nullable)
   - transcript_url (Text, nullable)
   - ai_summary (Text, nullable)
   - summary (JSONB, nullable)
   - meeting_type (Text, nullable)
   - account_id (UUID, FK accounts.id ON DELETE SET NULL, nullable)
   - skill_run_id (UUID, FK skill_runs.id, nullable)
   - processed_at (TIMESTAMP with timezone, nullable)
   - processing_status (Text, server_default 'pending')
   - created_at (TIMESTAMP with timezone, server_default now())
   - updated_at (TIMESTAMP with timezone, server_default now())
   - deleted_at (TIMESTAMP with timezone, nullable)

2. Create 4 partial indexes (follow 027_crm_tables.py `op.create_index` pattern):
   - `idx_meetings_dedup`: unique on (tenant_id, provider, external_id) WHERE external_id IS NOT NULL
   - `idx_meetings_account`: on (account_id, meeting_date DESC) WHERE deleted_at IS NULL
   - `idx_meetings_user`: on (tenant_id, user_id, meeting_date DESC) WHERE deleted_at IS NULL
   - `idx_meetings_pending`: on (tenant_id, processing_status) WHERE processing_status = 'pending'

3. RLS setup (follow 027_crm_tables.py pattern):
   - `ALTER TABLE meetings ENABLE ROW LEVEL SECURITY`
   - `ALTER TABLE meetings FORCE ROW LEVEL SECURITY`
   - `GRANT SELECT, INSERT, UPDATE, DELETE ON meetings TO app_user`

4. Two RLS policies (CRITICAL -- split-visibility, NOT the standard tenant-isolation pattern):
   - `meetings_tenant_read`: FOR SELECT, USING (tenant_id = current_setting('app.tenant_id', true)::uuid)
   - `meetings_owner_write`: FOR ALL, USING (tenant_id = current_setting('app.tenant_id', true)::uuid AND user_id = current_setting('app.user_id', true)::uuid), WITH CHECK (same condition)

   NOTE: Use `current_setting('app.tenant_id', true)` (with `true` for missing_ok) -- matches the pattern from 031_user_level_rls.py, NOT the spec's bare `current_setting('app.tenant_id')`.

**downgrade():**
- Drop policies, then drop table: `op.execute("DROP POLICY IF EXISTS meetings_tenant_read ON meetings")`, same for meetings_owner_write, then `op.drop_table("meetings")`
  </action>
  <verify>Run `cd backend && python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; c = Config('alembic.ini'); sd = ScriptDirectory.from_config(c); heads = sd.get_heads(); print('Heads:', heads); assert len(heads) == 1, f'Multiple heads: {heads}'"` to confirm single head chain.</verify>
  <done>Migration file exists with correct revision chain (031 -> 032), creates meetings table with all 20 columns, 4 partial indexes, split-visibility RLS (tenant_read SELECT + owner_write ALL), and GRANT to app_user.</done>
</task>

<task type="auto">
  <name>Task 2: Meeting ORM model in models.py</name>
  <files>backend/src/flywheel/db/models.py</files>
  <action>
Add `Meeting` class after `OutreachActivity` (after line ~1242) in the CRM TABLES section. Follow the exact pattern from MDE-02 in the spec:

```python
class Meeting(Base):
    """A meeting ingested from an external source (Granola, Fathom, etc.)
    or uploaded manually. First-class entity in the CRM data model.

    Privacy model: metadata is tenant-visible, transcript is user-scoped.
    """

    __tablename__ = "meetings"
    __table_args__ = (
        Index(
            "idx_meetings_dedup",
            "tenant_id", "provider", "external_id",
            unique=True,
            postgresql_where=text("external_id IS NOT NULL"),
        ),
        Index(
            "idx_meetings_account",
            "account_id", text("meeting_date DESC"),
            postgresql_where=text("deleted_at IS NULL"),
        ),
        Index(
            "idx_meetings_user",
            "tenant_id", "user_id", text("meeting_date DESC"),
            postgresql_where=text("deleted_at IS NULL"),
        ),
        Index(
            "idx_meetings_pending",
            "tenant_id", "processing_status",
            postgresql_where=text("processing_status = 'pending'"),
        ),
    )
```

All columns as Mapped[] fields per MDE-02. Include relationships:
- `account: Mapped["Account | None"] = relationship()`
- `skill_run: Mapped["SkillRun | None"] = relationship()`

Ensure all needed imports are already present (Index, text, ForeignKey, JSONB, TIMESTAMP, Integer, Text are all already imported at the top of models.py -- verified). The `Mapped`, `mapped_column`, `relationship` imports are also already present from other models.

Update the module docstring CRM tables count: "CRM tables (3 tables)" -> "CRM tables (4 tables)" and add "meetings" to the list.
  </action>
  <verify>Run `cd backend && python -c "from flywheel.db.models import Meeting; print('Meeting model:', Meeting.__tablename__); print('Columns:', [c.name for c in Meeting.__table__.columns])"` to confirm model loads and has all expected columns.</verify>
  <done>Meeting class is importable from `flywheel.db.models`, has all 20 columns matching MDE-02, 4 index definitions in __table_args__, and account/skill_run relationships.</done>
</task>

</tasks>

<verification>
- Single Alembic head: `032_create_meetings_table` descends from `031_user_level_rls`
- Meeting model importable: `from flywheel.db.models import Meeting`
- Model columns match migration columns exactly (20 columns)
- RLS has exactly 2 policies: tenant_read (SELECT) and owner_write (ALL)
- All 4 indexes are partial (have WHERE clauses)
- Dedup index is UNIQUE
</verification>

<success_criteria>
- Migration file creates meetings table with split-visibility RLS (two policies, not four)
- ORM model is importable and matches table schema
- Revision chain is unbroken (031 -> 032)
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/60-meeting-data-model-and-granola-adapter/60-01-SUMMARY.md`
</output>
