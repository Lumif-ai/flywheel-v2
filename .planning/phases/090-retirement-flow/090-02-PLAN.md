---
phase: 090-retirement-flow
plan: 02
type: execute
wave: 2
depends_on: ["090-01"]
files_modified:
  - frontend/src/features/pipeline/types/pipeline.ts
  - frontend/src/features/pipeline/api.ts
  - frontend/src/features/pipeline/hooks/usePipeline.ts
  - frontend/src/features/pipeline/components/PipelinePage.tsx
  - frontend/src/features/pipeline/components/PipelineToolbar.tsx
autonomous: true

must_haves:
  truths:
    - "Stale entries show a visual amber indicator in the grid (left border + background tint)"
    - "Retired entries are hidden from the grid by default"
    - "A 'Show retired' toggle in the toolbar reveals retired entries with dimmed styling"
    - "User can retire an active entry via a button in the side panel or context action"
    - "User can reactivate a retired entry via a button"
  artifacts:
    - path: "frontend/src/features/pipeline/types/pipeline.ts"
      provides: "stale_notified_at and retired_at on PipelineListItem + PipelineDetail, include_retired on PipelineParams"
      contains: "stale_notified_at"
    - path: "frontend/src/features/pipeline/api.ts"
      provides: "retirePipelineEntry and reactivatePipelineEntry API functions"
      contains: "retirePipelineEntry"
    - path: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      provides: "getRowClass for stale/retired styling, include_retired param passing"
      contains: "row-stale"
    - path: "frontend/src/features/pipeline/components/PipelineToolbar.tsx"
      provides: "Show retired toggle control"
      contains: "showRetired"
  key_links:
    - from: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      to: "frontend/src/features/pipeline/hooks/usePipeline.ts"
      via: "include_retired passed through params"
      pattern: "include_retired"
    - from: "frontend/src/features/pipeline/components/PipelineToolbar.tsx"
      to: "frontend/src/features/pipeline/components/PipelinePage.tsx"
      via: "showRetired state callback"
      pattern: "showRetired|onShowRetiredChange"
    - from: "frontend/src/features/pipeline/api.ts"
      to: "POST /pipeline/{id}/retire"
      via: "api.post call"
      pattern: "retire|reactivate"
---

<objective>
Wire the retirement flow into the frontend: add stale/retired fields to types, visual stale indicator in grid rows, "Show retired" toggle in toolbar, and retire/reactivate actions callable from the side panel.

Purpose: Users see which entries are going stale (amber indicator), retired entries stay out of the way, and users have manual control over retirement.
Output: Complete frontend retirement UX.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-retirement-flow/090-RESEARCH.md
@.planning/phases/090-retirement-flow/090-01-SUMMARY.md
@frontend/src/features/pipeline/types/pipeline.ts
@frontend/src/features/pipeline/api.ts
@frontend/src/features/pipeline/hooks/usePipeline.ts
@frontend/src/features/pipeline/components/PipelinePage.tsx
@frontend/src/features/pipeline/components/PipelineToolbar.tsx
@frontend/src/features/pipeline/components/PipelineSidePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, API functions, and hook updates</name>
  <files>
    frontend/src/features/pipeline/types/pipeline.ts
    frontend/src/features/pipeline/api.ts
    frontend/src/features/pipeline/hooks/usePipeline.ts
  </files>
  <action>
1. In `types/pipeline.ts`:
   - Add `stale_notified_at: string | null` and `retired_at: string | null` to `PipelineListItem` (after `created_at`)
   - Add the same two fields to `PipelineDetail` (after `created_at`)
   - Add `include_retired?: boolean` to `PipelineParams`

2. In `api.ts`:
   - The `fetchPipeline` function already serializes all params -- `include_retired` will flow through automatically since it's in PipelineParams. No changes needed to fetchPipeline.
   - Add two new API functions:

   ```typescript
   export function retirePipelineEntry(id: string): Promise<PipelineDetail> {
     return api.post<PipelineDetail>(`/pipeline/${id}/retire`)
   }

   export function reactivatePipelineEntry(id: string): Promise<PipelineDetail> {
     return api.post<PipelineDetail>(`/pipeline/${id}/reactivate`)
   }
   ```

3. In `hooks/usePipeline.ts`: No changes needed -- the hook already passes all PipelineParams to fetchPipeline, and include_retired will flow through from the params object.
  </action>
  <verify>
    - `cd frontend && npx tsc --noEmit 2>&1 | head -20` shows no type errors related to pipeline types
    - `grep "stale_notified_at" frontend/src/features/pipeline/types/pipeline.ts` matches 2 lines (ListItem + Detail)
    - `grep "retirePipelineEntry\|reactivatePipelineEntry" frontend/src/features/pipeline/api.ts` matches 2+ lines
  </verify>
  <done>Pipeline types include stale/retired fields, API functions for retire/reactivate exist, include_retired param flows through the data layer.</done>
</task>

<task type="auto">
  <name>Task 2: Grid stale/retired styling + toolbar toggle + retire/reactivate actions</name>
  <files>
    frontend/src/features/pipeline/components/PipelinePage.tsx
    frontend/src/features/pipeline/components/PipelineToolbar.tsx
    frontend/src/features/pipeline/components/PipelineSidePanel.tsx
  </files>
  <action>
1. In `PipelinePage.tsx`:
   - Add `showRetired` state: `const [showRetired, setShowRetired] = useState(false)`
   - Pass `include_retired: showRetired || undefined` into the params object that feeds `usePipeline`
   - Add `getRowClass` callback to the AgGridReact component:
     ```typescript
     getRowClass={(params) => {
       if (params.data?.retired_at) return 'row-retired'
       if (params.data?.stale_notified_at) return 'row-stale'
       return undefined
     }}
     ```
   - Add a `<style>` tag (or inline styles via a wrapper) for the row classes. Use CSS-in-JS or a style block:
     ```css
     .row-stale { background: rgba(245, 158, 11, 0.06) !important; border-left: 3px solid #F59E0B !important; }
     .row-retired { opacity: 0.5; }
     ```
     The simplest approach: add a `<style>` JSX element at the top of the component's return, or use the AG Grid theme's `withParams` if it supports row overrides. Prefer a `<style>` element for simplicity.
   - Pass `showRetired` and `onShowRetiredChange={setShowRetired}` to `PipelineToolbar`

2. In `PipelineToolbar.tsx`:
   - Add props: `showRetired: boolean` and `onShowRetiredChange: (v: boolean) => void` to `PipelineToolbarProps`
   - Add a toggle switch/checkbox in the right button group (before the filter toggle button). Use a simple labeled toggle:
     ```tsx
     <label style={{ display: 'inline-flex', alignItems: 'center', gap: '6px', fontSize: '13px', color: '#6B7280', cursor: 'pointer' }}>
       <input
         type="checkbox"
         checked={showRetired}
         onChange={(e) => onShowRetiredChange(e.target.checked)}
         style={{ accentColor: '#E94D35' }}
       />
       Show retired
     </label>
     ```
     Place it between the Save View button area and the Filter toggle button.

3. In `PipelineSidePanel.tsx`:
   - Add a Retire/Reactivate button in the panel header area (near the "Open full profile" button). Read the side panel component to find the exact location.
   - Import `retirePipelineEntry` and `reactivatePipelineEntry` from `../api`
   - Import `useQueryClient` from `@tanstack/react-query` for cache invalidation
   - Add the button:
     - If `item.retired_at` is set: show "Reactivate" button (green-ish, e.g., color: #22C55E)
     - If `item.retired_at` is null: show "Retire" button (muted, e.g., color: #6B7280)
   - On click: call the respective API function, then invalidate the `['pipeline']` query key to refresh the grid
   - Use a simple `onClick` handler with try/catch -- no need for a mutation hook for this simple action. Disable the button while the request is in flight using local `isLoading` state.
  </action>
  <verify>
    - `cd frontend && npx tsc --noEmit 2>&1 | head -20` shows no type errors
    - `grep "row-stale\|row-retired" frontend/src/features/pipeline/components/PipelinePage.tsx` matches
    - `grep "showRetired\|onShowRetiredChange" frontend/src/features/pipeline/components/PipelineToolbar.tsx` matches
    - `grep "retirePipelineEntry\|reactivatePipelineEntry" frontend/src/features/pipeline/components/PipelineSidePanel.tsx` matches
    - `cd frontend && npm run build` succeeds with no errors
  </verify>
  <done>Grid rows show amber stale indicator and dimmed retired styling, toolbar has "Show retired" toggle, side panel has retire/reactivate button with cache invalidation.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- Types include stale_notified_at and retired_at on both list and detail interfaces
- Grid applies visual stale indicator (amber left border) to entries with stale_notified_at
- Grid dims retired entries (opacity 0.5) when showRetired is enabled
- Toolbar toggle controls include_retired param flow to API
- Side panel shows Retire button for active entries, Reactivate for retired entries
</verification>

<success_criteria>
- RETIRE-01 frontend: Stale entries have visual amber indicator in grid
- RETIRE-02 frontend: Retired entries hidden by default from grid (include_retired=false)
- RETIRE-03: "Show retired" toggle in toolbar reveals retired entries with dimmed style
- RETIRE-04 frontend: Retire and Reactivate buttons in side panel call API and refresh grid
</success_criteria>

<output>
After completion, create `.planning/phases/090-retirement-flow/090-02-SUMMARY.md`
</output>
