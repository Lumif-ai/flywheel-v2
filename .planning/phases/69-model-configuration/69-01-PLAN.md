---
phase: 69-model-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/engines/model_config.py
  - backend/src/flywheel/engines/email_scorer.py
  - backend/src/flywheel/engines/email_drafter.py
  - backend/src/flywheel/engines/email_voice_updater.py
  - backend/src/flywheel/services/gmail_sync.py
autonomous: true

must_haves:
  truths:
    - "Calling get_engine_model(db, tenant_id, 'scoring') returns 'claude-sonnet-4-6' when no tenant config exists"
    - "Calling get_engine_model(db, tenant_id, 'scoring') returns the configured model string when tenant has email_engine_models in settings"
    - "No _HAIKU_MODEL or _SONNET_MODEL module-level constants remain in any engine file"
    - "An invalid or missing model config does not crash the sync loop — falls back to default with a warning log"
    - "All 5 engine keys (scoring, voice_extraction, voice_learning, drafting, context_extraction) have defaults in ENGINE_DEFAULTS"
  artifacts:
    - path: "backend/src/flywheel/engines/model_config.py"
      provides: "Shared get_engine_model() helper"
      exports: ["get_engine_model", "ENGINE_DEFAULTS"]
    - path: "backend/src/flywheel/engines/email_scorer.py"
      provides: "Scoring engine using configurable model"
      contains: "get_engine_model"
    - path: "backend/src/flywheel/engines/email_drafter.py"
      provides: "Drafting engine using configurable model"
      contains: "get_engine_model"
    - path: "backend/src/flywheel/engines/email_voice_updater.py"
      provides: "Voice learning engine using configurable model"
      contains: "get_engine_model"
    - path: "backend/src/flywheel/services/gmail_sync.py"
      provides: "Voice extraction using configurable model"
      contains: "get_engine_model"
  key_links:
    - from: "backend/src/flywheel/engines/email_scorer.py"
      to: "backend/src/flywheel/engines/model_config.py"
      via: "import get_engine_model"
      pattern: "from flywheel\\.engines\\.model_config import get_engine_model"
    - from: "backend/src/flywheel/services/gmail_sync.py"
      to: "backend/src/flywheel/engines/model_config.py"
      via: "import get_engine_model, resolve in voice_profile_init, pass to _extract_voice_profile"
      pattern: "get_engine_model.*voice_extraction"
    - from: "backend/src/flywheel/engines/model_config.py"
      to: "flywheel.db.models.Tenant"
      via: "select(Tenant.settings) query"
      pattern: "select\\(Tenant\\.settings\\)"
---

<objective>
Create a shared `get_engine_model()` helper and migrate all 5 email engine locations from hardcoded model constants to configurable per-tenant settings.

Purpose: Foundation for all v7.0 voice and intelligence work — model changes become config changes, not code changes.
Output: `model_config.py` helper + 4 modified engine files with no remaining hardcoded model constants.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-model-configuration/69-RESEARCH.md

@backend/src/flywheel/engines/email_scorer.py
@backend/src/flywheel/engines/email_drafter.py
@backend/src/flywheel/engines/email_voice_updater.py
@backend/src/flywheel/services/gmail_sync.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/services/entity_normalization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared get_engine_model() helper module</name>
  <files>backend/src/flywheel/engines/model_config.py</files>
  <action>
Create `backend/src/flywheel/engines/model_config.py` with:

1. `ENGINE_DEFAULTS` dict mapping all 5 engine keys to their default model:
   - `"scoring"` -> `"claude-sonnet-4-6"`
   - `"voice_extraction"` -> `"claude-sonnet-4-6"`
   - `"voice_learning"` -> `"claude-sonnet-4-6"`
   - `"drafting"` -> `"claude-sonnet-4-6"`
   - `"context_extraction"` -> `"claude-sonnet-4-6"`

   Note: scoring, voice_extraction, and voice_learning are intentionally upgrading from Haiku to Sonnet as the new default per spec. This is a deliberate behavior change.

2. `async def get_engine_model(db: AsyncSession, tenant_id: UUID, engine_key: str, default: str = "claude-sonnet-4-6") -> str`:
   - Query `select(Tenant.settings).where(Tenant.id == tenant_id)` (same pattern as entity_normalization.py lines 92-97)
   - Read `settings["email_engine_models"][engine_key]` from the JSONB result
   - If found and non-empty string: return it (with warning log if it doesn't start with "claude-")
   - If not found or key missing: return `ENGINE_DEFAULTS.get(engine_key, default)`
   - Wrap entire read in try/except: on ANY exception, log warning with engine_key and tenant_id (no PII), return default
   - NO commits, NO flushes — read-only query only
   - Import Tenant from `flywheel.db.models`, use standard `logging.getLogger(__name__)`
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python3 -c "from flywheel.engines.model_config import get_engine_model, ENGINE_DEFAULTS; print('OK', len(ENGINE_DEFAULTS))"` — should print "OK 5".
  </verify>
  <done>
model_config.py exists, exports get_engine_model and ENGINE_DEFAULTS with 5 engine keys, follows the established tenant settings read pattern, handles errors gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all engine files to use get_engine_model()</name>
  <files>
backend/src/flywheel/engines/email_scorer.py
backend/src/flywheel/engines/email_drafter.py
backend/src/flywheel/engines/email_voice_updater.py
backend/src/flywheel/services/gmail_sync.py
  </files>
  <action>
Update each of the 4 files to replace hardcoded model constants with the shared helper:

**email_scorer.py:**
- Remove line 51: `_HAIKU_MODEL = "claude-haiku-4-5-20251001"`
- Add import: `from flywheel.engines.model_config import get_engine_model`
- Inside `score_email()` (which already has `db` and `tenant_id` params), before the API call at line ~543:
  `model = await get_engine_model(db, tenant_id, "scoring")`
- Replace `model=_HAIKU_MODEL` with `model=model` in the `client.messages.create()` call

**email_drafter.py:**
- Remove line 59: `_SONNET_MODEL = "claude-sonnet-4-6"`
- Add import: `from flywheel.engines.model_config import get_engine_model`
- Inside `draft_email()` (which already has `db` and `tenant_id` params), before the API call at line ~459:
  `model = await get_engine_model(db, tenant_id, "drafting")`
- Replace `model=_SONNET_MODEL` with `model=model` in the `client.messages.create()` call

**email_voice_updater.py:**
- Remove line 35: `_HAIKU_MODEL = "claude-haiku-4-5-20251001"`
- Add import: `from flywheel.engines.model_config import get_engine_model`
- Inside `update_from_edit()` (which already has `db` and `tenant_id` params), before the API call at line ~183:
  `model = await get_engine_model(db, tenant_id, "voice_learning")`
- Replace `model=_HAIKU_MODEL` with `model=model` in the `client.messages.create()` call

**gmail_sync.py (voice extraction — special case):**
- Remove line 759: `_HAIKU_MODEL = "claude-haiku-4-5-20251001"`
- Add import: `from flywheel.engines.model_config import get_engine_model`
- In `voice_profile_init(db, integration)` (has db and integration.tenant_id):
  `model = await get_engine_model(db, integration.tenant_id, "voice_extraction")`
  Then pass `model=model` when calling `_extract_voice_profile()`
- Update `_extract_voice_profile(bodies: list[str])` signature to accept a `model` parameter:
  `async def _extract_voice_profile(bodies: list[str], model: str = "claude-sonnet-4-6") -> dict:`
- Replace `model=_HAIKU_MODEL` with `model=model` in the `client.messages.create()` call inside `_extract_voice_profile`

After all changes, verify NO occurrences of `_HAIKU_MODEL` or `_SONNET_MODEL` remain in any of the 4 files.
  </action>
  <verify>
Run all of these:
1. `cd /Users/sharan/Projects/flywheel-v2/backend && grep -rn "_HAIKU_MODEL\|_SONNET_MODEL" src/flywheel/engines/ src/flywheel/services/gmail_sync.py` — should return NO results
2. `cd /Users/sharan/Projects/flywheel-v2/backend && grep -rn "get_engine_model" src/flywheel/engines/ src/flywheel/services/gmail_sync.py` — should show imports and calls in all 4 files plus model_config.py
3. `cd /Users/sharan/Projects/flywheel-v2/backend && python3 -c "from flywheel.engines.email_scorer import score_email; from flywheel.engines.email_drafter import draft_email; from flywheel.engines.email_voice_updater import update_from_edit; print('All imports OK')"` — should succeed without import errors
  </verify>
  <done>
All 4 engine files import and use get_engine_model() with the correct engine key. No _HAIKU_MODEL or _SONNET_MODEL constants remain anywhere in the codebase. The gmail_sync.py _extract_voice_profile function accepts model as a parameter passed from voice_profile_init.
  </done>
</task>

</tasks>

<verification>
1. No hardcoded model constants: `grep -rn "_HAIKU_MODEL\|_SONNET_MODEL" backend/src/flywheel/` returns nothing
2. All engines use helper: `grep -rn "get_engine_model" backend/src/flywheel/` shows 5 files (model_config.py + 4 consumers)
3. All imports succeed: `python3 -c "from flywheel.engines.model_config import get_engine_model"` works
4. Default behavior preserved: ENGINE_DEFAULTS maps all 5 keys to claude-sonnet-4-6
5. context_extraction key registered in ENGINE_DEFAULTS even though the engine file does not exist yet (placeholder for Phase 74)
</verification>

<success_criteria>
- get_engine_model() returns "claude-sonnet-4-6" when no tenant config exists (default path)
- All 5 engine locations use the shared helper — zero hardcoded model constants remain
- gmail_sync.py resolves model in voice_profile_init and passes to _extract_voice_profile
- Error handling: exceptions in config read log a warning and fall back to default, never crash
- All module imports succeed without errors
</success_criteria>

<output>
After completion, create `.planning/phases/69-model-configuration/69-01-SUMMARY.md`
</output>
