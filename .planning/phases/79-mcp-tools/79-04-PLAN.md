---
phase: 79-mcp-tools
plan: 04
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - cli/flywheel_mcp/server.py
autonomous: true

must_haves:
  truths:
    - "Claude Code can save skill output as a document in the Flywheel library with a view URL"
    - "Claude Code can save a meeting summary back to a meeting record"
    - "Claude Code can update a task's status or priority"
    - "All 10 new tool descriptions match the spec verbatim for optimal Claude Code intent matching"
  artifacts:
    - path: "cli/flywheel_mcp/server.py"
      provides: "flywheel_save_document, flywheel_save_meeting_summary, flywheel_update_task tools"
      contains: "def flywheel_save_document"
  key_links:
    - from: "cli/flywheel_mcp/server.py"
      to: "cli/flywheel_mcp/api_client.py"
      via: "client.save_document(), client.save_meeting_summary(), client.update_task()"
      pattern: "client\\.save_document|client\\.save_meeting_summary|client\\.update_task"
---

<objective>
Implement the three write MCP tools (MCP-08, MCP-09, MCP-10) and verify all tool descriptions match the spec for INFRA-03 compliance.

Purpose: Write tools close the loop -- Claude Code can now discover skills, read data, AND persist results back to Flywheel.
Output: 3 new MCP tools in server.py, all 13 tools verified with spec-compliant descriptions
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-mcp-tools/79-RESEARCH.md
@.planning/phases/79-mcp-tools/79-01-SUMMARY.md
@cli/flywheel_mcp/server.py
@cli/flywheel_mcp/api_client.py
@.planning/SPEC-flywheel-platform-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement flywheel_save_document, flywheel_save_meeting_summary, flywheel_update_task (MCP-08, MCP-09, MCP-10)</name>
  <files>cli/flywheel_mcp/server.py</files>
  <action>
Add a section comment after the Action Tools section:

```python
# --------------------------------------------------------------------------
# Write Tools (MCP-08, MCP-09, MCP-10)
# --------------------------------------------------------------------------
```

**MCP-08: flywheel_save_document**
- Parameters: `title: str, content: str, skill_name: str = "", account_id: str = ""`
- Docstring verbatim from spec: "Save a skill's output to the Flywheel library. Send the raw content (markdown or structured text) -- Flywheel renders it with the design system. The document appears in the library UI immediately. Use this after every skill execution to persist the deliverable. Optionally link to a skill name and account."
- Implementation:
  - Build metadata dict: `metadata = {}; if account_id: metadata["account_id"] = account_id`
  - `client.save_document(title=title, skill_name=skill_name or "mcp-manual", markdown_content=content, metadata=metadata)`
  - Extract run_id: `result.get("run_id") or result.get("id", "?")`
  - Construct URL: `f"{client.frontend_url}/library"` (matching the existing flywheel_run_skill pattern)
  - Return: `f"Document '{title}' saved. View: {url}"`
  - Two-level except

**MCP-09: flywheel_save_meeting_summary**
- Parameters: `meeting_id: str, summary: str`
- Docstring verbatim from spec: "Save a processed meeting summary back to Flywheel. Call this after you've analyzed a meeting transcript and extracted insights. Updates the meeting record so the summary appears on the meeting detail page. Write extracted business intelligence to the context store separately via flywheel_write_context."
- Implementation:
  - `client.save_meeting_summary(meeting_id, summary)`
  - Extract title: `result.get("title", "meeting")`
  - Return: `f"Summary saved for '{title}'. Meeting status updated to completed."`
  - Two-level except

**MCP-10: flywheel_update_task**
- Parameters: `task_id: str, status: str = "", priority: str = ""`
- Docstring verbatim from spec: "Update a task's status, priority, or suggested skill. Use this to confirm tasks, mark them done, assign a skill for execution, or change priority. Call this after executing a task's skill to mark it complete, or during triage to confirm or dismiss detected tasks."
- Implementation:
  - Build fields dict from non-empty params: `fields = {}; if status: fields["status"] = status; if priority: fields["priority"] = priority`
  - If no fields: return "No fields to update. Provide status and/or priority."
  - `client.update_task(task_id, **fields)`
  - Extract: `title = result.get("title", "Unknown task"); new_status = result.get("status", "?")`
  - Return: `f"Task '{title}' updated. Status: {new_status}"`
  - Two-level except
  </action>
  <verify>Run `grep -c "def flywheel_save_document\|def flywheel_save_meeting_summary\|def flywheel_update_task" cli/flywheel_mcp/server.py` -- should return 3. Verify syntax: `python3 -c "import ast; ast.parse(open('cli/flywheel_mcp/server.py').read()); print('OK')"`</verify>
  <done>All three write tools implemented following existing pattern. Document save returns library URL. Meeting summary confirms title. Task update returns new status.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 13 tool descriptions and final tool count (INFRA-03)</name>
  <files>cli/flywheel_mcp/server.py</files>
  <action>
INFRA-03 requires: "Tool descriptions optimized for Claude Code matching."

Perform a verification pass on server.py:

1. **Count tools:** `grep -c "@mcp.tool()" cli/flywheel_mcp/server.py` should return 13 (3 original + 10 new).

2. **Verify docstring content for all 10 new tools** matches the spec verbatim. The spec descriptions are in `.planning/SPEC-flywheel-platform-architecture.md` lines 40-175. Compare each docstring.

3. **Verify section organization.** The file should have these section headers in order:
   - `# Tools` (existing 3: run_skill, read_context, write_context)
   - `# Skill Discovery Tools (MCP-01, MCP-02)`
   - `# Data Read Tools -- Meetings (MCP-03, MCP-04)`
   - `# Data Read Tools -- Tasks & Accounts (MCP-05, MCP-06)`
   - `# Action Tools (MCP-07)`
   - `# Write Tools (MCP-08, MCP-09, MCP-10)`
   - `# Entry point`

4. **Verify disambiguation signals.** Check that tools with overlapping domains have "NOT for" or differentiation lines. The existing tools already have these. The new tools differentiate via specific trigger language in the spec descriptions.

5. **Fix any discrepancies** if docstrings don't match spec verbatim. Minor punctuation or dash differences are acceptable (spec uses em-dash, code can use --), but the content and phrasing must match.

If everything checks out, this task is a read-and-verify with minimal changes. If any docstring deviates from spec, fix it.
  </action>
  <verify>Run `grep -c "@mcp.tool()" cli/flywheel_mcp/server.py` -- must return 13. Run `python3 -c "import ast; ast.parse(open('cli/flywheel_mcp/server.py').read()); print('OK')"` -- must pass.</verify>
  <done>All 13 tools present with spec-compliant descriptions. Tool organization uses domain-based section headers. INFRA-03 complete.</done>
</task>

</tasks>

<verification>
- `python3 -c "import ast; ast.parse(open('cli/flywheel_mcp/server.py').read()); print('OK')"` -- no syntax errors
- `grep -c "@mcp.tool()" cli/flywheel_mcp/server.py` returns exactly 13
- `grep "def flywheel_" cli/flywheel_mcp/server.py` lists all 13 tool function names
- All new tool docstrings match spec descriptions
- Response formats include entity IDs for tool chaining
- All tools use: sync function, -> str, fresh FlywheelClient(), two-level except
</verification>

<success_criteria>
- flywheel_save_document saves content and returns library URL
- flywheel_save_meeting_summary writes ai_summary and confirms with meeting title
- flywheel_update_task updates status/priority and confirms
- All 13 tools have spec-compliant descriptions (INFRA-03)
- server.py is well-organized with domain section headers
- Complete MCP tool surface: discover (2) + read (4) + action (1) + write (3) = 10 new tools
</success_criteria>

<output>
After completion, create `.planning/phases/79-mcp-tools/79-04-SUMMARY.md`
</output>
