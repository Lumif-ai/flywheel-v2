---
phase: 79-mcp-tools
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - cli/flywheel_mcp/server.py
autonomous: true

must_haves:
  truths:
    - "Claude Code can fetch unprocessed meetings with metadata and provider summary"
    - "Claude Code can fetch today's upcoming meetings with attendee details"
    - "Claude Code can trigger a meeting sync and see how many meetings were synced"
  artifacts:
    - path: "cli/flywheel_mcp/server.py"
      provides: "flywheel_fetch_meetings, flywheel_fetch_upcoming, flywheel_sync_meetings tools"
      contains: "def flywheel_fetch_meetings"
  key_links:
    - from: "cli/flywheel_mcp/server.py"
      to: "cli/flywheel_mcp/api_client.py"
      via: "client.fetch_meetings(processing_status='pending')"
      pattern: "client\\.fetch_meetings"
    - from: "cli/flywheel_mcp/server.py"
      to: "cli/flywheel_mcp/api_client.py"
      via: "client.fetch_upcoming()"
      pattern: "client\\.fetch_upcoming"
    - from: "cli/flywheel_mcp/server.py"
      to: "cli/flywheel_mcp/api_client.py"
      via: "client.sync_meetings()"
      pattern: "client\\.sync_meetings"
---

<objective>
Implement the three meeting-domain MCP tools: fetch unprocessed meetings (MCP-03), fetch upcoming meetings (MCP-04), and trigger meeting sync (MCP-07).

Purpose: Meeting tools are the core of the morning brief workflow -- sync, review unprocessed, prep upcoming.
Output: 3 new MCP tools in server.py
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-mcp-tools/79-RESEARCH.md
@.planning/phases/79-mcp-tools/79-01-SUMMARY.md
@cli/flywheel_mcp/server.py
@cli/flywheel_mcp/api_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement flywheel_fetch_meetings and flywheel_fetch_upcoming (MCP-03, MCP-04)</name>
  <files>cli/flywheel_mcp/server.py</files>
  <action>
Add a section comment and two new tools after the Skill Discovery Tools section:

```python
# --------------------------------------------------------------------------
# Data Read Tools — Meetings (MCP-03, MCP-04)
# --------------------------------------------------------------------------
```

**MCP-03: flywheel_fetch_meetings**
- Parameter: `limit: int = 10`
- Docstring verbatim from spec: "Fetch meetings that haven't been processed yet, including full transcripts. Use this during the morning brief to find meetings that need summarization and insight extraction. Returns meeting title, time, attendees, and transcript. Process each meeting's transcript, then save the summary via flywheel_save_meeting_summary."
- Implementation:
  - `client.fetch_meetings(processing_status="pending", limit=limit)` -- uses the processing_status param added in Plan 01
  - Empty case: "No unprocessed meetings found. All meetings have been summarized."
  - For each meeting, format as:
    ```
    ## {title}
    ID: {id}
    Date: {meeting_date}
    Attendees: {comma-separated attendee names or emails}
    Provider summary: {summary or "(none)"}
    ```
  - Attendees: iterate `m.get("attendees", [])`, for each attendee use `a.get("name", a.get("email", "?"))`. Handle case where attendees is not a list (use empty list).
  - Header: `f"Found {len(meetings)} unprocessed meetings (of {total} total):\n"` where total from `result.get("total", len(meetings))`
  - Join meeting blocks with `"\n\n---\n\n"`
  - NOTE: The list endpoint does NOT return transcript_url or ai_summary. We return provider `summary` field which is available on the list endpoint. This is sufficient for the morning brief workflow -- Claude Code can use flywheel_read_context for deeper meeting intel.

**MCP-04: flywheel_fetch_upcoming**
- Parameter: `limit: int = 10`
- Docstring verbatim from spec: "Fetch today's upcoming meetings with attendee details and linked accounts. Use this to identify meetings that need preparation. Returns meeting title, time, attendees (name + email), meeting type, and linked account if any. After fetching, prepare each meeting using the meeting-prep skill."
- Implementation:
  - `client.fetch_upcoming(limit=limit)`
  - Empty case: "No upcoming meetings today."
  - For each meeting, format as:
    ```
    ## {title}
    ID: {id}
    Time: {meeting_date}
    Type: {meeting_type or "unclassified"}
    Attendees: {name (email)} for each attendee
    Account: {account_id or "none linked"}
    ```
  - Header: `f"{len(meetings)} upcoming meetings today:\n"`
  - Join with `"\n\n---\n\n"`

Both tools: sync function, `-> str`, fresh FlywheelClient(), two-level except (FlywheelAPIError then Exception).
  </action>
  <verify>Run `grep -c "def flywheel_fetch_meetings\|def flywheel_fetch_upcoming" cli/flywheel_mcp/server.py` -- should return 2.</verify>
  <done>flywheel_fetch_meetings returns unprocessed meetings filtered by processing_status=pending. flywheel_fetch_upcoming returns today's upcoming meetings with attendee details.</done>
</task>

<task type="auto">
  <name>Task 2: Implement flywheel_sync_meetings (MCP-07)</name>
  <files>cli/flywheel_mcp/server.py</files>
  <action>
Add a section comment and one tool after the Data Read Tools section:

```python
# --------------------------------------------------------------------------
# Action Tools (MCP-07)
# --------------------------------------------------------------------------
```

**MCP-07: flywheel_sync_meetings**
- No parameters
- Docstring verbatim from spec: "Sync meetings from Granola calendar integration. Call this at the start of the morning brief or when the user asks to refresh their meeting data. Returns count of new and updated meetings. After syncing, use flywheel_fetch_meetings to get unprocessed meetings."
- Implementation:
  - `client.sync_meetings()`
  - Extract counts from result: `result.get("new", 0)` and `result.get("updated", 0)` (adjust field names based on actual API response shape -- the sync endpoint may return different field names)
  - Format: `f"Meeting sync complete. {new_count} new, {updated_count} updated meetings."`
  - If the response doesn't have obvious count fields, format as: `f"Meeting sync triggered. Response: {result}"` to ensure we don't lose info
  - Two-level except

Place this BEFORE the Write Tools section (which Plan 04 will add).
  </action>
  <verify>Run `grep -c "def flywheel_sync_meetings" cli/flywheel_mcp/server.py` -- should return 1. Verify syntax: `python3 -c "import ast; ast.parse(open('cli/flywheel_mcp/server.py').read()); print('OK')"`</verify>
  <done>flywheel_sync_meetings triggers calendar sync and returns result counts. All 3 meeting-domain tools follow the established pattern.</done>
</task>

</tasks>

<verification>
- `python3 -c "import ast; ast.parse(open('cli/flywheel_mcp/server.py').read()); print('OK')"` -- no syntax errors
- `grep "def flywheel_" cli/flywheel_mcp/server.py` shows 8 tools (3 existing + 2 from Plan 01 + 3 new)
- Each tool has a descriptive docstring matching the spec
- All tools use fresh FlywheelClient() and two-level except
</verification>

<success_criteria>
- flywheel_fetch_meetings returns pending meetings with metadata and provider summary
- flywheel_fetch_upcoming returns today's upcoming with attendees and account links
- flywheel_sync_meetings triggers sync and reports counts
- All entity IDs included in responses for tool chaining
</success_criteria>

<output>
After completion, create `.planning/phases/79-mcp-tools/79-02-SUMMARY.md`
</output>
