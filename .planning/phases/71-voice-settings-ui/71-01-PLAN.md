---
phase: 71-voice-settings-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/flywheel/api/email.py
autonomous: true

must_haves:
  truths:
    - "GET /email/voice-profile returns full 10-field profile for authenticated user"
    - "GET /email/voice-profile returns null (HTTP 200) when no profile exists"
    - "PATCH /email/voice-profile updates only tone and sign_off fields"
    - "PATCH /email/voice-profile returns 404 when no profile exists"
    - "POST /email/voice-profile/reset deletes profile and triggers background re-extraction"
    - "POST /email/voice-profile/reset returns 400 when no Gmail integration connected"
  artifacts:
    - path: "backend/src/flywheel/api/email.py"
      provides: "Three voice profile endpoints: GET, PATCH, POST reset"
      contains: "get_voice_profile"
  key_links:
    - from: "email.py reset endpoint"
      to: "gmail_sync.voice_profile_init"
      via: "BackgroundTasks after deleting existing row"
      pattern: "voice_profile_init"
---

<objective>
Add three REST endpoints to the existing email router for voice profile CRUD: GET returns the full 10-field profile (or null), PATCH updates only tone and sign_off, POST reset deletes the profile and triggers background re-extraction.

Purpose: Backend API layer that the Voice Profile Settings UI (plan 02) will consume.
Output: Three working endpoints in `backend/src/flywheel/api/email.py`.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-voice-settings-ui/71-RESEARCH.md
@backend/src/flywheel/api/email.py
@backend/src/flywheel/db/models.py
@backend/src/flywheel/services/gmail_sync.py
@backend/src/flywheel/api/deps.py
@backend/src/flywheel/db/session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic models and GET/PATCH voice profile endpoints</name>
  <files>backend/src/flywheel/api/email.py</files>
  <action>
Add to email.py (after existing Pydantic models section):

1. `VoiceProfileResponse` Pydantic model with all 10 voice fields plus `samples_analyzed: int` and `updated_at: datetime`. The `phrases` field should be `list[str]` with default `[]`. All other text fields are `str | None`. Both int fields (`avg_length`, `avg_sentences`) are `int | None`.

2. `VoiceProfilePatch` Pydantic model with ONLY `tone: str | None = None` and `sign_off: str | None = None`. Do NOT allow any other fields -- this enforces the "8 fields read-only" requirement.

3. `GET /voice-profile` endpoint:
   - Dependencies: `require_tenant` for auth, `get_tenant_db` for DB session
   - Query `EmailVoiceProfile` where `tenant_id == user.tenant_id` and `user_id == user.sub` (use `user.sub`, NOT `user.id`)
   - If no row found, return `None` (FastAPI serializes this as HTTP 200 with `null` body)
   - If found, map to `VoiceProfileResponse` and return
   - Return type annotation: `VoiceProfileResponse | None`

4. `PATCH /voice-profile` endpoint:
   - Dependencies: same auth pattern
   - Accept `VoiceProfilePatch` body
   - Query existing profile; if not found, raise `HTTPException(404, "No voice profile found")`
   - Build update dict from non-None fields only. If at least one field present, also set `updated_at = datetime.now(timezone.utc)`
   - Execute `update()` statement, commit, re-fetch and return as `VoiceProfileResponse`

Add `EmailVoiceProfile` to the imports from `flywheel.db.models`.

Follow exact patterns from existing endpoints in this file (same auth deps, same query style, same error handling).
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.email import router; print([r.path for r in router.routes])"` -- should include `/voice-profile` twice (GET and PATCH).
  </verify>
  <done>GET /email/voice-profile returns profile or null. PATCH /email/voice-profile updates only tone/sign_off and returns 404 when no profile exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST voice-profile/reset endpoint with background re-extraction</name>
  <files>backend/src/flywheel/api/email.py</files>
  <action>
Add to email.py after the PATCH endpoint:

1. `POST /voice-profile/reset` endpoint:
   - Dependencies: `require_tenant`, `get_tenant_db`, `BackgroundTasks` (already imported in the file)
   - First, check for a connected `gmail-read` Integration: query `Integration` where `tenant_id == user.tenant_id`, `user_id == user.sub`, `provider == "gmail-read"`, `status == "connected"`. If not found, raise `HTTPException(400, "No Gmail integration connected")`
   - Delete the existing `EmailVoiceProfile` row (using `delete()` statement with same tenant_id + user_id filter). Commit.
   - Capture `integration.id`, `str(user.tenant_id)`, and `str(user.sub)` into local variables BEFORE defining the background task (closure safety).
   - Define `async def _run_relearn()` that creates a new DB session using `get_session_factory()` and `tenant_session(factory, tenant_id, user_id)`, fetches the Integration by ID, and calls `voice_profile_init(bg_db, intg)` if found.
   - Add `_run_relearn` to `background_tasks`
   - Return `{"status": "relearning"}`

2. Add imports needed:
   - `from sqlalchemy import delete` (if not already imported -- check existing imports)
   - `from flywheel.services.gmail_sync import voice_profile_init`

Follow the exact background task pattern used by `trigger_sync()` in the same file for session management in the background task. The key detail: `tenant_session` is an async context manager from `flywheel.db.session` that sets the RLS tenant for the background DB session.
  </action>
  <verify>
Run: `cd /Users/sharan/Projects/flywheel-v2/backend && python -c "from flywheel.api.email import router; routes = [r.path for r in router.routes]; assert '/voice-profile/reset' in routes; print('OK:', routes)"` -- should include `/voice-profile/reset`.
  </verify>
  <done>POST /email/voice-profile/reset deletes existing profile, checks Gmail integration, triggers background re-extraction via voice_profile_init, returns {"status": "relearning"}. Returns 400 when no Gmail connected.</done>
</task>

</tasks>

<verification>
1. All three endpoints importable without errors: `python -c "from flywheel.api.email import router"`
2. Route list includes `/voice-profile` (GET), `/voice-profile` (PATCH), `/voice-profile/reset` (POST)
3. Pydantic models `VoiceProfileResponse` and `VoiceProfilePatch` are valid (no import errors)
4. PATCH model only accepts `tone` and `sign_off` -- no other fields
</verification>

<success_criteria>
- GET /email/voice-profile returns 200 with profile data or null
- PATCH /email/voice-profile updates tone/sign_off only, returns 404 when no profile
- POST /email/voice-profile/reset returns 400 without Gmail, deletes profile and triggers background re-extraction with Gmail
- All endpoints use user.sub (not user.id) for user scoping
- No new router file created -- all endpoints in existing email.py
</success_criteria>

<output>
After completion, create `.planning/phases/71-voice-settings-ui/71-01-SUMMARY.md`
</output>
