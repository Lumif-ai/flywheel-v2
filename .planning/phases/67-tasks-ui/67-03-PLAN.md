---
phase: 67-tasks-ui
plan: 03
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - frontend/src/features/tasks/components/MyCommitments.tsx
  - frontend/src/features/tasks/components/TaskCommitmentCard.tsx
  - frontend/src/features/tasks/components/TaskStatusBadge.tsx
  - frontend/src/features/tasks/components/TaskPriorityBadge.tsx
  - frontend/src/features/tasks/components/PromisesToMe.tsx
  - frontend/src/features/tasks/components/TaskWatchlistItem.tsx
autonomous: true

must_haves:
  truths:
    - "My Commitments shows yours tasks grouped by due date (Overdue/Today/This Week/Next Week/Later)"
    - "Overdue tasks show red due dates and group headers"
    - "Clicking a commitment card opens detail panel (wired in plan 04)"
    - "Promises to Me shows theirs/mutual tasks as lightweight rows"
    - "Overdue promises show red dot, days count, and Create Follow-up button"
    - "Empty groups are hidden, empty sections show EmptyState"
  artifacts:
    - path: "frontend/src/features/tasks/components/MyCommitments.tsx"
      provides: "Grouped commitment list section"
      min_lines: 50
    - path: "frontend/src/features/tasks/components/TaskCommitmentCard.tsx"
      provides: "Rich task card with provenance, status, priority, skill"
      min_lines: 60
    - path: "frontend/src/features/tasks/components/PromisesToMe.tsx"
      provides: "Watchlist section for others' commitments"
      min_lines: 50
    - path: "frontend/src/features/tasks/components/TaskWatchlistItem.tsx"
      provides: "Lightweight promise row with status indicator"
      min_lines: 40
  key_links:
    - from: "frontend/src/features/tasks/components/MyCommitments.tsx"
      to: "frontend/src/features/tasks/types/tasks.ts"
      via: "groupTasksByDueDate utility"
      pattern: "groupTasksByDueDate"
    - from: "frontend/src/features/tasks/components/TaskCommitmentCard.tsx"
      to: "/accounts/:id"
      via: "NavLink on account name"
      pattern: "NavLink.*accounts"
---

<objective>
Build the My Commitments grouped list (TASK-07) and Promises to Me watchlist (TASK-08) sections. These are the two main content sections showing active tasks the user owes and promises others have made.

Purpose: My Commitments is the accountability dashboard (what you owe); Promises to Me is the follow-up radar (what others owe you). Together they form the working surface of the Tasks page.
Output: Two complete sections with all card/row components, status/priority badges, date grouping
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/SPEC-tasks-ui.md
@.planning/DESIGN-BRIEF-tasks-ui.md
@.planning/phases/67-tasks-ui/67-RESEARCH.md
@.planning/phases/67-tasks-ui/67-01-SUMMARY.md

@frontend/src/components/ui/branded-card.tsx
@frontend/src/components/ui/badge.tsx
@frontend/src/components/ui/empty-state.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/lib/design-tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build My Commitments section with grouped TaskCommitmentCards</name>
  <files>
    frontend/src/features/tasks/components/MyCommitments.tsx
    frontend/src/features/tasks/components/TaskCommitmentCard.tsx
    frontend/src/features/tasks/components/TaskStatusBadge.tsx
    frontend/src/features/tasks/components/TaskPriorityBadge.tsx
  </files>
  <action>
  Build the atoms (badges) and the My Commitments section:

  **TaskStatusBadge.tsx (atom):**
  - Uses existing `Badge` component with custom inline styles for status-specific colors
  - Props: `status: TaskStatus`
  - Color mapping (use CSS custom properties where available, otherwise use rgba backgrounds):
    - detected: gray text (#6B7280), bg rgba(107,114,128,0.1)
    - in_review: amber text, bg rgba(245,158,11,0.1)
    - confirmed: blue text (#3B82F6), bg rgba(59,130,246,0.1)
    - in_progress: `var(--brand-coral)` text, `var(--brand-tint)` bg
    - done: green text (#22C55E), bg rgba(34,197,94,0.1)
    - blocked: red text (#EF4444), bg rgba(239,68,68,0.1)
    - dismissed: muted text (#9CA3AF), bg rgba(156,163,175,0.1)
    - deferred: amber (same as in_review)
  - Display: status label with first letter capitalized, replace underscores with spaces ("in_progress" -> "In progress")

  **TaskPriorityBadge.tsx (atom):**
  - Inline icon + label, no background
  - Props: `priority: Priority`
  - high: ChevronUp icon (filled) + "High" in `var(--error)` color
  - medium: Minus icon + "Medium" in `var(--warning)` color
  - low: ChevronDown icon + "Low" in `var(--secondary-text)` color
  - Text size: 12px/500

  **TaskCommitmentCard.tsx (molecule):**
  - Uses `BrandedCard` wrapper, variant based on state: `action` for overdue tasks, `info` for default
  - `hoverable` prop on BrandedCard for lift + shadow on hover
  - Internal padding: 20px 24px
  - Layout (flex-col, gap 12px):
    - Title: 15px/600 in `var(--heading-text)`, max 2 lines (line-clamp-2)
    - Provenance row: `MapPin` icon (14px) + meeting name + relative time (formatDistanceToNow), 13px/400 in `var(--secondary-text)`. If no meeting_id, show "Manual task"
    - Account row (conditional, only if account_id exists): `Building2` icon (14px) + account name as NavLink to `/accounts/${task.account_id}` in `var(--brand-coral)`, 13px/400
    - Status row: flex, gap 12px — `TaskStatusBadge` + `TaskPriorityBadge` + due date with Calendar icon (13px/500)
    - Skill row (conditional, only if suggested_skill exists): `TaskSkillChip` on left + "Generate ->" Button (variant outline, size sm, with Zap icon) on right — Generate button only shown when status is 'confirmed'
  - Due date color logic (use inline style):
    - Overdue: `var(--task-overdue-text)`
    - Due today: `var(--warning)`
    - Due this week: `var(--heading-text)`
    - Later or no date: `var(--secondary-text)`
  - onClick (on card body, not buttons): calls `onSelect(task.id)` prop — will open detail panel (wired in plan 04)
  - Props: `task: Task`, `onSelect: (id: string) => void`

  **MyCommitments.tsx (section):**
  - Fetches tasks using `useTasks()` (shares the same query as triage, no duplicate request due to React Query caching)
  - Filters: `commitment_direction === 'yours'` AND `status` in ['confirmed', 'in_progress', 'blocked']
  - Groups filtered tasks using `groupTasksByDueDate()` from types/tasks.ts
  - Section header via `TaskSectionHeader`: title "My Commitments", count = total filtered tasks
  - Renders groups in order: Overdue, Today, This Week, Next Week, Later
  - Each group: uppercase label (12px/600 tracking-wider in `var(--secondary-text)`) + count in parens + task cards below
  - Empty groups are NOT rendered (skip if group array is empty)
  - Empty section (no matching tasks at all): `EmptyState` with title "No active commitments", icon Target from lucide-react
  - Loading: show 2 skeleton cards
  - `onSelect` prop passes through to cards — currently a no-op, wired to detail panel in plan 04

  Wire MyCommitments into TasksPage as the second section (replace placeholder comment).
  </action>
  <verify>
  ```bash
  cd frontend && npx tsc --noEmit src/features/tasks/components/MyCommitments.tsx src/features/tasks/components/TaskCommitmentCard.tsx src/features/tasks/components/TaskStatusBadge.tsx src/features/tasks/components/TaskPriorityBadge.tsx 2>&1 | head -20
  ```
  </verify>
  <done>
  - My Commitments shows yours tasks grouped by Overdue/Today/This Week/Next Week/Later
  - Empty groups hidden, empty section shows EmptyState
  - Cards show title, provenance, account link, status badge, priority badge, due date, skill chip
  - Due dates colored by urgency
  - Generate button appears only on confirmed tasks with suggested_skill
  - Account names link to /accounts/:id
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Promises to Me watchlist section</name>
  <files>
    frontend/src/features/tasks/components/PromisesToMe.tsx
    frontend/src/features/tasks/components/TaskWatchlistItem.tsx
  </files>
  <action>
  Build the Promises to Me section with lightweight watchlist rows:

  **TaskWatchlistItem.tsx (molecule):**
  - NO card border — items are separated by `divide-y` in the parent container
  - Internal padding: 16px 20px
  - Layout (flex-col, gap 4px):
    - Row 1: Avatar circle (24px, bg `var(--brand-tint)`, initials from meeting/task context in `var(--brand-coral)` 10px/600, centered) + person/meeting name (14px/500 `var(--heading-text)`) + dot separator + company/account name (14px/400 `var(--secondary-text)`)
    - Row 2: Promise text (task title) in italics, quoted with typographic quotes. 14px/400 `var(--heading-text)`. `"${task.title}"`
    - Row 3: flex justify-between
      - Left: Provenance — "From: [meeting name] . [date]" or "From: Manual" if no meeting. 13px/400 `var(--secondary-text)`
      - Right: Status indicator
        - On track (due_date >= today or no due_date): green dot (8px circle, `var(--task-ontrack-dot)`) + "On track" + due date. 13px/400
        - Overdue (due_date < today): red dot (8px, `var(--task-overdue-dot)`) + "Overdue (N days)" in 13px/500 `var(--task-overdue-text)` + "Create Follow-up" Button (variant ghost, size xs, destructive-ish styling)
  - "Create Follow-up" onClick: calls `onCreateFollowUp(task)` prop
  - Hover: subtle background tint (`var(--brand-tint)` at low opacity)
  - Props: `task: Task`, `onCreateFollowUp: (task: Task) => void`, `hasFollowUp?: boolean`
  - If `hasFollowUp` is true, show "Follow-up created" Badge (variant secondary) instead of "Create Follow-up" button

  **PromisesToMe.tsx (section):**
  - Fetches from same `useTasks()` query (cached)
  - Filters: `commitment_direction` in ['theirs', 'mutual'] AND `status` NOT in ['done', 'dismissed']
  - Sort: overdue items first, then by created_at DESC
  - Section header: "Promises to Me" + count badge
  - Container: BrandedCard variant `info`, with `divide-y divide-[var(--subtle-border)]` on children for row separators
  - Renders `TaskWatchlistItem` for each promise
  - "Create Follow-up" handler:
    - For now, calls `useCreateTask()` mutation with pre-filled data:
      - title: `"Follow up with [meeting/person context] re: ${task.title}"`
      - commitment_direction: "yours"
      - account_id: task.account_id (if exists)
      - task_type: "followup"
      - priority: "medium"
    - After creation: track the task.id in a local Set state `followUpCreated` to show the "Follow-up created" badge
    - Show toast: "Follow-up task created"
  - Empty section: `EmptyState` with title "No outstanding promises", icon Handshake from lucide-react
  - Loading: 2 skeleton rows

  Wire PromisesToMe into TasksPage as the third section.
  </action>
  <verify>
  ```bash
  cd frontend && npx tsc --noEmit src/features/tasks/components/PromisesToMe.tsx src/features/tasks/components/TaskWatchlistItem.tsx 2>&1 | head -20
  ```
  Build:
  ```bash
  cd frontend && npm run build 2>&1 | tail -10
  ```
  </verify>
  <done>
  - Promises to Me shows theirs/mutual tasks as lightweight rows
  - Overdue items show red dot, "Overdue (N days)", and "Create Follow-up" button
  - On-track items show green dot and "On track" with due date
  - "Create Follow-up" creates a follow-up task and shows badge
  - Items without meeting show "Manual" provenance
  - Empty section shows "No outstanding promises" EmptyState
  - Rows separated by divide-y (not individual cards)
  </done>
</task>

</tasks>

<verification>
- My Commitments section renders with grouped tasks
- Date grouping correctly classifies overdue/today/this week/next week/later
- Promises to Me section renders with overdue flagging
- "Create Follow-up" works and shows badge
- Both sections show appropriate empty states
- Build passes: `npm run build`
</verification>

<success_criteria>
- Both content sections render correctly with real task data
- Due date grouping and color coding works
- Follow-up creation flow works end-to-end
- All cards/rows are accessible (aria-labels, semantic HTML)
- Visual polish matches design brief
</success_criteria>

<output>
After completion, create `.planning/phases/67-tasks-ui/67-03-SUMMARY.md`
</output>
