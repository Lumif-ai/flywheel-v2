---
phase: 098-today-and-tasks-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/briefing/components/TodaySection.tsx
  - frontend/src/features/briefing/components/BriefingPageV2.tsx
autonomous: true

must_haves:
  truths:
    - "Founder sees meeting cards showing attendee names, company, and time"
    - "Each meeting card has a Prep button that opens briefing or triggers prep skill"
    - "When no meetings exist, user sees empty state with calendar connect link"
  artifacts:
    - path: "frontend/src/features/briefing/components/TodaySection.tsx"
      provides: "Meeting cards with prep button, loading skeleton, empty state"
      min_lines: 80
  key_links:
    - from: "frontend/src/features/briefing/components/TodaySection.tsx"
      to: "frontend/src/features/briefing/components/BriefingPageV2.tsx"
      via: "props: meetings + isLoading from useBriefingV2"
      pattern: "TodaySection.*meetings.*isLoading"
    - from: "frontend/src/features/briefing/components/TodaySection.tsx"
      to: "/settings/integrations"
      via: "NavLink in empty state"
      pattern: "settings/integrations"
---

<objective>
Build the TodaySection component showing meeting cards with attendee info and prep button, then wire it into BriefingPageV2.

Purpose: Founders see their day's meetings at a glance with one-click prep access -- the "what's on my calendar" answer.
Output: TodaySection.tsx mounted in BriefingPageV2, showing meeting cards or empty state.
</objective>

<execution_context>
@/Users/sharan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sharan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/features/briefing/components/BriefingPageV2.tsx
@frontend/src/features/briefing/components/DailyBriefSection.tsx
@frontend/src/features/briefing/types/briefing-v2.ts
@frontend/src/lib/design-tokens.ts
@frontend/src/components/ui/branded-card.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/ui/empty-state.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TodaySection component with meeting cards and prep button</name>
  <files>frontend/src/features/briefing/components/TodaySection.tsx</files>
  <action>
Create `TodaySection.tsx` following the DailyBriefSection pattern (props-driven, BrandedCard wrapper, design tokens).

**Props interface:**
```typescript
interface TodaySectionProps {
  meetings: MeetingItem[] | undefined
  isLoading: boolean
}
```

**Three states (same pattern as DailyBriefSection):**

1. **Loading state:** BrandedCard with "Today" h2 title + 3 Skeleton rows (h-12 each, simulating meeting card shapes).

2. **Empty state:** BrandedCard with "Today" h2 title + message "No meetings today. Connect your calendar to see upcoming calls." with a NavLink (from react-router) to `/settings/integrations` styled as a coral text link. Do NOT use the EmptyState component (it's too large/centered for an inline section). Keep it compact: just the text + link inside the card.

3. **Loaded state:** BrandedCard with "Today" h2 title. Map over meetings to render MeetingCard rows.

**MeetingCard (inline in same file, not exported):**
- Layout: flex row, items-center, justify-between, py-3, border-b on all but last item.
- Left side (flex-1 min-w-0):
  - Meeting title: 15px, fontWeight 500, colors.headingText. Fallback to "Untitled meeting".
  - Subtitle line: 13px, colors.secondaryText. Format: `{time} · {company} · {attendee1, attendee2, attendee3}`. Use `date-fns` `format(new Date(meeting.time), 'h:mm a')` for time. Only show company segment if non-null. Only show attendees segment if array is non-empty.
  - Attendees extraction: `(meeting.attendees || []).map(a => (a as Record<string, string>).name || (a as Record<string, string>).displayName || (a as Record<string, string>).email || 'Unknown').slice(0, 3)`. If more than 3 attendees, append "+N more".
- Right side: PrepButton.

**PrepButton (inline in same file):**
- When `meeting.prep_status === 'available'`: Render a NavLink to `/documents?meeting_id=${meeting.id}` styled as a small button with text "View Prep". Style: 13px, px-3 py-1.5, rounded-lg, bg coral (#E94D35), white text, hover opacity.
- When `meeting.prep_status !== 'available'` (or null/none): Render a button with text "Prep" that navigates to `/chat` with a pre-filled message. Use `useNavigate` from react-router: `navigate('/chat', { state: { prefill: \`Prepare me for my meeting: ${meeting.title}\` } })`. Style: 13px, px-3 py-1.5, rounded-lg, border 1px solid colors.subtleBorder, colors.headingText, hover bg gray-50.

**Icons:** Use `Calendar` from lucide-react as a small icon (size 14) next to the time in the subtitle if desired, but keep it minimal. Not required.

**Section title "Today" styling:** Match DailyBriefSection's h2 exactly: typography.sectionTitle.size/weight/lineHeight, colors.headingText, margin 0, marginBottom spacing.element.
  </action>
  <verify>
Run `npx tsc --noEmit` from frontend/ directory -- no type errors in TodaySection.tsx. Visually inspect that the component exports `TodaySection` with correct props interface.
  </verify>
  <done>
TodaySection.tsx exists with: MeetingCard rendering (title, time, company, attendees), PrepButton with available/none logic, loading skeleton state, empty state with /settings/integrations link. No type errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire TodaySection into BriefingPageV2 shell</name>
  <files>frontend/src/features/briefing/components/BriefingPageV2.tsx</files>
  <action>
Update BriefingPageV2.tsx:

1. Add import: `import { TodaySection } from '@/features/briefing/components/TodaySection'`

2. Replace the comment `{/* Phase 98: TodaySection, TasksSection */}` with:
```tsx
{/* Today's meetings */}
<div style={{ marginTop: spacing.section }}>
  <TodaySection meetings={data?.today?.meetings} isLoading={isLoading} />
</div>

{/* Phase 98 Plan 02: TasksSection */}
```

This passes the meetings array from the existing useBriefingV2 hook data through to TodaySection. The `spacing.section` (48px) provides consistent vertical rhythm between DailyBriefSection and TodaySection.
  </action>
  <verify>
Run `npx tsc --noEmit` from frontend/ directory -- no type errors. Run `npm run build` from frontend/ directory -- build succeeds with no errors.
  </verify>
  <done>
BriefingPageV2 imports and renders TodaySection with meetings data from useBriefingV2. Build passes. Placeholder comment updated for Plan 02.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- zero type errors
2. `cd frontend && npm run build` -- successful build
3. TodaySection.tsx exists and exports TodaySection component
4. BriefingPageV2.tsx imports and renders TodaySection with correct props
5. Empty state contains "/settings/integrations" link text
6. PrepButton handles both "available" and "none" prep_status values
</verification>

<success_criteria>
- Meeting cards render with title, formatted time, company, and up to 3 attendee names
- Prep button shows "View Prep" (coral, links to documents) when prep_status is available
- Prep button shows "Prep" (outlined, navigates to chat with prefill) when prep_status is none
- Empty state shows calendar connect message with /settings/integrations link
- Loading state shows skeleton placeholders
- Component follows DailyBriefSection pattern (BrandedCard, design tokens, props-driven)
</success_criteria>

<output>
After completion, create `.planning/phases/098-today-and-tasks-sections/098-01-SUMMARY.md`
</output>
