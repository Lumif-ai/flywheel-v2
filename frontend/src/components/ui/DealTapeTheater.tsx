/**
 * DealTapeTheater — animated deal-tape showcase for the landing page.
 * Stub: renders a placeholder until the full animation is implemented.
 */
export function DealTapeTheater({ compact }: { compact?: boolean }) {
  return <div style={{ width: compact ? 200 : 400 }} />
}
