/**
 * CompoundingFlywheel — animated flywheel visualization for the landing page.
 * Stub: renders a placeholder until the full animation is implemented.
 */
export function CompoundingFlywheel({ size }: { size?: number }) {
  return <div style={{ width: size ?? 280, height: size ?? 280 }} />
}
