---
name: legal-doc-compare
version: 2.0
description: >
  DEPRECATED — redirects to the unified `legal` skill. Use `legal` instead for redline comparisons with PII redaction.
---

# Legal Document Compare — DEPRECATED

> **This skill has been superseded by the unified `legal/` skill (v1.0).**

Redline comparison functionality is now handled by the `legal` skill in Compare mode. The full comparison instructions live in `~/.claude/skills/legal/workers/compare.md` and are loaded automatically when two document versions are detected.

**What to do:** Use the `legal` skill instead.

## Context Store

This skill is context-aware. Follow the protocol in `~/.claude/skills/_shared/context-protocol.md` for pre-read, post-write, and knowledge overflow handling.
